// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.35.1
// source: cua.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "runanywhere.v1";

/**
 * Model-agnostic action type parsed from a CUA model's output. Values match
 * the C enum `rac_cua_action_type_t` (rac_cua.h) one-for-one.
 */
export enum CuaActionType {
  /** CUA_ACTION_TYPE_UNSPECIFIED - RAC_CUA_ACTION_UNKNOWN */
  CUA_ACTION_TYPE_UNSPECIFIED = 0,
  CUA_ACTION_TYPE_LEFT_CLICK = 1,
  CUA_ACTION_TYPE_RIGHT_CLICK = 2,
  CUA_ACTION_TYPE_DOUBLE_CLICK = 3,
  CUA_ACTION_TYPE_TRIPLE_CLICK = 4,
  CUA_ACTION_TYPE_MOUSE_MOVE = 5,
  CUA_ACTION_TYPE_LEFT_CLICK_DRAG = 6,
  CUA_ACTION_TYPE_TYPE = 7,
  CUA_ACTION_TYPE_KEY = 8,
  CUA_ACTION_TYPE_SCROLL = 9,
  CUA_ACTION_TYPE_HSCROLL = 10,
  CUA_ACTION_TYPE_VISIT_URL = 11,
  CUA_ACTION_TYPE_HISTORY_BACK = 12,
  CUA_ACTION_TYPE_WEB_SEARCH = 13,
  CUA_ACTION_TYPE_READ_PAGE_ANSWER = 14,
  CUA_ACTION_TYPE_PAUSE_MEMORIZE = 15,
  CUA_ACTION_TYPE_ASK_USER = 16,
  CUA_ACTION_TYPE_WAIT = 17,
  CUA_ACTION_TYPE_TERMINATE = 18,
  UNRECOGNIZED = -1,
}

export function cuaActionTypeFromJSON(object: any): CuaActionType {
  switch (object) {
    case 0:
    case "CUA_ACTION_TYPE_UNSPECIFIED":
      return CuaActionType.CUA_ACTION_TYPE_UNSPECIFIED;
    case 1:
    case "CUA_ACTION_TYPE_LEFT_CLICK":
      return CuaActionType.CUA_ACTION_TYPE_LEFT_CLICK;
    case 2:
    case "CUA_ACTION_TYPE_RIGHT_CLICK":
      return CuaActionType.CUA_ACTION_TYPE_RIGHT_CLICK;
    case 3:
    case "CUA_ACTION_TYPE_DOUBLE_CLICK":
      return CuaActionType.CUA_ACTION_TYPE_DOUBLE_CLICK;
    case 4:
    case "CUA_ACTION_TYPE_TRIPLE_CLICK":
      return CuaActionType.CUA_ACTION_TYPE_TRIPLE_CLICK;
    case 5:
    case "CUA_ACTION_TYPE_MOUSE_MOVE":
      return CuaActionType.CUA_ACTION_TYPE_MOUSE_MOVE;
    case 6:
    case "CUA_ACTION_TYPE_LEFT_CLICK_DRAG":
      return CuaActionType.CUA_ACTION_TYPE_LEFT_CLICK_DRAG;
    case 7:
    case "CUA_ACTION_TYPE_TYPE":
      return CuaActionType.CUA_ACTION_TYPE_TYPE;
    case 8:
    case "CUA_ACTION_TYPE_KEY":
      return CuaActionType.CUA_ACTION_TYPE_KEY;
    case 9:
    case "CUA_ACTION_TYPE_SCROLL":
      return CuaActionType.CUA_ACTION_TYPE_SCROLL;
    case 10:
    case "CUA_ACTION_TYPE_HSCROLL":
      return CuaActionType.CUA_ACTION_TYPE_HSCROLL;
    case 11:
    case "CUA_ACTION_TYPE_VISIT_URL":
      return CuaActionType.CUA_ACTION_TYPE_VISIT_URL;
    case 12:
    case "CUA_ACTION_TYPE_HISTORY_BACK":
      return CuaActionType.CUA_ACTION_TYPE_HISTORY_BACK;
    case 13:
    case "CUA_ACTION_TYPE_WEB_SEARCH":
      return CuaActionType.CUA_ACTION_TYPE_WEB_SEARCH;
    case 14:
    case "CUA_ACTION_TYPE_READ_PAGE_ANSWER":
      return CuaActionType.CUA_ACTION_TYPE_READ_PAGE_ANSWER;
    case 15:
    case "CUA_ACTION_TYPE_PAUSE_MEMORIZE":
      return CuaActionType.CUA_ACTION_TYPE_PAUSE_MEMORIZE;
    case 16:
    case "CUA_ACTION_TYPE_ASK_USER":
      return CuaActionType.CUA_ACTION_TYPE_ASK_USER;
    case 17:
    case "CUA_ACTION_TYPE_WAIT":
      return CuaActionType.CUA_ACTION_TYPE_WAIT;
    case 18:
    case "CUA_ACTION_TYPE_TERMINATE":
      return CuaActionType.CUA_ACTION_TYPE_TERMINATE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CuaActionType.UNRECOGNIZED;
  }
}

export function cuaActionTypeToJSON(object: CuaActionType): string {
  switch (object) {
    case CuaActionType.CUA_ACTION_TYPE_UNSPECIFIED:
      return "CUA_ACTION_TYPE_UNSPECIFIED";
    case CuaActionType.CUA_ACTION_TYPE_LEFT_CLICK:
      return "CUA_ACTION_TYPE_LEFT_CLICK";
    case CuaActionType.CUA_ACTION_TYPE_RIGHT_CLICK:
      return "CUA_ACTION_TYPE_RIGHT_CLICK";
    case CuaActionType.CUA_ACTION_TYPE_DOUBLE_CLICK:
      return "CUA_ACTION_TYPE_DOUBLE_CLICK";
    case CuaActionType.CUA_ACTION_TYPE_TRIPLE_CLICK:
      return "CUA_ACTION_TYPE_TRIPLE_CLICK";
    case CuaActionType.CUA_ACTION_TYPE_MOUSE_MOVE:
      return "CUA_ACTION_TYPE_MOUSE_MOVE";
    case CuaActionType.CUA_ACTION_TYPE_LEFT_CLICK_DRAG:
      return "CUA_ACTION_TYPE_LEFT_CLICK_DRAG";
    case CuaActionType.CUA_ACTION_TYPE_TYPE:
      return "CUA_ACTION_TYPE_TYPE";
    case CuaActionType.CUA_ACTION_TYPE_KEY:
      return "CUA_ACTION_TYPE_KEY";
    case CuaActionType.CUA_ACTION_TYPE_SCROLL:
      return "CUA_ACTION_TYPE_SCROLL";
    case CuaActionType.CUA_ACTION_TYPE_HSCROLL:
      return "CUA_ACTION_TYPE_HSCROLL";
    case CuaActionType.CUA_ACTION_TYPE_VISIT_URL:
      return "CUA_ACTION_TYPE_VISIT_URL";
    case CuaActionType.CUA_ACTION_TYPE_HISTORY_BACK:
      return "CUA_ACTION_TYPE_HISTORY_BACK";
    case CuaActionType.CUA_ACTION_TYPE_WEB_SEARCH:
      return "CUA_ACTION_TYPE_WEB_SEARCH";
    case CuaActionType.CUA_ACTION_TYPE_READ_PAGE_ANSWER:
      return "CUA_ACTION_TYPE_READ_PAGE_ANSWER";
    case CuaActionType.CUA_ACTION_TYPE_PAUSE_MEMORIZE:
      return "CUA_ACTION_TYPE_PAUSE_MEMORIZE";
    case CuaActionType.CUA_ACTION_TYPE_ASK_USER:
      return "CUA_ACTION_TYPE_ASK_USER";
    case CuaActionType.CUA_ACTION_TYPE_WAIT:
      return "CUA_ACTION_TYPE_WAIT";
    case CuaActionType.CUA_ACTION_TYPE_TERMINATE:
      return "CUA_ACTION_TYPE_TERMINATE";
    case CuaActionType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * A single parsed CUA action. Coordinates are already scaled to the caller's
 * viewport. `text` is the action's primary string argument, keyed by `type`:
 * TYPE->text, VISIT_URL->url, WEB_SEARCH->query, TERMINATE->answer,
 * ASK_USER/READ_PAGE_ANSWER->question, PAUSE_MEMORIZE->fact, KEY->space-joined
 * keys. `reasoning` holds any chain-of-thought preceding the tool_call.
 *
 * COORDINATE CONTRACT: x/y are integers in the SAME pixel space as the
 * viewport you passed to parse_action, origin at the TOP-LEFT. That viewport
 * must be the pixel dimensions of the exact image you handed to the VLM — if
 * you downscaled the screenshot before sending it, pass the downscaled
 * dimensions. On a DPR-2/3/4 display, passing logical points while sending a
 * physical-pixel screenshot offsets every click by that factor, silently (see
 * RunanywhereAI/runanywhere-ios ComputerUseAgentViewModel.swift for the correct
 * computation). parse_action has already rescaled out of the profile's own
 * space (1000x1000 for `fara`), so no further scaling is ever correct.
 *
 * LEFT_CLICK_DRAG: x/y are the drag DESTINATION only. Fara emits no origin (it
 * drags from the current cursor), and a touch screen has no cursor, so the
 * HOST must supply the press point — typically the last MOUSE_MOVE target.
 *
 * LENGTH: `text` and `reasoning` are TRUNCATED at 2047 bytes on a UTF-8 lead
 * byte by the fixed C buffers behind them (rac_cua_action_t.text[2048]); no
 * field records that truncation happened. This also caps a TERMINATE answer.
 */
export interface CuaAction {
  type: CuaActionType;
  /** viewport pixels from the LEFT edge; presence = "has a coordinate" */
  x?:
    | number
    | undefined;
  /** viewport pixels from the TOP edge */
  y?:
    | number
    | undefined;
  /**
   * HSCROLL/SCROLL axis split. Value is the model's raw `pixels` output,
   * copied verbatim per axis — the sign is UNVERIFIED against any real
   * device trace, so no direction convention is asserted here.
   */
  scrollX: number;
  /** SCROLL */
  scrollY: number;
  /**
   * WAIT: fractional seconds. Clamped by commons to [0, 100] because the
   * value comes from untrusted model output; an unbounded parse would wedge
   * the agent loop. 100s is a RunAnywhere-chosen ceiling, not inherited from
   * any vendor API.
   */
  waitSeconds: number;
  /** primary string arg (see above) */
  text: string;
  /** CoT before the tool_call, if any */
  reasoning: string;
  /** true if a valid tool_call was found */
  isValid: boolean;
}

function createBaseCuaAction(): CuaAction {
  return {
    type: 0,
    x: undefined,
    y: undefined,
    scrollX: 0,
    scrollY: 0,
    waitSeconds: 0,
    text: "",
    reasoning: "",
    isValid: false,
  };
}

export const CuaAction: MessageFns<CuaAction> = {
  encode(message: CuaAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    if (message.x !== undefined) {
      writer.uint32(16).int32(message.x);
    }
    if (message.y !== undefined) {
      writer.uint32(24).int32(message.y);
    }
    if (message.scrollX !== 0) {
      writer.uint32(32).int32(message.scrollX);
    }
    if (message.scrollY !== 0) {
      writer.uint32(40).int32(message.scrollY);
    }
    if (message.waitSeconds !== 0) {
      writer.uint32(49).double(message.waitSeconds);
    }
    if (message.text !== "") {
      writer.uint32(58).string(message.text);
    }
    if (message.reasoning !== "") {
      writer.uint32(66).string(message.reasoning);
    }
    if (message.isValid !== false) {
      writer.uint32(72).bool(message.isValid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CuaAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCuaAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.x = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.y = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.scrollX = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.scrollY = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 49) {
            break;
          }

          message.waitSeconds = reader.double();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.reasoning = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.isValid = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CuaAction {
    return {
      type: isSet(object.type) ? cuaActionTypeFromJSON(object.type) : 0,
      x: isSet(object.x) ? globalThis.Number(object.x) : undefined,
      y: isSet(object.y) ? globalThis.Number(object.y) : undefined,
      scrollX: isSet(object.scrollX)
        ? globalThis.Number(object.scrollX)
        : isSet(object.scroll_x)
        ? globalThis.Number(object.scroll_x)
        : 0,
      scrollY: isSet(object.scrollY)
        ? globalThis.Number(object.scrollY)
        : isSet(object.scroll_y)
        ? globalThis.Number(object.scroll_y)
        : 0,
      waitSeconds: isSet(object.waitSeconds)
        ? globalThis.Number(object.waitSeconds)
        : isSet(object.wait_seconds)
        ? globalThis.Number(object.wait_seconds)
        : 0,
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      reasoning: isSet(object.reasoning) ? globalThis.String(object.reasoning) : "",
      isValid: isSet(object.isValid)
        ? globalThis.Boolean(object.isValid)
        : isSet(object.is_valid)
        ? globalThis.Boolean(object.is_valid)
        : false,
    };
  },

  toJSON(message: CuaAction): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = cuaActionTypeToJSON(message.type);
    }
    if (message.x !== undefined) {
      obj.x = Math.round(message.x);
    }
    if (message.y !== undefined) {
      obj.y = Math.round(message.y);
    }
    if (message.scrollX !== 0) {
      obj.scrollX = Math.round(message.scrollX);
    }
    if (message.scrollY !== 0) {
      obj.scrollY = Math.round(message.scrollY);
    }
    if (message.waitSeconds !== 0) {
      obj.waitSeconds = message.waitSeconds;
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.reasoning !== "") {
      obj.reasoning = message.reasoning;
    }
    if (message.isValid !== false) {
      obj.isValid = message.isValid;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CuaAction>, I>>(base?: I): CuaAction {
    return CuaAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CuaAction>, I>>(object: I): CuaAction {
    const message = createBaseCuaAction();
    message.type = object.type ?? 0;
    message.x = object.x ?? undefined;
    message.y = object.y ?? undefined;
    message.scrollX = object.scrollX ?? 0;
    message.scrollY = object.scrollY ?? 0;
    message.waitSeconds = object.waitSeconds ?? 0;
    message.text = object.text ?? "";
    message.reasoning = object.reasoning ?? "";
    message.isValid = object.isValid ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
