/**
 * `@runanywhere/electron-llamacpp` — LlamaCPP backend for the Electron SDK.
 *
 * Peer-depends on `@runanywhere/electron`. The app declares this package; core
 * does not optionalDepend on it.
 */

export { LlamaCPP } from './LlamaCPP';
export type { LlamaCPPRegisterOptions } from './LlamaCPP';
