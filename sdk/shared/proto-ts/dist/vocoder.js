"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.35.1
// source: vocoder.proto
Object.defineProperty(exports, "__esModule", { value: true });
exports.VocoderResult = exports.VocoderRequest = exports.protobufPackage = void 0;
/* eslint-disable */
const wire_1 = require("@bufbuild/protobuf/wire");
exports.protobufPackage = "runanywhere.v1";
function createBaseVocoderRequest() {
    return { melSpectrogramF32Le: new Uint8Array(0), batchSize: 0, melBinCount: 0, frameCount: 0 };
}
exports.VocoderRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.melSpectrogramF32Le.length !== 0) {
            writer.uint32(10).bytes(message.melSpectrogramF32Le);
        }
        if (message.batchSize !== 0) {
            writer.uint32(16).uint32(message.batchSize);
        }
        if (message.melBinCount !== 0) {
            writer.uint32(24).uint32(message.melBinCount);
        }
        if (message.frameCount !== 0) {
            writer.uint32(32).uint32(message.frameCount);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseVocoderRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.melSpectrogramF32Le = reader.bytes();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.batchSize = reader.uint32();
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.melBinCount = reader.uint32();
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.frameCount = reader.uint32();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            melSpectrogramF32Le: isSet(object.melSpectrogramF32Le)
                ? bytesFromBase64(object.melSpectrogramF32Le)
                : isSet(object.mel_spectrogram_f32_le)
                    ? bytesFromBase64(object.mel_spectrogram_f32_le)
                    : new Uint8Array(0),
            batchSize: isSet(object.batchSize)
                ? globalThis.Number(object.batchSize)
                : isSet(object.batch_size)
                    ? globalThis.Number(object.batch_size)
                    : 0,
            melBinCount: isSet(object.melBinCount)
                ? globalThis.Number(object.melBinCount)
                : isSet(object.mel_bin_count)
                    ? globalThis.Number(object.mel_bin_count)
                    : 0,
            frameCount: isSet(object.frameCount)
                ? globalThis.Number(object.frameCount)
                : isSet(object.frame_count)
                    ? globalThis.Number(object.frame_count)
                    : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.melSpectrogramF32Le.length !== 0) {
            obj.melSpectrogramF32Le = base64FromBytes(message.melSpectrogramF32Le);
        }
        if (message.batchSize !== 0) {
            obj.batchSize = Math.round(message.batchSize);
        }
        if (message.melBinCount !== 0) {
            obj.melBinCount = Math.round(message.melBinCount);
        }
        if (message.frameCount !== 0) {
            obj.frameCount = Math.round(message.frameCount);
        }
        return obj;
    },
    create(base) {
        return exports.VocoderRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseVocoderRequest();
        message.melSpectrogramF32Le = object.melSpectrogramF32Le ?? new Uint8Array(0);
        message.batchSize = object.batchSize ?? 0;
        message.melBinCount = object.melBinCount ?? 0;
        message.frameCount = object.frameCount ?? 0;
        return message;
    },
};
function createBaseVocoderResult() {
    return {
        samplesF32Le: new Uint8Array(0),
        batchSize: 0,
        channelCount: 0,
        sampleCount: 0,
        sampleRateHz: 0,
        hopLength: 0,
        processingTimeMs: 0,
        modelId: "",
    };
}
exports.VocoderResult = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.samplesF32Le.length !== 0) {
            writer.uint32(10).bytes(message.samplesF32Le);
        }
        if (message.batchSize !== 0) {
            writer.uint32(16).uint32(message.batchSize);
        }
        if (message.channelCount !== 0) {
            writer.uint32(24).uint32(message.channelCount);
        }
        if (message.sampleCount !== 0) {
            writer.uint32(32).uint32(message.sampleCount);
        }
        if (message.sampleRateHz !== 0) {
            writer.uint32(40).uint32(message.sampleRateHz);
        }
        if (message.hopLength !== 0) {
            writer.uint32(48).uint32(message.hopLength);
        }
        if (message.processingTimeMs !== 0) {
            writer.uint32(56).int64(message.processingTimeMs);
        }
        if (message.modelId !== "") {
            writer.uint32(66).string(message.modelId);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseVocoderResult();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.samplesF32Le = reader.bytes();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.batchSize = reader.uint32();
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.channelCount = reader.uint32();
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.sampleCount = reader.uint32();
                    continue;
                }
                case 5: {
                    if (tag !== 40) {
                        break;
                    }
                    message.sampleRateHz = reader.uint32();
                    continue;
                }
                case 6: {
                    if (tag !== 48) {
                        break;
                    }
                    message.hopLength = reader.uint32();
                    continue;
                }
                case 7: {
                    if (tag !== 56) {
                        break;
                    }
                    message.processingTimeMs = longToNumber(reader.int64());
                    continue;
                }
                case 8: {
                    if (tag !== 66) {
                        break;
                    }
                    message.modelId = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            samplesF32Le: isSet(object.samplesF32Le)
                ? bytesFromBase64(object.samplesF32Le)
                : isSet(object.samples_f32_le)
                    ? bytesFromBase64(object.samples_f32_le)
                    : new Uint8Array(0),
            batchSize: isSet(object.batchSize)
                ? globalThis.Number(object.batchSize)
                : isSet(object.batch_size)
                    ? globalThis.Number(object.batch_size)
                    : 0,
            channelCount: isSet(object.channelCount)
                ? globalThis.Number(object.channelCount)
                : isSet(object.channel_count)
                    ? globalThis.Number(object.channel_count)
                    : 0,
            sampleCount: isSet(object.sampleCount)
                ? globalThis.Number(object.sampleCount)
                : isSet(object.sample_count)
                    ? globalThis.Number(object.sample_count)
                    : 0,
            sampleRateHz: isSet(object.sampleRateHz)
                ? globalThis.Number(object.sampleRateHz)
                : isSet(object.sample_rate_hz)
                    ? globalThis.Number(object.sample_rate_hz)
                    : 0,
            hopLength: isSet(object.hopLength)
                ? globalThis.Number(object.hopLength)
                : isSet(object.hop_length)
                    ? globalThis.Number(object.hop_length)
                    : 0,
            processingTimeMs: isSet(object.processingTimeMs)
                ? globalThis.Number(object.processingTimeMs)
                : isSet(object.processing_time_ms)
                    ? globalThis.Number(object.processing_time_ms)
                    : 0,
            modelId: isSet(object.modelId)
                ? globalThis.String(object.modelId)
                : isSet(object.model_id)
                    ? globalThis.String(object.model_id)
                    : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.samplesF32Le.length !== 0) {
            obj.samplesF32Le = base64FromBytes(message.samplesF32Le);
        }
        if (message.batchSize !== 0) {
            obj.batchSize = Math.round(message.batchSize);
        }
        if (message.channelCount !== 0) {
            obj.channelCount = Math.round(message.channelCount);
        }
        if (message.sampleCount !== 0) {
            obj.sampleCount = Math.round(message.sampleCount);
        }
        if (message.sampleRateHz !== 0) {
            obj.sampleRateHz = Math.round(message.sampleRateHz);
        }
        if (message.hopLength !== 0) {
            obj.hopLength = Math.round(message.hopLength);
        }
        if (message.processingTimeMs !== 0) {
            obj.processingTimeMs = Math.round(message.processingTimeMs);
        }
        if (message.modelId !== "") {
            obj.modelId = message.modelId;
        }
        return obj;
    },
    create(base) {
        return exports.VocoderResult.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseVocoderResult();
        message.samplesF32Le = object.samplesF32Le ?? new Uint8Array(0);
        message.batchSize = object.batchSize ?? 0;
        message.channelCount = object.channelCount ?? 0;
        message.sampleCount = object.sampleCount ?? 0;
        message.sampleRateHz = object.sampleRateHz ?? 0;
        message.hopLength = object.hopLength ?? 0;
        message.processingTimeMs = object.processingTimeMs ?? 0;
        message.modelId = object.modelId ?? "";
        return message;
    },
};
function bytesFromBase64(b64) {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
        arr[i] = bin.charCodeAt(i);
    }
    return arr;
}
function base64FromBytes(arr) {
    const bin = [];
    arr.forEach((byte) => {
        bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
}
function longToNumber(int64) {
    const num = globalThis.Number(int64.toString());
    if (num > globalThis.Number.MAX_SAFE_INTEGER) {
        throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
    }
    if (num < globalThis.Number.MIN_SAFE_INTEGER) {
        throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
    }
    return num;
}
function isSet(value) {
    return value !== null && value !== undefined;
}
