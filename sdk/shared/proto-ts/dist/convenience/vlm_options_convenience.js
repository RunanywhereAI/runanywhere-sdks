"use strict";
// GENERATED by idl/codegen/generate_ts_convenience.py — DO NOT EDIT.
//
// Source: idl/vlm_options.proto
//
// Convenience helpers derived from RunAnywhere's `rac_*` proto
// annotations (see idl/rac_options.proto):
//   * `<enumName>WireString`         (rac_wire_string)
//   * `<enumName>FromWireString`     (reverse of rac_wire_string)
//   * `<enumName>DisplayName`        (rac_display_name)
//   * `<enumName>AnalyticsKey`       (rac_analytics_key)
//   * `<msgName>Defaults`            (rac_default)
//   * `validate<MsgName>`            (rac_required / rac_min / rac_max /
//                                     rac_min_float / rac_max_float)
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateVLMGenerationOptions = exports.vLMGenerationOptionsDefaults = void 0;
const _errors_1 = require("./_errors");
const vLMGenerationOptionsDefaults = () => ({
    prompt: '',
    maxTokens: 2048,
    temperature: 0.7,
    topP: 0.9,
    topK: 0,
    stopSequences: [],
    streamingEnabled: true,
    maxImageSize: 0,
    nThreads: 0,
    useGpu: true,
    modelFamily: 0,
    seed: 0,
    repetitionPenalty: 1.1,
    minP: 0,
    emitImageEmbeddings: false,
});
exports.vLMGenerationOptionsDefaults = vLMGenerationOptionsDefaults;
const validateVLMGenerationOptions = (m) => {
    if (m.maxTokens < 0) {
        throw new _errors_1.ValidationError({
            fieldPath: 'VLMGenerationOptions.max_tokens',
            message: `max_tokens must be >= 0 (got ${m.maxTokens})`,
        });
    }
    if (!Number.isFinite(m.temperature) || m.temperature < 0.0 || m.temperature > 2.0) {
        throw new _errors_1.ValidationError({
            fieldPath: 'VLMGenerationOptions.temperature',
            message: `temperature must be in 0.0...2.0 (got ${m.temperature})`,
        });
    }
    if (!Number.isFinite(m.topP) || m.topP < 0.0 || m.topP > 1.0) {
        throw new _errors_1.ValidationError({
            fieldPath: 'VLMGenerationOptions.top_p',
            message: `top_p must be in 0.0...1.0 (got ${m.topP})`,
        });
    }
    if (m.topK < 0) {
        throw new _errors_1.ValidationError({
            fieldPath: 'VLMGenerationOptions.top_k',
            message: `top_k must be >= 0 (got ${m.topK})`,
        });
    }
    if (!Number.isFinite(m.repetitionPenalty) || m.repetitionPenalty < 0.0) {
        throw new _errors_1.ValidationError({
            fieldPath: 'VLMGenerationOptions.repetition_penalty',
            message: `repetition_penalty must be >= 0.0 (got ${m.repetitionPenalty})`,
        });
    }
};
exports.validateVLMGenerationOptions = validateVLMGenerationOptions;
