"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.35.1
// source: segmentation.proto
Object.defineProperty(exports, "__esModule", { value: true });
exports.SegmentationResult = exports.SegmentationClassSummary = exports.SegmentationRequest = exports.SegmentationOptions = exports.SegmentationImage = exports.SegmentationPixelFormat = exports.protobufPackage = void 0;
exports.segmentationPixelFormatFromJSON = segmentationPixelFormatFromJSON;
exports.segmentationPixelFormatToJSON = segmentationPixelFormatToJSON;
/* eslint-disable */
const wire_1 = require("@bufbuild/protobuf/wire");
exports.protobufPackage = "runanywhere.v1";
/**
 * Packed 8-bit pixel layouts accepted at the SDK boundary. Rows are tightly
 * packed; callers must not include per-row padding.
 */
var SegmentationPixelFormat;
(function (SegmentationPixelFormat) {
    SegmentationPixelFormat[SegmentationPixelFormat["SEGMENTATION_PIXEL_FORMAT_UNSPECIFIED"] = 0] = "SEGMENTATION_PIXEL_FORMAT_UNSPECIFIED";
    SegmentationPixelFormat[SegmentationPixelFormat["SEGMENTATION_PIXEL_FORMAT_RGB8"] = 1] = "SEGMENTATION_PIXEL_FORMAT_RGB8";
    SegmentationPixelFormat[SegmentationPixelFormat["SEGMENTATION_PIXEL_FORMAT_RGBA8"] = 2] = "SEGMENTATION_PIXEL_FORMAT_RGBA8";
    SegmentationPixelFormat[SegmentationPixelFormat["SEGMENTATION_PIXEL_FORMAT_BGRA8"] = 3] = "SEGMENTATION_PIXEL_FORMAT_BGRA8";
    SegmentationPixelFormat[SegmentationPixelFormat["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(SegmentationPixelFormat || (exports.SegmentationPixelFormat = SegmentationPixelFormat = {}));
function segmentationPixelFormatFromJSON(object) {
    switch (object) {
        case 0:
        case "SEGMENTATION_PIXEL_FORMAT_UNSPECIFIED":
            return SegmentationPixelFormat.SEGMENTATION_PIXEL_FORMAT_UNSPECIFIED;
        case 1:
        case "SEGMENTATION_PIXEL_FORMAT_RGB8":
            return SegmentationPixelFormat.SEGMENTATION_PIXEL_FORMAT_RGB8;
        case 2:
        case "SEGMENTATION_PIXEL_FORMAT_RGBA8":
            return SegmentationPixelFormat.SEGMENTATION_PIXEL_FORMAT_RGBA8;
        case 3:
        case "SEGMENTATION_PIXEL_FORMAT_BGRA8":
            return SegmentationPixelFormat.SEGMENTATION_PIXEL_FORMAT_BGRA8;
        case -1:
        case "UNRECOGNIZED":
        default:
            return SegmentationPixelFormat.UNRECOGNIZED;
    }
}
function segmentationPixelFormatToJSON(object) {
    switch (object) {
        case SegmentationPixelFormat.SEGMENTATION_PIXEL_FORMAT_UNSPECIFIED:
            return "SEGMENTATION_PIXEL_FORMAT_UNSPECIFIED";
        case SegmentationPixelFormat.SEGMENTATION_PIXEL_FORMAT_RGB8:
            return "SEGMENTATION_PIXEL_FORMAT_RGB8";
        case SegmentationPixelFormat.SEGMENTATION_PIXEL_FORMAT_RGBA8:
            return "SEGMENTATION_PIXEL_FORMAT_RGBA8";
        case SegmentationPixelFormat.SEGMENTATION_PIXEL_FORMAT_BGRA8:
            return "SEGMENTATION_PIXEL_FORMAT_BGRA8";
        case SegmentationPixelFormat.UNRECOGNIZED:
        default:
            return "UNRECOGNIZED";
    }
}
function createBaseSegmentationImage() {
    return { data: new Uint8Array(0), width: 0, height: 0, pixelFormat: 0 };
}
exports.SegmentationImage = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.data.length !== 0) {
            writer.uint32(10).bytes(message.data);
        }
        if (message.width !== 0) {
            writer.uint32(16).uint32(message.width);
        }
        if (message.height !== 0) {
            writer.uint32(24).uint32(message.height);
        }
        if (message.pixelFormat !== 0) {
            writer.uint32(32).int32(message.pixelFormat);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSegmentationImage();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.data = reader.bytes();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.width = reader.uint32();
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.height = reader.uint32();
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.pixelFormat = reader.int32();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
            width: isSet(object.width) ? globalThis.Number(object.width) : 0,
            height: isSet(object.height) ? globalThis.Number(object.height) : 0,
            pixelFormat: isSet(object.pixelFormat)
                ? segmentationPixelFormatFromJSON(object.pixelFormat)
                : isSet(object.pixel_format)
                    ? segmentationPixelFormatFromJSON(object.pixel_format)
                    : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.data.length !== 0) {
            obj.data = base64FromBytes(message.data);
        }
        if (message.width !== 0) {
            obj.width = Math.round(message.width);
        }
        if (message.height !== 0) {
            obj.height = Math.round(message.height);
        }
        if (message.pixelFormat !== 0) {
            obj.pixelFormat = segmentationPixelFormatToJSON(message.pixelFormat);
        }
        return obj;
    },
    create(base) {
        return exports.SegmentationImage.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSegmentationImage();
        message.data = object.data ?? new Uint8Array(0);
        message.width = object.width ?? 0;
        message.height = object.height ?? 0;
        message.pixelFormat = object.pixelFormat ?? 0;
        return message;
    },
};
function createBaseSegmentationOptions() {
    return { includeDiagnosticRgba: false };
}
exports.SegmentationOptions = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.includeDiagnosticRgba !== false) {
            writer.uint32(8).bool(message.includeDiagnosticRgba);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSegmentationOptions();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.includeDiagnosticRgba = reader.bool();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            includeDiagnosticRgba: isSet(object.includeDiagnosticRgba)
                ? globalThis.Boolean(object.includeDiagnosticRgba)
                : isSet(object.include_diagnostic_rgba)
                    ? globalThis.Boolean(object.include_diagnostic_rgba)
                    : false,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.includeDiagnosticRgba !== false) {
            obj.includeDiagnosticRgba = message.includeDiagnosticRgba;
        }
        return obj;
    },
    create(base) {
        return exports.SegmentationOptions.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSegmentationOptions();
        message.includeDiagnosticRgba = object.includeDiagnosticRgba ?? false;
        return message;
    },
};
function createBaseSegmentationRequest() {
    return { image: undefined, options: undefined };
}
exports.SegmentationRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.image !== undefined) {
            exports.SegmentationImage.encode(message.image, writer.uint32(10).fork()).join();
        }
        if (message.options !== undefined) {
            exports.SegmentationOptions.encode(message.options, writer.uint32(18).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSegmentationRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.image = exports.SegmentationImage.decode(reader, reader.uint32());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.options = exports.SegmentationOptions.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            image: isSet(object.image) ? exports.SegmentationImage.fromJSON(object.image) : undefined,
            options: isSet(object.options) ? exports.SegmentationOptions.fromJSON(object.options) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.image !== undefined) {
            obj.image = exports.SegmentationImage.toJSON(message.image);
        }
        if (message.options !== undefined) {
            obj.options = exports.SegmentationOptions.toJSON(message.options);
        }
        return obj;
    },
    create(base) {
        return exports.SegmentationRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSegmentationRequest();
        message.image = (object.image !== undefined && object.image !== null)
            ? exports.SegmentationImage.fromPartial(object.image)
            : undefined;
        message.options = (object.options !== undefined && object.options !== null)
            ? exports.SegmentationOptions.fromPartial(object.options)
            : undefined;
        return message;
    },
};
function createBaseSegmentationClassSummary() {
    return { classId: 0, pixelCount: 0, fraction: 0, label: "" };
}
exports.SegmentationClassSummary = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.classId !== 0) {
            writer.uint32(8).uint32(message.classId);
        }
        if (message.pixelCount !== 0) {
            writer.uint32(16).uint64(message.pixelCount);
        }
        if (message.fraction !== 0) {
            writer.uint32(29).float(message.fraction);
        }
        if (message.label !== "") {
            writer.uint32(34).string(message.label);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSegmentationClassSummary();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.classId = reader.uint32();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.pixelCount = longToNumber(reader.uint64());
                    continue;
                }
                case 3: {
                    if (tag !== 29) {
                        break;
                    }
                    message.fraction = reader.float();
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.label = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            classId: isSet(object.classId)
                ? globalThis.Number(object.classId)
                : isSet(object.class_id)
                    ? globalThis.Number(object.class_id)
                    : 0,
            pixelCount: isSet(object.pixelCount)
                ? globalThis.Number(object.pixelCount)
                : isSet(object.pixel_count)
                    ? globalThis.Number(object.pixel_count)
                    : 0,
            fraction: isSet(object.fraction) ? globalThis.Number(object.fraction) : 0,
            label: isSet(object.label) ? globalThis.String(object.label) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.classId !== 0) {
            obj.classId = Math.round(message.classId);
        }
        if (message.pixelCount !== 0) {
            obj.pixelCount = Math.round(message.pixelCount);
        }
        if (message.fraction !== 0) {
            obj.fraction = message.fraction;
        }
        if (message.label !== "") {
            obj.label = message.label;
        }
        return obj;
    },
    create(base) {
        return exports.SegmentationClassSummary.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSegmentationClassSummary();
        message.classId = object.classId ?? 0;
        message.pixelCount = object.pixelCount ?? 0;
        message.fraction = object.fraction ?? 0;
        message.label = object.label ?? "";
        return message;
    },
};
function createBaseSegmentationResult() {
    return {
        width: 0,
        height: 0,
        classMaskU16Le: new Uint8Array(0),
        diagnosticRgba: undefined,
        classSummaries: [],
        processingTimeMs: 0,
        modelId: "",
    };
}
exports.SegmentationResult = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.width !== 0) {
            writer.uint32(8).uint32(message.width);
        }
        if (message.height !== 0) {
            writer.uint32(16).uint32(message.height);
        }
        if (message.classMaskU16Le.length !== 0) {
            writer.uint32(26).bytes(message.classMaskU16Le);
        }
        if (message.diagnosticRgba !== undefined) {
            writer.uint32(34).bytes(message.diagnosticRgba);
        }
        for (const v of message.classSummaries) {
            exports.SegmentationClassSummary.encode(v, writer.uint32(42).fork()).join();
        }
        if (message.processingTimeMs !== 0) {
            writer.uint32(48).int64(message.processingTimeMs);
        }
        if (message.modelId !== "") {
            writer.uint32(58).string(message.modelId);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSegmentationResult();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.width = reader.uint32();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.height = reader.uint32();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.classMaskU16Le = reader.bytes();
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.diagnosticRgba = reader.bytes();
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.classSummaries.push(exports.SegmentationClassSummary.decode(reader, reader.uint32()));
                    continue;
                }
                case 6: {
                    if (tag !== 48) {
                        break;
                    }
                    message.processingTimeMs = longToNumber(reader.int64());
                    continue;
                }
                case 7: {
                    if (tag !== 58) {
                        break;
                    }
                    message.modelId = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            width: isSet(object.width) ? globalThis.Number(object.width) : 0,
            height: isSet(object.height) ? globalThis.Number(object.height) : 0,
            classMaskU16Le: isSet(object.classMaskU16Le)
                ? bytesFromBase64(object.classMaskU16Le)
                : isSet(object.class_mask_u16_le)
                    ? bytesFromBase64(object.class_mask_u16_le)
                    : new Uint8Array(0),
            diagnosticRgba: isSet(object.diagnosticRgba)
                ? bytesFromBase64(object.diagnosticRgba)
                : isSet(object.diagnostic_rgba)
                    ? bytesFromBase64(object.diagnostic_rgba)
                    : undefined,
            classSummaries: globalThis.Array.isArray(object?.classSummaries)
                ? object.classSummaries.map((e) => exports.SegmentationClassSummary.fromJSON(e))
                : globalThis.Array.isArray(object?.class_summaries)
                    ? object.class_summaries.map((e) => exports.SegmentationClassSummary.fromJSON(e))
                    : [],
            processingTimeMs: isSet(object.processingTimeMs)
                ? globalThis.Number(object.processingTimeMs)
                : isSet(object.processing_time_ms)
                    ? globalThis.Number(object.processing_time_ms)
                    : 0,
            modelId: isSet(object.modelId)
                ? globalThis.String(object.modelId)
                : isSet(object.model_id)
                    ? globalThis.String(object.model_id)
                    : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.width !== 0) {
            obj.width = Math.round(message.width);
        }
        if (message.height !== 0) {
            obj.height = Math.round(message.height);
        }
        if (message.classMaskU16Le.length !== 0) {
            obj.classMaskU16Le = base64FromBytes(message.classMaskU16Le);
        }
        if (message.diagnosticRgba !== undefined) {
            obj.diagnosticRgba = base64FromBytes(message.diagnosticRgba);
        }
        if (message.classSummaries?.length) {
            obj.classSummaries = message.classSummaries.map((e) => exports.SegmentationClassSummary.toJSON(e));
        }
        if (message.processingTimeMs !== 0) {
            obj.processingTimeMs = Math.round(message.processingTimeMs);
        }
        if (message.modelId !== "") {
            obj.modelId = message.modelId;
        }
        return obj;
    },
    create(base) {
        return exports.SegmentationResult.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSegmentationResult();
        message.width = object.width ?? 0;
        message.height = object.height ?? 0;
        message.classMaskU16Le = object.classMaskU16Le ?? new Uint8Array(0);
        message.diagnosticRgba = object.diagnosticRgba ?? undefined;
        message.classSummaries = object.classSummaries?.map((e) => exports.SegmentationClassSummary.fromPartial(e)) || [];
        message.processingTimeMs = object.processingTimeMs ?? 0;
        message.modelId = object.modelId ?? "";
        return message;
    },
};
function bytesFromBase64(b64) {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
        arr[i] = bin.charCodeAt(i);
    }
    return arr;
}
function base64FromBytes(arr) {
    const bin = [];
    arr.forEach((byte) => {
        bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
}
function longToNumber(int64) {
    const num = globalThis.Number(int64.toString());
    if (num > globalThis.Number.MAX_SAFE_INTEGER) {
        throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
    }
    if (num < globalThis.Number.MIN_SAFE_INTEGER) {
        throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
    }
    return num;
}
function isSet(value) {
    return value !== null && value !== undefined;
}
