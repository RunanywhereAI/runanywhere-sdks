"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.35.1
// source: rerank.proto
Object.defineProperty(exports, "__esModule", { value: true });
exports.RerankResult = exports.RerankScoredItem = exports.RerankRequest = exports.RerankOptions = exports.RerankCandidate = exports.protobufPackage = void 0;
/* eslint-disable */
const wire_1 = require("@bufbuild/protobuf/wire");
exports.protobufPackage = "runanywhere.v1";
function createBaseRerankCandidate() {
    return { id: "", text: "" };
}
exports.RerankCandidate = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.id !== "") {
            writer.uint32(10).string(message.id);
        }
        if (message.text !== "") {
            writer.uint32(18).string(message.text);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseRerankCandidate();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.id = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.text = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            id: isSet(object.id) ? globalThis.String(object.id) : "",
            text: isSet(object.text) ? globalThis.String(object.text) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.id !== "") {
            obj.id = message.id;
        }
        if (message.text !== "") {
            obj.text = message.text;
        }
        return obj;
    },
    create(base) {
        return exports.RerankCandidate.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseRerankCandidate();
        message.id = object.id ?? "";
        message.text = object.text ?? "";
        return message;
    },
};
function createBaseRerankOptions() {
    return { topN: 0 };
}
exports.RerankOptions = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.topN !== 0) {
            writer.uint32(8).uint32(message.topN);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseRerankOptions();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.topN = reader.uint32();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            topN: isSet(object.topN)
                ? globalThis.Number(object.topN)
                : isSet(object.top_n)
                    ? globalThis.Number(object.top_n)
                    : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.topN !== 0) {
            obj.topN = Math.round(message.topN);
        }
        return obj;
    },
    create(base) {
        return exports.RerankOptions.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseRerankOptions();
        message.topN = object.topN ?? 0;
        return message;
    },
};
function createBaseRerankRequest() {
    return { query: "", candidates: [], options: undefined };
}
exports.RerankRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.query !== "") {
            writer.uint32(10).string(message.query);
        }
        for (const v of message.candidates) {
            exports.RerankCandidate.encode(v, writer.uint32(18).fork()).join();
        }
        if (message.options !== undefined) {
            exports.RerankOptions.encode(message.options, writer.uint32(26).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseRerankRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.query = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.candidates.push(exports.RerankCandidate.decode(reader, reader.uint32()));
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.options = exports.RerankOptions.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            query: isSet(object.query) ? globalThis.String(object.query) : "",
            candidates: globalThis.Array.isArray(object?.candidates)
                ? object.candidates.map((e) => exports.RerankCandidate.fromJSON(e))
                : [],
            options: isSet(object.options) ? exports.RerankOptions.fromJSON(object.options) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.query !== "") {
            obj.query = message.query;
        }
        if (message.candidates?.length) {
            obj.candidates = message.candidates.map((e) => exports.RerankCandidate.toJSON(e));
        }
        if (message.options !== undefined) {
            obj.options = exports.RerankOptions.toJSON(message.options);
        }
        return obj;
    },
    create(base) {
        return exports.RerankRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseRerankRequest();
        message.query = object.query ?? "";
        message.candidates = object.candidates?.map((e) => exports.RerankCandidate.fromPartial(e)) || [];
        message.options = (object.options !== undefined && object.options !== null)
            ? exports.RerankOptions.fromPartial(object.options)
            : undefined;
        return message;
    },
};
function createBaseRerankScoredItem() {
    return { id: "", score: 0, originalIndex: 0, rank: 0 };
}
exports.RerankScoredItem = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.id !== "") {
            writer.uint32(10).string(message.id);
        }
        if (message.score !== 0) {
            writer.uint32(21).float(message.score);
        }
        if (message.originalIndex !== 0) {
            writer.uint32(24).uint32(message.originalIndex);
        }
        if (message.rank !== 0) {
            writer.uint32(32).uint32(message.rank);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseRerankScoredItem();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.id = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 21) {
                        break;
                    }
                    message.score = reader.float();
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.originalIndex = reader.uint32();
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.rank = reader.uint32();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            id: isSet(object.id) ? globalThis.String(object.id) : "",
            score: isSet(object.score) ? globalThis.Number(object.score) : 0,
            originalIndex: isSet(object.originalIndex)
                ? globalThis.Number(object.originalIndex)
                : isSet(object.original_index)
                    ? globalThis.Number(object.original_index)
                    : 0,
            rank: isSet(object.rank) ? globalThis.Number(object.rank) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.id !== "") {
            obj.id = message.id;
        }
        if (message.score !== 0) {
            obj.score = message.score;
        }
        if (message.originalIndex !== 0) {
            obj.originalIndex = Math.round(message.originalIndex);
        }
        if (message.rank !== 0) {
            obj.rank = Math.round(message.rank);
        }
        return obj;
    },
    create(base) {
        return exports.RerankScoredItem.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseRerankScoredItem();
        message.id = object.id ?? "";
        message.score = object.score ?? 0;
        message.originalIndex = object.originalIndex ?? 0;
        message.rank = object.rank ?? 0;
        return message;
    },
};
function createBaseRerankResult() {
    return { items: [], processingTimeMs: 0, modelId: "" };
}
exports.RerankResult = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        for (const v of message.items) {
            exports.RerankScoredItem.encode(v, writer.uint32(10).fork()).join();
        }
        if (message.processingTimeMs !== 0) {
            writer.uint32(16).int64(message.processingTimeMs);
        }
        if (message.modelId !== "") {
            writer.uint32(26).string(message.modelId);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseRerankResult();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.items.push(exports.RerankScoredItem.decode(reader, reader.uint32()));
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.processingTimeMs = longToNumber(reader.int64());
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.modelId = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            items: globalThis.Array.isArray(object?.items) ? object.items.map((e) => exports.RerankScoredItem.fromJSON(e)) : [],
            processingTimeMs: isSet(object.processingTimeMs)
                ? globalThis.Number(object.processingTimeMs)
                : isSet(object.processing_time_ms)
                    ? globalThis.Number(object.processing_time_ms)
                    : 0,
            modelId: isSet(object.modelId)
                ? globalThis.String(object.modelId)
                : isSet(object.model_id)
                    ? globalThis.String(object.model_id)
                    : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.items?.length) {
            obj.items = message.items.map((e) => exports.RerankScoredItem.toJSON(e));
        }
        if (message.processingTimeMs !== 0) {
            obj.processingTimeMs = Math.round(message.processingTimeMs);
        }
        if (message.modelId !== "") {
            obj.modelId = message.modelId;
        }
        return obj;
    },
    create(base) {
        return exports.RerankResult.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseRerankResult();
        message.items = object.items?.map((e) => exports.RerankScoredItem.fromPartial(e)) || [];
        message.processingTimeMs = object.processingTimeMs ?? 0;
        message.modelId = object.modelId ?? "";
        return message;
    },
};
function longToNumber(int64) {
    const num = globalThis.Number(int64.toString());
    if (num > globalThis.Number.MAX_SAFE_INTEGER) {
        throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
    }
    if (num < globalThis.Number.MIN_SAFE_INTEGER) {
        throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
    }
    return num;
}
function isSet(value) {
    return value !== null && value !== undefined;
}
