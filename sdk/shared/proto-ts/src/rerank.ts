// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.35.1
// source: rerank.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "runanywhere.v1";

/**
 * A single candidate document/passage to be scored against the query. The id is
 * caller-supplied and echoed back on the scored item so callers can correlate
 * results with their own records without relying on ordering.
 */
export interface RerankCandidate {
  id: string;
  text: string;
}

export interface RerankOptions {
  /**
   * When > 0, only the top_n highest-scoring candidates are returned (every
   * candidate is still scored). 0 = return all candidates, ranked.
   */
  topN: number;
}

export interface RerankRequest {
  query: string;
  candidates: RerankCandidate[];
  options?: RerankOptions | undefined;
}

export interface RerankScoredItem {
  /** Echo of RerankCandidate.id for correlation. */
  id: string;
  /**
   * Raw relevance score from the reranker (higher = more relevant). Not
   * normalized to a fixed range; comparable only within one result set.
   */
  score: number;
  /** Index of this candidate in the original RerankRequest.candidates list. */
  originalIndex: number;
  /** 0-based position after sorting by score descending (0 = most relevant). */
  rank: number;
}

export interface RerankResult {
  /**
   * Sorted by score descending. When RerankOptions.top_n > 0, truncated to the
   * top_n most relevant items.
   */
  items: RerankScoredItem[];
  processingTimeMs: number;
  modelId: string;
}

function createBaseRerankCandidate(): RerankCandidate {
  return { id: "", text: "" };
}

export const RerankCandidate: MessageFns<RerankCandidate> = {
  encode(message: RerankCandidate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.text !== "") {
      writer.uint32(18).string(message.text);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RerankCandidate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRerankCandidate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.text = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RerankCandidate {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      text: isSet(object.text) ? globalThis.String(object.text) : "",
    };
  },

  toJSON(message: RerankCandidate): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RerankCandidate>, I>>(base?: I): RerankCandidate {
    return RerankCandidate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RerankCandidate>, I>>(object: I): RerankCandidate {
    const message = createBaseRerankCandidate();
    message.id = object.id ?? "";
    message.text = object.text ?? "";
    return message;
  },
};

function createBaseRerankOptions(): RerankOptions {
  return { topN: 0 };
}

export const RerankOptions: MessageFns<RerankOptions> = {
  encode(message: RerankOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.topN !== 0) {
      writer.uint32(8).uint32(message.topN);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RerankOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRerankOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.topN = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RerankOptions {
    return {
      topN: isSet(object.topN)
        ? globalThis.Number(object.topN)
        : isSet(object.top_n)
        ? globalThis.Number(object.top_n)
        : 0,
    };
  },

  toJSON(message: RerankOptions): unknown {
    const obj: any = {};
    if (message.topN !== 0) {
      obj.topN = Math.round(message.topN);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RerankOptions>, I>>(base?: I): RerankOptions {
    return RerankOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RerankOptions>, I>>(object: I): RerankOptions {
    const message = createBaseRerankOptions();
    message.topN = object.topN ?? 0;
    return message;
  },
};

function createBaseRerankRequest(): RerankRequest {
  return { query: "", candidates: [], options: undefined };
}

export const RerankRequest: MessageFns<RerankRequest> = {
  encode(message: RerankRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== "") {
      writer.uint32(10).string(message.query);
    }
    for (const v of message.candidates) {
      RerankCandidate.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.options !== undefined) {
      RerankOptions.encode(message.options, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RerankRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRerankRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.candidates.push(RerankCandidate.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.options = RerankOptions.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RerankRequest {
    return {
      query: isSet(object.query) ? globalThis.String(object.query) : "",
      candidates: globalThis.Array.isArray(object?.candidates)
        ? object.candidates.map((e: any) => RerankCandidate.fromJSON(e))
        : [],
      options: isSet(object.options) ? RerankOptions.fromJSON(object.options) : undefined,
    };
  },

  toJSON(message: RerankRequest): unknown {
    const obj: any = {};
    if (message.query !== "") {
      obj.query = message.query;
    }
    if (message.candidates?.length) {
      obj.candidates = message.candidates.map((e) => RerankCandidate.toJSON(e));
    }
    if (message.options !== undefined) {
      obj.options = RerankOptions.toJSON(message.options);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RerankRequest>, I>>(base?: I): RerankRequest {
    return RerankRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RerankRequest>, I>>(object: I): RerankRequest {
    const message = createBaseRerankRequest();
    message.query = object.query ?? "";
    message.candidates = object.candidates?.map((e) => RerankCandidate.fromPartial(e)) || [];
    message.options = (object.options !== undefined && object.options !== null)
      ? RerankOptions.fromPartial(object.options)
      : undefined;
    return message;
  },
};

function createBaseRerankScoredItem(): RerankScoredItem {
  return { id: "", score: 0, originalIndex: 0, rank: 0 };
}

export const RerankScoredItem: MessageFns<RerankScoredItem> = {
  encode(message: RerankScoredItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.score !== 0) {
      writer.uint32(21).float(message.score);
    }
    if (message.originalIndex !== 0) {
      writer.uint32(24).uint32(message.originalIndex);
    }
    if (message.rank !== 0) {
      writer.uint32(32).uint32(message.rank);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RerankScoredItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRerankScoredItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 21) {
            break;
          }

          message.score = reader.float();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.originalIndex = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.rank = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RerankScoredItem {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      score: isSet(object.score) ? globalThis.Number(object.score) : 0,
      originalIndex: isSet(object.originalIndex)
        ? globalThis.Number(object.originalIndex)
        : isSet(object.original_index)
        ? globalThis.Number(object.original_index)
        : 0,
      rank: isSet(object.rank) ? globalThis.Number(object.rank) : 0,
    };
  },

  toJSON(message: RerankScoredItem): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.score !== 0) {
      obj.score = message.score;
    }
    if (message.originalIndex !== 0) {
      obj.originalIndex = Math.round(message.originalIndex);
    }
    if (message.rank !== 0) {
      obj.rank = Math.round(message.rank);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RerankScoredItem>, I>>(base?: I): RerankScoredItem {
    return RerankScoredItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RerankScoredItem>, I>>(object: I): RerankScoredItem {
    const message = createBaseRerankScoredItem();
    message.id = object.id ?? "";
    message.score = object.score ?? 0;
    message.originalIndex = object.originalIndex ?? 0;
    message.rank = object.rank ?? 0;
    return message;
  },
};

function createBaseRerankResult(): RerankResult {
  return { items: [], processingTimeMs: 0, modelId: "" };
}

export const RerankResult: MessageFns<RerankResult> = {
  encode(message: RerankResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      RerankScoredItem.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.processingTimeMs !== 0) {
      writer.uint32(16).int64(message.processingTimeMs);
    }
    if (message.modelId !== "") {
      writer.uint32(26).string(message.modelId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RerankResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRerankResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(RerankScoredItem.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.processingTimeMs = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RerankResult {
    return {
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => RerankScoredItem.fromJSON(e)) : [],
      processingTimeMs: isSet(object.processingTimeMs)
        ? globalThis.Number(object.processingTimeMs)
        : isSet(object.processing_time_ms)
        ? globalThis.Number(object.processing_time_ms)
        : 0,
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : "",
    };
  },

  toJSON(message: RerankResult): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => RerankScoredItem.toJSON(e));
    }
    if (message.processingTimeMs !== 0) {
      obj.processingTimeMs = Math.round(message.processingTimeMs);
    }
    if (message.modelId !== "") {
      obj.modelId = message.modelId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RerankResult>, I>>(base?: I): RerankResult {
    return RerankResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RerankResult>, I>>(object: I): RerankResult {
    const message = createBaseRerankResult();
    message.items = object.items?.map((e) => RerankScoredItem.fromPartial(e)) || [];
    message.processingTimeMs = object.processingTimeMs ?? 0;
    message.modelId = object.modelId ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
