// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.35.1
// source: connect.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { LLMGenerateRequest, LLMStreamEvent } from "./llm_service";

export const protobufPackage = "runanywhere.v1";

/**
 * Platform identity is explicit so commons can evaluate role availability
 * from one policy table. Platform SDKs must not hardcode the host/client
 * matrix in UI or transport code.
 */
export enum ConnectPlatform {
  CONNECT_PLATFORM_UNSPECIFIED = 0,
  CONNECT_PLATFORM_MACOS = 1,
  CONNECT_PLATFORM_IOS = 2,
  CONNECT_PLATFORM_IPADOS = 3,
  /**
   * CONNECT_PLATFORM_ANDROID - Reserved for the follow-on SDK integrations. Keeping the values in the
   * canonical IDL avoids a later wire-format migration.
   */
  CONNECT_PLATFORM_ANDROID = 4,
  CONNECT_PLATFORM_REACT_NATIVE = 5,
  CONNECT_PLATFORM_FLUTTER = 6,
  CONNECT_PLATFORM_WEB = 7,
  /**
   * CONNECT_PLATFORM_WINDOWS - Reserved now so adding the planned Windows host adapter does not require
   * a platform-identity wire migration. Its host role remains PLANNED until
   * the native transport, discovery, and protected-storage adapter ships.
   */
  CONNECT_PLATFORM_WINDOWS = 8,
  UNRECOGNIZED = -1,
}

export function connectPlatformFromJSON(object: any): ConnectPlatform {
  switch (object) {
    case 0:
    case "CONNECT_PLATFORM_UNSPECIFIED":
      return ConnectPlatform.CONNECT_PLATFORM_UNSPECIFIED;
    case 1:
    case "CONNECT_PLATFORM_MACOS":
      return ConnectPlatform.CONNECT_PLATFORM_MACOS;
    case 2:
    case "CONNECT_PLATFORM_IOS":
      return ConnectPlatform.CONNECT_PLATFORM_IOS;
    case 3:
    case "CONNECT_PLATFORM_IPADOS":
      return ConnectPlatform.CONNECT_PLATFORM_IPADOS;
    case 4:
    case "CONNECT_PLATFORM_ANDROID":
      return ConnectPlatform.CONNECT_PLATFORM_ANDROID;
    case 5:
    case "CONNECT_PLATFORM_REACT_NATIVE":
      return ConnectPlatform.CONNECT_PLATFORM_REACT_NATIVE;
    case 6:
    case "CONNECT_PLATFORM_FLUTTER":
      return ConnectPlatform.CONNECT_PLATFORM_FLUTTER;
    case 7:
    case "CONNECT_PLATFORM_WEB":
      return ConnectPlatform.CONNECT_PLATFORM_WEB;
    case 8:
    case "CONNECT_PLATFORM_WINDOWS":
      return ConnectPlatform.CONNECT_PLATFORM_WINDOWS;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ConnectPlatform.UNRECOGNIZED;
  }
}

export function connectPlatformToJSON(object: ConnectPlatform): string {
  switch (object) {
    case ConnectPlatform.CONNECT_PLATFORM_UNSPECIFIED:
      return "CONNECT_PLATFORM_UNSPECIFIED";
    case ConnectPlatform.CONNECT_PLATFORM_MACOS:
      return "CONNECT_PLATFORM_MACOS";
    case ConnectPlatform.CONNECT_PLATFORM_IOS:
      return "CONNECT_PLATFORM_IOS";
    case ConnectPlatform.CONNECT_PLATFORM_IPADOS:
      return "CONNECT_PLATFORM_IPADOS";
    case ConnectPlatform.CONNECT_PLATFORM_ANDROID:
      return "CONNECT_PLATFORM_ANDROID";
    case ConnectPlatform.CONNECT_PLATFORM_REACT_NATIVE:
      return "CONNECT_PLATFORM_REACT_NATIVE";
    case ConnectPlatform.CONNECT_PLATFORM_FLUTTER:
      return "CONNECT_PLATFORM_FLUTTER";
    case ConnectPlatform.CONNECT_PLATFORM_WEB:
      return "CONNECT_PLATFORM_WEB";
    case ConnectPlatform.CONNECT_PLATFORM_WINDOWS:
      return "CONNECT_PLATFORM_WINDOWS";
    case ConnectPlatform.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Role availability is richer than a boolean so the wire contract can reserve
 * planned platforms without accidentally advertising them as usable.
 */
export enum ConnectRoleAvailability {
  CONNECT_ROLE_AVAILABILITY_UNSPECIFIED = 0,
  CONNECT_ROLE_AVAILABILITY_DISABLED = 1,
  CONNECT_ROLE_AVAILABILITY_PLANNED = 2,
  CONNECT_ROLE_AVAILABILITY_ENABLED = 3,
  UNRECOGNIZED = -1,
}

export function connectRoleAvailabilityFromJSON(object: any): ConnectRoleAvailability {
  switch (object) {
    case 0:
    case "CONNECT_ROLE_AVAILABILITY_UNSPECIFIED":
      return ConnectRoleAvailability.CONNECT_ROLE_AVAILABILITY_UNSPECIFIED;
    case 1:
    case "CONNECT_ROLE_AVAILABILITY_DISABLED":
      return ConnectRoleAvailability.CONNECT_ROLE_AVAILABILITY_DISABLED;
    case 2:
    case "CONNECT_ROLE_AVAILABILITY_PLANNED":
      return ConnectRoleAvailability.CONNECT_ROLE_AVAILABILITY_PLANNED;
    case 3:
    case "CONNECT_ROLE_AVAILABILITY_ENABLED":
      return ConnectRoleAvailability.CONNECT_ROLE_AVAILABILITY_ENABLED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ConnectRoleAvailability.UNRECOGNIZED;
  }
}

export function connectRoleAvailabilityToJSON(object: ConnectRoleAvailability): string {
  switch (object) {
    case ConnectRoleAvailability.CONNECT_ROLE_AVAILABILITY_UNSPECIFIED:
      return "CONNECT_ROLE_AVAILABILITY_UNSPECIFIED";
    case ConnectRoleAvailability.CONNECT_ROLE_AVAILABILITY_DISABLED:
      return "CONNECT_ROLE_AVAILABILITY_DISABLED";
    case ConnectRoleAvailability.CONNECT_ROLE_AVAILABILITY_PLANNED:
      return "CONNECT_ROLE_AVAILABILITY_PLANNED";
    case ConnectRoleAvailability.CONNECT_ROLE_AVAILABILITY_ENABLED:
      return "CONNECT_ROLE_AVAILABILITY_ENABLED";
    case ConnectRoleAvailability.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ConnectHandshakeStatus {
  CONNECT_HANDSHAKE_STATUS_UNSPECIFIED = 0,
  CONNECT_HANDSHAKE_STATUS_ACCEPTED = 1,
  CONNECT_HANDSHAKE_STATUS_REJECTED = 2,
  UNRECOGNIZED = -1,
}

export function connectHandshakeStatusFromJSON(object: any): ConnectHandshakeStatus {
  switch (object) {
    case 0:
    case "CONNECT_HANDSHAKE_STATUS_UNSPECIFIED":
      return ConnectHandshakeStatus.CONNECT_HANDSHAKE_STATUS_UNSPECIFIED;
    case 1:
    case "CONNECT_HANDSHAKE_STATUS_ACCEPTED":
      return ConnectHandshakeStatus.CONNECT_HANDSHAKE_STATUS_ACCEPTED;
    case 2:
    case "CONNECT_HANDSHAKE_STATUS_REJECTED":
      return ConnectHandshakeStatus.CONNECT_HANDSHAKE_STATUS_REJECTED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ConnectHandshakeStatus.UNRECOGNIZED;
  }
}

export function connectHandshakeStatusToJSON(object: ConnectHandshakeStatus): string {
  switch (object) {
    case ConnectHandshakeStatus.CONNECT_HANDSHAKE_STATUS_UNSPECIFIED:
      return "CONNECT_HANDSHAKE_STATUS_UNSPECIFIED";
    case ConnectHandshakeStatus.CONNECT_HANDSHAKE_STATUS_ACCEPTED:
      return "CONNECT_HANDSHAKE_STATUS_ACCEPTED";
    case ConnectHandshakeStatus.CONNECT_HANDSHAKE_STATUS_REJECTED:
      return "CONNECT_HANDSHAKE_STATUS_REJECTED";
    case ConnectHandshakeStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ConnectSessionState {
  CONNECT_SESSION_STATE_UNSPECIFIED = 0,
  CONNECT_SESSION_STATE_CONNECTING = 1,
  CONNECT_SESSION_STATE_CONNECTED = 2,
  CONNECT_SESSION_STATE_DISCONNECTED = 3,
  CONNECT_SESSION_STATE_FAILED = 4,
  UNRECOGNIZED = -1,
}

export function connectSessionStateFromJSON(object: any): ConnectSessionState {
  switch (object) {
    case 0:
    case "CONNECT_SESSION_STATE_UNSPECIFIED":
      return ConnectSessionState.CONNECT_SESSION_STATE_UNSPECIFIED;
    case 1:
    case "CONNECT_SESSION_STATE_CONNECTING":
      return ConnectSessionState.CONNECT_SESSION_STATE_CONNECTING;
    case 2:
    case "CONNECT_SESSION_STATE_CONNECTED":
      return ConnectSessionState.CONNECT_SESSION_STATE_CONNECTED;
    case 3:
    case "CONNECT_SESSION_STATE_DISCONNECTED":
      return ConnectSessionState.CONNECT_SESSION_STATE_DISCONNECTED;
    case 4:
    case "CONNECT_SESSION_STATE_FAILED":
      return ConnectSessionState.CONNECT_SESSION_STATE_FAILED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ConnectSessionState.UNRECOGNIZED;
  }
}

export function connectSessionStateToJSON(object: ConnectSessionState): string {
  switch (object) {
    case ConnectSessionState.CONNECT_SESSION_STATE_UNSPECIFIED:
      return "CONNECT_SESSION_STATE_UNSPECIFIED";
    case ConnectSessionState.CONNECT_SESSION_STATE_CONNECTING:
      return "CONNECT_SESSION_STATE_CONNECTING";
    case ConnectSessionState.CONNECT_SESSION_STATE_CONNECTED:
      return "CONNECT_SESSION_STATE_CONNECTED";
    case ConnectSessionState.CONNECT_SESSION_STATE_DISCONNECTED:
      return "CONNECT_SESSION_STATE_DISCONNECTED";
    case ConnectSessionState.CONNECT_SESSION_STATE_FAILED:
      return "CONNECT_SESSION_STATE_FAILED";
    case ConnectSessionState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface ConnectPlatformPolicyRequest {
  platform: ConnectPlatform;
}

/**
 * Commons is the authority for this policy. SDKs may query it to shape UI,
 * but every host/client entrypoint also enforces it inside C++.
 */
export interface ConnectPlatformPolicy {
  platform: ConnectPlatform;
  hostRole: ConnectRoleAvailability;
  clientRole: ConnectRoleAvailability;
}

/**
 * Non-secret metadata published through LAN service discovery and echoed by
 * the handshake. `instance_id` is generated anew whenever the host starts;
 * it is not a persistent device identifier or a credential.
 */
export interface ConnectDiscoveryMetadata {
  instanceId: string;
  displayName: string;
  platform: ConnectPlatform;
  protocolVersion: number;
}

/**
 * The single language model currently shared by a host. A host must select a
 * loaded model before it starts publishing; this lets clients enter chat
 * immediately without downloading or selecting a local model.
 */
export interface ConnectModelDescriptor {
  modelId: string;
  displayName: string;
  framework: string;
  contextWindow: number;
  supportsStreaming: boolean;
}

export interface ConnectHostStartRequest {
  displayName: string;
  platform: ConnectPlatform;
  protocolVersion: number;
  model?: ConnectModelDescriptor | undefined;
}

export interface ConnectHostStopRequest {
}

export interface ConnectHostState {
  isHosting: boolean;
  discoveryMetadata?: ConnectDiscoveryMetadata | undefined;
  activeClientCount: number;
  errorMessage: string;
  model?: ConnectModelDescriptor | undefined;
}

export interface ConnectClientStartRequest {
  displayName: string;
  platform: ConnectPlatform;
  protocolVersion: number;
}

/** Sent by a client immediately after the platform transport is connected. */
export interface ConnectClientHello {
  instanceId: string;
  displayName: string;
  platform: ConnectPlatform;
  protocolVersion: number;
}

/** Sent by the host after commons has accepted or rejected a client hello. */
export interface ConnectHandshakeResponse {
  status: ConnectHandshakeStatus;
  sessionId: string;
  host?: ConnectDiscoveryMetadata | undefined;
  rejectionReason: string;
  model?: ConnectModelDescriptor | undefined;
}

/**
 * The client validates the host response through commons and receives the
 * public session state it can expose to its platform UI.
 */
export interface ConnectClientSessionState {
  state: ConnectSessionState;
  sessionId: string;
  host?: ConnectDiscoveryMetadata | undefined;
  errorMessage: string;
  model?: ConnectModelDescriptor | undefined;
}

export interface ConnectSessionCloseRequest {
  sessionId: string;
}

/**
 * A client sends the existing typed LLM request to the selected host model.
 * `session_id` binds the request to the prior handshake; `generation.model_id`
 * must match the model the host published for that session.
 */
export interface ConnectInvocationRequest {
  sessionId: string;
  requestId: string;
  generation?: LLMGenerateRequest | undefined;
}

/**
 * Commons validates that an invocation belongs to an active session and uses
 * the host's published model before any platform runtime receives the prompt.
 */
export interface ConnectInvocationValidation {
  accepted: boolean;
  rejectionReason: string;
}

/**
 * Hosts forward the SDK's canonical stream events without translating them to
 * a platform-specific token shape. This is the portable streaming surface for
 * future Kotlin, React Native, Flutter, and Web clients.
 */
export interface ConnectInvocationEvent {
  requestId: string;
  event?: LLMStreamEvent | undefined;
}

/**
 * The connection stays open between generations, so the client needs a
 * control-plane exchange that can detect a host stopped while chat is idle.
 * These frames deliberately remain separate from LLM invocation payloads:
 * a health check must never reach a model or appear as an assistant message.
 */
export interface ConnectHeartbeatRequest {
  sessionId: string;
  sequence: number;
}

export interface ConnectHeartbeatResponse {
  sessionId: string;
  sequence: number;
}

/**
 * Every frame after the initial ClientHello handshake is carried in one of
 * these explicit envelopes. This leaves typed inference traffic untouched
 * while allowing clients to verify an otherwise-idle host connection.
 */
export interface ConnectClientFrame {
  invocation?: ConnectInvocationRequest | undefined;
  heartbeat?: ConnectHeartbeatRequest | undefined;
}

export interface ConnectHostFrame {
  invocationEvent?: ConnectInvocationEvent | undefined;
  heartbeat?: ConnectHeartbeatResponse | undefined;
}

function createBaseConnectPlatformPolicyRequest(): ConnectPlatformPolicyRequest {
  return { platform: 0 };
}

export const ConnectPlatformPolicyRequest: MessageFns<ConnectPlatformPolicyRequest> = {
  encode(message: ConnectPlatformPolicyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.platform !== 0) {
      writer.uint32(8).int32(message.platform);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectPlatformPolicyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectPlatformPolicyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.platform = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectPlatformPolicyRequest {
    return { platform: isSet(object.platform) ? connectPlatformFromJSON(object.platform) : 0 };
  },

  toJSON(message: ConnectPlatformPolicyRequest): unknown {
    const obj: any = {};
    if (message.platform !== 0) {
      obj.platform = connectPlatformToJSON(message.platform);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectPlatformPolicyRequest>, I>>(base?: I): ConnectPlatformPolicyRequest {
    return ConnectPlatformPolicyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectPlatformPolicyRequest>, I>>(object: I): ConnectPlatformPolicyRequest {
    const message = createBaseConnectPlatformPolicyRequest();
    message.platform = object.platform ?? 0;
    return message;
  },
};

function createBaseConnectPlatformPolicy(): ConnectPlatformPolicy {
  return { platform: 0, hostRole: 0, clientRole: 0 };
}

export const ConnectPlatformPolicy: MessageFns<ConnectPlatformPolicy> = {
  encode(message: ConnectPlatformPolicy, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.platform !== 0) {
      writer.uint32(8).int32(message.platform);
    }
    if (message.hostRole !== 0) {
      writer.uint32(16).int32(message.hostRole);
    }
    if (message.clientRole !== 0) {
      writer.uint32(24).int32(message.clientRole);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectPlatformPolicy {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectPlatformPolicy();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.platform = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.hostRole = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.clientRole = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectPlatformPolicy {
    return {
      platform: isSet(object.platform) ? connectPlatformFromJSON(object.platform) : 0,
      hostRole: isSet(object.hostRole)
        ? connectRoleAvailabilityFromJSON(object.hostRole)
        : isSet(object.host_role)
        ? connectRoleAvailabilityFromJSON(object.host_role)
        : 0,
      clientRole: isSet(object.clientRole)
        ? connectRoleAvailabilityFromJSON(object.clientRole)
        : isSet(object.client_role)
        ? connectRoleAvailabilityFromJSON(object.client_role)
        : 0,
    };
  },

  toJSON(message: ConnectPlatformPolicy): unknown {
    const obj: any = {};
    if (message.platform !== 0) {
      obj.platform = connectPlatformToJSON(message.platform);
    }
    if (message.hostRole !== 0) {
      obj.hostRole = connectRoleAvailabilityToJSON(message.hostRole);
    }
    if (message.clientRole !== 0) {
      obj.clientRole = connectRoleAvailabilityToJSON(message.clientRole);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectPlatformPolicy>, I>>(base?: I): ConnectPlatformPolicy {
    return ConnectPlatformPolicy.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectPlatformPolicy>, I>>(object: I): ConnectPlatformPolicy {
    const message = createBaseConnectPlatformPolicy();
    message.platform = object.platform ?? 0;
    message.hostRole = object.hostRole ?? 0;
    message.clientRole = object.clientRole ?? 0;
    return message;
  },
};

function createBaseConnectDiscoveryMetadata(): ConnectDiscoveryMetadata {
  return { instanceId: "", displayName: "", platform: 0, protocolVersion: 0 };
}

export const ConnectDiscoveryMetadata: MessageFns<ConnectDiscoveryMetadata> = {
  encode(message: ConnectDiscoveryMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.instanceId !== "") {
      writer.uint32(10).string(message.instanceId);
    }
    if (message.displayName !== "") {
      writer.uint32(18).string(message.displayName);
    }
    if (message.platform !== 0) {
      writer.uint32(24).int32(message.platform);
    }
    if (message.protocolVersion !== 0) {
      writer.uint32(32).uint32(message.protocolVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectDiscoveryMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectDiscoveryMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.instanceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.platform = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.protocolVersion = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectDiscoveryMetadata {
    return {
      instanceId: isSet(object.instanceId)
        ? globalThis.String(object.instanceId)
        : isSet(object.instance_id)
        ? globalThis.String(object.instance_id)
        : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      platform: isSet(object.platform) ? connectPlatformFromJSON(object.platform) : 0,
      protocolVersion: isSet(object.protocolVersion)
        ? globalThis.Number(object.protocolVersion)
        : isSet(object.protocol_version)
        ? globalThis.Number(object.protocol_version)
        : 0,
    };
  },

  toJSON(message: ConnectDiscoveryMetadata): unknown {
    const obj: any = {};
    if (message.instanceId !== "") {
      obj.instanceId = message.instanceId;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.platform !== 0) {
      obj.platform = connectPlatformToJSON(message.platform);
    }
    if (message.protocolVersion !== 0) {
      obj.protocolVersion = Math.round(message.protocolVersion);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectDiscoveryMetadata>, I>>(base?: I): ConnectDiscoveryMetadata {
    return ConnectDiscoveryMetadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectDiscoveryMetadata>, I>>(object: I): ConnectDiscoveryMetadata {
    const message = createBaseConnectDiscoveryMetadata();
    message.instanceId = object.instanceId ?? "";
    message.displayName = object.displayName ?? "";
    message.platform = object.platform ?? 0;
    message.protocolVersion = object.protocolVersion ?? 0;
    return message;
  },
};

function createBaseConnectModelDescriptor(): ConnectModelDescriptor {
  return { modelId: "", displayName: "", framework: "", contextWindow: 0, supportsStreaming: false };
}

export const ConnectModelDescriptor: MessageFns<ConnectModelDescriptor> = {
  encode(message: ConnectModelDescriptor, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.modelId !== "") {
      writer.uint32(10).string(message.modelId);
    }
    if (message.displayName !== "") {
      writer.uint32(18).string(message.displayName);
    }
    if (message.framework !== "") {
      writer.uint32(26).string(message.framework);
    }
    if (message.contextWindow !== 0) {
      writer.uint32(32).uint32(message.contextWindow);
    }
    if (message.supportsStreaming !== false) {
      writer.uint32(40).bool(message.supportsStreaming);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectModelDescriptor {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectModelDescriptor();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.framework = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.contextWindow = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.supportsStreaming = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectModelDescriptor {
    return {
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      framework: isSet(object.framework) ? globalThis.String(object.framework) : "",
      contextWindow: isSet(object.contextWindow)
        ? globalThis.Number(object.contextWindow)
        : isSet(object.context_window)
        ? globalThis.Number(object.context_window)
        : 0,
      supportsStreaming: isSet(object.supportsStreaming)
        ? globalThis.Boolean(object.supportsStreaming)
        : isSet(object.supports_streaming)
        ? globalThis.Boolean(object.supports_streaming)
        : false,
    };
  },

  toJSON(message: ConnectModelDescriptor): unknown {
    const obj: any = {};
    if (message.modelId !== "") {
      obj.modelId = message.modelId;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.framework !== "") {
      obj.framework = message.framework;
    }
    if (message.contextWindow !== 0) {
      obj.contextWindow = Math.round(message.contextWindow);
    }
    if (message.supportsStreaming !== false) {
      obj.supportsStreaming = message.supportsStreaming;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectModelDescriptor>, I>>(base?: I): ConnectModelDescriptor {
    return ConnectModelDescriptor.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectModelDescriptor>, I>>(object: I): ConnectModelDescriptor {
    const message = createBaseConnectModelDescriptor();
    message.modelId = object.modelId ?? "";
    message.displayName = object.displayName ?? "";
    message.framework = object.framework ?? "";
    message.contextWindow = object.contextWindow ?? 0;
    message.supportsStreaming = object.supportsStreaming ?? false;
    return message;
  },
};

function createBaseConnectHostStartRequest(): ConnectHostStartRequest {
  return { displayName: "", platform: 0, protocolVersion: 0, model: undefined };
}

export const ConnectHostStartRequest: MessageFns<ConnectHostStartRequest> = {
  encode(message: ConnectHostStartRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.displayName !== "") {
      writer.uint32(10).string(message.displayName);
    }
    if (message.platform !== 0) {
      writer.uint32(16).int32(message.platform);
    }
    if (message.protocolVersion !== 0) {
      writer.uint32(24).uint32(message.protocolVersion);
    }
    if (message.model !== undefined) {
      ConnectModelDescriptor.encode(message.model, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectHostStartRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectHostStartRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.platform = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.protocolVersion = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.model = ConnectModelDescriptor.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectHostStartRequest {
    return {
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      platform: isSet(object.platform) ? connectPlatformFromJSON(object.platform) : 0,
      protocolVersion: isSet(object.protocolVersion)
        ? globalThis.Number(object.protocolVersion)
        : isSet(object.protocol_version)
        ? globalThis.Number(object.protocol_version)
        : 0,
      model: isSet(object.model) ? ConnectModelDescriptor.fromJSON(object.model) : undefined,
    };
  },

  toJSON(message: ConnectHostStartRequest): unknown {
    const obj: any = {};
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.platform !== 0) {
      obj.platform = connectPlatformToJSON(message.platform);
    }
    if (message.protocolVersion !== 0) {
      obj.protocolVersion = Math.round(message.protocolVersion);
    }
    if (message.model !== undefined) {
      obj.model = ConnectModelDescriptor.toJSON(message.model);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectHostStartRequest>, I>>(base?: I): ConnectHostStartRequest {
    return ConnectHostStartRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectHostStartRequest>, I>>(object: I): ConnectHostStartRequest {
    const message = createBaseConnectHostStartRequest();
    message.displayName = object.displayName ?? "";
    message.platform = object.platform ?? 0;
    message.protocolVersion = object.protocolVersion ?? 0;
    message.model = (object.model !== undefined && object.model !== null)
      ? ConnectModelDescriptor.fromPartial(object.model)
      : undefined;
    return message;
  },
};

function createBaseConnectHostStopRequest(): ConnectHostStopRequest {
  return {};
}

export const ConnectHostStopRequest: MessageFns<ConnectHostStopRequest> = {
  encode(_: ConnectHostStopRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectHostStopRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectHostStopRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ConnectHostStopRequest {
    return {};
  },

  toJSON(_: ConnectHostStopRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectHostStopRequest>, I>>(base?: I): ConnectHostStopRequest {
    return ConnectHostStopRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectHostStopRequest>, I>>(_: I): ConnectHostStopRequest {
    const message = createBaseConnectHostStopRequest();
    return message;
  },
};

function createBaseConnectHostState(): ConnectHostState {
  return { isHosting: false, discoveryMetadata: undefined, activeClientCount: 0, errorMessage: "", model: undefined };
}

export const ConnectHostState: MessageFns<ConnectHostState> = {
  encode(message: ConnectHostState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.isHosting !== false) {
      writer.uint32(8).bool(message.isHosting);
    }
    if (message.discoveryMetadata !== undefined) {
      ConnectDiscoveryMetadata.encode(message.discoveryMetadata, writer.uint32(18).fork()).join();
    }
    if (message.activeClientCount !== 0) {
      writer.uint32(24).uint32(message.activeClientCount);
    }
    if (message.errorMessage !== "") {
      writer.uint32(34).string(message.errorMessage);
    }
    if (message.model !== undefined) {
      ConnectModelDescriptor.encode(message.model, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectHostState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectHostState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.isHosting = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.discoveryMetadata = ConnectDiscoveryMetadata.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.activeClientCount = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.model = ConnectModelDescriptor.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectHostState {
    return {
      isHosting: isSet(object.isHosting)
        ? globalThis.Boolean(object.isHosting)
        : isSet(object.is_hosting)
        ? globalThis.Boolean(object.is_hosting)
        : false,
      discoveryMetadata: isSet(object.discoveryMetadata)
        ? ConnectDiscoveryMetadata.fromJSON(object.discoveryMetadata)
        : isSet(object.discovery_metadata)
        ? ConnectDiscoveryMetadata.fromJSON(object.discovery_metadata)
        : undefined,
      activeClientCount: isSet(object.activeClientCount)
        ? globalThis.Number(object.activeClientCount)
        : isSet(object.active_client_count)
        ? globalThis.Number(object.active_client_count)
        : 0,
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : "",
      model: isSet(object.model) ? ConnectModelDescriptor.fromJSON(object.model) : undefined,
    };
  },

  toJSON(message: ConnectHostState): unknown {
    const obj: any = {};
    if (message.isHosting !== false) {
      obj.isHosting = message.isHosting;
    }
    if (message.discoveryMetadata !== undefined) {
      obj.discoveryMetadata = ConnectDiscoveryMetadata.toJSON(message.discoveryMetadata);
    }
    if (message.activeClientCount !== 0) {
      obj.activeClientCount = Math.round(message.activeClientCount);
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    if (message.model !== undefined) {
      obj.model = ConnectModelDescriptor.toJSON(message.model);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectHostState>, I>>(base?: I): ConnectHostState {
    return ConnectHostState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectHostState>, I>>(object: I): ConnectHostState {
    const message = createBaseConnectHostState();
    message.isHosting = object.isHosting ?? false;
    message.discoveryMetadata = (object.discoveryMetadata !== undefined && object.discoveryMetadata !== null)
      ? ConnectDiscoveryMetadata.fromPartial(object.discoveryMetadata)
      : undefined;
    message.activeClientCount = object.activeClientCount ?? 0;
    message.errorMessage = object.errorMessage ?? "";
    message.model = (object.model !== undefined && object.model !== null)
      ? ConnectModelDescriptor.fromPartial(object.model)
      : undefined;
    return message;
  },
};

function createBaseConnectClientStartRequest(): ConnectClientStartRequest {
  return { displayName: "", platform: 0, protocolVersion: 0 };
}

export const ConnectClientStartRequest: MessageFns<ConnectClientStartRequest> = {
  encode(message: ConnectClientStartRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.displayName !== "") {
      writer.uint32(10).string(message.displayName);
    }
    if (message.platform !== 0) {
      writer.uint32(16).int32(message.platform);
    }
    if (message.protocolVersion !== 0) {
      writer.uint32(24).uint32(message.protocolVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectClientStartRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectClientStartRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.platform = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.protocolVersion = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectClientStartRequest {
    return {
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      platform: isSet(object.platform) ? connectPlatformFromJSON(object.platform) : 0,
      protocolVersion: isSet(object.protocolVersion)
        ? globalThis.Number(object.protocolVersion)
        : isSet(object.protocol_version)
        ? globalThis.Number(object.protocol_version)
        : 0,
    };
  },

  toJSON(message: ConnectClientStartRequest): unknown {
    const obj: any = {};
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.platform !== 0) {
      obj.platform = connectPlatformToJSON(message.platform);
    }
    if (message.protocolVersion !== 0) {
      obj.protocolVersion = Math.round(message.protocolVersion);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectClientStartRequest>, I>>(base?: I): ConnectClientStartRequest {
    return ConnectClientStartRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectClientStartRequest>, I>>(object: I): ConnectClientStartRequest {
    const message = createBaseConnectClientStartRequest();
    message.displayName = object.displayName ?? "";
    message.platform = object.platform ?? 0;
    message.protocolVersion = object.protocolVersion ?? 0;
    return message;
  },
};

function createBaseConnectClientHello(): ConnectClientHello {
  return { instanceId: "", displayName: "", platform: 0, protocolVersion: 0 };
}

export const ConnectClientHello: MessageFns<ConnectClientHello> = {
  encode(message: ConnectClientHello, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.instanceId !== "") {
      writer.uint32(10).string(message.instanceId);
    }
    if (message.displayName !== "") {
      writer.uint32(18).string(message.displayName);
    }
    if (message.platform !== 0) {
      writer.uint32(24).int32(message.platform);
    }
    if (message.protocolVersion !== 0) {
      writer.uint32(32).uint32(message.protocolVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectClientHello {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectClientHello();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.instanceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.platform = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.protocolVersion = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectClientHello {
    return {
      instanceId: isSet(object.instanceId)
        ? globalThis.String(object.instanceId)
        : isSet(object.instance_id)
        ? globalThis.String(object.instance_id)
        : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      platform: isSet(object.platform) ? connectPlatformFromJSON(object.platform) : 0,
      protocolVersion: isSet(object.protocolVersion)
        ? globalThis.Number(object.protocolVersion)
        : isSet(object.protocol_version)
        ? globalThis.Number(object.protocol_version)
        : 0,
    };
  },

  toJSON(message: ConnectClientHello): unknown {
    const obj: any = {};
    if (message.instanceId !== "") {
      obj.instanceId = message.instanceId;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.platform !== 0) {
      obj.platform = connectPlatformToJSON(message.platform);
    }
    if (message.protocolVersion !== 0) {
      obj.protocolVersion = Math.round(message.protocolVersion);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectClientHello>, I>>(base?: I): ConnectClientHello {
    return ConnectClientHello.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectClientHello>, I>>(object: I): ConnectClientHello {
    const message = createBaseConnectClientHello();
    message.instanceId = object.instanceId ?? "";
    message.displayName = object.displayName ?? "";
    message.platform = object.platform ?? 0;
    message.protocolVersion = object.protocolVersion ?? 0;
    return message;
  },
};

function createBaseConnectHandshakeResponse(): ConnectHandshakeResponse {
  return { status: 0, sessionId: "", host: undefined, rejectionReason: "", model: undefined };
}

export const ConnectHandshakeResponse: MessageFns<ConnectHandshakeResponse> = {
  encode(message: ConnectHandshakeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    if (message.sessionId !== "") {
      writer.uint32(18).string(message.sessionId);
    }
    if (message.host !== undefined) {
      ConnectDiscoveryMetadata.encode(message.host, writer.uint32(26).fork()).join();
    }
    if (message.rejectionReason !== "") {
      writer.uint32(34).string(message.rejectionReason);
    }
    if (message.model !== undefined) {
      ConnectModelDescriptor.encode(message.model, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectHandshakeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectHandshakeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.host = ConnectDiscoveryMetadata.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.rejectionReason = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.model = ConnectModelDescriptor.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectHandshakeResponse {
    return {
      status: isSet(object.status) ? connectHandshakeStatusFromJSON(object.status) : 0,
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      host: isSet(object.host) ? ConnectDiscoveryMetadata.fromJSON(object.host) : undefined,
      rejectionReason: isSet(object.rejectionReason)
        ? globalThis.String(object.rejectionReason)
        : isSet(object.rejection_reason)
        ? globalThis.String(object.rejection_reason)
        : "",
      model: isSet(object.model) ? ConnectModelDescriptor.fromJSON(object.model) : undefined,
    };
  },

  toJSON(message: ConnectHandshakeResponse): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = connectHandshakeStatusToJSON(message.status);
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.host !== undefined) {
      obj.host = ConnectDiscoveryMetadata.toJSON(message.host);
    }
    if (message.rejectionReason !== "") {
      obj.rejectionReason = message.rejectionReason;
    }
    if (message.model !== undefined) {
      obj.model = ConnectModelDescriptor.toJSON(message.model);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectHandshakeResponse>, I>>(base?: I): ConnectHandshakeResponse {
    return ConnectHandshakeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectHandshakeResponse>, I>>(object: I): ConnectHandshakeResponse {
    const message = createBaseConnectHandshakeResponse();
    message.status = object.status ?? 0;
    message.sessionId = object.sessionId ?? "";
    message.host = (object.host !== undefined && object.host !== null)
      ? ConnectDiscoveryMetadata.fromPartial(object.host)
      : undefined;
    message.rejectionReason = object.rejectionReason ?? "";
    message.model = (object.model !== undefined && object.model !== null)
      ? ConnectModelDescriptor.fromPartial(object.model)
      : undefined;
    return message;
  },
};

function createBaseConnectClientSessionState(): ConnectClientSessionState {
  return { state: 0, sessionId: "", host: undefined, errorMessage: "", model: undefined };
}

export const ConnectClientSessionState: MessageFns<ConnectClientSessionState> = {
  encode(message: ConnectClientSessionState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.state !== 0) {
      writer.uint32(8).int32(message.state);
    }
    if (message.sessionId !== "") {
      writer.uint32(18).string(message.sessionId);
    }
    if (message.host !== undefined) {
      ConnectDiscoveryMetadata.encode(message.host, writer.uint32(26).fork()).join();
    }
    if (message.errorMessage !== "") {
      writer.uint32(34).string(message.errorMessage);
    }
    if (message.model !== undefined) {
      ConnectModelDescriptor.encode(message.model, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectClientSessionState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectClientSessionState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.host = ConnectDiscoveryMetadata.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.model = ConnectModelDescriptor.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectClientSessionState {
    return {
      state: isSet(object.state) ? connectSessionStateFromJSON(object.state) : 0,
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      host: isSet(object.host) ? ConnectDiscoveryMetadata.fromJSON(object.host) : undefined,
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : "",
      model: isSet(object.model) ? ConnectModelDescriptor.fromJSON(object.model) : undefined,
    };
  },

  toJSON(message: ConnectClientSessionState): unknown {
    const obj: any = {};
    if (message.state !== 0) {
      obj.state = connectSessionStateToJSON(message.state);
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.host !== undefined) {
      obj.host = ConnectDiscoveryMetadata.toJSON(message.host);
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    if (message.model !== undefined) {
      obj.model = ConnectModelDescriptor.toJSON(message.model);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectClientSessionState>, I>>(base?: I): ConnectClientSessionState {
    return ConnectClientSessionState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectClientSessionState>, I>>(object: I): ConnectClientSessionState {
    const message = createBaseConnectClientSessionState();
    message.state = object.state ?? 0;
    message.sessionId = object.sessionId ?? "";
    message.host = (object.host !== undefined && object.host !== null)
      ? ConnectDiscoveryMetadata.fromPartial(object.host)
      : undefined;
    message.errorMessage = object.errorMessage ?? "";
    message.model = (object.model !== undefined && object.model !== null)
      ? ConnectModelDescriptor.fromPartial(object.model)
      : undefined;
    return message;
  },
};

function createBaseConnectSessionCloseRequest(): ConnectSessionCloseRequest {
  return { sessionId: "" };
}

export const ConnectSessionCloseRequest: MessageFns<ConnectSessionCloseRequest> = {
  encode(message: ConnectSessionCloseRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectSessionCloseRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectSessionCloseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectSessionCloseRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
    };
  },

  toJSON(message: ConnectSessionCloseRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectSessionCloseRequest>, I>>(base?: I): ConnectSessionCloseRequest {
    return ConnectSessionCloseRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectSessionCloseRequest>, I>>(object: I): ConnectSessionCloseRequest {
    const message = createBaseConnectSessionCloseRequest();
    message.sessionId = object.sessionId ?? "";
    return message;
  },
};

function createBaseConnectInvocationRequest(): ConnectInvocationRequest {
  return { sessionId: "", requestId: "", generation: undefined };
}

export const ConnectInvocationRequest: MessageFns<ConnectInvocationRequest> = {
  encode(message: ConnectInvocationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.requestId !== "") {
      writer.uint32(18).string(message.requestId);
    }
    if (message.generation !== undefined) {
      LLMGenerateRequest.encode(message.generation, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectInvocationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectInvocationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.generation = LLMGenerateRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectInvocationRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
      generation: isSet(object.generation) ? LLMGenerateRequest.fromJSON(object.generation) : undefined,
    };
  },

  toJSON(message: ConnectInvocationRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.generation !== undefined) {
      obj.generation = LLMGenerateRequest.toJSON(message.generation);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectInvocationRequest>, I>>(base?: I): ConnectInvocationRequest {
    return ConnectInvocationRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectInvocationRequest>, I>>(object: I): ConnectInvocationRequest {
    const message = createBaseConnectInvocationRequest();
    message.sessionId = object.sessionId ?? "";
    message.requestId = object.requestId ?? "";
    message.generation = (object.generation !== undefined && object.generation !== null)
      ? LLMGenerateRequest.fromPartial(object.generation)
      : undefined;
    return message;
  },
};

function createBaseConnectInvocationValidation(): ConnectInvocationValidation {
  return { accepted: false, rejectionReason: "" };
}

export const ConnectInvocationValidation: MessageFns<ConnectInvocationValidation> = {
  encode(message: ConnectInvocationValidation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accepted !== false) {
      writer.uint32(8).bool(message.accepted);
    }
    if (message.rejectionReason !== "") {
      writer.uint32(18).string(message.rejectionReason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectInvocationValidation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectInvocationValidation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.accepted = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.rejectionReason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectInvocationValidation {
    return {
      accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false,
      rejectionReason: isSet(object.rejectionReason)
        ? globalThis.String(object.rejectionReason)
        : isSet(object.rejection_reason)
        ? globalThis.String(object.rejection_reason)
        : "",
    };
  },

  toJSON(message: ConnectInvocationValidation): unknown {
    const obj: any = {};
    if (message.accepted !== false) {
      obj.accepted = message.accepted;
    }
    if (message.rejectionReason !== "") {
      obj.rejectionReason = message.rejectionReason;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectInvocationValidation>, I>>(base?: I): ConnectInvocationValidation {
    return ConnectInvocationValidation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectInvocationValidation>, I>>(object: I): ConnectInvocationValidation {
    const message = createBaseConnectInvocationValidation();
    message.accepted = object.accepted ?? false;
    message.rejectionReason = object.rejectionReason ?? "";
    return message;
  },
};

function createBaseConnectInvocationEvent(): ConnectInvocationEvent {
  return { requestId: "", event: undefined };
}

export const ConnectInvocationEvent: MessageFns<ConnectInvocationEvent> = {
  encode(message: ConnectInvocationEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    if (message.event !== undefined) {
      LLMStreamEvent.encode(message.event, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectInvocationEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectInvocationEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.event = LLMStreamEvent.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectInvocationEvent {
    return {
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
      event: isSet(object.event) ? LLMStreamEvent.fromJSON(object.event) : undefined,
    };
  },

  toJSON(message: ConnectInvocationEvent): unknown {
    const obj: any = {};
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.event !== undefined) {
      obj.event = LLMStreamEvent.toJSON(message.event);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectInvocationEvent>, I>>(base?: I): ConnectInvocationEvent {
    return ConnectInvocationEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectInvocationEvent>, I>>(object: I): ConnectInvocationEvent {
    const message = createBaseConnectInvocationEvent();
    message.requestId = object.requestId ?? "";
    message.event = (object.event !== undefined && object.event !== null)
      ? LLMStreamEvent.fromPartial(object.event)
      : undefined;
    return message;
  },
};

function createBaseConnectHeartbeatRequest(): ConnectHeartbeatRequest {
  return { sessionId: "", sequence: 0 };
}

export const ConnectHeartbeatRequest: MessageFns<ConnectHeartbeatRequest> = {
  encode(message: ConnectHeartbeatRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.sequence !== 0) {
      writer.uint32(16).uint64(message.sequence);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectHeartbeatRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectHeartbeatRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sequence = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectHeartbeatRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      sequence: isSet(object.sequence) ? globalThis.Number(object.sequence) : 0,
    };
  },

  toJSON(message: ConnectHeartbeatRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.sequence !== 0) {
      obj.sequence = Math.round(message.sequence);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectHeartbeatRequest>, I>>(base?: I): ConnectHeartbeatRequest {
    return ConnectHeartbeatRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectHeartbeatRequest>, I>>(object: I): ConnectHeartbeatRequest {
    const message = createBaseConnectHeartbeatRequest();
    message.sessionId = object.sessionId ?? "";
    message.sequence = object.sequence ?? 0;
    return message;
  },
};

function createBaseConnectHeartbeatResponse(): ConnectHeartbeatResponse {
  return { sessionId: "", sequence: 0 };
}

export const ConnectHeartbeatResponse: MessageFns<ConnectHeartbeatResponse> = {
  encode(message: ConnectHeartbeatResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.sequence !== 0) {
      writer.uint32(16).uint64(message.sequence);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectHeartbeatResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectHeartbeatResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sequence = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectHeartbeatResponse {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      sequence: isSet(object.sequence) ? globalThis.Number(object.sequence) : 0,
    };
  },

  toJSON(message: ConnectHeartbeatResponse): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.sequence !== 0) {
      obj.sequence = Math.round(message.sequence);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectHeartbeatResponse>, I>>(base?: I): ConnectHeartbeatResponse {
    return ConnectHeartbeatResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectHeartbeatResponse>, I>>(object: I): ConnectHeartbeatResponse {
    const message = createBaseConnectHeartbeatResponse();
    message.sessionId = object.sessionId ?? "";
    message.sequence = object.sequence ?? 0;
    return message;
  },
};

function createBaseConnectClientFrame(): ConnectClientFrame {
  return { invocation: undefined, heartbeat: undefined };
}

export const ConnectClientFrame: MessageFns<ConnectClientFrame> = {
  encode(message: ConnectClientFrame, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.invocation !== undefined) {
      ConnectInvocationRequest.encode(message.invocation, writer.uint32(10).fork()).join();
    }
    if (message.heartbeat !== undefined) {
      ConnectHeartbeatRequest.encode(message.heartbeat, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectClientFrame {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectClientFrame();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.invocation = ConnectInvocationRequest.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.heartbeat = ConnectHeartbeatRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectClientFrame {
    return {
      invocation: isSet(object.invocation) ? ConnectInvocationRequest.fromJSON(object.invocation) : undefined,
      heartbeat: isSet(object.heartbeat) ? ConnectHeartbeatRequest.fromJSON(object.heartbeat) : undefined,
    };
  },

  toJSON(message: ConnectClientFrame): unknown {
    const obj: any = {};
    if (message.invocation !== undefined) {
      obj.invocation = ConnectInvocationRequest.toJSON(message.invocation);
    }
    if (message.heartbeat !== undefined) {
      obj.heartbeat = ConnectHeartbeatRequest.toJSON(message.heartbeat);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectClientFrame>, I>>(base?: I): ConnectClientFrame {
    return ConnectClientFrame.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectClientFrame>, I>>(object: I): ConnectClientFrame {
    const message = createBaseConnectClientFrame();
    message.invocation = (object.invocation !== undefined && object.invocation !== null)
      ? ConnectInvocationRequest.fromPartial(object.invocation)
      : undefined;
    message.heartbeat = (object.heartbeat !== undefined && object.heartbeat !== null)
      ? ConnectHeartbeatRequest.fromPartial(object.heartbeat)
      : undefined;
    return message;
  },
};

function createBaseConnectHostFrame(): ConnectHostFrame {
  return { invocationEvent: undefined, heartbeat: undefined };
}

export const ConnectHostFrame: MessageFns<ConnectHostFrame> = {
  encode(message: ConnectHostFrame, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.invocationEvent !== undefined) {
      ConnectInvocationEvent.encode(message.invocationEvent, writer.uint32(10).fork()).join();
    }
    if (message.heartbeat !== undefined) {
      ConnectHeartbeatResponse.encode(message.heartbeat, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConnectHostFrame {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectHostFrame();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.invocationEvent = ConnectInvocationEvent.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.heartbeat = ConnectHeartbeatResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectHostFrame {
    return {
      invocationEvent: isSet(object.invocationEvent)
        ? ConnectInvocationEvent.fromJSON(object.invocationEvent)
        : isSet(object.invocation_event)
        ? ConnectInvocationEvent.fromJSON(object.invocation_event)
        : undefined,
      heartbeat: isSet(object.heartbeat) ? ConnectHeartbeatResponse.fromJSON(object.heartbeat) : undefined,
    };
  },

  toJSON(message: ConnectHostFrame): unknown {
    const obj: any = {};
    if (message.invocationEvent !== undefined) {
      obj.invocationEvent = ConnectInvocationEvent.toJSON(message.invocationEvent);
    }
    if (message.heartbeat !== undefined) {
      obj.heartbeat = ConnectHeartbeatResponse.toJSON(message.heartbeat);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectHostFrame>, I>>(base?: I): ConnectHostFrame {
    return ConnectHostFrame.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectHostFrame>, I>>(object: I): ConnectHostFrame {
    const message = createBaseConnectHostFrame();
    message.invocationEvent = (object.invocationEvent !== undefined && object.invocationEvent !== null)
      ? ConnectInvocationEvent.fromPartial(object.invocationEvent)
      : undefined;
    message.heartbeat = (object.heartbeat !== undefined && object.heartbeat !== null)
      ? ConnectHeartbeatResponse.fromPartial(object.heartbeat)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
