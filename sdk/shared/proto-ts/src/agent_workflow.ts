// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.35.1
// source: agent_workflow.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { SDKError } from "./errors";
import { LLMGenerationOptions } from "./llm_options";
import { ModelCategory, modelCategoryFromJSON, modelCategoryToJSON } from "./model_types";

export const protobufPackage = "runanywhere.v1";

export enum ToolArgumentType {
  TOOL_ARGUMENT_TYPE_UNSPECIFIED = 0,
  TOOL_ARGUMENT_TYPE_STRING = 1,
  TOOL_ARGUMENT_TYPE_NUMBER = 2,
  TOOL_ARGUMENT_TYPE_BOOLEAN = 3,
  TOOL_ARGUMENT_TYPE_OBJECT = 4,
  TOOL_ARGUMENT_TYPE_ARRAY = 5,
  UNRECOGNIZED = -1,
}

export function toolArgumentTypeFromJSON(object: any): ToolArgumentType {
  switch (object) {
    case 0:
    case "TOOL_ARGUMENT_TYPE_UNSPECIFIED":
      return ToolArgumentType.TOOL_ARGUMENT_TYPE_UNSPECIFIED;
    case 1:
    case "TOOL_ARGUMENT_TYPE_STRING":
      return ToolArgumentType.TOOL_ARGUMENT_TYPE_STRING;
    case 2:
    case "TOOL_ARGUMENT_TYPE_NUMBER":
      return ToolArgumentType.TOOL_ARGUMENT_TYPE_NUMBER;
    case 3:
    case "TOOL_ARGUMENT_TYPE_BOOLEAN":
      return ToolArgumentType.TOOL_ARGUMENT_TYPE_BOOLEAN;
    case 4:
    case "TOOL_ARGUMENT_TYPE_OBJECT":
      return ToolArgumentType.TOOL_ARGUMENT_TYPE_OBJECT;
    case 5:
    case "TOOL_ARGUMENT_TYPE_ARRAY":
      return ToolArgumentType.TOOL_ARGUMENT_TYPE_ARRAY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ToolArgumentType.UNRECOGNIZED;
  }
}

export function toolArgumentTypeToJSON(object: ToolArgumentType): string {
  switch (object) {
    case ToolArgumentType.TOOL_ARGUMENT_TYPE_UNSPECIFIED:
      return "TOOL_ARGUMENT_TYPE_UNSPECIFIED";
    case ToolArgumentType.TOOL_ARGUMENT_TYPE_STRING:
      return "TOOL_ARGUMENT_TYPE_STRING";
    case ToolArgumentType.TOOL_ARGUMENT_TYPE_NUMBER:
      return "TOOL_ARGUMENT_TYPE_NUMBER";
    case ToolArgumentType.TOOL_ARGUMENT_TYPE_BOOLEAN:
      return "TOOL_ARGUMENT_TYPE_BOOLEAN";
    case ToolArgumentType.TOOL_ARGUMENT_TYPE_OBJECT:
      return "TOOL_ARGUMENT_TYPE_OBJECT";
    case ToolArgumentType.TOOL_ARGUMENT_TYPE_ARRAY:
      return "TOOL_ARGUMENT_TYPE_ARRAY";
    case ToolArgumentType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ComparisonOperator {
  COMPARISON_OPERATOR_UNSPECIFIED = 0,
  COMPARISON_OPERATOR_EQUALS = 1,
  COMPARISON_OPERATOR_NOT_EQUALS = 2,
  COMPARISON_OPERATOR_CONTAINS = 3,
  COMPARISON_OPERATOR_GREATER_THAN = 4,
  COMPARISON_OPERATOR_LESS_THAN = 5,
  COMPARISON_OPERATOR_IS_EMPTY = 6,
  UNRECOGNIZED = -1,
}

export function comparisonOperatorFromJSON(object: any): ComparisonOperator {
  switch (object) {
    case 0:
    case "COMPARISON_OPERATOR_UNSPECIFIED":
      return ComparisonOperator.COMPARISON_OPERATOR_UNSPECIFIED;
    case 1:
    case "COMPARISON_OPERATOR_EQUALS":
      return ComparisonOperator.COMPARISON_OPERATOR_EQUALS;
    case 2:
    case "COMPARISON_OPERATOR_NOT_EQUALS":
      return ComparisonOperator.COMPARISON_OPERATOR_NOT_EQUALS;
    case 3:
    case "COMPARISON_OPERATOR_CONTAINS":
      return ComparisonOperator.COMPARISON_OPERATOR_CONTAINS;
    case 4:
    case "COMPARISON_OPERATOR_GREATER_THAN":
      return ComparisonOperator.COMPARISON_OPERATOR_GREATER_THAN;
    case 5:
    case "COMPARISON_OPERATOR_LESS_THAN":
      return ComparisonOperator.COMPARISON_OPERATOR_LESS_THAN;
    case 6:
    case "COMPARISON_OPERATOR_IS_EMPTY":
      return ComparisonOperator.COMPARISON_OPERATOR_IS_EMPTY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ComparisonOperator.UNRECOGNIZED;
  }
}

export function comparisonOperatorToJSON(object: ComparisonOperator): string {
  switch (object) {
    case ComparisonOperator.COMPARISON_OPERATOR_UNSPECIFIED:
      return "COMPARISON_OPERATOR_UNSPECIFIED";
    case ComparisonOperator.COMPARISON_OPERATOR_EQUALS:
      return "COMPARISON_OPERATOR_EQUALS";
    case ComparisonOperator.COMPARISON_OPERATOR_NOT_EQUALS:
      return "COMPARISON_OPERATOR_NOT_EQUALS";
    case ComparisonOperator.COMPARISON_OPERATOR_CONTAINS:
      return "COMPARISON_OPERATOR_CONTAINS";
    case ComparisonOperator.COMPARISON_OPERATOR_GREATER_THAN:
      return "COMPARISON_OPERATOR_GREATER_THAN";
    case ComparisonOperator.COMPARISON_OPERATOR_LESS_THAN:
      return "COMPARISON_OPERATOR_LESS_THAN";
    case ComparisonOperator.COMPARISON_OPERATOR_IS_EMPTY:
      return "COMPARISON_OPERATOR_IS_EMPTY";
    case ComparisonOperator.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum HttpAuthKind {
  HTTP_AUTH_KIND_UNSPECIFIED = 0,
  HTTP_AUTH_KIND_BEARER = 1,
  HTTP_AUTH_KIND_HEADER = 2,
  HTTP_AUTH_KIND_QUERY = 3,
  UNRECOGNIZED = -1,
}

export function httpAuthKindFromJSON(object: any): HttpAuthKind {
  switch (object) {
    case 0:
    case "HTTP_AUTH_KIND_UNSPECIFIED":
      return HttpAuthKind.HTTP_AUTH_KIND_UNSPECIFIED;
    case 1:
    case "HTTP_AUTH_KIND_BEARER":
      return HttpAuthKind.HTTP_AUTH_KIND_BEARER;
    case 2:
    case "HTTP_AUTH_KIND_HEADER":
      return HttpAuthKind.HTTP_AUTH_KIND_HEADER;
    case 3:
    case "HTTP_AUTH_KIND_QUERY":
      return HttpAuthKind.HTTP_AUTH_KIND_QUERY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return HttpAuthKind.UNRECOGNIZED;
  }
}

export function httpAuthKindToJSON(object: HttpAuthKind): string {
  switch (object) {
    case HttpAuthKind.HTTP_AUTH_KIND_UNSPECIFIED:
      return "HTTP_AUTH_KIND_UNSPECIFIED";
    case HttpAuthKind.HTTP_AUTH_KIND_BEARER:
      return "HTTP_AUTH_KIND_BEARER";
    case HttpAuthKind.HTTP_AUTH_KIND_HEADER:
      return "HTTP_AUTH_KIND_HEADER";
    case HttpAuthKind.HTTP_AUTH_KIND_QUERY:
      return "HTTP_AUTH_KIND_QUERY";
    case HttpAuthKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum HttpMethod {
  HTTP_METHOD_UNSPECIFIED = 0,
  HTTP_METHOD_GET = 1,
  HTTP_METHOD_POST = 2,
  HTTP_METHOD_PUT = 3,
  HTTP_METHOD_PATCH = 4,
  HTTP_METHOD_DELETE = 5,
  UNRECOGNIZED = -1,
}

export function httpMethodFromJSON(object: any): HttpMethod {
  switch (object) {
    case 0:
    case "HTTP_METHOD_UNSPECIFIED":
      return HttpMethod.HTTP_METHOD_UNSPECIFIED;
    case 1:
    case "HTTP_METHOD_GET":
      return HttpMethod.HTTP_METHOD_GET;
    case 2:
    case "HTTP_METHOD_POST":
      return HttpMethod.HTTP_METHOD_POST;
    case 3:
    case "HTTP_METHOD_PUT":
      return HttpMethod.HTTP_METHOD_PUT;
    case 4:
    case "HTTP_METHOD_PATCH":
      return HttpMethod.HTTP_METHOD_PATCH;
    case 5:
    case "HTTP_METHOD_DELETE":
      return HttpMethod.HTTP_METHOD_DELETE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return HttpMethod.UNRECOGNIZED;
  }
}

export function httpMethodToJSON(object: HttpMethod): string {
  switch (object) {
    case HttpMethod.HTTP_METHOD_UNSPECIFIED:
      return "HTTP_METHOD_UNSPECIFIED";
    case HttpMethod.HTTP_METHOD_GET:
      return "HTTP_METHOD_GET";
    case HttpMethod.HTTP_METHOD_POST:
      return "HTTP_METHOD_POST";
    case HttpMethod.HTTP_METHOD_PUT:
      return "HTTP_METHOD_PUT";
    case HttpMethod.HTTP_METHOD_PATCH:
      return "HTTP_METHOD_PATCH";
    case HttpMethod.HTTP_METHOD_DELETE:
      return "HTTP_METHOD_DELETE";
    case HttpMethod.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ScheduleKind {
  SCHEDULE_KIND_UNSPECIFIED = 0,
  SCHEDULE_KIND_INTERVAL = 1,
  SCHEDULE_KIND_DAILY = 2,
  SCHEDULE_KIND_CRON = 3,
  UNRECOGNIZED = -1,
}

export function scheduleKindFromJSON(object: any): ScheduleKind {
  switch (object) {
    case 0:
    case "SCHEDULE_KIND_UNSPECIFIED":
      return ScheduleKind.SCHEDULE_KIND_UNSPECIFIED;
    case 1:
    case "SCHEDULE_KIND_INTERVAL":
      return ScheduleKind.SCHEDULE_KIND_INTERVAL;
    case 2:
    case "SCHEDULE_KIND_DAILY":
      return ScheduleKind.SCHEDULE_KIND_DAILY;
    case 3:
    case "SCHEDULE_KIND_CRON":
      return ScheduleKind.SCHEDULE_KIND_CRON;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ScheduleKind.UNRECOGNIZED;
  }
}

export function scheduleKindToJSON(object: ScheduleKind): string {
  switch (object) {
    case ScheduleKind.SCHEDULE_KIND_UNSPECIFIED:
      return "SCHEDULE_KIND_UNSPECIFIED";
    case ScheduleKind.SCHEDULE_KIND_INTERVAL:
      return "SCHEDULE_KIND_INTERVAL";
    case ScheduleKind.SCHEDULE_KIND_DAILY:
      return "SCHEDULE_KIND_DAILY";
    case ScheduleKind.SCHEDULE_KIND_CRON:
      return "SCHEDULE_KIND_CRON";
    case ScheduleKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum BundleItemKind {
  BUNDLE_ITEM_KIND_UNSPECIFIED = 0,
  BUNDLE_ITEM_KIND_WORKFLOW = 1,
  BUNDLE_ITEM_KIND_PACK = 2,
  UNRECOGNIZED = -1,
}

export function bundleItemKindFromJSON(object: any): BundleItemKind {
  switch (object) {
    case 0:
    case "BUNDLE_ITEM_KIND_UNSPECIFIED":
      return BundleItemKind.BUNDLE_ITEM_KIND_UNSPECIFIED;
    case 1:
    case "BUNDLE_ITEM_KIND_WORKFLOW":
      return BundleItemKind.BUNDLE_ITEM_KIND_WORKFLOW;
    case 2:
    case "BUNDLE_ITEM_KIND_PACK":
      return BundleItemKind.BUNDLE_ITEM_KIND_PACK;
    case -1:
    case "UNRECOGNIZED":
    default:
      return BundleItemKind.UNRECOGNIZED;
  }
}

export function bundleItemKindToJSON(object: BundleItemKind): string {
  switch (object) {
    case BundleItemKind.BUNDLE_ITEM_KIND_UNSPECIFIED:
      return "BUNDLE_ITEM_KIND_UNSPECIFIED";
    case BundleItemKind.BUNDLE_ITEM_KIND_WORKFLOW:
      return "BUNDLE_ITEM_KIND_WORKFLOW";
    case BundleItemKind.BUNDLE_ITEM_KIND_PACK:
      return "BUNDLE_ITEM_KIND_PACK";
    case BundleItemKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum NodeRunState {
  NODE_RUN_STATE_UNSPECIFIED = 0,
  NODE_RUN_STATE_PENDING = 1,
  NODE_RUN_STATE_RUNNING = 2,
  NODE_RUN_STATE_SUCCEEDED = 3,
  NODE_RUN_STATE_FAILED = 4,
  /** NODE_RUN_STATE_SKIPPED - The untaken side of a Condition. Not a failure. */
  NODE_RUN_STATE_SKIPPED = 5,
  UNRECOGNIZED = -1,
}

export function nodeRunStateFromJSON(object: any): NodeRunState {
  switch (object) {
    case 0:
    case "NODE_RUN_STATE_UNSPECIFIED":
      return NodeRunState.NODE_RUN_STATE_UNSPECIFIED;
    case 1:
    case "NODE_RUN_STATE_PENDING":
      return NodeRunState.NODE_RUN_STATE_PENDING;
    case 2:
    case "NODE_RUN_STATE_RUNNING":
      return NodeRunState.NODE_RUN_STATE_RUNNING;
    case 3:
    case "NODE_RUN_STATE_SUCCEEDED":
      return NodeRunState.NODE_RUN_STATE_SUCCEEDED;
    case 4:
    case "NODE_RUN_STATE_FAILED":
      return NodeRunState.NODE_RUN_STATE_FAILED;
    case 5:
    case "NODE_RUN_STATE_SKIPPED":
      return NodeRunState.NODE_RUN_STATE_SKIPPED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return NodeRunState.UNRECOGNIZED;
  }
}

export function nodeRunStateToJSON(object: NodeRunState): string {
  switch (object) {
    case NodeRunState.NODE_RUN_STATE_UNSPECIFIED:
      return "NODE_RUN_STATE_UNSPECIFIED";
    case NodeRunState.NODE_RUN_STATE_PENDING:
      return "NODE_RUN_STATE_PENDING";
    case NodeRunState.NODE_RUN_STATE_RUNNING:
      return "NODE_RUN_STATE_RUNNING";
    case NodeRunState.NODE_RUN_STATE_SUCCEEDED:
      return "NODE_RUN_STATE_SUCCEEDED";
    case NodeRunState.NODE_RUN_STATE_FAILED:
      return "NODE_RUN_STATE_FAILED";
    case NodeRunState.NODE_RUN_STATE_SKIPPED:
      return "NODE_RUN_STATE_SKIPPED";
    case NodeRunState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum WorkflowRunState {
  WORKFLOW_RUN_STATE_UNSPECIFIED = 0,
  WORKFLOW_RUN_STATE_RUNNING = 1,
  WORKFLOW_RUN_STATE_SUCCEEDED = 2,
  WORKFLOW_RUN_STATE_FAILED = 3,
  WORKFLOW_RUN_STATE_CANCELLED = 4,
  UNRECOGNIZED = -1,
}

export function workflowRunStateFromJSON(object: any): WorkflowRunState {
  switch (object) {
    case 0:
    case "WORKFLOW_RUN_STATE_UNSPECIFIED":
      return WorkflowRunState.WORKFLOW_RUN_STATE_UNSPECIFIED;
    case 1:
    case "WORKFLOW_RUN_STATE_RUNNING":
      return WorkflowRunState.WORKFLOW_RUN_STATE_RUNNING;
    case 2:
    case "WORKFLOW_RUN_STATE_SUCCEEDED":
      return WorkflowRunState.WORKFLOW_RUN_STATE_SUCCEEDED;
    case 3:
    case "WORKFLOW_RUN_STATE_FAILED":
      return WorkflowRunState.WORKFLOW_RUN_STATE_FAILED;
    case 4:
    case "WORKFLOW_RUN_STATE_CANCELLED":
      return WorkflowRunState.WORKFLOW_RUN_STATE_CANCELLED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return WorkflowRunState.UNRECOGNIZED;
  }
}

export function workflowRunStateToJSON(object: WorkflowRunState): string {
  switch (object) {
    case WorkflowRunState.WORKFLOW_RUN_STATE_UNSPECIFIED:
      return "WORKFLOW_RUN_STATE_UNSPECIFIED";
    case WorkflowRunState.WORKFLOW_RUN_STATE_RUNNING:
      return "WORKFLOW_RUN_STATE_RUNNING";
    case WorkflowRunState.WORKFLOW_RUN_STATE_SUCCEEDED:
      return "WORKFLOW_RUN_STATE_SUCCEEDED";
    case WorkflowRunState.WORKFLOW_RUN_STATE_FAILED:
      return "WORKFLOW_RUN_STATE_FAILED";
    case WorkflowRunState.WORKFLOW_RUN_STATE_CANCELLED:
      return "WORKFLOW_RUN_STATE_CANCELLED";
    case WorkflowRunState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface WorkflowDocument {
  id: string;
  name: string;
  nodes: WorkflowNode[];
  edges: WorkflowEdge[];
  createdAtMs: number;
  updatedAtMs: number;
  /**
   * Shape of this message, not the user's edit count. Readers reject a
   * document from a newer schema rather than silently dropping fields.
   */
  schemaVersion: number;
}

export interface NodePosition {
  x: number;
  y: number;
}

export interface WorkflowNode {
  id: string;
  /** Display label, and the name expressions address this node by. */
  name: string;
  position?: NodePosition | undefined;
  manualTrigger?: ManualTriggerConfig | undefined;
  toolCall?: ToolCallConfig | undefined;
  llmGenerate?: LlmGenerateConfig | undefined;
  condition?: ConditionConfig | undefined;
  loopOverItems?: LoopOverItemsConfig | undefined;
  code?: CodeConfig | undefined;
  httpRequest?: HttpRequestConfig | undefined;
  setTransform?: SetTransformConfig | undefined;
  scheduleTrigger?: ScheduleTriggerConfig | undefined;
  merge?: MergeConfig | undefined;
  filter?: FilterConfig | undefined;
  splitOut?: SplitOutConfig | undefined;
  aggregate?: AggregateConfig | undefined;
  wait?: WaitConfig | undefined;
  fileRead?: FileReadConfig | undefined;
  fileWrite?: FileWriteConfig | undefined;
  llmStructured?: LlmStructuredConfig | undefined;
  vision?: VisionConfig | undefined;
  embed?: EmbedConfig | undefined;
  rerank?: RerankConfig | undefined;
  transcribe?: TranscribeConfig | undefined;
  speak?: SpeakConfig | undefined;
  detectVoice?: DetectVoiceConfig | undefined;
  diarize?: DiarizeConfig | undefined;
  segment?: SegmentConfig | undefined;
  ragQuery?: RagQueryConfig | undefined;
  ragIngest?: RagIngestConfig | undefined;
  loadModel?: LoadModelConfig | undefined;
  packNode?: PackNodeConfig | undefined;
}

/**
 * An instance of an installed NodePack on a canvas.
 *
 * The port list is mirrored from the pack at drop time rather than looked up at
 * run time, so a document that references a pack the machine does not have
 * still opens with the right shape. `missing` is set on load in that case and
 * the canvas draws a placeholder instead of failing the whole workflow, which
 * is the behaviour Sketchware gets right and most block editors do not.
 */
export interface PackNodeConfig {
  packId: string;
  arguments: { [key: string]: string };
  ports: ToolArgumentPort[];
  missing: boolean;
  /**
   * Mirrored from NodePack.outputs at drop time, same reasoning as `ports`.
   * Empty means the pack has the single unnamed output every other node
   * type has ("out").
   */
  outputs: string[];
}

export interface PackNodeConfig_ArgumentsEntry {
  key: string;
  value: string;
}

export interface WorkflowEdge {
  fromNode: string;
  /** "out" on every node except Condition, which emits "true" and "false". */
  fromPort: string;
  toNode: string;
  toPort: string;
}

export interface ManualTriggerConfig {
  /**
   * Seed items for the first node, as a JSON array. Empty means one item
   * with an empty object, so a workflow with no input still executes once.
   */
  initialItemsJson: string;
}

export interface ToolCallConfig {
  /**
   * Resolved against the host's tool registry at run time, not at save time,
   * so a workflow referencing a tool the host no longer registers fails on
   * the run rather than becoming unopenable.
   */
  toolName: string;
  /** Literal or expression per argument, used for any port left unconnected. */
  arguments: { [key: string]: string };
  /**
   * The tool's argument list, copied into the document when the user picks
   * the tool. Commons validates edges against this rather than against the
   * host registry, which it cannot see — and it keeps a saved workflow
   * self-describing, so the graph still renders with the right shape on a
   * machine where that tool is not installed.
   */
  ports: ToolArgumentPort[];
}

export interface ToolCallConfig_ArgumentsEntry {
  key: string;
  value: string;
}

export interface ToolArgumentPort {
  name: string;
  description: string;
  required: boolean;
  type: ToolArgumentType;
}

export interface LlmGenerateConfig {
  prompt: string;
  systemPrompt?:
    | string
    | undefined;
  /** Absent uses whichever model the host currently has loaded. */
  modelId?: string | undefined;
  generation?: LLMGenerationOptions | undefined;
}

export interface ConditionConfig {
  left: string;
  operator: ComparisonOperator;
  right: string;
}

export interface LoopOverItemsConfig {
  /** Expression yielding the list to walk. */
  items: string;
  /**
   * Nodes run once per item. They stay in WorkflowDocument.nodes but are
   * owned by this loop, so the outer graph never contains a cycle and the
   * topological sort stays valid.
   */
  bodyNodeIds: string[];
  /** Hard ceiling. 0 means the runner's built-in cap applies. */
  maxIterations: number;
}

export interface CodeConfig {
  /**
   * JavaScript. Commons has no JS engine; the host evaluates this through
   * the code callback (JavaScriptCore on Apple, node vm on Electron).
   */
  source: string;
}

export interface HttpRequestConfig {
  method: HttpMethod;
  url: string;
  headers: { [key: string]: string };
  body?: string | undefined;
  timeoutMs?: number | undefined;
  auth?: HttpAuth | undefined;
}

export interface HttpRequestConfig_HeadersEntry {
  key: string;
  value: string;
}

/**
 * Credentials are kept out of the headers map so the editor can mask them and
 * a future release can move the secret into the platform keystore without
 * changing the wire shape.
 */
export interface HttpAuth {
  kind: HttpAuthKind;
  /** Bearer token, or the API key value for the header and query kinds. */
  secret: string;
  /**
   * Header name for HTTP_AUTH_KIND_HEADER, query parameter name for
   * HTTP_AUTH_KIND_QUERY. Ignored by the bearer kind.
   */
  name: string;
}

export interface SetTransformConfig {
  assignments: FieldAssignment[];
  /** Drop input fields no assignment names. */
  keepOnlyAssigned: boolean;
}

export interface FieldAssignment {
  field: string;
  value: string;
}

/**
 * Fires the workflow on a timer. Commons validates and runs the graph; the
 * host owns the clock, because a timer that survives sleep and relaunch is a
 * platform concern and differs between macOS and Electron.
 */
export interface ScheduleTriggerConfig {
  kind: ScheduleKind;
  /** SCHEDULE_KIND_INTERVAL: seconds between runs. */
  intervalSeconds: number;
  /** SCHEDULE_KIND_DAILY: local wall-clock time to fire at. */
  hour: number;
  minute: number;
  /** SCHEDULE_KIND_CRON: a five-field expression, for hosts that support it. */
  cron: string;
  /** Seed items handed to the first node on each firing, as a JSON array. */
  initialItemsJson: string;
}

/**
 * Concatenates every branch that reaches it. The only node with more than one
 * input port, so a fan-in does not need a dummy step.
 */
export interface MergeConfig {
  inputCount: number;
  /** Drop items whose JSON body is byte-identical to one already seen. */
  deduplicate: boolean;
}

export interface FilterConfig {
  left: string;
  operator: ComparisonOperator;
  right: string;
  /** Evaluate per item, so `{{ item.field }}` addresses the item under test. */
  keepMatching: boolean;
}

/** One item per element of a list, the inverse of Aggregate. */
export interface SplitOutConfig {
  field: string;
}

/** Collapses every input item into one, the inverse of Split Out. */
export interface AggregateConfig {
  field: string;
}

export interface WaitConfig {
  seconds: number;
}

export interface FileReadConfig {
  path: string;
  /**
   * Text lands in the item's json body; binary lands in the binary map under
   * this key, which is how audio and images reach the speech and vision
   * nodes.
   */
  binary: boolean;
  binaryKey: string;
  mimeType: string;
}

export interface FileWriteConfig {
  path: string;
  /**
   * Expression for the text to write. Ignored when binary_key names an
   * attachment on the incoming item.
   */
  content: string;
  binaryKey: string;
  append: boolean;
}

export interface LlmStructuredConfig {
  prompt: string;
  /**
   * JSON Schema the model's answer must satisfy. The result lands parsed, so
   * downstream expressions address its fields directly.
   */
  jsonSchema: string;
  systemPrompt?: string | undefined;
  modelId?: string | undefined;
  generation?: LLMGenerationOptions | undefined;
}

export interface VisionConfig {
  prompt: string;
  /** Attachment on the incoming item holding the image bytes. */
  binaryKey: string;
  modelId?: string | undefined;
  generation?: LLMGenerationOptions | undefined;
}

export interface EmbedConfig {
  text: string;
  modelId?: string | undefined;
}

export interface RerankConfig {
  query: string;
  /** Expression resolving to a list of strings. */
  documents: string;
  topN: number;
  modelId?: string | undefined;
}

export interface TranscribeConfig {
  binaryKey: string;
  modelId?: string | undefined;
  language?: string | undefined;
}

export interface SpeakConfig {
  text: string;
  /** Play through the speakers rather than returning the audio. */
  play: boolean;
  binaryKey: string;
  modelId?: string | undefined;
  voice?: string | undefined;
}

export interface DetectVoiceConfig {
  binaryKey: string;
  threshold?: number | undefined;
}

export interface DiarizeConfig {
  binaryKey: string;
  modelId?: string | undefined;
  speakerCount?: number | undefined;
}

export interface SegmentConfig {
  text: string;
  modelId?: string | undefined;
}

export interface RagQueryConfig {
  question: string;
  embeddingModelId: string;
  llmModelId: string;
  topK: number;
}

export interface RagIngestConfig {
  text: string;
  embeddingModelId: string;
  llmModelId: string;
  documentId: string;
}

/**
 * Bring a model up before later nodes need it, so a long first load is a step
 * the user can see rather than an unexplained pause inside another node.
 */
export interface LoadModelConfig {
  modelId: string;
  category?: ModelCategory | undefined;
}

export interface WorkflowItem {
  /**
   * Arbitrary user-shaped data. Carried as serialized JSON because the shape
   * is defined by whatever the previous node emitted.
   */
  json: string;
  binary: { [key: string]: WorkflowBinary };
}

export interface WorkflowItem_BinaryEntry {
  key: string;
  value?: WorkflowBinary | undefined;
}

export interface WorkflowBinary {
  data: Uint8Array;
  mimeType: string;
  fileName?: string | undefined;
}

/**
 * What a user exports and shares: one or more workflows plus any node packs
 * they depend on, so a bundle opens on another machine without a second
 * download.
 */
export interface WorkflowBundle {
  /**
   * Shape of this envelope. A reader refuses a newer one rather than
   * importing a bundle it would silently misread.
   */
  formatVersion: number;
  /** Free text for humans reading the file, never parsed. */
  generator: string;
  exportedAtMs: number;
  workflows: WorkflowDocument[];
  packs: NodePack[];
}

/**
 * Request naming which workflows to export. Every pack any of them
 * references — transitively, through a composite pack's own subgraph — is
 * resolved and bundled automatically, so there is nothing else to specify.
 */
export interface WorkflowBundleExportRequest {
  workflowIds: string[];
}

export interface BundleImportIssue {
  kind: BundleItemKind;
  id: string;
  message: string;
}

/**
 * Per-item outcome of an import, so one bad workflow or pack does not abort
 * the rest of the bundle.
 */
export interface WorkflowBundleImportResult {
  importedWorkflowIds: string[];
  importedPackIds: string[];
  skipped: BundleImportIssue[];
}

/**
 * A reusable node the user installs, exports, and shares.
 *
 * Two implementations, and the difference is what they are allowed to do. A
 * composite is a subgraph built from nodes that already exist, so importing one
 * grants no capability the host did not already have. A script carries
 * JavaScript, which is why it declares capabilities and the importer shows them
 * before enabling it.
 */
export interface NodePack {
  id: string;
  name: string;
  description: string;
  author: string;
  version: string;
  /** Palette placement and card styling. */
  category: string;
  icon: string;
  accentRgb: number;
  inputs: ToolArgumentPort[];
  outputs: string[];
  composite?: CompositeImplementation | undefined;
  script?: ScriptImplementation | undefined;
  capabilities?: NodePackCapabilities | undefined;
}

export interface CompositeImplementation {
  /**
   * The subgraph. Runs inline when the pack node executes, the same way a
   * loop body does, so it needs no separate scheduler.
   */
  graph?:
    | WorkflowDocument
    | undefined;
  /**
   * Where the pack's inputs enter and its outputs leave. Empty entry means
   * the graph's own trigger; empty exit means the last node in topological
   * order.
   */
  entryNodeId: string;
  exitNodeId: string;
}

export interface ScriptImplementation {
  /**
   * Evaluated through the same host callback the Code node uses, so a script
   * pack gains no reach a Code node does not already have.
   */
  source: string;
}

/**
 * Declared by the pack, shown at import, and enforced by the host. A pack that
 * asks for nothing is safe to enable without a prompt.
 */
export interface NodePackCapabilities {
  network: boolean;
  filesystem: boolean;
  /** Empty tool_names with tools set means any registered tool. */
  tools: boolean;
  toolNames: string[];
}

export interface NodePackList {
  packs: NodePack[];
}

/**
 * Enough to populate a workflow list without loading every node of every
 * document off disk.
 */
export interface WorkflowSummary {
  id: string;
  name: string;
  createdAtMs: number;
  updatedAtMs: number;
  nodeCount: number;
}

export interface WorkflowList {
  workflows: WorkflowSummary[];
}

export interface WorkflowValidationResult {
  valid: boolean;
  issues: WorkflowValidationIssue[];
}

export interface WorkflowValidationIssue {
  message: string;
  /** Whichever of these the issue attaches to; a cycle sets neither. */
  nodeId?: string | undefined;
  edgeFrom?: string | undefined;
}

export interface NodeRun {
  nodeId: string;
  state: NodeRunState;
  startedAtMs: number;
  finishedAtMs: number;
  output: WorkflowItem[];
  error?: SDKError | undefined;
}

/**
 * Persisted per run so a finished run stays inspectable and a failed one can
 * be resumed from the last succeeded node.
 */
export interface WorkflowRunRecord {
  runId: string;
  workflowId: string;
  state: WorkflowRunState;
  startedAtMs: number;
  finishedAtMs: number;
  nodeRuns: NodeRun[];
  error?: SDKError | undefined;
}

export interface WorkflowRunRequest {
  workflowId: string;
  /** Overrides ManualTriggerConfig.initial_items_json when non-empty. */
  initialItems: WorkflowItem[];
}

export interface WorkflowRunEvent {
  runId: string;
  runStarted?: RunStarted | undefined;
  nodeStateChanged?: NodeStateChanged | undefined;
  runFinished?: RunFinished | undefined;
}

export interface RunStarted {
  workflowId: string;
  startedAtMs: number;
}

export interface NodeStateChanged {
  nodeId: string;
  state: NodeRunState;
  /**
   * Populated on SUCCEEDED so the canvas can show output without reloading
   * the run record.
   */
  output: WorkflowItem[];
  error?: SDKError | undefined;
}

export interface RunFinished {
  state: WorkflowRunState;
  finishedAtMs: number;
  error?: SDKError | undefined;
}

export interface ToolInvocation {
  runId: string;
  nodeId: string;
  toolName: string;
  argumentsJson: string;
}

export interface ToolInvocationResult {
  resultJson?: string | undefined;
  error?: SDKError | undefined;
}

export interface CodeInvocation {
  runId: string;
  nodeId: string;
  source: string;
  inputItemsJson: string;
}

export interface CodeInvocationResult {
  outputItemsJson?: string | undefined;
  error?: SDKError | undefined;
}

function createBaseWorkflowDocument(): WorkflowDocument {
  return { id: "", name: "", nodes: [], edges: [], createdAtMs: 0, updatedAtMs: 0, schemaVersion: 0 };
}

export const WorkflowDocument: MessageFns<WorkflowDocument> = {
  encode(message: WorkflowDocument, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    for (const v of message.nodes) {
      WorkflowNode.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.edges) {
      WorkflowEdge.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.createdAtMs !== 0) {
      writer.uint32(40).int64(message.createdAtMs);
    }
    if (message.updatedAtMs !== 0) {
      writer.uint32(48).int64(message.updatedAtMs);
    }
    if (message.schemaVersion !== 0) {
      writer.uint32(56).uint32(message.schemaVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowDocument {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowDocument();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.nodes.push(WorkflowNode.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.edges.push(WorkflowEdge.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.createdAtMs = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.updatedAtMs = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.schemaVersion = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowDocument {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      nodes: globalThis.Array.isArray(object?.nodes) ? object.nodes.map((e: any) => WorkflowNode.fromJSON(e)) : [],
      edges: globalThis.Array.isArray(object?.edges) ? object.edges.map((e: any) => WorkflowEdge.fromJSON(e)) : [],
      createdAtMs: isSet(object.createdAtMs)
        ? globalThis.Number(object.createdAtMs)
        : isSet(object.created_at_ms)
        ? globalThis.Number(object.created_at_ms)
        : 0,
      updatedAtMs: isSet(object.updatedAtMs)
        ? globalThis.Number(object.updatedAtMs)
        : isSet(object.updated_at_ms)
        ? globalThis.Number(object.updated_at_ms)
        : 0,
      schemaVersion: isSet(object.schemaVersion)
        ? globalThis.Number(object.schemaVersion)
        : isSet(object.schema_version)
        ? globalThis.Number(object.schema_version)
        : 0,
    };
  },

  toJSON(message: WorkflowDocument): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.nodes?.length) {
      obj.nodes = message.nodes.map((e) => WorkflowNode.toJSON(e));
    }
    if (message.edges?.length) {
      obj.edges = message.edges.map((e) => WorkflowEdge.toJSON(e));
    }
    if (message.createdAtMs !== 0) {
      obj.createdAtMs = Math.round(message.createdAtMs);
    }
    if (message.updatedAtMs !== 0) {
      obj.updatedAtMs = Math.round(message.updatedAtMs);
    }
    if (message.schemaVersion !== 0) {
      obj.schemaVersion = Math.round(message.schemaVersion);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowDocument>, I>>(base?: I): WorkflowDocument {
    return WorkflowDocument.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowDocument>, I>>(object: I): WorkflowDocument {
    const message = createBaseWorkflowDocument();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.nodes = object.nodes?.map((e) => WorkflowNode.fromPartial(e)) || [];
    message.edges = object.edges?.map((e) => WorkflowEdge.fromPartial(e)) || [];
    message.createdAtMs = object.createdAtMs ?? 0;
    message.updatedAtMs = object.updatedAtMs ?? 0;
    message.schemaVersion = object.schemaVersion ?? 0;
    return message;
  },
};

function createBaseNodePosition(): NodePosition {
  return { x: 0, y: 0 };
}

export const NodePosition: MessageFns<NodePosition> = {
  encode(message: NodePosition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.x !== 0) {
      writer.uint32(13).float(message.x);
    }
    if (message.y !== 0) {
      writer.uint32(21).float(message.y);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NodePosition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNodePosition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 13) {
            break;
          }

          message.x = reader.float();
          continue;
        }
        case 2: {
          if (tag !== 21) {
            break;
          }

          message.y = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NodePosition {
    return {
      x: isSet(object.x) ? globalThis.Number(object.x) : 0,
      y: isSet(object.y) ? globalThis.Number(object.y) : 0,
    };
  },

  toJSON(message: NodePosition): unknown {
    const obj: any = {};
    if (message.x !== 0) {
      obj.x = message.x;
    }
    if (message.y !== 0) {
      obj.y = message.y;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NodePosition>, I>>(base?: I): NodePosition {
    return NodePosition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NodePosition>, I>>(object: I): NodePosition {
    const message = createBaseNodePosition();
    message.x = object.x ?? 0;
    message.y = object.y ?? 0;
    return message;
  },
};

function createBaseWorkflowNode(): WorkflowNode {
  return {
    id: "",
    name: "",
    position: undefined,
    manualTrigger: undefined,
    toolCall: undefined,
    llmGenerate: undefined,
    condition: undefined,
    loopOverItems: undefined,
    code: undefined,
    httpRequest: undefined,
    setTransform: undefined,
    scheduleTrigger: undefined,
    merge: undefined,
    filter: undefined,
    splitOut: undefined,
    aggregate: undefined,
    wait: undefined,
    fileRead: undefined,
    fileWrite: undefined,
    llmStructured: undefined,
    vision: undefined,
    embed: undefined,
    rerank: undefined,
    transcribe: undefined,
    speak: undefined,
    detectVoice: undefined,
    diarize: undefined,
    segment: undefined,
    ragQuery: undefined,
    ragIngest: undefined,
    loadModel: undefined,
    packNode: undefined,
  };
}

export const WorkflowNode: MessageFns<WorkflowNode> = {
  encode(message: WorkflowNode, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.position !== undefined) {
      NodePosition.encode(message.position, writer.uint32(26).fork()).join();
    }
    if (message.manualTrigger !== undefined) {
      ManualTriggerConfig.encode(message.manualTrigger, writer.uint32(82).fork()).join();
    }
    if (message.toolCall !== undefined) {
      ToolCallConfig.encode(message.toolCall, writer.uint32(90).fork()).join();
    }
    if (message.llmGenerate !== undefined) {
      LlmGenerateConfig.encode(message.llmGenerate, writer.uint32(98).fork()).join();
    }
    if (message.condition !== undefined) {
      ConditionConfig.encode(message.condition, writer.uint32(106).fork()).join();
    }
    if (message.loopOverItems !== undefined) {
      LoopOverItemsConfig.encode(message.loopOverItems, writer.uint32(114).fork()).join();
    }
    if (message.code !== undefined) {
      CodeConfig.encode(message.code, writer.uint32(122).fork()).join();
    }
    if (message.httpRequest !== undefined) {
      HttpRequestConfig.encode(message.httpRequest, writer.uint32(130).fork()).join();
    }
    if (message.setTransform !== undefined) {
      SetTransformConfig.encode(message.setTransform, writer.uint32(138).fork()).join();
    }
    if (message.scheduleTrigger !== undefined) {
      ScheduleTriggerConfig.encode(message.scheduleTrigger, writer.uint32(146).fork()).join();
    }
    if (message.merge !== undefined) {
      MergeConfig.encode(message.merge, writer.uint32(154).fork()).join();
    }
    if (message.filter !== undefined) {
      FilterConfig.encode(message.filter, writer.uint32(162).fork()).join();
    }
    if (message.splitOut !== undefined) {
      SplitOutConfig.encode(message.splitOut, writer.uint32(170).fork()).join();
    }
    if (message.aggregate !== undefined) {
      AggregateConfig.encode(message.aggregate, writer.uint32(178).fork()).join();
    }
    if (message.wait !== undefined) {
      WaitConfig.encode(message.wait, writer.uint32(186).fork()).join();
    }
    if (message.fileRead !== undefined) {
      FileReadConfig.encode(message.fileRead, writer.uint32(194).fork()).join();
    }
    if (message.fileWrite !== undefined) {
      FileWriteConfig.encode(message.fileWrite, writer.uint32(202).fork()).join();
    }
    if (message.llmStructured !== undefined) {
      LlmStructuredConfig.encode(message.llmStructured, writer.uint32(210).fork()).join();
    }
    if (message.vision !== undefined) {
      VisionConfig.encode(message.vision, writer.uint32(218).fork()).join();
    }
    if (message.embed !== undefined) {
      EmbedConfig.encode(message.embed, writer.uint32(226).fork()).join();
    }
    if (message.rerank !== undefined) {
      RerankConfig.encode(message.rerank, writer.uint32(234).fork()).join();
    }
    if (message.transcribe !== undefined) {
      TranscribeConfig.encode(message.transcribe, writer.uint32(242).fork()).join();
    }
    if (message.speak !== undefined) {
      SpeakConfig.encode(message.speak, writer.uint32(250).fork()).join();
    }
    if (message.detectVoice !== undefined) {
      DetectVoiceConfig.encode(message.detectVoice, writer.uint32(258).fork()).join();
    }
    if (message.diarize !== undefined) {
      DiarizeConfig.encode(message.diarize, writer.uint32(266).fork()).join();
    }
    if (message.segment !== undefined) {
      SegmentConfig.encode(message.segment, writer.uint32(274).fork()).join();
    }
    if (message.ragQuery !== undefined) {
      RagQueryConfig.encode(message.ragQuery, writer.uint32(282).fork()).join();
    }
    if (message.ragIngest !== undefined) {
      RagIngestConfig.encode(message.ragIngest, writer.uint32(290).fork()).join();
    }
    if (message.loadModel !== undefined) {
      LoadModelConfig.encode(message.loadModel, writer.uint32(298).fork()).join();
    }
    if (message.packNode !== undefined) {
      PackNodeConfig.encode(message.packNode, writer.uint32(306).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowNode {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowNode();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.position = NodePosition.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.manualTrigger = ManualTriggerConfig.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.toolCall = ToolCallConfig.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.llmGenerate = LlmGenerateConfig.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.condition = ConditionConfig.decode(reader, reader.uint32());
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.loopOverItems = LoopOverItemsConfig.decode(reader, reader.uint32());
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.code = CodeConfig.decode(reader, reader.uint32());
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.httpRequest = HttpRequestConfig.decode(reader, reader.uint32());
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.setTransform = SetTransformConfig.decode(reader, reader.uint32());
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.scheduleTrigger = ScheduleTriggerConfig.decode(reader, reader.uint32());
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.merge = MergeConfig.decode(reader, reader.uint32());
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }

          message.filter = FilterConfig.decode(reader, reader.uint32());
          continue;
        }
        case 21: {
          if (tag !== 170) {
            break;
          }

          message.splitOut = SplitOutConfig.decode(reader, reader.uint32());
          continue;
        }
        case 22: {
          if (tag !== 178) {
            break;
          }

          message.aggregate = AggregateConfig.decode(reader, reader.uint32());
          continue;
        }
        case 23: {
          if (tag !== 186) {
            break;
          }

          message.wait = WaitConfig.decode(reader, reader.uint32());
          continue;
        }
        case 24: {
          if (tag !== 194) {
            break;
          }

          message.fileRead = FileReadConfig.decode(reader, reader.uint32());
          continue;
        }
        case 25: {
          if (tag !== 202) {
            break;
          }

          message.fileWrite = FileWriteConfig.decode(reader, reader.uint32());
          continue;
        }
        case 26: {
          if (tag !== 210) {
            break;
          }

          message.llmStructured = LlmStructuredConfig.decode(reader, reader.uint32());
          continue;
        }
        case 27: {
          if (tag !== 218) {
            break;
          }

          message.vision = VisionConfig.decode(reader, reader.uint32());
          continue;
        }
        case 28: {
          if (tag !== 226) {
            break;
          }

          message.embed = EmbedConfig.decode(reader, reader.uint32());
          continue;
        }
        case 29: {
          if (tag !== 234) {
            break;
          }

          message.rerank = RerankConfig.decode(reader, reader.uint32());
          continue;
        }
        case 30: {
          if (tag !== 242) {
            break;
          }

          message.transcribe = TranscribeConfig.decode(reader, reader.uint32());
          continue;
        }
        case 31: {
          if (tag !== 250) {
            break;
          }

          message.speak = SpeakConfig.decode(reader, reader.uint32());
          continue;
        }
        case 32: {
          if (tag !== 258) {
            break;
          }

          message.detectVoice = DetectVoiceConfig.decode(reader, reader.uint32());
          continue;
        }
        case 33: {
          if (tag !== 266) {
            break;
          }

          message.diarize = DiarizeConfig.decode(reader, reader.uint32());
          continue;
        }
        case 34: {
          if (tag !== 274) {
            break;
          }

          message.segment = SegmentConfig.decode(reader, reader.uint32());
          continue;
        }
        case 35: {
          if (tag !== 282) {
            break;
          }

          message.ragQuery = RagQueryConfig.decode(reader, reader.uint32());
          continue;
        }
        case 36: {
          if (tag !== 290) {
            break;
          }

          message.ragIngest = RagIngestConfig.decode(reader, reader.uint32());
          continue;
        }
        case 37: {
          if (tag !== 298) {
            break;
          }

          message.loadModel = LoadModelConfig.decode(reader, reader.uint32());
          continue;
        }
        case 38: {
          if (tag !== 306) {
            break;
          }

          message.packNode = PackNodeConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowNode {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      position: isSet(object.position) ? NodePosition.fromJSON(object.position) : undefined,
      manualTrigger: isSet(object.manualTrigger)
        ? ManualTriggerConfig.fromJSON(object.manualTrigger)
        : isSet(object.manual_trigger)
        ? ManualTriggerConfig.fromJSON(object.manual_trigger)
        : undefined,
      toolCall: isSet(object.toolCall)
        ? ToolCallConfig.fromJSON(object.toolCall)
        : isSet(object.tool_call)
        ? ToolCallConfig.fromJSON(object.tool_call)
        : undefined,
      llmGenerate: isSet(object.llmGenerate)
        ? LlmGenerateConfig.fromJSON(object.llmGenerate)
        : isSet(object.llm_generate)
        ? LlmGenerateConfig.fromJSON(object.llm_generate)
        : undefined,
      condition: isSet(object.condition) ? ConditionConfig.fromJSON(object.condition) : undefined,
      loopOverItems: isSet(object.loopOverItems)
        ? LoopOverItemsConfig.fromJSON(object.loopOverItems)
        : isSet(object.loop_over_items)
        ? LoopOverItemsConfig.fromJSON(object.loop_over_items)
        : undefined,
      code: isSet(object.code) ? CodeConfig.fromJSON(object.code) : undefined,
      httpRequest: isSet(object.httpRequest)
        ? HttpRequestConfig.fromJSON(object.httpRequest)
        : isSet(object.http_request)
        ? HttpRequestConfig.fromJSON(object.http_request)
        : undefined,
      setTransform: isSet(object.setTransform)
        ? SetTransformConfig.fromJSON(object.setTransform)
        : isSet(object.set_transform)
        ? SetTransformConfig.fromJSON(object.set_transform)
        : undefined,
      scheduleTrigger: isSet(object.scheduleTrigger)
        ? ScheduleTriggerConfig.fromJSON(object.scheduleTrigger)
        : isSet(object.schedule_trigger)
        ? ScheduleTriggerConfig.fromJSON(object.schedule_trigger)
        : undefined,
      merge: isSet(object.merge) ? MergeConfig.fromJSON(object.merge) : undefined,
      filter: isSet(object.filter) ? FilterConfig.fromJSON(object.filter) : undefined,
      splitOut: isSet(object.splitOut)
        ? SplitOutConfig.fromJSON(object.splitOut)
        : isSet(object.split_out)
        ? SplitOutConfig.fromJSON(object.split_out)
        : undefined,
      aggregate: isSet(object.aggregate) ? AggregateConfig.fromJSON(object.aggregate) : undefined,
      wait: isSet(object.wait) ? WaitConfig.fromJSON(object.wait) : undefined,
      fileRead: isSet(object.fileRead)
        ? FileReadConfig.fromJSON(object.fileRead)
        : isSet(object.file_read)
        ? FileReadConfig.fromJSON(object.file_read)
        : undefined,
      fileWrite: isSet(object.fileWrite)
        ? FileWriteConfig.fromJSON(object.fileWrite)
        : isSet(object.file_write)
        ? FileWriteConfig.fromJSON(object.file_write)
        : undefined,
      llmStructured: isSet(object.llmStructured)
        ? LlmStructuredConfig.fromJSON(object.llmStructured)
        : isSet(object.llm_structured)
        ? LlmStructuredConfig.fromJSON(object.llm_structured)
        : undefined,
      vision: isSet(object.vision) ? VisionConfig.fromJSON(object.vision) : undefined,
      embed: isSet(object.embed) ? EmbedConfig.fromJSON(object.embed) : undefined,
      rerank: isSet(object.rerank) ? RerankConfig.fromJSON(object.rerank) : undefined,
      transcribe: isSet(object.transcribe) ? TranscribeConfig.fromJSON(object.transcribe) : undefined,
      speak: isSet(object.speak) ? SpeakConfig.fromJSON(object.speak) : undefined,
      detectVoice: isSet(object.detectVoice)
        ? DetectVoiceConfig.fromJSON(object.detectVoice)
        : isSet(object.detect_voice)
        ? DetectVoiceConfig.fromJSON(object.detect_voice)
        : undefined,
      diarize: isSet(object.diarize) ? DiarizeConfig.fromJSON(object.diarize) : undefined,
      segment: isSet(object.segment) ? SegmentConfig.fromJSON(object.segment) : undefined,
      ragQuery: isSet(object.ragQuery)
        ? RagQueryConfig.fromJSON(object.ragQuery)
        : isSet(object.rag_query)
        ? RagQueryConfig.fromJSON(object.rag_query)
        : undefined,
      ragIngest: isSet(object.ragIngest)
        ? RagIngestConfig.fromJSON(object.ragIngest)
        : isSet(object.rag_ingest)
        ? RagIngestConfig.fromJSON(object.rag_ingest)
        : undefined,
      loadModel: isSet(object.loadModel)
        ? LoadModelConfig.fromJSON(object.loadModel)
        : isSet(object.load_model)
        ? LoadModelConfig.fromJSON(object.load_model)
        : undefined,
      packNode: isSet(object.packNode)
        ? PackNodeConfig.fromJSON(object.packNode)
        : isSet(object.pack_node)
        ? PackNodeConfig.fromJSON(object.pack_node)
        : undefined,
    };
  },

  toJSON(message: WorkflowNode): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.position !== undefined) {
      obj.position = NodePosition.toJSON(message.position);
    }
    if (message.manualTrigger !== undefined) {
      obj.manualTrigger = ManualTriggerConfig.toJSON(message.manualTrigger);
    }
    if (message.toolCall !== undefined) {
      obj.toolCall = ToolCallConfig.toJSON(message.toolCall);
    }
    if (message.llmGenerate !== undefined) {
      obj.llmGenerate = LlmGenerateConfig.toJSON(message.llmGenerate);
    }
    if (message.condition !== undefined) {
      obj.condition = ConditionConfig.toJSON(message.condition);
    }
    if (message.loopOverItems !== undefined) {
      obj.loopOverItems = LoopOverItemsConfig.toJSON(message.loopOverItems);
    }
    if (message.code !== undefined) {
      obj.code = CodeConfig.toJSON(message.code);
    }
    if (message.httpRequest !== undefined) {
      obj.httpRequest = HttpRequestConfig.toJSON(message.httpRequest);
    }
    if (message.setTransform !== undefined) {
      obj.setTransform = SetTransformConfig.toJSON(message.setTransform);
    }
    if (message.scheduleTrigger !== undefined) {
      obj.scheduleTrigger = ScheduleTriggerConfig.toJSON(message.scheduleTrigger);
    }
    if (message.merge !== undefined) {
      obj.merge = MergeConfig.toJSON(message.merge);
    }
    if (message.filter !== undefined) {
      obj.filter = FilterConfig.toJSON(message.filter);
    }
    if (message.splitOut !== undefined) {
      obj.splitOut = SplitOutConfig.toJSON(message.splitOut);
    }
    if (message.aggregate !== undefined) {
      obj.aggregate = AggregateConfig.toJSON(message.aggregate);
    }
    if (message.wait !== undefined) {
      obj.wait = WaitConfig.toJSON(message.wait);
    }
    if (message.fileRead !== undefined) {
      obj.fileRead = FileReadConfig.toJSON(message.fileRead);
    }
    if (message.fileWrite !== undefined) {
      obj.fileWrite = FileWriteConfig.toJSON(message.fileWrite);
    }
    if (message.llmStructured !== undefined) {
      obj.llmStructured = LlmStructuredConfig.toJSON(message.llmStructured);
    }
    if (message.vision !== undefined) {
      obj.vision = VisionConfig.toJSON(message.vision);
    }
    if (message.embed !== undefined) {
      obj.embed = EmbedConfig.toJSON(message.embed);
    }
    if (message.rerank !== undefined) {
      obj.rerank = RerankConfig.toJSON(message.rerank);
    }
    if (message.transcribe !== undefined) {
      obj.transcribe = TranscribeConfig.toJSON(message.transcribe);
    }
    if (message.speak !== undefined) {
      obj.speak = SpeakConfig.toJSON(message.speak);
    }
    if (message.detectVoice !== undefined) {
      obj.detectVoice = DetectVoiceConfig.toJSON(message.detectVoice);
    }
    if (message.diarize !== undefined) {
      obj.diarize = DiarizeConfig.toJSON(message.diarize);
    }
    if (message.segment !== undefined) {
      obj.segment = SegmentConfig.toJSON(message.segment);
    }
    if (message.ragQuery !== undefined) {
      obj.ragQuery = RagQueryConfig.toJSON(message.ragQuery);
    }
    if (message.ragIngest !== undefined) {
      obj.ragIngest = RagIngestConfig.toJSON(message.ragIngest);
    }
    if (message.loadModel !== undefined) {
      obj.loadModel = LoadModelConfig.toJSON(message.loadModel);
    }
    if (message.packNode !== undefined) {
      obj.packNode = PackNodeConfig.toJSON(message.packNode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowNode>, I>>(base?: I): WorkflowNode {
    return WorkflowNode.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowNode>, I>>(object: I): WorkflowNode {
    const message = createBaseWorkflowNode();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.position = (object.position !== undefined && object.position !== null)
      ? NodePosition.fromPartial(object.position)
      : undefined;
    message.manualTrigger = (object.manualTrigger !== undefined && object.manualTrigger !== null)
      ? ManualTriggerConfig.fromPartial(object.manualTrigger)
      : undefined;
    message.toolCall = (object.toolCall !== undefined && object.toolCall !== null)
      ? ToolCallConfig.fromPartial(object.toolCall)
      : undefined;
    message.llmGenerate = (object.llmGenerate !== undefined && object.llmGenerate !== null)
      ? LlmGenerateConfig.fromPartial(object.llmGenerate)
      : undefined;
    message.condition = (object.condition !== undefined && object.condition !== null)
      ? ConditionConfig.fromPartial(object.condition)
      : undefined;
    message.loopOverItems = (object.loopOverItems !== undefined && object.loopOverItems !== null)
      ? LoopOverItemsConfig.fromPartial(object.loopOverItems)
      : undefined;
    message.code = (object.code !== undefined && object.code !== null)
      ? CodeConfig.fromPartial(object.code)
      : undefined;
    message.httpRequest = (object.httpRequest !== undefined && object.httpRequest !== null)
      ? HttpRequestConfig.fromPartial(object.httpRequest)
      : undefined;
    message.setTransform = (object.setTransform !== undefined && object.setTransform !== null)
      ? SetTransformConfig.fromPartial(object.setTransform)
      : undefined;
    message.scheduleTrigger = (object.scheduleTrigger !== undefined && object.scheduleTrigger !== null)
      ? ScheduleTriggerConfig.fromPartial(object.scheduleTrigger)
      : undefined;
    message.merge = (object.merge !== undefined && object.merge !== null)
      ? MergeConfig.fromPartial(object.merge)
      : undefined;
    message.filter = (object.filter !== undefined && object.filter !== null)
      ? FilterConfig.fromPartial(object.filter)
      : undefined;
    message.splitOut = (object.splitOut !== undefined && object.splitOut !== null)
      ? SplitOutConfig.fromPartial(object.splitOut)
      : undefined;
    message.aggregate = (object.aggregate !== undefined && object.aggregate !== null)
      ? AggregateConfig.fromPartial(object.aggregate)
      : undefined;
    message.wait = (object.wait !== undefined && object.wait !== null)
      ? WaitConfig.fromPartial(object.wait)
      : undefined;
    message.fileRead = (object.fileRead !== undefined && object.fileRead !== null)
      ? FileReadConfig.fromPartial(object.fileRead)
      : undefined;
    message.fileWrite = (object.fileWrite !== undefined && object.fileWrite !== null)
      ? FileWriteConfig.fromPartial(object.fileWrite)
      : undefined;
    message.llmStructured = (object.llmStructured !== undefined && object.llmStructured !== null)
      ? LlmStructuredConfig.fromPartial(object.llmStructured)
      : undefined;
    message.vision = (object.vision !== undefined && object.vision !== null)
      ? VisionConfig.fromPartial(object.vision)
      : undefined;
    message.embed = (object.embed !== undefined && object.embed !== null)
      ? EmbedConfig.fromPartial(object.embed)
      : undefined;
    message.rerank = (object.rerank !== undefined && object.rerank !== null)
      ? RerankConfig.fromPartial(object.rerank)
      : undefined;
    message.transcribe = (object.transcribe !== undefined && object.transcribe !== null)
      ? TranscribeConfig.fromPartial(object.transcribe)
      : undefined;
    message.speak = (object.speak !== undefined && object.speak !== null)
      ? SpeakConfig.fromPartial(object.speak)
      : undefined;
    message.detectVoice = (object.detectVoice !== undefined && object.detectVoice !== null)
      ? DetectVoiceConfig.fromPartial(object.detectVoice)
      : undefined;
    message.diarize = (object.diarize !== undefined && object.diarize !== null)
      ? DiarizeConfig.fromPartial(object.diarize)
      : undefined;
    message.segment = (object.segment !== undefined && object.segment !== null)
      ? SegmentConfig.fromPartial(object.segment)
      : undefined;
    message.ragQuery = (object.ragQuery !== undefined && object.ragQuery !== null)
      ? RagQueryConfig.fromPartial(object.ragQuery)
      : undefined;
    message.ragIngest = (object.ragIngest !== undefined && object.ragIngest !== null)
      ? RagIngestConfig.fromPartial(object.ragIngest)
      : undefined;
    message.loadModel = (object.loadModel !== undefined && object.loadModel !== null)
      ? LoadModelConfig.fromPartial(object.loadModel)
      : undefined;
    message.packNode = (object.packNode !== undefined && object.packNode !== null)
      ? PackNodeConfig.fromPartial(object.packNode)
      : undefined;
    return message;
  },
};

function createBasePackNodeConfig(): PackNodeConfig {
  return { packId: "", arguments: {}, ports: [], missing: false, outputs: [] };
}

export const PackNodeConfig: MessageFns<PackNodeConfig> = {
  encode(message: PackNodeConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.packId !== "") {
      writer.uint32(10).string(message.packId);
    }
    globalThis.Object.entries(message.arguments).forEach(([key, value]: [string, string]) => {
      PackNodeConfig_ArgumentsEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    for (const v of message.ports) {
      ToolArgumentPort.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.missing !== false) {
      writer.uint32(32).bool(message.missing);
    }
    for (const v of message.outputs) {
      writer.uint32(42).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PackNodeConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePackNodeConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.packId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = PackNodeConfig_ArgumentsEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.arguments[entry2.key] = entry2.value;
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.ports.push(ToolArgumentPort.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.missing = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.outputs.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PackNodeConfig {
    return {
      packId: isSet(object.packId)
        ? globalThis.String(object.packId)
        : isSet(object.pack_id)
        ? globalThis.String(object.pack_id)
        : "",
      arguments: isObject(object.arguments)
        ? (globalThis.Object.entries(object.arguments) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      ports: globalThis.Array.isArray(object?.ports) ? object.ports.map((e: any) => ToolArgumentPort.fromJSON(e)) : [],
      missing: isSet(object.missing) ? globalThis.Boolean(object.missing) : false,
      outputs: globalThis.Array.isArray(object?.outputs) ? object.outputs.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: PackNodeConfig): unknown {
    const obj: any = {};
    if (message.packId !== "") {
      obj.packId = message.packId;
    }
    if (message.arguments) {
      const entries = globalThis.Object.entries(message.arguments) as [string, string][];
      if (entries.length > 0) {
        obj.arguments = {};
        entries.forEach(([k, v]) => {
          obj.arguments[k] = v;
        });
      }
    }
    if (message.ports?.length) {
      obj.ports = message.ports.map((e) => ToolArgumentPort.toJSON(e));
    }
    if (message.missing !== false) {
      obj.missing = message.missing;
    }
    if (message.outputs?.length) {
      obj.outputs = message.outputs;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PackNodeConfig>, I>>(base?: I): PackNodeConfig {
    return PackNodeConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PackNodeConfig>, I>>(object: I): PackNodeConfig {
    const message = createBasePackNodeConfig();
    message.packId = object.packId ?? "";
    message.arguments = (globalThis.Object.entries(object.arguments ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.ports = object.ports?.map((e) => ToolArgumentPort.fromPartial(e)) || [];
    message.missing = object.missing ?? false;
    message.outputs = object.outputs?.map((e) => e) || [];
    return message;
  },
};

function createBasePackNodeConfig_ArgumentsEntry(): PackNodeConfig_ArgumentsEntry {
  return { key: "", value: "" };
}

export const PackNodeConfig_ArgumentsEntry: MessageFns<PackNodeConfig_ArgumentsEntry> = {
  encode(message: PackNodeConfig_ArgumentsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PackNodeConfig_ArgumentsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePackNodeConfig_ArgumentsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PackNodeConfig_ArgumentsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: PackNodeConfig_ArgumentsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PackNodeConfig_ArgumentsEntry>, I>>(base?: I): PackNodeConfig_ArgumentsEntry {
    return PackNodeConfig_ArgumentsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PackNodeConfig_ArgumentsEntry>, I>>(
    object: I,
  ): PackNodeConfig_ArgumentsEntry {
    const message = createBasePackNodeConfig_ArgumentsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseWorkflowEdge(): WorkflowEdge {
  return { fromNode: "", fromPort: "", toNode: "", toPort: "" };
}

export const WorkflowEdge: MessageFns<WorkflowEdge> = {
  encode(message: WorkflowEdge, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fromNode !== "") {
      writer.uint32(10).string(message.fromNode);
    }
    if (message.fromPort !== "") {
      writer.uint32(18).string(message.fromPort);
    }
    if (message.toNode !== "") {
      writer.uint32(26).string(message.toNode);
    }
    if (message.toPort !== "") {
      writer.uint32(34).string(message.toPort);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowEdge {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowEdge();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.fromNode = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fromPort = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.toNode = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.toPort = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowEdge {
    return {
      fromNode: isSet(object.fromNode)
        ? globalThis.String(object.fromNode)
        : isSet(object.from_node)
        ? globalThis.String(object.from_node)
        : "",
      fromPort: isSet(object.fromPort)
        ? globalThis.String(object.fromPort)
        : isSet(object.from_port)
        ? globalThis.String(object.from_port)
        : "",
      toNode: isSet(object.toNode)
        ? globalThis.String(object.toNode)
        : isSet(object.to_node)
        ? globalThis.String(object.to_node)
        : "",
      toPort: isSet(object.toPort)
        ? globalThis.String(object.toPort)
        : isSet(object.to_port)
        ? globalThis.String(object.to_port)
        : "",
    };
  },

  toJSON(message: WorkflowEdge): unknown {
    const obj: any = {};
    if (message.fromNode !== "") {
      obj.fromNode = message.fromNode;
    }
    if (message.fromPort !== "") {
      obj.fromPort = message.fromPort;
    }
    if (message.toNode !== "") {
      obj.toNode = message.toNode;
    }
    if (message.toPort !== "") {
      obj.toPort = message.toPort;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowEdge>, I>>(base?: I): WorkflowEdge {
    return WorkflowEdge.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowEdge>, I>>(object: I): WorkflowEdge {
    const message = createBaseWorkflowEdge();
    message.fromNode = object.fromNode ?? "";
    message.fromPort = object.fromPort ?? "";
    message.toNode = object.toNode ?? "";
    message.toPort = object.toPort ?? "";
    return message;
  },
};

function createBaseManualTriggerConfig(): ManualTriggerConfig {
  return { initialItemsJson: "" };
}

export const ManualTriggerConfig: MessageFns<ManualTriggerConfig> = {
  encode(message: ManualTriggerConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.initialItemsJson !== "") {
      writer.uint32(10).string(message.initialItemsJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ManualTriggerConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseManualTriggerConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.initialItemsJson = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ManualTriggerConfig {
    return {
      initialItemsJson: isSet(object.initialItemsJson)
        ? globalThis.String(object.initialItemsJson)
        : isSet(object.initial_items_json)
        ? globalThis.String(object.initial_items_json)
        : "",
    };
  },

  toJSON(message: ManualTriggerConfig): unknown {
    const obj: any = {};
    if (message.initialItemsJson !== "") {
      obj.initialItemsJson = message.initialItemsJson;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ManualTriggerConfig>, I>>(base?: I): ManualTriggerConfig {
    return ManualTriggerConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ManualTriggerConfig>, I>>(object: I): ManualTriggerConfig {
    const message = createBaseManualTriggerConfig();
    message.initialItemsJson = object.initialItemsJson ?? "";
    return message;
  },
};

function createBaseToolCallConfig(): ToolCallConfig {
  return { toolName: "", arguments: {}, ports: [] };
}

export const ToolCallConfig: MessageFns<ToolCallConfig> = {
  encode(message: ToolCallConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.toolName !== "") {
      writer.uint32(10).string(message.toolName);
    }
    globalThis.Object.entries(message.arguments).forEach(([key, value]: [string, string]) => {
      ToolCallConfig_ArgumentsEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    for (const v of message.ports) {
      ToolArgumentPort.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolCallConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolCallConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.toolName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = ToolCallConfig_ArgumentsEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.arguments[entry2.key] = entry2.value;
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.ports.push(ToolArgumentPort.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolCallConfig {
    return {
      toolName: isSet(object.toolName)
        ? globalThis.String(object.toolName)
        : isSet(object.tool_name)
        ? globalThis.String(object.tool_name)
        : "",
      arguments: isObject(object.arguments)
        ? (globalThis.Object.entries(object.arguments) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      ports: globalThis.Array.isArray(object?.ports) ? object.ports.map((e: any) => ToolArgumentPort.fromJSON(e)) : [],
    };
  },

  toJSON(message: ToolCallConfig): unknown {
    const obj: any = {};
    if (message.toolName !== "") {
      obj.toolName = message.toolName;
    }
    if (message.arguments) {
      const entries = globalThis.Object.entries(message.arguments) as [string, string][];
      if (entries.length > 0) {
        obj.arguments = {};
        entries.forEach(([k, v]) => {
          obj.arguments[k] = v;
        });
      }
    }
    if (message.ports?.length) {
      obj.ports = message.ports.map((e) => ToolArgumentPort.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolCallConfig>, I>>(base?: I): ToolCallConfig {
    return ToolCallConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolCallConfig>, I>>(object: I): ToolCallConfig {
    const message = createBaseToolCallConfig();
    message.toolName = object.toolName ?? "";
    message.arguments = (globalThis.Object.entries(object.arguments ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.ports = object.ports?.map((e) => ToolArgumentPort.fromPartial(e)) || [];
    return message;
  },
};

function createBaseToolCallConfig_ArgumentsEntry(): ToolCallConfig_ArgumentsEntry {
  return { key: "", value: "" };
}

export const ToolCallConfig_ArgumentsEntry: MessageFns<ToolCallConfig_ArgumentsEntry> = {
  encode(message: ToolCallConfig_ArgumentsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolCallConfig_ArgumentsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolCallConfig_ArgumentsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolCallConfig_ArgumentsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ToolCallConfig_ArgumentsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolCallConfig_ArgumentsEntry>, I>>(base?: I): ToolCallConfig_ArgumentsEntry {
    return ToolCallConfig_ArgumentsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolCallConfig_ArgumentsEntry>, I>>(
    object: I,
  ): ToolCallConfig_ArgumentsEntry {
    const message = createBaseToolCallConfig_ArgumentsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseToolArgumentPort(): ToolArgumentPort {
  return { name: "", description: "", required: false, type: 0 };
}

export const ToolArgumentPort: MessageFns<ToolArgumentPort> = {
  encode(message: ToolArgumentPort, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.required !== false) {
      writer.uint32(24).bool(message.required);
    }
    if (message.type !== 0) {
      writer.uint32(32).int32(message.type);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolArgumentPort {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolArgumentPort();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.required = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolArgumentPort {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      required: isSet(object.required) ? globalThis.Boolean(object.required) : false,
      type: isSet(object.type) ? toolArgumentTypeFromJSON(object.type) : 0,
    };
  },

  toJSON(message: ToolArgumentPort): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.required !== false) {
      obj.required = message.required;
    }
    if (message.type !== 0) {
      obj.type = toolArgumentTypeToJSON(message.type);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolArgumentPort>, I>>(base?: I): ToolArgumentPort {
    return ToolArgumentPort.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolArgumentPort>, I>>(object: I): ToolArgumentPort {
    const message = createBaseToolArgumentPort();
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.required = object.required ?? false;
    message.type = object.type ?? 0;
    return message;
  },
};

function createBaseLlmGenerateConfig(): LlmGenerateConfig {
  return { prompt: "", systemPrompt: undefined, modelId: undefined, generation: undefined };
}

export const LlmGenerateConfig: MessageFns<LlmGenerateConfig> = {
  encode(message: LlmGenerateConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.prompt !== "") {
      writer.uint32(10).string(message.prompt);
    }
    if (message.systemPrompt !== undefined) {
      writer.uint32(18).string(message.systemPrompt);
    }
    if (message.modelId !== undefined) {
      writer.uint32(26).string(message.modelId);
    }
    if (message.generation !== undefined) {
      LLMGenerationOptions.encode(message.generation, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LlmGenerateConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLlmGenerateConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.prompt = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.systemPrompt = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.generation = LLMGenerationOptions.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LlmGenerateConfig {
    return {
      prompt: isSet(object.prompt) ? globalThis.String(object.prompt) : "",
      systemPrompt: isSet(object.systemPrompt)
        ? globalThis.String(object.systemPrompt)
        : isSet(object.system_prompt)
        ? globalThis.String(object.system_prompt)
        : undefined,
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : undefined,
      generation: isSet(object.generation) ? LLMGenerationOptions.fromJSON(object.generation) : undefined,
    };
  },

  toJSON(message: LlmGenerateConfig): unknown {
    const obj: any = {};
    if (message.prompt !== "") {
      obj.prompt = message.prompt;
    }
    if (message.systemPrompt !== undefined) {
      obj.systemPrompt = message.systemPrompt;
    }
    if (message.modelId !== undefined) {
      obj.modelId = message.modelId;
    }
    if (message.generation !== undefined) {
      obj.generation = LLMGenerationOptions.toJSON(message.generation);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LlmGenerateConfig>, I>>(base?: I): LlmGenerateConfig {
    return LlmGenerateConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LlmGenerateConfig>, I>>(object: I): LlmGenerateConfig {
    const message = createBaseLlmGenerateConfig();
    message.prompt = object.prompt ?? "";
    message.systemPrompt = object.systemPrompt ?? undefined;
    message.modelId = object.modelId ?? undefined;
    message.generation = (object.generation !== undefined && object.generation !== null)
      ? LLMGenerationOptions.fromPartial(object.generation)
      : undefined;
    return message;
  },
};

function createBaseConditionConfig(): ConditionConfig {
  return { left: "", operator: 0, right: "" };
}

export const ConditionConfig: MessageFns<ConditionConfig> = {
  encode(message: ConditionConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.left !== "") {
      writer.uint32(10).string(message.left);
    }
    if (message.operator !== 0) {
      writer.uint32(16).int32(message.operator);
    }
    if (message.right !== "") {
      writer.uint32(26).string(message.right);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConditionConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConditionConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.left = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.operator = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.right = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConditionConfig {
    return {
      left: isSet(object.left) ? globalThis.String(object.left) : "",
      operator: isSet(object.operator) ? comparisonOperatorFromJSON(object.operator) : 0,
      right: isSet(object.right) ? globalThis.String(object.right) : "",
    };
  },

  toJSON(message: ConditionConfig): unknown {
    const obj: any = {};
    if (message.left !== "") {
      obj.left = message.left;
    }
    if (message.operator !== 0) {
      obj.operator = comparisonOperatorToJSON(message.operator);
    }
    if (message.right !== "") {
      obj.right = message.right;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConditionConfig>, I>>(base?: I): ConditionConfig {
    return ConditionConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConditionConfig>, I>>(object: I): ConditionConfig {
    const message = createBaseConditionConfig();
    message.left = object.left ?? "";
    message.operator = object.operator ?? 0;
    message.right = object.right ?? "";
    return message;
  },
};

function createBaseLoopOverItemsConfig(): LoopOverItemsConfig {
  return { items: "", bodyNodeIds: [], maxIterations: 0 };
}

export const LoopOverItemsConfig: MessageFns<LoopOverItemsConfig> = {
  encode(message: LoopOverItemsConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.items !== "") {
      writer.uint32(10).string(message.items);
    }
    for (const v of message.bodyNodeIds) {
      writer.uint32(18).string(v!);
    }
    if (message.maxIterations !== 0) {
      writer.uint32(24).uint32(message.maxIterations);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LoopOverItemsConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLoopOverItemsConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.bodyNodeIds.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.maxIterations = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LoopOverItemsConfig {
    return {
      items: isSet(object.items) ? globalThis.String(object.items) : "",
      bodyNodeIds: globalThis.Array.isArray(object?.bodyNodeIds)
        ? object.bodyNodeIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.body_node_ids)
        ? object.body_node_ids.map((e: any) => globalThis.String(e))
        : [],
      maxIterations: isSet(object.maxIterations)
        ? globalThis.Number(object.maxIterations)
        : isSet(object.max_iterations)
        ? globalThis.Number(object.max_iterations)
        : 0,
    };
  },

  toJSON(message: LoopOverItemsConfig): unknown {
    const obj: any = {};
    if (message.items !== "") {
      obj.items = message.items;
    }
    if (message.bodyNodeIds?.length) {
      obj.bodyNodeIds = message.bodyNodeIds;
    }
    if (message.maxIterations !== 0) {
      obj.maxIterations = Math.round(message.maxIterations);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LoopOverItemsConfig>, I>>(base?: I): LoopOverItemsConfig {
    return LoopOverItemsConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LoopOverItemsConfig>, I>>(object: I): LoopOverItemsConfig {
    const message = createBaseLoopOverItemsConfig();
    message.items = object.items ?? "";
    message.bodyNodeIds = object.bodyNodeIds?.map((e) => e) || [];
    message.maxIterations = object.maxIterations ?? 0;
    return message;
  },
};

function createBaseCodeConfig(): CodeConfig {
  return { source: "" };
}

export const CodeConfig: MessageFns<CodeConfig> = {
  encode(message: CodeConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.source !== "") {
      writer.uint32(10).string(message.source);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CodeConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCodeConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.source = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CodeConfig {
    return { source: isSet(object.source) ? globalThis.String(object.source) : "" };
  },

  toJSON(message: CodeConfig): unknown {
    const obj: any = {};
    if (message.source !== "") {
      obj.source = message.source;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CodeConfig>, I>>(base?: I): CodeConfig {
    return CodeConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CodeConfig>, I>>(object: I): CodeConfig {
    const message = createBaseCodeConfig();
    message.source = object.source ?? "";
    return message;
  },
};

function createBaseHttpRequestConfig(): HttpRequestConfig {
  return { method: 0, url: "", headers: {}, body: undefined, timeoutMs: undefined, auth: undefined };
}

export const HttpRequestConfig: MessageFns<HttpRequestConfig> = {
  encode(message: HttpRequestConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.method !== 0) {
      writer.uint32(8).int32(message.method);
    }
    if (message.url !== "") {
      writer.uint32(18).string(message.url);
    }
    globalThis.Object.entries(message.headers).forEach(([key, value]: [string, string]) => {
      HttpRequestConfig_HeadersEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    if (message.body !== undefined) {
      writer.uint32(34).string(message.body);
    }
    if (message.timeoutMs !== undefined) {
      writer.uint32(40).uint32(message.timeoutMs);
    }
    if (message.auth !== undefined) {
      HttpAuth.encode(message.auth, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HttpRequestConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHttpRequestConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.method = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = HttpRequestConfig_HeadersEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.headers[entry3.key] = entry3.value;
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.body = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.timeoutMs = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.auth = HttpAuth.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HttpRequestConfig {
    return {
      method: isSet(object.method) ? httpMethodFromJSON(object.method) : 0,
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      headers: isObject(object.headers)
        ? (globalThis.Object.entries(object.headers) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      body: isSet(object.body) ? globalThis.String(object.body) : undefined,
      timeoutMs: isSet(object.timeoutMs)
        ? globalThis.Number(object.timeoutMs)
        : isSet(object.timeout_ms)
        ? globalThis.Number(object.timeout_ms)
        : undefined,
      auth: isSet(object.auth) ? HttpAuth.fromJSON(object.auth) : undefined,
    };
  },

  toJSON(message: HttpRequestConfig): unknown {
    const obj: any = {};
    if (message.method !== 0) {
      obj.method = httpMethodToJSON(message.method);
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.headers) {
      const entries = globalThis.Object.entries(message.headers) as [string, string][];
      if (entries.length > 0) {
        obj.headers = {};
        entries.forEach(([k, v]) => {
          obj.headers[k] = v;
        });
      }
    }
    if (message.body !== undefined) {
      obj.body = message.body;
    }
    if (message.timeoutMs !== undefined) {
      obj.timeoutMs = Math.round(message.timeoutMs);
    }
    if (message.auth !== undefined) {
      obj.auth = HttpAuth.toJSON(message.auth);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HttpRequestConfig>, I>>(base?: I): HttpRequestConfig {
    return HttpRequestConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HttpRequestConfig>, I>>(object: I): HttpRequestConfig {
    const message = createBaseHttpRequestConfig();
    message.method = object.method ?? 0;
    message.url = object.url ?? "";
    message.headers = (globalThis.Object.entries(object.headers ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.body = object.body ?? undefined;
    message.timeoutMs = object.timeoutMs ?? undefined;
    message.auth = (object.auth !== undefined && object.auth !== null) ? HttpAuth.fromPartial(object.auth) : undefined;
    return message;
  },
};

function createBaseHttpRequestConfig_HeadersEntry(): HttpRequestConfig_HeadersEntry {
  return { key: "", value: "" };
}

export const HttpRequestConfig_HeadersEntry: MessageFns<HttpRequestConfig_HeadersEntry> = {
  encode(message: HttpRequestConfig_HeadersEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HttpRequestConfig_HeadersEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHttpRequestConfig_HeadersEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HttpRequestConfig_HeadersEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: HttpRequestConfig_HeadersEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HttpRequestConfig_HeadersEntry>, I>>(base?: I): HttpRequestConfig_HeadersEntry {
    return HttpRequestConfig_HeadersEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HttpRequestConfig_HeadersEntry>, I>>(
    object: I,
  ): HttpRequestConfig_HeadersEntry {
    const message = createBaseHttpRequestConfig_HeadersEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseHttpAuth(): HttpAuth {
  return { kind: 0, secret: "", name: "" };
}

export const HttpAuth: MessageFns<HttpAuth> = {
  encode(message: HttpAuth, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.kind !== 0) {
      writer.uint32(8).int32(message.kind);
    }
    if (message.secret !== "") {
      writer.uint32(18).string(message.secret);
    }
    if (message.name !== "") {
      writer.uint32(26).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HttpAuth {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHttpAuth();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.secret = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HttpAuth {
    return {
      kind: isSet(object.kind) ? httpAuthKindFromJSON(object.kind) : 0,
      secret: isSet(object.secret) ? globalThis.String(object.secret) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
    };
  },

  toJSON(message: HttpAuth): unknown {
    const obj: any = {};
    if (message.kind !== 0) {
      obj.kind = httpAuthKindToJSON(message.kind);
    }
    if (message.secret !== "") {
      obj.secret = message.secret;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HttpAuth>, I>>(base?: I): HttpAuth {
    return HttpAuth.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HttpAuth>, I>>(object: I): HttpAuth {
    const message = createBaseHttpAuth();
    message.kind = object.kind ?? 0;
    message.secret = object.secret ?? "";
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseSetTransformConfig(): SetTransformConfig {
  return { assignments: [], keepOnlyAssigned: false };
}

export const SetTransformConfig: MessageFns<SetTransformConfig> = {
  encode(message: SetTransformConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.assignments) {
      FieldAssignment.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.keepOnlyAssigned !== false) {
      writer.uint32(16).bool(message.keepOnlyAssigned);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetTransformConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetTransformConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.assignments.push(FieldAssignment.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.keepOnlyAssigned = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetTransformConfig {
    return {
      assignments: globalThis.Array.isArray(object?.assignments)
        ? object.assignments.map((e: any) => FieldAssignment.fromJSON(e))
        : [],
      keepOnlyAssigned: isSet(object.keepOnlyAssigned)
        ? globalThis.Boolean(object.keepOnlyAssigned)
        : isSet(object.keep_only_assigned)
        ? globalThis.Boolean(object.keep_only_assigned)
        : false,
    };
  },

  toJSON(message: SetTransformConfig): unknown {
    const obj: any = {};
    if (message.assignments?.length) {
      obj.assignments = message.assignments.map((e) => FieldAssignment.toJSON(e));
    }
    if (message.keepOnlyAssigned !== false) {
      obj.keepOnlyAssigned = message.keepOnlyAssigned;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetTransformConfig>, I>>(base?: I): SetTransformConfig {
    return SetTransformConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetTransformConfig>, I>>(object: I): SetTransformConfig {
    const message = createBaseSetTransformConfig();
    message.assignments = object.assignments?.map((e) => FieldAssignment.fromPartial(e)) || [];
    message.keepOnlyAssigned = object.keepOnlyAssigned ?? false;
    return message;
  },
};

function createBaseFieldAssignment(): FieldAssignment {
  return { field: "", value: "" };
}

export const FieldAssignment: MessageFns<FieldAssignment> = {
  encode(message: FieldAssignment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.field !== "") {
      writer.uint32(10).string(message.field);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FieldAssignment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFieldAssignment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.field = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FieldAssignment {
    return {
      field: isSet(object.field) ? globalThis.String(object.field) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: FieldAssignment): unknown {
    const obj: any = {};
    if (message.field !== "") {
      obj.field = message.field;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FieldAssignment>, I>>(base?: I): FieldAssignment {
    return FieldAssignment.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FieldAssignment>, I>>(object: I): FieldAssignment {
    const message = createBaseFieldAssignment();
    message.field = object.field ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseScheduleTriggerConfig(): ScheduleTriggerConfig {
  return { kind: 0, intervalSeconds: 0, hour: 0, minute: 0, cron: "", initialItemsJson: "" };
}

export const ScheduleTriggerConfig: MessageFns<ScheduleTriggerConfig> = {
  encode(message: ScheduleTriggerConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.kind !== 0) {
      writer.uint32(8).int32(message.kind);
    }
    if (message.intervalSeconds !== 0) {
      writer.uint32(16).uint32(message.intervalSeconds);
    }
    if (message.hour !== 0) {
      writer.uint32(24).uint32(message.hour);
    }
    if (message.minute !== 0) {
      writer.uint32(32).uint32(message.minute);
    }
    if (message.cron !== "") {
      writer.uint32(42).string(message.cron);
    }
    if (message.initialItemsJson !== "") {
      writer.uint32(50).string(message.initialItemsJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScheduleTriggerConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScheduleTriggerConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.intervalSeconds = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.hour = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.minute = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.cron = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.initialItemsJson = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScheduleTriggerConfig {
    return {
      kind: isSet(object.kind) ? scheduleKindFromJSON(object.kind) : 0,
      intervalSeconds: isSet(object.intervalSeconds)
        ? globalThis.Number(object.intervalSeconds)
        : isSet(object.interval_seconds)
        ? globalThis.Number(object.interval_seconds)
        : 0,
      hour: isSet(object.hour) ? globalThis.Number(object.hour) : 0,
      minute: isSet(object.minute) ? globalThis.Number(object.minute) : 0,
      cron: isSet(object.cron) ? globalThis.String(object.cron) : "",
      initialItemsJson: isSet(object.initialItemsJson)
        ? globalThis.String(object.initialItemsJson)
        : isSet(object.initial_items_json)
        ? globalThis.String(object.initial_items_json)
        : "",
    };
  },

  toJSON(message: ScheduleTriggerConfig): unknown {
    const obj: any = {};
    if (message.kind !== 0) {
      obj.kind = scheduleKindToJSON(message.kind);
    }
    if (message.intervalSeconds !== 0) {
      obj.intervalSeconds = Math.round(message.intervalSeconds);
    }
    if (message.hour !== 0) {
      obj.hour = Math.round(message.hour);
    }
    if (message.minute !== 0) {
      obj.minute = Math.round(message.minute);
    }
    if (message.cron !== "") {
      obj.cron = message.cron;
    }
    if (message.initialItemsJson !== "") {
      obj.initialItemsJson = message.initialItemsJson;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ScheduleTriggerConfig>, I>>(base?: I): ScheduleTriggerConfig {
    return ScheduleTriggerConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ScheduleTriggerConfig>, I>>(object: I): ScheduleTriggerConfig {
    const message = createBaseScheduleTriggerConfig();
    message.kind = object.kind ?? 0;
    message.intervalSeconds = object.intervalSeconds ?? 0;
    message.hour = object.hour ?? 0;
    message.minute = object.minute ?? 0;
    message.cron = object.cron ?? "";
    message.initialItemsJson = object.initialItemsJson ?? "";
    return message;
  },
};

function createBaseMergeConfig(): MergeConfig {
  return { inputCount: 0, deduplicate: false };
}

export const MergeConfig: MessageFns<MergeConfig> = {
  encode(message: MergeConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.inputCount !== 0) {
      writer.uint32(8).uint32(message.inputCount);
    }
    if (message.deduplicate !== false) {
      writer.uint32(16).bool(message.deduplicate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MergeConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMergeConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.inputCount = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.deduplicate = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MergeConfig {
    return {
      inputCount: isSet(object.inputCount)
        ? globalThis.Number(object.inputCount)
        : isSet(object.input_count)
        ? globalThis.Number(object.input_count)
        : 0,
      deduplicate: isSet(object.deduplicate) ? globalThis.Boolean(object.deduplicate) : false,
    };
  },

  toJSON(message: MergeConfig): unknown {
    const obj: any = {};
    if (message.inputCount !== 0) {
      obj.inputCount = Math.round(message.inputCount);
    }
    if (message.deduplicate !== false) {
      obj.deduplicate = message.deduplicate;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MergeConfig>, I>>(base?: I): MergeConfig {
    return MergeConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MergeConfig>, I>>(object: I): MergeConfig {
    const message = createBaseMergeConfig();
    message.inputCount = object.inputCount ?? 0;
    message.deduplicate = object.deduplicate ?? false;
    return message;
  },
};

function createBaseFilterConfig(): FilterConfig {
  return { left: "", operator: 0, right: "", keepMatching: false };
}

export const FilterConfig: MessageFns<FilterConfig> = {
  encode(message: FilterConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.left !== "") {
      writer.uint32(10).string(message.left);
    }
    if (message.operator !== 0) {
      writer.uint32(16).int32(message.operator);
    }
    if (message.right !== "") {
      writer.uint32(26).string(message.right);
    }
    if (message.keepMatching !== false) {
      writer.uint32(32).bool(message.keepMatching);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FilterConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFilterConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.left = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.operator = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.right = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.keepMatching = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FilterConfig {
    return {
      left: isSet(object.left) ? globalThis.String(object.left) : "",
      operator: isSet(object.operator) ? comparisonOperatorFromJSON(object.operator) : 0,
      right: isSet(object.right) ? globalThis.String(object.right) : "",
      keepMatching: isSet(object.keepMatching)
        ? globalThis.Boolean(object.keepMatching)
        : isSet(object.keep_matching)
        ? globalThis.Boolean(object.keep_matching)
        : false,
    };
  },

  toJSON(message: FilterConfig): unknown {
    const obj: any = {};
    if (message.left !== "") {
      obj.left = message.left;
    }
    if (message.operator !== 0) {
      obj.operator = comparisonOperatorToJSON(message.operator);
    }
    if (message.right !== "") {
      obj.right = message.right;
    }
    if (message.keepMatching !== false) {
      obj.keepMatching = message.keepMatching;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FilterConfig>, I>>(base?: I): FilterConfig {
    return FilterConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FilterConfig>, I>>(object: I): FilterConfig {
    const message = createBaseFilterConfig();
    message.left = object.left ?? "";
    message.operator = object.operator ?? 0;
    message.right = object.right ?? "";
    message.keepMatching = object.keepMatching ?? false;
    return message;
  },
};

function createBaseSplitOutConfig(): SplitOutConfig {
  return { field: "" };
}

export const SplitOutConfig: MessageFns<SplitOutConfig> = {
  encode(message: SplitOutConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.field !== "") {
      writer.uint32(10).string(message.field);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SplitOutConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSplitOutConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.field = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SplitOutConfig {
    return { field: isSet(object.field) ? globalThis.String(object.field) : "" };
  },

  toJSON(message: SplitOutConfig): unknown {
    const obj: any = {};
    if (message.field !== "") {
      obj.field = message.field;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SplitOutConfig>, I>>(base?: I): SplitOutConfig {
    return SplitOutConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SplitOutConfig>, I>>(object: I): SplitOutConfig {
    const message = createBaseSplitOutConfig();
    message.field = object.field ?? "";
    return message;
  },
};

function createBaseAggregateConfig(): AggregateConfig {
  return { field: "" };
}

export const AggregateConfig: MessageFns<AggregateConfig> = {
  encode(message: AggregateConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.field !== "") {
      writer.uint32(10).string(message.field);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AggregateConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAggregateConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.field = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AggregateConfig {
    return { field: isSet(object.field) ? globalThis.String(object.field) : "" };
  },

  toJSON(message: AggregateConfig): unknown {
    const obj: any = {};
    if (message.field !== "") {
      obj.field = message.field;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AggregateConfig>, I>>(base?: I): AggregateConfig {
    return AggregateConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AggregateConfig>, I>>(object: I): AggregateConfig {
    const message = createBaseAggregateConfig();
    message.field = object.field ?? "";
    return message;
  },
};

function createBaseWaitConfig(): WaitConfig {
  return { seconds: 0 };
}

export const WaitConfig: MessageFns<WaitConfig> = {
  encode(message: WaitConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.seconds !== 0) {
      writer.uint32(8).uint32(message.seconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WaitConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWaitConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.seconds = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WaitConfig {
    return { seconds: isSet(object.seconds) ? globalThis.Number(object.seconds) : 0 };
  },

  toJSON(message: WaitConfig): unknown {
    const obj: any = {};
    if (message.seconds !== 0) {
      obj.seconds = Math.round(message.seconds);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WaitConfig>, I>>(base?: I): WaitConfig {
    return WaitConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WaitConfig>, I>>(object: I): WaitConfig {
    const message = createBaseWaitConfig();
    message.seconds = object.seconds ?? 0;
    return message;
  },
};

function createBaseFileReadConfig(): FileReadConfig {
  return { path: "", binary: false, binaryKey: "", mimeType: "" };
}

export const FileReadConfig: MessageFns<FileReadConfig> = {
  encode(message: FileReadConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.binary !== false) {
      writer.uint32(16).bool(message.binary);
    }
    if (message.binaryKey !== "") {
      writer.uint32(26).string(message.binaryKey);
    }
    if (message.mimeType !== "") {
      writer.uint32(34).string(message.mimeType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileReadConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileReadConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.binary = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.binaryKey = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.mimeType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileReadConfig {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      binary: isSet(object.binary) ? globalThis.Boolean(object.binary) : false,
      binaryKey: isSet(object.binaryKey)
        ? globalThis.String(object.binaryKey)
        : isSet(object.binary_key)
        ? globalThis.String(object.binary_key)
        : "",
      mimeType: isSet(object.mimeType)
        ? globalThis.String(object.mimeType)
        : isSet(object.mime_type)
        ? globalThis.String(object.mime_type)
        : "",
    };
  },

  toJSON(message: FileReadConfig): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.binary !== false) {
      obj.binary = message.binary;
    }
    if (message.binaryKey !== "") {
      obj.binaryKey = message.binaryKey;
    }
    if (message.mimeType !== "") {
      obj.mimeType = message.mimeType;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FileReadConfig>, I>>(base?: I): FileReadConfig {
    return FileReadConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FileReadConfig>, I>>(object: I): FileReadConfig {
    const message = createBaseFileReadConfig();
    message.path = object.path ?? "";
    message.binary = object.binary ?? false;
    message.binaryKey = object.binaryKey ?? "";
    message.mimeType = object.mimeType ?? "";
    return message;
  },
};

function createBaseFileWriteConfig(): FileWriteConfig {
  return { path: "", content: "", binaryKey: "", append: false };
}

export const FileWriteConfig: MessageFns<FileWriteConfig> = {
  encode(message: FileWriteConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.content !== "") {
      writer.uint32(18).string(message.content);
    }
    if (message.binaryKey !== "") {
      writer.uint32(26).string(message.binaryKey);
    }
    if (message.append !== false) {
      writer.uint32(32).bool(message.append);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileWriteConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileWriteConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.binaryKey = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.append = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileWriteConfig {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      content: isSet(object.content) ? globalThis.String(object.content) : "",
      binaryKey: isSet(object.binaryKey)
        ? globalThis.String(object.binaryKey)
        : isSet(object.binary_key)
        ? globalThis.String(object.binary_key)
        : "",
      append: isSet(object.append) ? globalThis.Boolean(object.append) : false,
    };
  },

  toJSON(message: FileWriteConfig): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    if (message.binaryKey !== "") {
      obj.binaryKey = message.binaryKey;
    }
    if (message.append !== false) {
      obj.append = message.append;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FileWriteConfig>, I>>(base?: I): FileWriteConfig {
    return FileWriteConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FileWriteConfig>, I>>(object: I): FileWriteConfig {
    const message = createBaseFileWriteConfig();
    message.path = object.path ?? "";
    message.content = object.content ?? "";
    message.binaryKey = object.binaryKey ?? "";
    message.append = object.append ?? false;
    return message;
  },
};

function createBaseLlmStructuredConfig(): LlmStructuredConfig {
  return { prompt: "", jsonSchema: "", systemPrompt: undefined, modelId: undefined, generation: undefined };
}

export const LlmStructuredConfig: MessageFns<LlmStructuredConfig> = {
  encode(message: LlmStructuredConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.prompt !== "") {
      writer.uint32(10).string(message.prompt);
    }
    if (message.jsonSchema !== "") {
      writer.uint32(18).string(message.jsonSchema);
    }
    if (message.systemPrompt !== undefined) {
      writer.uint32(26).string(message.systemPrompt);
    }
    if (message.modelId !== undefined) {
      writer.uint32(34).string(message.modelId);
    }
    if (message.generation !== undefined) {
      LLMGenerationOptions.encode(message.generation, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LlmStructuredConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLlmStructuredConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.prompt = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.jsonSchema = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.systemPrompt = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.generation = LLMGenerationOptions.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LlmStructuredConfig {
    return {
      prompt: isSet(object.prompt) ? globalThis.String(object.prompt) : "",
      jsonSchema: isSet(object.jsonSchema)
        ? globalThis.String(object.jsonSchema)
        : isSet(object.json_schema)
        ? globalThis.String(object.json_schema)
        : "",
      systemPrompt: isSet(object.systemPrompt)
        ? globalThis.String(object.systemPrompt)
        : isSet(object.system_prompt)
        ? globalThis.String(object.system_prompt)
        : undefined,
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : undefined,
      generation: isSet(object.generation) ? LLMGenerationOptions.fromJSON(object.generation) : undefined,
    };
  },

  toJSON(message: LlmStructuredConfig): unknown {
    const obj: any = {};
    if (message.prompt !== "") {
      obj.prompt = message.prompt;
    }
    if (message.jsonSchema !== "") {
      obj.jsonSchema = message.jsonSchema;
    }
    if (message.systemPrompt !== undefined) {
      obj.systemPrompt = message.systemPrompt;
    }
    if (message.modelId !== undefined) {
      obj.modelId = message.modelId;
    }
    if (message.generation !== undefined) {
      obj.generation = LLMGenerationOptions.toJSON(message.generation);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LlmStructuredConfig>, I>>(base?: I): LlmStructuredConfig {
    return LlmStructuredConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LlmStructuredConfig>, I>>(object: I): LlmStructuredConfig {
    const message = createBaseLlmStructuredConfig();
    message.prompt = object.prompt ?? "";
    message.jsonSchema = object.jsonSchema ?? "";
    message.systemPrompt = object.systemPrompt ?? undefined;
    message.modelId = object.modelId ?? undefined;
    message.generation = (object.generation !== undefined && object.generation !== null)
      ? LLMGenerationOptions.fromPartial(object.generation)
      : undefined;
    return message;
  },
};

function createBaseVisionConfig(): VisionConfig {
  return { prompt: "", binaryKey: "", modelId: undefined, generation: undefined };
}

export const VisionConfig: MessageFns<VisionConfig> = {
  encode(message: VisionConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.prompt !== "") {
      writer.uint32(10).string(message.prompt);
    }
    if (message.binaryKey !== "") {
      writer.uint32(18).string(message.binaryKey);
    }
    if (message.modelId !== undefined) {
      writer.uint32(26).string(message.modelId);
    }
    if (message.generation !== undefined) {
      LLMGenerationOptions.encode(message.generation, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VisionConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVisionConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.prompt = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.binaryKey = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.generation = LLMGenerationOptions.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VisionConfig {
    return {
      prompt: isSet(object.prompt) ? globalThis.String(object.prompt) : "",
      binaryKey: isSet(object.binaryKey)
        ? globalThis.String(object.binaryKey)
        : isSet(object.binary_key)
        ? globalThis.String(object.binary_key)
        : "",
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : undefined,
      generation: isSet(object.generation) ? LLMGenerationOptions.fromJSON(object.generation) : undefined,
    };
  },

  toJSON(message: VisionConfig): unknown {
    const obj: any = {};
    if (message.prompt !== "") {
      obj.prompt = message.prompt;
    }
    if (message.binaryKey !== "") {
      obj.binaryKey = message.binaryKey;
    }
    if (message.modelId !== undefined) {
      obj.modelId = message.modelId;
    }
    if (message.generation !== undefined) {
      obj.generation = LLMGenerationOptions.toJSON(message.generation);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VisionConfig>, I>>(base?: I): VisionConfig {
    return VisionConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VisionConfig>, I>>(object: I): VisionConfig {
    const message = createBaseVisionConfig();
    message.prompt = object.prompt ?? "";
    message.binaryKey = object.binaryKey ?? "";
    message.modelId = object.modelId ?? undefined;
    message.generation = (object.generation !== undefined && object.generation !== null)
      ? LLMGenerationOptions.fromPartial(object.generation)
      : undefined;
    return message;
  },
};

function createBaseEmbedConfig(): EmbedConfig {
  return { text: "", modelId: undefined };
}

export const EmbedConfig: MessageFns<EmbedConfig> = {
  encode(message: EmbedConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.modelId !== undefined) {
      writer.uint32(18).string(message.modelId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EmbedConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEmbedConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EmbedConfig {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : undefined,
    };
  },

  toJSON(message: EmbedConfig): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.modelId !== undefined) {
      obj.modelId = message.modelId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EmbedConfig>, I>>(base?: I): EmbedConfig {
    return EmbedConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EmbedConfig>, I>>(object: I): EmbedConfig {
    const message = createBaseEmbedConfig();
    message.text = object.text ?? "";
    message.modelId = object.modelId ?? undefined;
    return message;
  },
};

function createBaseRerankConfig(): RerankConfig {
  return { query: "", documents: "", topN: 0, modelId: undefined };
}

export const RerankConfig: MessageFns<RerankConfig> = {
  encode(message: RerankConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== "") {
      writer.uint32(10).string(message.query);
    }
    if (message.documents !== "") {
      writer.uint32(18).string(message.documents);
    }
    if (message.topN !== 0) {
      writer.uint32(24).uint32(message.topN);
    }
    if (message.modelId !== undefined) {
      writer.uint32(34).string(message.modelId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RerankConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRerankConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.documents = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.topN = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RerankConfig {
    return {
      query: isSet(object.query) ? globalThis.String(object.query) : "",
      documents: isSet(object.documents) ? globalThis.String(object.documents) : "",
      topN: isSet(object.topN)
        ? globalThis.Number(object.topN)
        : isSet(object.top_n)
        ? globalThis.Number(object.top_n)
        : 0,
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : undefined,
    };
  },

  toJSON(message: RerankConfig): unknown {
    const obj: any = {};
    if (message.query !== "") {
      obj.query = message.query;
    }
    if (message.documents !== "") {
      obj.documents = message.documents;
    }
    if (message.topN !== 0) {
      obj.topN = Math.round(message.topN);
    }
    if (message.modelId !== undefined) {
      obj.modelId = message.modelId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RerankConfig>, I>>(base?: I): RerankConfig {
    return RerankConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RerankConfig>, I>>(object: I): RerankConfig {
    const message = createBaseRerankConfig();
    message.query = object.query ?? "";
    message.documents = object.documents ?? "";
    message.topN = object.topN ?? 0;
    message.modelId = object.modelId ?? undefined;
    return message;
  },
};

function createBaseTranscribeConfig(): TranscribeConfig {
  return { binaryKey: "", modelId: undefined, language: undefined };
}

export const TranscribeConfig: MessageFns<TranscribeConfig> = {
  encode(message: TranscribeConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.binaryKey !== "") {
      writer.uint32(10).string(message.binaryKey);
    }
    if (message.modelId !== undefined) {
      writer.uint32(18).string(message.modelId);
    }
    if (message.language !== undefined) {
      writer.uint32(26).string(message.language);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TranscribeConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTranscribeConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.binaryKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.language = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TranscribeConfig {
    return {
      binaryKey: isSet(object.binaryKey)
        ? globalThis.String(object.binaryKey)
        : isSet(object.binary_key)
        ? globalThis.String(object.binary_key)
        : "",
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : undefined,
      language: isSet(object.language) ? globalThis.String(object.language) : undefined,
    };
  },

  toJSON(message: TranscribeConfig): unknown {
    const obj: any = {};
    if (message.binaryKey !== "") {
      obj.binaryKey = message.binaryKey;
    }
    if (message.modelId !== undefined) {
      obj.modelId = message.modelId;
    }
    if (message.language !== undefined) {
      obj.language = message.language;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TranscribeConfig>, I>>(base?: I): TranscribeConfig {
    return TranscribeConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TranscribeConfig>, I>>(object: I): TranscribeConfig {
    const message = createBaseTranscribeConfig();
    message.binaryKey = object.binaryKey ?? "";
    message.modelId = object.modelId ?? undefined;
    message.language = object.language ?? undefined;
    return message;
  },
};

function createBaseSpeakConfig(): SpeakConfig {
  return { text: "", play: false, binaryKey: "", modelId: undefined, voice: undefined };
}

export const SpeakConfig: MessageFns<SpeakConfig> = {
  encode(message: SpeakConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.play !== false) {
      writer.uint32(16).bool(message.play);
    }
    if (message.binaryKey !== "") {
      writer.uint32(26).string(message.binaryKey);
    }
    if (message.modelId !== undefined) {
      writer.uint32(34).string(message.modelId);
    }
    if (message.voice !== undefined) {
      writer.uint32(42).string(message.voice);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SpeakConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSpeakConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.play = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.binaryKey = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.voice = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SpeakConfig {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      play: isSet(object.play) ? globalThis.Boolean(object.play) : false,
      binaryKey: isSet(object.binaryKey)
        ? globalThis.String(object.binaryKey)
        : isSet(object.binary_key)
        ? globalThis.String(object.binary_key)
        : "",
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : undefined,
      voice: isSet(object.voice) ? globalThis.String(object.voice) : undefined,
    };
  },

  toJSON(message: SpeakConfig): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.play !== false) {
      obj.play = message.play;
    }
    if (message.binaryKey !== "") {
      obj.binaryKey = message.binaryKey;
    }
    if (message.modelId !== undefined) {
      obj.modelId = message.modelId;
    }
    if (message.voice !== undefined) {
      obj.voice = message.voice;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SpeakConfig>, I>>(base?: I): SpeakConfig {
    return SpeakConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SpeakConfig>, I>>(object: I): SpeakConfig {
    const message = createBaseSpeakConfig();
    message.text = object.text ?? "";
    message.play = object.play ?? false;
    message.binaryKey = object.binaryKey ?? "";
    message.modelId = object.modelId ?? undefined;
    message.voice = object.voice ?? undefined;
    return message;
  },
};

function createBaseDetectVoiceConfig(): DetectVoiceConfig {
  return { binaryKey: "", threshold: undefined };
}

export const DetectVoiceConfig: MessageFns<DetectVoiceConfig> = {
  encode(message: DetectVoiceConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.binaryKey !== "") {
      writer.uint32(10).string(message.binaryKey);
    }
    if (message.threshold !== undefined) {
      writer.uint32(21).float(message.threshold);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DetectVoiceConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDetectVoiceConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.binaryKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 21) {
            break;
          }

          message.threshold = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DetectVoiceConfig {
    return {
      binaryKey: isSet(object.binaryKey)
        ? globalThis.String(object.binaryKey)
        : isSet(object.binary_key)
        ? globalThis.String(object.binary_key)
        : "",
      threshold: isSet(object.threshold) ? globalThis.Number(object.threshold) : undefined,
    };
  },

  toJSON(message: DetectVoiceConfig): unknown {
    const obj: any = {};
    if (message.binaryKey !== "") {
      obj.binaryKey = message.binaryKey;
    }
    if (message.threshold !== undefined) {
      obj.threshold = message.threshold;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DetectVoiceConfig>, I>>(base?: I): DetectVoiceConfig {
    return DetectVoiceConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DetectVoiceConfig>, I>>(object: I): DetectVoiceConfig {
    const message = createBaseDetectVoiceConfig();
    message.binaryKey = object.binaryKey ?? "";
    message.threshold = object.threshold ?? undefined;
    return message;
  },
};

function createBaseDiarizeConfig(): DiarizeConfig {
  return { binaryKey: "", modelId: undefined, speakerCount: undefined };
}

export const DiarizeConfig: MessageFns<DiarizeConfig> = {
  encode(message: DiarizeConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.binaryKey !== "") {
      writer.uint32(10).string(message.binaryKey);
    }
    if (message.modelId !== undefined) {
      writer.uint32(18).string(message.modelId);
    }
    if (message.speakerCount !== undefined) {
      writer.uint32(24).uint32(message.speakerCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DiarizeConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiarizeConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.binaryKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.speakerCount = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DiarizeConfig {
    return {
      binaryKey: isSet(object.binaryKey)
        ? globalThis.String(object.binaryKey)
        : isSet(object.binary_key)
        ? globalThis.String(object.binary_key)
        : "",
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : undefined,
      speakerCount: isSet(object.speakerCount)
        ? globalThis.Number(object.speakerCount)
        : isSet(object.speaker_count)
        ? globalThis.Number(object.speaker_count)
        : undefined,
    };
  },

  toJSON(message: DiarizeConfig): unknown {
    const obj: any = {};
    if (message.binaryKey !== "") {
      obj.binaryKey = message.binaryKey;
    }
    if (message.modelId !== undefined) {
      obj.modelId = message.modelId;
    }
    if (message.speakerCount !== undefined) {
      obj.speakerCount = Math.round(message.speakerCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DiarizeConfig>, I>>(base?: I): DiarizeConfig {
    return DiarizeConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DiarizeConfig>, I>>(object: I): DiarizeConfig {
    const message = createBaseDiarizeConfig();
    message.binaryKey = object.binaryKey ?? "";
    message.modelId = object.modelId ?? undefined;
    message.speakerCount = object.speakerCount ?? undefined;
    return message;
  },
};

function createBaseSegmentConfig(): SegmentConfig {
  return { text: "", modelId: undefined };
}

export const SegmentConfig: MessageFns<SegmentConfig> = {
  encode(message: SegmentConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.modelId !== undefined) {
      writer.uint32(18).string(message.modelId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SegmentConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSegmentConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SegmentConfig {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : undefined,
    };
  },

  toJSON(message: SegmentConfig): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.modelId !== undefined) {
      obj.modelId = message.modelId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SegmentConfig>, I>>(base?: I): SegmentConfig {
    return SegmentConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SegmentConfig>, I>>(object: I): SegmentConfig {
    const message = createBaseSegmentConfig();
    message.text = object.text ?? "";
    message.modelId = object.modelId ?? undefined;
    return message;
  },
};

function createBaseRagQueryConfig(): RagQueryConfig {
  return { question: "", embeddingModelId: "", llmModelId: "", topK: 0 };
}

export const RagQueryConfig: MessageFns<RagQueryConfig> = {
  encode(message: RagQueryConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.question !== "") {
      writer.uint32(10).string(message.question);
    }
    if (message.embeddingModelId !== "") {
      writer.uint32(18).string(message.embeddingModelId);
    }
    if (message.llmModelId !== "") {
      writer.uint32(26).string(message.llmModelId);
    }
    if (message.topK !== 0) {
      writer.uint32(32).uint32(message.topK);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RagQueryConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRagQueryConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.question = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.embeddingModelId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.llmModelId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.topK = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RagQueryConfig {
    return {
      question: isSet(object.question) ? globalThis.String(object.question) : "",
      embeddingModelId: isSet(object.embeddingModelId)
        ? globalThis.String(object.embeddingModelId)
        : isSet(object.embedding_model_id)
        ? globalThis.String(object.embedding_model_id)
        : "",
      llmModelId: isSet(object.llmModelId)
        ? globalThis.String(object.llmModelId)
        : isSet(object.llm_model_id)
        ? globalThis.String(object.llm_model_id)
        : "",
      topK: isSet(object.topK)
        ? globalThis.Number(object.topK)
        : isSet(object.top_k)
        ? globalThis.Number(object.top_k)
        : 0,
    };
  },

  toJSON(message: RagQueryConfig): unknown {
    const obj: any = {};
    if (message.question !== "") {
      obj.question = message.question;
    }
    if (message.embeddingModelId !== "") {
      obj.embeddingModelId = message.embeddingModelId;
    }
    if (message.llmModelId !== "") {
      obj.llmModelId = message.llmModelId;
    }
    if (message.topK !== 0) {
      obj.topK = Math.round(message.topK);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RagQueryConfig>, I>>(base?: I): RagQueryConfig {
    return RagQueryConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RagQueryConfig>, I>>(object: I): RagQueryConfig {
    const message = createBaseRagQueryConfig();
    message.question = object.question ?? "";
    message.embeddingModelId = object.embeddingModelId ?? "";
    message.llmModelId = object.llmModelId ?? "";
    message.topK = object.topK ?? 0;
    return message;
  },
};

function createBaseRagIngestConfig(): RagIngestConfig {
  return { text: "", embeddingModelId: "", llmModelId: "", documentId: "" };
}

export const RagIngestConfig: MessageFns<RagIngestConfig> = {
  encode(message: RagIngestConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.embeddingModelId !== "") {
      writer.uint32(18).string(message.embeddingModelId);
    }
    if (message.llmModelId !== "") {
      writer.uint32(26).string(message.llmModelId);
    }
    if (message.documentId !== "") {
      writer.uint32(34).string(message.documentId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RagIngestConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRagIngestConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.embeddingModelId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.llmModelId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.documentId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RagIngestConfig {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      embeddingModelId: isSet(object.embeddingModelId)
        ? globalThis.String(object.embeddingModelId)
        : isSet(object.embedding_model_id)
        ? globalThis.String(object.embedding_model_id)
        : "",
      llmModelId: isSet(object.llmModelId)
        ? globalThis.String(object.llmModelId)
        : isSet(object.llm_model_id)
        ? globalThis.String(object.llm_model_id)
        : "",
      documentId: isSet(object.documentId)
        ? globalThis.String(object.documentId)
        : isSet(object.document_id)
        ? globalThis.String(object.document_id)
        : "",
    };
  },

  toJSON(message: RagIngestConfig): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.embeddingModelId !== "") {
      obj.embeddingModelId = message.embeddingModelId;
    }
    if (message.llmModelId !== "") {
      obj.llmModelId = message.llmModelId;
    }
    if (message.documentId !== "") {
      obj.documentId = message.documentId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RagIngestConfig>, I>>(base?: I): RagIngestConfig {
    return RagIngestConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RagIngestConfig>, I>>(object: I): RagIngestConfig {
    const message = createBaseRagIngestConfig();
    message.text = object.text ?? "";
    message.embeddingModelId = object.embeddingModelId ?? "";
    message.llmModelId = object.llmModelId ?? "";
    message.documentId = object.documentId ?? "";
    return message;
  },
};

function createBaseLoadModelConfig(): LoadModelConfig {
  return { modelId: "", category: undefined };
}

export const LoadModelConfig: MessageFns<LoadModelConfig> = {
  encode(message: LoadModelConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.modelId !== "") {
      writer.uint32(10).string(message.modelId);
    }
    if (message.category !== undefined) {
      writer.uint32(16).int32(message.category);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LoadModelConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLoadModelConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.category = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LoadModelConfig {
    return {
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : "",
      category: isSet(object.category) ? modelCategoryFromJSON(object.category) : undefined,
    };
  },

  toJSON(message: LoadModelConfig): unknown {
    const obj: any = {};
    if (message.modelId !== "") {
      obj.modelId = message.modelId;
    }
    if (message.category !== undefined) {
      obj.category = modelCategoryToJSON(message.category);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LoadModelConfig>, I>>(base?: I): LoadModelConfig {
    return LoadModelConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LoadModelConfig>, I>>(object: I): LoadModelConfig {
    const message = createBaseLoadModelConfig();
    message.modelId = object.modelId ?? "";
    message.category = object.category ?? undefined;
    return message;
  },
};

function createBaseWorkflowItem(): WorkflowItem {
  return { json: "", binary: {} };
}

export const WorkflowItem: MessageFns<WorkflowItem> = {
  encode(message: WorkflowItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.json !== "") {
      writer.uint32(10).string(message.json);
    }
    globalThis.Object.entries(message.binary).forEach(([key, value]: [string, WorkflowBinary]) => {
      WorkflowItem_BinaryEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.json = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = WorkflowItem_BinaryEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.binary[entry2.key] = entry2.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowItem {
    return {
      json: isSet(object.json) ? globalThis.String(object.json) : "",
      binary: isObject(object.binary)
        ? (globalThis.Object.entries(object.binary) as [string, any][]).reduce(
          (acc: { [key: string]: WorkflowBinary }, [key, value]: [string, any]) => {
            acc[key] = WorkflowBinary.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: WorkflowItem): unknown {
    const obj: any = {};
    if (message.json !== "") {
      obj.json = message.json;
    }
    if (message.binary) {
      const entries = globalThis.Object.entries(message.binary) as [string, WorkflowBinary][];
      if (entries.length > 0) {
        obj.binary = {};
        entries.forEach(([k, v]) => {
          obj.binary[k] = WorkflowBinary.toJSON(v);
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowItem>, I>>(base?: I): WorkflowItem {
    return WorkflowItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowItem>, I>>(object: I): WorkflowItem {
    const message = createBaseWorkflowItem();
    message.json = object.json ?? "";
    message.binary = (globalThis.Object.entries(object.binary ?? {}) as [string, WorkflowBinary][]).reduce(
      (acc: { [key: string]: WorkflowBinary }, [key, value]: [string, WorkflowBinary]) => {
        if (value !== undefined) {
          acc[key] = WorkflowBinary.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseWorkflowItem_BinaryEntry(): WorkflowItem_BinaryEntry {
  return { key: "", value: undefined };
}

export const WorkflowItem_BinaryEntry: MessageFns<WorkflowItem_BinaryEntry> = {
  encode(message: WorkflowItem_BinaryEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      WorkflowBinary.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowItem_BinaryEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowItem_BinaryEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = WorkflowBinary.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowItem_BinaryEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? WorkflowBinary.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: WorkflowItem_BinaryEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = WorkflowBinary.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowItem_BinaryEntry>, I>>(base?: I): WorkflowItem_BinaryEntry {
    return WorkflowItem_BinaryEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowItem_BinaryEntry>, I>>(object: I): WorkflowItem_BinaryEntry {
    const message = createBaseWorkflowItem_BinaryEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? WorkflowBinary.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseWorkflowBinary(): WorkflowBinary {
  return { data: new Uint8Array(0), mimeType: "", fileName: undefined };
}

export const WorkflowBinary: MessageFns<WorkflowBinary> = {
  encode(message: WorkflowBinary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data.length !== 0) {
      writer.uint32(10).bytes(message.data);
    }
    if (message.mimeType !== "") {
      writer.uint32(18).string(message.mimeType);
    }
    if (message.fileName !== undefined) {
      writer.uint32(26).string(message.fileName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowBinary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowBinary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.mimeType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fileName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowBinary {
    return {
      data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
      mimeType: isSet(object.mimeType)
        ? globalThis.String(object.mimeType)
        : isSet(object.mime_type)
        ? globalThis.String(object.mime_type)
        : "",
      fileName: isSet(object.fileName)
        ? globalThis.String(object.fileName)
        : isSet(object.file_name)
        ? globalThis.String(object.file_name)
        : undefined,
    };
  },

  toJSON(message: WorkflowBinary): unknown {
    const obj: any = {};
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    if (message.mimeType !== "") {
      obj.mimeType = message.mimeType;
    }
    if (message.fileName !== undefined) {
      obj.fileName = message.fileName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowBinary>, I>>(base?: I): WorkflowBinary {
    return WorkflowBinary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowBinary>, I>>(object: I): WorkflowBinary {
    const message = createBaseWorkflowBinary();
    message.data = object.data ?? new Uint8Array(0);
    message.mimeType = object.mimeType ?? "";
    message.fileName = object.fileName ?? undefined;
    return message;
  },
};

function createBaseWorkflowBundle(): WorkflowBundle {
  return { formatVersion: 0, generator: "", exportedAtMs: 0, workflows: [], packs: [] };
}

export const WorkflowBundle: MessageFns<WorkflowBundle> = {
  encode(message: WorkflowBundle, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.formatVersion !== 0) {
      writer.uint32(8).uint32(message.formatVersion);
    }
    if (message.generator !== "") {
      writer.uint32(18).string(message.generator);
    }
    if (message.exportedAtMs !== 0) {
      writer.uint32(24).int64(message.exportedAtMs);
    }
    for (const v of message.workflows) {
      WorkflowDocument.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.packs) {
      NodePack.encode(v!, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowBundle {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowBundle();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.formatVersion = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.generator = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.exportedAtMs = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.workflows.push(WorkflowDocument.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.packs.push(NodePack.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowBundle {
    return {
      formatVersion: isSet(object.formatVersion)
        ? globalThis.Number(object.formatVersion)
        : isSet(object.format_version)
        ? globalThis.Number(object.format_version)
        : 0,
      generator: isSet(object.generator) ? globalThis.String(object.generator) : "",
      exportedAtMs: isSet(object.exportedAtMs)
        ? globalThis.Number(object.exportedAtMs)
        : isSet(object.exported_at_ms)
        ? globalThis.Number(object.exported_at_ms)
        : 0,
      workflows: globalThis.Array.isArray(object?.workflows)
        ? object.workflows.map((e: any) => WorkflowDocument.fromJSON(e))
        : [],
      packs: globalThis.Array.isArray(object?.packs) ? object.packs.map((e: any) => NodePack.fromJSON(e)) : [],
    };
  },

  toJSON(message: WorkflowBundle): unknown {
    const obj: any = {};
    if (message.formatVersion !== 0) {
      obj.formatVersion = Math.round(message.formatVersion);
    }
    if (message.generator !== "") {
      obj.generator = message.generator;
    }
    if (message.exportedAtMs !== 0) {
      obj.exportedAtMs = Math.round(message.exportedAtMs);
    }
    if (message.workflows?.length) {
      obj.workflows = message.workflows.map((e) => WorkflowDocument.toJSON(e));
    }
    if (message.packs?.length) {
      obj.packs = message.packs.map((e) => NodePack.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowBundle>, I>>(base?: I): WorkflowBundle {
    return WorkflowBundle.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowBundle>, I>>(object: I): WorkflowBundle {
    const message = createBaseWorkflowBundle();
    message.formatVersion = object.formatVersion ?? 0;
    message.generator = object.generator ?? "";
    message.exportedAtMs = object.exportedAtMs ?? 0;
    message.workflows = object.workflows?.map((e) => WorkflowDocument.fromPartial(e)) || [];
    message.packs = object.packs?.map((e) => NodePack.fromPartial(e)) || [];
    return message;
  },
};

function createBaseWorkflowBundleExportRequest(): WorkflowBundleExportRequest {
  return { workflowIds: [] };
}

export const WorkflowBundleExportRequest: MessageFns<WorkflowBundleExportRequest> = {
  encode(message: WorkflowBundleExportRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.workflowIds) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowBundleExportRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowBundleExportRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workflowIds.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowBundleExportRequest {
    return {
      workflowIds: globalThis.Array.isArray(object?.workflowIds)
        ? object.workflowIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.workflow_ids)
        ? object.workflow_ids.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: WorkflowBundleExportRequest): unknown {
    const obj: any = {};
    if (message.workflowIds?.length) {
      obj.workflowIds = message.workflowIds;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowBundleExportRequest>, I>>(base?: I): WorkflowBundleExportRequest {
    return WorkflowBundleExportRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowBundleExportRequest>, I>>(object: I): WorkflowBundleExportRequest {
    const message = createBaseWorkflowBundleExportRequest();
    message.workflowIds = object.workflowIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseBundleImportIssue(): BundleImportIssue {
  return { kind: 0, id: "", message: "" };
}

export const BundleImportIssue: MessageFns<BundleImportIssue> = {
  encode(message: BundleImportIssue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.kind !== 0) {
      writer.uint32(8).int32(message.kind);
    }
    if (message.id !== "") {
      writer.uint32(18).string(message.id);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BundleImportIssue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBundleImportIssue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BundleImportIssue {
    return {
      kind: isSet(object.kind) ? bundleItemKindFromJSON(object.kind) : 0,
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: BundleImportIssue): unknown {
    const obj: any = {};
    if (message.kind !== 0) {
      obj.kind = bundleItemKindToJSON(message.kind);
    }
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BundleImportIssue>, I>>(base?: I): BundleImportIssue {
    return BundleImportIssue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BundleImportIssue>, I>>(object: I): BundleImportIssue {
    const message = createBaseBundleImportIssue();
    message.kind = object.kind ?? 0;
    message.id = object.id ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseWorkflowBundleImportResult(): WorkflowBundleImportResult {
  return { importedWorkflowIds: [], importedPackIds: [], skipped: [] };
}

export const WorkflowBundleImportResult: MessageFns<WorkflowBundleImportResult> = {
  encode(message: WorkflowBundleImportResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.importedWorkflowIds) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.importedPackIds) {
      writer.uint32(18).string(v!);
    }
    for (const v of message.skipped) {
      BundleImportIssue.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowBundleImportResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowBundleImportResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.importedWorkflowIds.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.importedPackIds.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.skipped.push(BundleImportIssue.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowBundleImportResult {
    return {
      importedWorkflowIds: globalThis.Array.isArray(object?.importedWorkflowIds)
        ? object.importedWorkflowIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.imported_workflow_ids)
        ? object.imported_workflow_ids.map((e: any) => globalThis.String(e))
        : [],
      importedPackIds: globalThis.Array.isArray(object?.importedPackIds)
        ? object.importedPackIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.imported_pack_ids)
        ? object.imported_pack_ids.map((e: any) => globalThis.String(e))
        : [],
      skipped: globalThis.Array.isArray(object?.skipped)
        ? object.skipped.map((e: any) => BundleImportIssue.fromJSON(e))
        : [],
    };
  },

  toJSON(message: WorkflowBundleImportResult): unknown {
    const obj: any = {};
    if (message.importedWorkflowIds?.length) {
      obj.importedWorkflowIds = message.importedWorkflowIds;
    }
    if (message.importedPackIds?.length) {
      obj.importedPackIds = message.importedPackIds;
    }
    if (message.skipped?.length) {
      obj.skipped = message.skipped.map((e) => BundleImportIssue.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowBundleImportResult>, I>>(base?: I): WorkflowBundleImportResult {
    return WorkflowBundleImportResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowBundleImportResult>, I>>(object: I): WorkflowBundleImportResult {
    const message = createBaseWorkflowBundleImportResult();
    message.importedWorkflowIds = object.importedWorkflowIds?.map((e) => e) || [];
    message.importedPackIds = object.importedPackIds?.map((e) => e) || [];
    message.skipped = object.skipped?.map((e) => BundleImportIssue.fromPartial(e)) || [];
    return message;
  },
};

function createBaseNodePack(): NodePack {
  return {
    id: "",
    name: "",
    description: "",
    author: "",
    version: "",
    category: "",
    icon: "",
    accentRgb: 0,
    inputs: [],
    outputs: [],
    composite: undefined,
    script: undefined,
    capabilities: undefined,
  };
}

export const NodePack: MessageFns<NodePack> = {
  encode(message: NodePack, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.author !== "") {
      writer.uint32(34).string(message.author);
    }
    if (message.version !== "") {
      writer.uint32(42).string(message.version);
    }
    if (message.category !== "") {
      writer.uint32(50).string(message.category);
    }
    if (message.icon !== "") {
      writer.uint32(58).string(message.icon);
    }
    if (message.accentRgb !== 0) {
      writer.uint32(64).uint32(message.accentRgb);
    }
    for (const v of message.inputs) {
      ToolArgumentPort.encode(v!, writer.uint32(74).fork()).join();
    }
    for (const v of message.outputs) {
      writer.uint32(82).string(v!);
    }
    if (message.composite !== undefined) {
      CompositeImplementation.encode(message.composite, writer.uint32(90).fork()).join();
    }
    if (message.script !== undefined) {
      ScriptImplementation.encode(message.script, writer.uint32(98).fork()).join();
    }
    if (message.capabilities !== undefined) {
      NodePackCapabilities.encode(message.capabilities, writer.uint32(106).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NodePack {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNodePack();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.author = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.category = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.icon = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.accentRgb = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.inputs.push(ToolArgumentPort.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.outputs.push(reader.string());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.composite = CompositeImplementation.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.script = ScriptImplementation.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.capabilities = NodePackCapabilities.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NodePack {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      author: isSet(object.author) ? globalThis.String(object.author) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      category: isSet(object.category) ? globalThis.String(object.category) : "",
      icon: isSet(object.icon) ? globalThis.String(object.icon) : "",
      accentRgb: isSet(object.accentRgb)
        ? globalThis.Number(object.accentRgb)
        : isSet(object.accent_rgb)
        ? globalThis.Number(object.accent_rgb)
        : 0,
      inputs: globalThis.Array.isArray(object?.inputs)
        ? object.inputs.map((e: any) => ToolArgumentPort.fromJSON(e))
        : [],
      outputs: globalThis.Array.isArray(object?.outputs) ? object.outputs.map((e: any) => globalThis.String(e)) : [],
      composite: isSet(object.composite) ? CompositeImplementation.fromJSON(object.composite) : undefined,
      script: isSet(object.script) ? ScriptImplementation.fromJSON(object.script) : undefined,
      capabilities: isSet(object.capabilities) ? NodePackCapabilities.fromJSON(object.capabilities) : undefined,
    };
  },

  toJSON(message: NodePack): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.author !== "") {
      obj.author = message.author;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.category !== "") {
      obj.category = message.category;
    }
    if (message.icon !== "") {
      obj.icon = message.icon;
    }
    if (message.accentRgb !== 0) {
      obj.accentRgb = Math.round(message.accentRgb);
    }
    if (message.inputs?.length) {
      obj.inputs = message.inputs.map((e) => ToolArgumentPort.toJSON(e));
    }
    if (message.outputs?.length) {
      obj.outputs = message.outputs;
    }
    if (message.composite !== undefined) {
      obj.composite = CompositeImplementation.toJSON(message.composite);
    }
    if (message.script !== undefined) {
      obj.script = ScriptImplementation.toJSON(message.script);
    }
    if (message.capabilities !== undefined) {
      obj.capabilities = NodePackCapabilities.toJSON(message.capabilities);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NodePack>, I>>(base?: I): NodePack {
    return NodePack.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NodePack>, I>>(object: I): NodePack {
    const message = createBaseNodePack();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.author = object.author ?? "";
    message.version = object.version ?? "";
    message.category = object.category ?? "";
    message.icon = object.icon ?? "";
    message.accentRgb = object.accentRgb ?? 0;
    message.inputs = object.inputs?.map((e) => ToolArgumentPort.fromPartial(e)) || [];
    message.outputs = object.outputs?.map((e) => e) || [];
    message.composite = (object.composite !== undefined && object.composite !== null)
      ? CompositeImplementation.fromPartial(object.composite)
      : undefined;
    message.script = (object.script !== undefined && object.script !== null)
      ? ScriptImplementation.fromPartial(object.script)
      : undefined;
    message.capabilities = (object.capabilities !== undefined && object.capabilities !== null)
      ? NodePackCapabilities.fromPartial(object.capabilities)
      : undefined;
    return message;
  },
};

function createBaseCompositeImplementation(): CompositeImplementation {
  return { graph: undefined, entryNodeId: "", exitNodeId: "" };
}

export const CompositeImplementation: MessageFns<CompositeImplementation> = {
  encode(message: CompositeImplementation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.graph !== undefined) {
      WorkflowDocument.encode(message.graph, writer.uint32(10).fork()).join();
    }
    if (message.entryNodeId !== "") {
      writer.uint32(18).string(message.entryNodeId);
    }
    if (message.exitNodeId !== "") {
      writer.uint32(26).string(message.exitNodeId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CompositeImplementation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCompositeImplementation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.graph = WorkflowDocument.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.entryNodeId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.exitNodeId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CompositeImplementation {
    return {
      graph: isSet(object.graph) ? WorkflowDocument.fromJSON(object.graph) : undefined,
      entryNodeId: isSet(object.entryNodeId)
        ? globalThis.String(object.entryNodeId)
        : isSet(object.entry_node_id)
        ? globalThis.String(object.entry_node_id)
        : "",
      exitNodeId: isSet(object.exitNodeId)
        ? globalThis.String(object.exitNodeId)
        : isSet(object.exit_node_id)
        ? globalThis.String(object.exit_node_id)
        : "",
    };
  },

  toJSON(message: CompositeImplementation): unknown {
    const obj: any = {};
    if (message.graph !== undefined) {
      obj.graph = WorkflowDocument.toJSON(message.graph);
    }
    if (message.entryNodeId !== "") {
      obj.entryNodeId = message.entryNodeId;
    }
    if (message.exitNodeId !== "") {
      obj.exitNodeId = message.exitNodeId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CompositeImplementation>, I>>(base?: I): CompositeImplementation {
    return CompositeImplementation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CompositeImplementation>, I>>(object: I): CompositeImplementation {
    const message = createBaseCompositeImplementation();
    message.graph = (object.graph !== undefined && object.graph !== null)
      ? WorkflowDocument.fromPartial(object.graph)
      : undefined;
    message.entryNodeId = object.entryNodeId ?? "";
    message.exitNodeId = object.exitNodeId ?? "";
    return message;
  },
};

function createBaseScriptImplementation(): ScriptImplementation {
  return { source: "" };
}

export const ScriptImplementation: MessageFns<ScriptImplementation> = {
  encode(message: ScriptImplementation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.source !== "") {
      writer.uint32(10).string(message.source);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScriptImplementation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScriptImplementation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.source = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScriptImplementation {
    return { source: isSet(object.source) ? globalThis.String(object.source) : "" };
  },

  toJSON(message: ScriptImplementation): unknown {
    const obj: any = {};
    if (message.source !== "") {
      obj.source = message.source;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ScriptImplementation>, I>>(base?: I): ScriptImplementation {
    return ScriptImplementation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ScriptImplementation>, I>>(object: I): ScriptImplementation {
    const message = createBaseScriptImplementation();
    message.source = object.source ?? "";
    return message;
  },
};

function createBaseNodePackCapabilities(): NodePackCapabilities {
  return { network: false, filesystem: false, tools: false, toolNames: [] };
}

export const NodePackCapabilities: MessageFns<NodePackCapabilities> = {
  encode(message: NodePackCapabilities, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.network !== false) {
      writer.uint32(8).bool(message.network);
    }
    if (message.filesystem !== false) {
      writer.uint32(16).bool(message.filesystem);
    }
    if (message.tools !== false) {
      writer.uint32(24).bool(message.tools);
    }
    for (const v of message.toolNames) {
      writer.uint32(34).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NodePackCapabilities {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNodePackCapabilities();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.network = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.filesystem = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.tools = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.toolNames.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NodePackCapabilities {
    return {
      network: isSet(object.network) ? globalThis.Boolean(object.network) : false,
      filesystem: isSet(object.filesystem) ? globalThis.Boolean(object.filesystem) : false,
      tools: isSet(object.tools) ? globalThis.Boolean(object.tools) : false,
      toolNames: globalThis.Array.isArray(object?.toolNames)
        ? object.toolNames.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.tool_names)
        ? object.tool_names.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: NodePackCapabilities): unknown {
    const obj: any = {};
    if (message.network !== false) {
      obj.network = message.network;
    }
    if (message.filesystem !== false) {
      obj.filesystem = message.filesystem;
    }
    if (message.tools !== false) {
      obj.tools = message.tools;
    }
    if (message.toolNames?.length) {
      obj.toolNames = message.toolNames;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NodePackCapabilities>, I>>(base?: I): NodePackCapabilities {
    return NodePackCapabilities.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NodePackCapabilities>, I>>(object: I): NodePackCapabilities {
    const message = createBaseNodePackCapabilities();
    message.network = object.network ?? false;
    message.filesystem = object.filesystem ?? false;
    message.tools = object.tools ?? false;
    message.toolNames = object.toolNames?.map((e) => e) || [];
    return message;
  },
};

function createBaseNodePackList(): NodePackList {
  return { packs: [] };
}

export const NodePackList: MessageFns<NodePackList> = {
  encode(message: NodePackList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.packs) {
      NodePack.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NodePackList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNodePackList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.packs.push(NodePack.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NodePackList {
    return { packs: globalThis.Array.isArray(object?.packs) ? object.packs.map((e: any) => NodePack.fromJSON(e)) : [] };
  },

  toJSON(message: NodePackList): unknown {
    const obj: any = {};
    if (message.packs?.length) {
      obj.packs = message.packs.map((e) => NodePack.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NodePackList>, I>>(base?: I): NodePackList {
    return NodePackList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NodePackList>, I>>(object: I): NodePackList {
    const message = createBaseNodePackList();
    message.packs = object.packs?.map((e) => NodePack.fromPartial(e)) || [];
    return message;
  },
};

function createBaseWorkflowSummary(): WorkflowSummary {
  return { id: "", name: "", createdAtMs: 0, updatedAtMs: 0, nodeCount: 0 };
}

export const WorkflowSummary: MessageFns<WorkflowSummary> = {
  encode(message: WorkflowSummary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.createdAtMs !== 0) {
      writer.uint32(24).int64(message.createdAtMs);
    }
    if (message.updatedAtMs !== 0) {
      writer.uint32(32).int64(message.updatedAtMs);
    }
    if (message.nodeCount !== 0) {
      writer.uint32(40).uint32(message.nodeCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowSummary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowSummary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.createdAtMs = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.updatedAtMs = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.nodeCount = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowSummary {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      createdAtMs: isSet(object.createdAtMs)
        ? globalThis.Number(object.createdAtMs)
        : isSet(object.created_at_ms)
        ? globalThis.Number(object.created_at_ms)
        : 0,
      updatedAtMs: isSet(object.updatedAtMs)
        ? globalThis.Number(object.updatedAtMs)
        : isSet(object.updated_at_ms)
        ? globalThis.Number(object.updated_at_ms)
        : 0,
      nodeCount: isSet(object.nodeCount)
        ? globalThis.Number(object.nodeCount)
        : isSet(object.node_count)
        ? globalThis.Number(object.node_count)
        : 0,
    };
  },

  toJSON(message: WorkflowSummary): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.createdAtMs !== 0) {
      obj.createdAtMs = Math.round(message.createdAtMs);
    }
    if (message.updatedAtMs !== 0) {
      obj.updatedAtMs = Math.round(message.updatedAtMs);
    }
    if (message.nodeCount !== 0) {
      obj.nodeCount = Math.round(message.nodeCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowSummary>, I>>(base?: I): WorkflowSummary {
    return WorkflowSummary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowSummary>, I>>(object: I): WorkflowSummary {
    const message = createBaseWorkflowSummary();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.createdAtMs = object.createdAtMs ?? 0;
    message.updatedAtMs = object.updatedAtMs ?? 0;
    message.nodeCount = object.nodeCount ?? 0;
    return message;
  },
};

function createBaseWorkflowList(): WorkflowList {
  return { workflows: [] };
}

export const WorkflowList: MessageFns<WorkflowList> = {
  encode(message: WorkflowList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.workflows) {
      WorkflowSummary.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workflows.push(WorkflowSummary.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowList {
    return {
      workflows: globalThis.Array.isArray(object?.workflows)
        ? object.workflows.map((e: any) => WorkflowSummary.fromJSON(e))
        : [],
    };
  },

  toJSON(message: WorkflowList): unknown {
    const obj: any = {};
    if (message.workflows?.length) {
      obj.workflows = message.workflows.map((e) => WorkflowSummary.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowList>, I>>(base?: I): WorkflowList {
    return WorkflowList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowList>, I>>(object: I): WorkflowList {
    const message = createBaseWorkflowList();
    message.workflows = object.workflows?.map((e) => WorkflowSummary.fromPartial(e)) || [];
    return message;
  },
};

function createBaseWorkflowValidationResult(): WorkflowValidationResult {
  return { valid: false, issues: [] };
}

export const WorkflowValidationResult: MessageFns<WorkflowValidationResult> = {
  encode(message: WorkflowValidationResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.valid !== false) {
      writer.uint32(8).bool(message.valid);
    }
    for (const v of message.issues) {
      WorkflowValidationIssue.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowValidationResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowValidationResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.valid = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.issues.push(WorkflowValidationIssue.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowValidationResult {
    return {
      valid: isSet(object.valid) ? globalThis.Boolean(object.valid) : false,
      issues: globalThis.Array.isArray(object?.issues)
        ? object.issues.map((e: any) => WorkflowValidationIssue.fromJSON(e))
        : [],
    };
  },

  toJSON(message: WorkflowValidationResult): unknown {
    const obj: any = {};
    if (message.valid !== false) {
      obj.valid = message.valid;
    }
    if (message.issues?.length) {
      obj.issues = message.issues.map((e) => WorkflowValidationIssue.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowValidationResult>, I>>(base?: I): WorkflowValidationResult {
    return WorkflowValidationResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowValidationResult>, I>>(object: I): WorkflowValidationResult {
    const message = createBaseWorkflowValidationResult();
    message.valid = object.valid ?? false;
    message.issues = object.issues?.map((e) => WorkflowValidationIssue.fromPartial(e)) || [];
    return message;
  },
};

function createBaseWorkflowValidationIssue(): WorkflowValidationIssue {
  return { message: "", nodeId: undefined, edgeFrom: undefined };
}

export const WorkflowValidationIssue: MessageFns<WorkflowValidationIssue> = {
  encode(message: WorkflowValidationIssue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    if (message.nodeId !== undefined) {
      writer.uint32(18).string(message.nodeId);
    }
    if (message.edgeFrom !== undefined) {
      writer.uint32(26).string(message.edgeFrom);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowValidationIssue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowValidationIssue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nodeId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.edgeFrom = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowValidationIssue {
    return {
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      nodeId: isSet(object.nodeId)
        ? globalThis.String(object.nodeId)
        : isSet(object.node_id)
        ? globalThis.String(object.node_id)
        : undefined,
      edgeFrom: isSet(object.edgeFrom)
        ? globalThis.String(object.edgeFrom)
        : isSet(object.edge_from)
        ? globalThis.String(object.edge_from)
        : undefined,
    };
  },

  toJSON(message: WorkflowValidationIssue): unknown {
    const obj: any = {};
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.nodeId !== undefined) {
      obj.nodeId = message.nodeId;
    }
    if (message.edgeFrom !== undefined) {
      obj.edgeFrom = message.edgeFrom;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowValidationIssue>, I>>(base?: I): WorkflowValidationIssue {
    return WorkflowValidationIssue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowValidationIssue>, I>>(object: I): WorkflowValidationIssue {
    const message = createBaseWorkflowValidationIssue();
    message.message = object.message ?? "";
    message.nodeId = object.nodeId ?? undefined;
    message.edgeFrom = object.edgeFrom ?? undefined;
    return message;
  },
};

function createBaseNodeRun(): NodeRun {
  return { nodeId: "", state: 0, startedAtMs: 0, finishedAtMs: 0, output: [], error: undefined };
}

export const NodeRun: MessageFns<NodeRun> = {
  encode(message: NodeRun, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nodeId !== "") {
      writer.uint32(10).string(message.nodeId);
    }
    if (message.state !== 0) {
      writer.uint32(16).int32(message.state);
    }
    if (message.startedAtMs !== 0) {
      writer.uint32(24).int64(message.startedAtMs);
    }
    if (message.finishedAtMs !== 0) {
      writer.uint32(32).int64(message.finishedAtMs);
    }
    for (const v of message.output) {
      WorkflowItem.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.error !== undefined) {
      SDKError.encode(message.error, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NodeRun {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNodeRun();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nodeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.startedAtMs = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.finishedAtMs = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.output.push(WorkflowItem.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.error = SDKError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NodeRun {
    return {
      nodeId: isSet(object.nodeId)
        ? globalThis.String(object.nodeId)
        : isSet(object.node_id)
        ? globalThis.String(object.node_id)
        : "",
      state: isSet(object.state) ? nodeRunStateFromJSON(object.state) : 0,
      startedAtMs: isSet(object.startedAtMs)
        ? globalThis.Number(object.startedAtMs)
        : isSet(object.started_at_ms)
        ? globalThis.Number(object.started_at_ms)
        : 0,
      finishedAtMs: isSet(object.finishedAtMs)
        ? globalThis.Number(object.finishedAtMs)
        : isSet(object.finished_at_ms)
        ? globalThis.Number(object.finished_at_ms)
        : 0,
      output: globalThis.Array.isArray(object?.output)
        ? object.output.map((e: any) => WorkflowItem.fromJSON(e))
        : [],
      error: isSet(object.error) ? SDKError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: NodeRun): unknown {
    const obj: any = {};
    if (message.nodeId !== "") {
      obj.nodeId = message.nodeId;
    }
    if (message.state !== 0) {
      obj.state = nodeRunStateToJSON(message.state);
    }
    if (message.startedAtMs !== 0) {
      obj.startedAtMs = Math.round(message.startedAtMs);
    }
    if (message.finishedAtMs !== 0) {
      obj.finishedAtMs = Math.round(message.finishedAtMs);
    }
    if (message.output?.length) {
      obj.output = message.output.map((e) => WorkflowItem.toJSON(e));
    }
    if (message.error !== undefined) {
      obj.error = SDKError.toJSON(message.error);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NodeRun>, I>>(base?: I): NodeRun {
    return NodeRun.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NodeRun>, I>>(object: I): NodeRun {
    const message = createBaseNodeRun();
    message.nodeId = object.nodeId ?? "";
    message.state = object.state ?? 0;
    message.startedAtMs = object.startedAtMs ?? 0;
    message.finishedAtMs = object.finishedAtMs ?? 0;
    message.output = object.output?.map((e) => WorkflowItem.fromPartial(e)) || [];
    message.error = (object.error !== undefined && object.error !== null)
      ? SDKError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

function createBaseWorkflowRunRecord(): WorkflowRunRecord {
  return { runId: "", workflowId: "", state: 0, startedAtMs: 0, finishedAtMs: 0, nodeRuns: [], error: undefined };
}

export const WorkflowRunRecord: MessageFns<WorkflowRunRecord> = {
  encode(message: WorkflowRunRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.runId !== "") {
      writer.uint32(10).string(message.runId);
    }
    if (message.workflowId !== "") {
      writer.uint32(18).string(message.workflowId);
    }
    if (message.state !== 0) {
      writer.uint32(24).int32(message.state);
    }
    if (message.startedAtMs !== 0) {
      writer.uint32(32).int64(message.startedAtMs);
    }
    if (message.finishedAtMs !== 0) {
      writer.uint32(40).int64(message.finishedAtMs);
    }
    for (const v of message.nodeRuns) {
      NodeRun.encode(v!, writer.uint32(50).fork()).join();
    }
    if (message.error !== undefined) {
      SDKError.encode(message.error, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowRunRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowRunRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.runId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workflowId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.startedAtMs = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.finishedAtMs = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.nodeRuns.push(NodeRun.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.error = SDKError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowRunRecord {
    return {
      runId: isSet(object.runId)
        ? globalThis.String(object.runId)
        : isSet(object.run_id)
        ? globalThis.String(object.run_id)
        : "",
      workflowId: isSet(object.workflowId)
        ? globalThis.String(object.workflowId)
        : isSet(object.workflow_id)
        ? globalThis.String(object.workflow_id)
        : "",
      state: isSet(object.state) ? workflowRunStateFromJSON(object.state) : 0,
      startedAtMs: isSet(object.startedAtMs)
        ? globalThis.Number(object.startedAtMs)
        : isSet(object.started_at_ms)
        ? globalThis.Number(object.started_at_ms)
        : 0,
      finishedAtMs: isSet(object.finishedAtMs)
        ? globalThis.Number(object.finishedAtMs)
        : isSet(object.finished_at_ms)
        ? globalThis.Number(object.finished_at_ms)
        : 0,
      nodeRuns: globalThis.Array.isArray(object?.nodeRuns)
        ? object.nodeRuns.map((e: any) => NodeRun.fromJSON(e))
        : globalThis.Array.isArray(object?.node_runs)
        ? object.node_runs.map((e: any) => NodeRun.fromJSON(e))
        : [],
      error: isSet(object.error) ? SDKError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: WorkflowRunRecord): unknown {
    const obj: any = {};
    if (message.runId !== "") {
      obj.runId = message.runId;
    }
    if (message.workflowId !== "") {
      obj.workflowId = message.workflowId;
    }
    if (message.state !== 0) {
      obj.state = workflowRunStateToJSON(message.state);
    }
    if (message.startedAtMs !== 0) {
      obj.startedAtMs = Math.round(message.startedAtMs);
    }
    if (message.finishedAtMs !== 0) {
      obj.finishedAtMs = Math.round(message.finishedAtMs);
    }
    if (message.nodeRuns?.length) {
      obj.nodeRuns = message.nodeRuns.map((e) => NodeRun.toJSON(e));
    }
    if (message.error !== undefined) {
      obj.error = SDKError.toJSON(message.error);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowRunRecord>, I>>(base?: I): WorkflowRunRecord {
    return WorkflowRunRecord.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowRunRecord>, I>>(object: I): WorkflowRunRecord {
    const message = createBaseWorkflowRunRecord();
    message.runId = object.runId ?? "";
    message.workflowId = object.workflowId ?? "";
    message.state = object.state ?? 0;
    message.startedAtMs = object.startedAtMs ?? 0;
    message.finishedAtMs = object.finishedAtMs ?? 0;
    message.nodeRuns = object.nodeRuns?.map((e) => NodeRun.fromPartial(e)) || [];
    message.error = (object.error !== undefined && object.error !== null)
      ? SDKError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

function createBaseWorkflowRunRequest(): WorkflowRunRequest {
  return { workflowId: "", initialItems: [] };
}

export const WorkflowRunRequest: MessageFns<WorkflowRunRequest> = {
  encode(message: WorkflowRunRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workflowId !== "") {
      writer.uint32(10).string(message.workflowId);
    }
    for (const v of message.initialItems) {
      WorkflowItem.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowRunRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowRunRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workflowId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.initialItems.push(WorkflowItem.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowRunRequest {
    return {
      workflowId: isSet(object.workflowId)
        ? globalThis.String(object.workflowId)
        : isSet(object.workflow_id)
        ? globalThis.String(object.workflow_id)
        : "",
      initialItems: globalThis.Array.isArray(object?.initialItems)
        ? object.initialItems.map((e: any) => WorkflowItem.fromJSON(e))
        : globalThis.Array.isArray(object?.initial_items)
        ? object.initial_items.map((e: any) => WorkflowItem.fromJSON(e))
        : [],
    };
  },

  toJSON(message: WorkflowRunRequest): unknown {
    const obj: any = {};
    if (message.workflowId !== "") {
      obj.workflowId = message.workflowId;
    }
    if (message.initialItems?.length) {
      obj.initialItems = message.initialItems.map((e) => WorkflowItem.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowRunRequest>, I>>(base?: I): WorkflowRunRequest {
    return WorkflowRunRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowRunRequest>, I>>(object: I): WorkflowRunRequest {
    const message = createBaseWorkflowRunRequest();
    message.workflowId = object.workflowId ?? "";
    message.initialItems = object.initialItems?.map((e) => WorkflowItem.fromPartial(e)) || [];
    return message;
  },
};

function createBaseWorkflowRunEvent(): WorkflowRunEvent {
  return { runId: "", runStarted: undefined, nodeStateChanged: undefined, runFinished: undefined };
}

export const WorkflowRunEvent: MessageFns<WorkflowRunEvent> = {
  encode(message: WorkflowRunEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.runId !== "") {
      writer.uint32(10).string(message.runId);
    }
    if (message.runStarted !== undefined) {
      RunStarted.encode(message.runStarted, writer.uint32(82).fork()).join();
    }
    if (message.nodeStateChanged !== undefined) {
      NodeStateChanged.encode(message.nodeStateChanged, writer.uint32(90).fork()).join();
    }
    if (message.runFinished !== undefined) {
      RunFinished.encode(message.runFinished, writer.uint32(98).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowRunEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowRunEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.runId = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.runStarted = RunStarted.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.nodeStateChanged = NodeStateChanged.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.runFinished = RunFinished.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowRunEvent {
    return {
      runId: isSet(object.runId)
        ? globalThis.String(object.runId)
        : isSet(object.run_id)
        ? globalThis.String(object.run_id)
        : "",
      runStarted: isSet(object.runStarted)
        ? RunStarted.fromJSON(object.runStarted)
        : isSet(object.run_started)
        ? RunStarted.fromJSON(object.run_started)
        : undefined,
      nodeStateChanged: isSet(object.nodeStateChanged)
        ? NodeStateChanged.fromJSON(object.nodeStateChanged)
        : isSet(object.node_state_changed)
        ? NodeStateChanged.fromJSON(object.node_state_changed)
        : undefined,
      runFinished: isSet(object.runFinished)
        ? RunFinished.fromJSON(object.runFinished)
        : isSet(object.run_finished)
        ? RunFinished.fromJSON(object.run_finished)
        : undefined,
    };
  },

  toJSON(message: WorkflowRunEvent): unknown {
    const obj: any = {};
    if (message.runId !== "") {
      obj.runId = message.runId;
    }
    if (message.runStarted !== undefined) {
      obj.runStarted = RunStarted.toJSON(message.runStarted);
    }
    if (message.nodeStateChanged !== undefined) {
      obj.nodeStateChanged = NodeStateChanged.toJSON(message.nodeStateChanged);
    }
    if (message.runFinished !== undefined) {
      obj.runFinished = RunFinished.toJSON(message.runFinished);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowRunEvent>, I>>(base?: I): WorkflowRunEvent {
    return WorkflowRunEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowRunEvent>, I>>(object: I): WorkflowRunEvent {
    const message = createBaseWorkflowRunEvent();
    message.runId = object.runId ?? "";
    message.runStarted = (object.runStarted !== undefined && object.runStarted !== null)
      ? RunStarted.fromPartial(object.runStarted)
      : undefined;
    message.nodeStateChanged = (object.nodeStateChanged !== undefined && object.nodeStateChanged !== null)
      ? NodeStateChanged.fromPartial(object.nodeStateChanged)
      : undefined;
    message.runFinished = (object.runFinished !== undefined && object.runFinished !== null)
      ? RunFinished.fromPartial(object.runFinished)
      : undefined;
    return message;
  },
};

function createBaseRunStarted(): RunStarted {
  return { workflowId: "", startedAtMs: 0 };
}

export const RunStarted: MessageFns<RunStarted> = {
  encode(message: RunStarted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workflowId !== "") {
      writer.uint32(10).string(message.workflowId);
    }
    if (message.startedAtMs !== 0) {
      writer.uint32(16).int64(message.startedAtMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RunStarted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRunStarted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workflowId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.startedAtMs = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RunStarted {
    return {
      workflowId: isSet(object.workflowId)
        ? globalThis.String(object.workflowId)
        : isSet(object.workflow_id)
        ? globalThis.String(object.workflow_id)
        : "",
      startedAtMs: isSet(object.startedAtMs)
        ? globalThis.Number(object.startedAtMs)
        : isSet(object.started_at_ms)
        ? globalThis.Number(object.started_at_ms)
        : 0,
    };
  },

  toJSON(message: RunStarted): unknown {
    const obj: any = {};
    if (message.workflowId !== "") {
      obj.workflowId = message.workflowId;
    }
    if (message.startedAtMs !== 0) {
      obj.startedAtMs = Math.round(message.startedAtMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RunStarted>, I>>(base?: I): RunStarted {
    return RunStarted.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RunStarted>, I>>(object: I): RunStarted {
    const message = createBaseRunStarted();
    message.workflowId = object.workflowId ?? "";
    message.startedAtMs = object.startedAtMs ?? 0;
    return message;
  },
};

function createBaseNodeStateChanged(): NodeStateChanged {
  return { nodeId: "", state: 0, output: [], error: undefined };
}

export const NodeStateChanged: MessageFns<NodeStateChanged> = {
  encode(message: NodeStateChanged, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nodeId !== "") {
      writer.uint32(10).string(message.nodeId);
    }
    if (message.state !== 0) {
      writer.uint32(16).int32(message.state);
    }
    for (const v of message.output) {
      WorkflowItem.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.error !== undefined) {
      SDKError.encode(message.error, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NodeStateChanged {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNodeStateChanged();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nodeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.output.push(WorkflowItem.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.error = SDKError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NodeStateChanged {
    return {
      nodeId: isSet(object.nodeId)
        ? globalThis.String(object.nodeId)
        : isSet(object.node_id)
        ? globalThis.String(object.node_id)
        : "",
      state: isSet(object.state) ? nodeRunStateFromJSON(object.state) : 0,
      output: globalThis.Array.isArray(object?.output) ? object.output.map((e: any) => WorkflowItem.fromJSON(e)) : [],
      error: isSet(object.error) ? SDKError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: NodeStateChanged): unknown {
    const obj: any = {};
    if (message.nodeId !== "") {
      obj.nodeId = message.nodeId;
    }
    if (message.state !== 0) {
      obj.state = nodeRunStateToJSON(message.state);
    }
    if (message.output?.length) {
      obj.output = message.output.map((e) => WorkflowItem.toJSON(e));
    }
    if (message.error !== undefined) {
      obj.error = SDKError.toJSON(message.error);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NodeStateChanged>, I>>(base?: I): NodeStateChanged {
    return NodeStateChanged.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NodeStateChanged>, I>>(object: I): NodeStateChanged {
    const message = createBaseNodeStateChanged();
    message.nodeId = object.nodeId ?? "";
    message.state = object.state ?? 0;
    message.output = object.output?.map((e) => WorkflowItem.fromPartial(e)) || [];
    message.error = (object.error !== undefined && object.error !== null)
      ? SDKError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

function createBaseRunFinished(): RunFinished {
  return { state: 0, finishedAtMs: 0, error: undefined };
}

export const RunFinished: MessageFns<RunFinished> = {
  encode(message: RunFinished, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.state !== 0) {
      writer.uint32(8).int32(message.state);
    }
    if (message.finishedAtMs !== 0) {
      writer.uint32(16).int64(message.finishedAtMs);
    }
    if (message.error !== undefined) {
      SDKError.encode(message.error, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RunFinished {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRunFinished();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.finishedAtMs = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.error = SDKError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RunFinished {
    return {
      state: isSet(object.state) ? workflowRunStateFromJSON(object.state) : 0,
      finishedAtMs: isSet(object.finishedAtMs)
        ? globalThis.Number(object.finishedAtMs)
        : isSet(object.finished_at_ms)
        ? globalThis.Number(object.finished_at_ms)
        : 0,
      error: isSet(object.error) ? SDKError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: RunFinished): unknown {
    const obj: any = {};
    if (message.state !== 0) {
      obj.state = workflowRunStateToJSON(message.state);
    }
    if (message.finishedAtMs !== 0) {
      obj.finishedAtMs = Math.round(message.finishedAtMs);
    }
    if (message.error !== undefined) {
      obj.error = SDKError.toJSON(message.error);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RunFinished>, I>>(base?: I): RunFinished {
    return RunFinished.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RunFinished>, I>>(object: I): RunFinished {
    const message = createBaseRunFinished();
    message.state = object.state ?? 0;
    message.finishedAtMs = object.finishedAtMs ?? 0;
    message.error = (object.error !== undefined && object.error !== null)
      ? SDKError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

function createBaseToolInvocation(): ToolInvocation {
  return { runId: "", nodeId: "", toolName: "", argumentsJson: "" };
}

export const ToolInvocation: MessageFns<ToolInvocation> = {
  encode(message: ToolInvocation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.runId !== "") {
      writer.uint32(10).string(message.runId);
    }
    if (message.nodeId !== "") {
      writer.uint32(18).string(message.nodeId);
    }
    if (message.toolName !== "") {
      writer.uint32(26).string(message.toolName);
    }
    if (message.argumentsJson !== "") {
      writer.uint32(34).string(message.argumentsJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolInvocation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolInvocation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.runId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nodeId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.toolName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.argumentsJson = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolInvocation {
    return {
      runId: isSet(object.runId)
        ? globalThis.String(object.runId)
        : isSet(object.run_id)
        ? globalThis.String(object.run_id)
        : "",
      nodeId: isSet(object.nodeId)
        ? globalThis.String(object.nodeId)
        : isSet(object.node_id)
        ? globalThis.String(object.node_id)
        : "",
      toolName: isSet(object.toolName)
        ? globalThis.String(object.toolName)
        : isSet(object.tool_name)
        ? globalThis.String(object.tool_name)
        : "",
      argumentsJson: isSet(object.argumentsJson)
        ? globalThis.String(object.argumentsJson)
        : isSet(object.arguments_json)
        ? globalThis.String(object.arguments_json)
        : "",
    };
  },

  toJSON(message: ToolInvocation): unknown {
    const obj: any = {};
    if (message.runId !== "") {
      obj.runId = message.runId;
    }
    if (message.nodeId !== "") {
      obj.nodeId = message.nodeId;
    }
    if (message.toolName !== "") {
      obj.toolName = message.toolName;
    }
    if (message.argumentsJson !== "") {
      obj.argumentsJson = message.argumentsJson;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolInvocation>, I>>(base?: I): ToolInvocation {
    return ToolInvocation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolInvocation>, I>>(object: I): ToolInvocation {
    const message = createBaseToolInvocation();
    message.runId = object.runId ?? "";
    message.nodeId = object.nodeId ?? "";
    message.toolName = object.toolName ?? "";
    message.argumentsJson = object.argumentsJson ?? "";
    return message;
  },
};

function createBaseToolInvocationResult(): ToolInvocationResult {
  return { resultJson: undefined, error: undefined };
}

export const ToolInvocationResult: MessageFns<ToolInvocationResult> = {
  encode(message: ToolInvocationResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.resultJson !== undefined) {
      writer.uint32(10).string(message.resultJson);
    }
    if (message.error !== undefined) {
      SDKError.encode(message.error, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolInvocationResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolInvocationResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.resultJson = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = SDKError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolInvocationResult {
    return {
      resultJson: isSet(object.resultJson)
        ? globalThis.String(object.resultJson)
        : isSet(object.result_json)
        ? globalThis.String(object.result_json)
        : undefined,
      error: isSet(object.error) ? SDKError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: ToolInvocationResult): unknown {
    const obj: any = {};
    if (message.resultJson !== undefined) {
      obj.resultJson = message.resultJson;
    }
    if (message.error !== undefined) {
      obj.error = SDKError.toJSON(message.error);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolInvocationResult>, I>>(base?: I): ToolInvocationResult {
    return ToolInvocationResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolInvocationResult>, I>>(object: I): ToolInvocationResult {
    const message = createBaseToolInvocationResult();
    message.resultJson = object.resultJson ?? undefined;
    message.error = (object.error !== undefined && object.error !== null)
      ? SDKError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

function createBaseCodeInvocation(): CodeInvocation {
  return { runId: "", nodeId: "", source: "", inputItemsJson: "" };
}

export const CodeInvocation: MessageFns<CodeInvocation> = {
  encode(message: CodeInvocation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.runId !== "") {
      writer.uint32(10).string(message.runId);
    }
    if (message.nodeId !== "") {
      writer.uint32(18).string(message.nodeId);
    }
    if (message.source !== "") {
      writer.uint32(26).string(message.source);
    }
    if (message.inputItemsJson !== "") {
      writer.uint32(34).string(message.inputItemsJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CodeInvocation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCodeInvocation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.runId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nodeId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.inputItemsJson = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CodeInvocation {
    return {
      runId: isSet(object.runId)
        ? globalThis.String(object.runId)
        : isSet(object.run_id)
        ? globalThis.String(object.run_id)
        : "",
      nodeId: isSet(object.nodeId)
        ? globalThis.String(object.nodeId)
        : isSet(object.node_id)
        ? globalThis.String(object.node_id)
        : "",
      source: isSet(object.source) ? globalThis.String(object.source) : "",
      inputItemsJson: isSet(object.inputItemsJson)
        ? globalThis.String(object.inputItemsJson)
        : isSet(object.input_items_json)
        ? globalThis.String(object.input_items_json)
        : "",
    };
  },

  toJSON(message: CodeInvocation): unknown {
    const obj: any = {};
    if (message.runId !== "") {
      obj.runId = message.runId;
    }
    if (message.nodeId !== "") {
      obj.nodeId = message.nodeId;
    }
    if (message.source !== "") {
      obj.source = message.source;
    }
    if (message.inputItemsJson !== "") {
      obj.inputItemsJson = message.inputItemsJson;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CodeInvocation>, I>>(base?: I): CodeInvocation {
    return CodeInvocation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CodeInvocation>, I>>(object: I): CodeInvocation {
    const message = createBaseCodeInvocation();
    message.runId = object.runId ?? "";
    message.nodeId = object.nodeId ?? "";
    message.source = object.source ?? "";
    message.inputItemsJson = object.inputItemsJson ?? "";
    return message;
  },
};

function createBaseCodeInvocationResult(): CodeInvocationResult {
  return { outputItemsJson: undefined, error: undefined };
}

export const CodeInvocationResult: MessageFns<CodeInvocationResult> = {
  encode(message: CodeInvocationResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.outputItemsJson !== undefined) {
      writer.uint32(10).string(message.outputItemsJson);
    }
    if (message.error !== undefined) {
      SDKError.encode(message.error, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CodeInvocationResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCodeInvocationResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.outputItemsJson = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = SDKError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CodeInvocationResult {
    return {
      outputItemsJson: isSet(object.outputItemsJson)
        ? globalThis.String(object.outputItemsJson)
        : isSet(object.output_items_json)
        ? globalThis.String(object.output_items_json)
        : undefined,
      error: isSet(object.error) ? SDKError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: CodeInvocationResult): unknown {
    const obj: any = {};
    if (message.outputItemsJson !== undefined) {
      obj.outputItemsJson = message.outputItemsJson;
    }
    if (message.error !== undefined) {
      obj.error = SDKError.toJSON(message.error);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CodeInvocationResult>, I>>(base?: I): CodeInvocationResult {
    return CodeInvocationResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CodeInvocationResult>, I>>(object: I): CodeInvocationResult {
    const message = createBaseCodeInvocationResult();
    message.outputItemsJson = object.outputItemsJson ?? undefined;
    message.error = (object.error !== undefined && object.error !== null)
      ? SDKError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  const bin = globalThis.atob(b64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; ++i) {
    arr[i] = bin.charCodeAt(i);
  }
  return arr;
}

function base64FromBytes(arr: Uint8Array): string {
  const bin: string[] = [];
  arr.forEach((byte) => {
    bin.push(globalThis.String.fromCharCode(byte));
  });
  return globalThis.btoa(bin.join(""));
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
