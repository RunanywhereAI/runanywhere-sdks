// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.35.1
// source: stt_options.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { SDKError } from "./errors";
import {
  AudioEncoding,
  audioEncodingFromJSON,
  audioEncodingToJSON,
  AudioFormat,
  audioFormatFromJSON,
  audioFormatToJSON,
  InferenceFramework,
  inferenceFrameworkFromJSON,
  inferenceFrameworkToJSON,
} from "./model_types";

export const protobufPackage = "runanywhere.v1";

export enum STTStreamEventKind {
  STT_STREAM_EVENT_KIND_UNSPECIFIED = 0,
  STT_STREAM_EVENT_KIND_STARTED = 1,
  STT_STREAM_EVENT_KIND_PARTIAL = 2,
  STT_STREAM_EVENT_KIND_FINAL = 3,
  STT_STREAM_EVENT_KIND_ENDPOINT = 4,
  STT_STREAM_EVENT_KIND_ERROR = 5,
  UNRECOGNIZED = -1,
}

export function sTTStreamEventKindFromJSON(object: any): STTStreamEventKind {
  switch (object) {
    case 0:
    case "STT_STREAM_EVENT_KIND_UNSPECIFIED":
      return STTStreamEventKind.STT_STREAM_EVENT_KIND_UNSPECIFIED;
    case 1:
    case "STT_STREAM_EVENT_KIND_STARTED":
      return STTStreamEventKind.STT_STREAM_EVENT_KIND_STARTED;
    case 2:
    case "STT_STREAM_EVENT_KIND_PARTIAL":
      return STTStreamEventKind.STT_STREAM_EVENT_KIND_PARTIAL;
    case 3:
    case "STT_STREAM_EVENT_KIND_FINAL":
      return STTStreamEventKind.STT_STREAM_EVENT_KIND_FINAL;
    case 4:
    case "STT_STREAM_EVENT_KIND_ENDPOINT":
      return STTStreamEventKind.STT_STREAM_EVENT_KIND_ENDPOINT;
    case 5:
    case "STT_STREAM_EVENT_KIND_ERROR":
      return STTStreamEventKind.STT_STREAM_EVENT_KIND_ERROR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return STTStreamEventKind.UNRECOGNIZED;
  }
}

export function sTTStreamEventKindToJSON(object: STTStreamEventKind): string {
  switch (object) {
    case STTStreamEventKind.STT_STREAM_EVENT_KIND_UNSPECIFIED:
      return "STT_STREAM_EVENT_KIND_UNSPECIFIED";
    case STTStreamEventKind.STT_STREAM_EVENT_KIND_STARTED:
      return "STT_STREAM_EVENT_KIND_STARTED";
    case STTStreamEventKind.STT_STREAM_EVENT_KIND_PARTIAL:
      return "STT_STREAM_EVENT_KIND_PARTIAL";
    case STTStreamEventKind.STT_STREAM_EVENT_KIND_FINAL:
      return "STT_STREAM_EVENT_KIND_FINAL";
    case STTStreamEventKind.STT_STREAM_EVENT_KIND_ENDPOINT:
      return "STT_STREAM_EVENT_KIND_ENDPOINT";
    case STTStreamEventKind.STT_STREAM_EVENT_KIND_ERROR:
      return "STT_STREAM_EVENT_KIND_ERROR";
    case STTStreamEventKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Init-time settings. Per-call knobs live on STTOptions; adapters mirror the
 * transcription defaults below into STTOptions when building a request.
 */
export interface STTConfiguration {
  modelId: string;
  language?: string | undefined;
  sampleRate: number;
  enableVad: boolean;
  audioFormat: AudioFormat;
  enablePunctuation: boolean;
  enableDiarization: boolean;
  enableWordTimestamps: boolean;
  preferredFramework?: InferenceFramework | undefined;
}

/** Per-call overrides. */
export interface STTOptions {
  /**
   * BCP-47 tag (bare ISO-639-1 accepted). Unset or "" means auto-detect;
   * the code the engine settles on comes back on STTOutput.language.
   */
  language?: string | undefined;
  enablePunctuation: boolean;
  /**
   * Attribute each word to a speaker. Labels come back on
   * WordTimestamp.speaker_id and STTOutput.speaker_ids. Deepgram `diarize`,
   * AssemblyAI `speaker_labels`.
   */
  diarize: boolean;
  /**
   * Hint for how many distinct speakers to expect. AssemblyAI
   * `speakers_expected`. Unset = let the engine decide.
   */
  speakersExpected?: number | undefined;
  enableWordTimestamps: boolean;
  /**
   * Trailing silence after which the utterance is finalized and an
   * STT_STREAM_EVENT_KIND_ENDPOINT + FINAL pair is emitted. 0 = engine
   * default.
   */
  silenceDurationMs: number;
}

export interface STTAudioSource {
  /** Audio bytes, in-process. */
  audioData?:
    | Uint8Array
    | undefined;
  /** Local path or file:// URI, read and decoded by the platform adapter. */
  fileUri?: string | undefined;
  encoding: AudioEncoding;
  audioFormat: AudioFormat;
  sampleRate: number;
  /**
   * Interleaved channel count; 0 or 1 = mono. Non-mono audio is downmixed
   * before decoding and MUST be divided out of the duration estimate.
   */
  channels: number;
  durationMs: number;
}

export interface STTTranscriptionRequest {
  requestId: string;
  audio?: STTAudioSource | undefined;
  options?: STTOptions | undefined;
  metadata: { [key: string]: string };
}

export interface STTTranscriptionRequest_MetadataEntry {
  key: string;
  value: string;
}

export interface WordTimestamp {
  word: string;
  startMs: number;
  endMs: number;
  confidence: number;
  /**
   * Always empty today: no backend fills this. Kept live -- read by all
   * five SDK facades as public API -- pending real diarization wiring.
   */
  speakerId?: string | undefined;
}

/** One n-best hypothesis. Per-word breakdown only when the backend emits it. */
export interface TranscriptionAlternative {
  text: string;
  confidence: number;
  words: WordTimestamp[];
}

/** Which model ran, and how long it took. */
export interface TranscriptionMetadata {
  modelId: string;
  processingTimeMs: number;
}

export interface STTOutput {
  text: string;
  confidence: number;
  /** Detected language, BCP-47. Empty = unknown. */
  language?: string | undefined;
  words: WordTimestamp[];
  alternatives: TranscriptionAlternative[];
  metadata?:
    | TranscriptionMetadata
    | undefined;
  /** Milliseconds since epoch. */
  timestampMs: number;
  durationMs: number;
  /** For long-running or streaming transcription. */
  segmentIndex: number;
  error?: SDKError | undefined;
}

/**
 * An in-progress hypothesis. `text` replaces the previous partial in full.
 * The finished result arrives on STTStreamEvent.final_output and never here;
 * correlation (request_id, seq) and failures live on the STTStreamEvent
 * envelope.
 */
export interface STTPartialResult {
  text: string;
  isFinal: boolean;
  language?: string | undefined;
}

export interface STTStreamEvent {
  seq: number;
  timestampUs: number;
  requestId: string;
  kind: STTStreamEventKind;
  partial?: STTPartialResult | undefined;
  finalOutput?: STTOutput | undefined;
  error?: SDKError | undefined;
}

export interface STTServiceState {
  isReady: boolean;
  currentModel?: string | undefined;
  supportsStreaming: boolean;
  supportedLanguageCodes: string[];
}

function createBaseSTTConfiguration(): STTConfiguration {
  return {
    modelId: "",
    language: undefined,
    sampleRate: 0,
    enableVad: false,
    audioFormat: 0,
    enablePunctuation: false,
    enableDiarization: false,
    enableWordTimestamps: false,
    preferredFramework: undefined,
  };
}

export const STTConfiguration: MessageFns<STTConfiguration> = {
  encode(message: STTConfiguration, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.modelId !== "") {
      writer.uint32(10).string(message.modelId);
    }
    if (message.language !== undefined) {
      writer.uint32(106).string(message.language);
    }
    if (message.sampleRate !== 0) {
      writer.uint32(24).int32(message.sampleRate);
    }
    if (message.enableVad !== false) {
      writer.uint32(32).bool(message.enableVad);
    }
    if (message.audioFormat !== 0) {
      writer.uint32(40).int32(message.audioFormat);
    }
    if (message.enablePunctuation !== false) {
      writer.uint32(48).bool(message.enablePunctuation);
    }
    if (message.enableDiarization !== false) {
      writer.uint32(56).bool(message.enableDiarization);
    }
    if (message.enableWordTimestamps !== false) {
      writer.uint32(80).bool(message.enableWordTimestamps);
    }
    if (message.preferredFramework !== undefined) {
      writer.uint32(88).int32(message.preferredFramework);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): STTConfiguration {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSTTConfiguration();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.language = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sampleRate = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.enableVad = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.audioFormat = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.enablePunctuation = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.enableDiarization = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.enableWordTimestamps = reader.bool();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.preferredFramework = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): STTConfiguration {
    return {
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : "",
      language: isSet(object.language) ? globalThis.String(object.language) : undefined,
      sampleRate: isSet(object.sampleRate)
        ? globalThis.Number(object.sampleRate)
        : isSet(object.sample_rate)
        ? globalThis.Number(object.sample_rate)
        : 0,
      enableVad: isSet(object.enableVad)
        ? globalThis.Boolean(object.enableVad)
        : isSet(object.enable_vad)
        ? globalThis.Boolean(object.enable_vad)
        : false,
      audioFormat: isSet(object.audioFormat)
        ? audioFormatFromJSON(object.audioFormat)
        : isSet(object.audio_format)
        ? audioFormatFromJSON(object.audio_format)
        : 0,
      enablePunctuation: isSet(object.enablePunctuation)
        ? globalThis.Boolean(object.enablePunctuation)
        : isSet(object.enable_punctuation)
        ? globalThis.Boolean(object.enable_punctuation)
        : false,
      enableDiarization: isSet(object.enableDiarization)
        ? globalThis.Boolean(object.enableDiarization)
        : isSet(object.enable_diarization)
        ? globalThis.Boolean(object.enable_diarization)
        : false,
      enableWordTimestamps: isSet(object.enableWordTimestamps)
        ? globalThis.Boolean(object.enableWordTimestamps)
        : isSet(object.enable_word_timestamps)
        ? globalThis.Boolean(object.enable_word_timestamps)
        : false,
      preferredFramework: isSet(object.preferredFramework)
        ? inferenceFrameworkFromJSON(object.preferredFramework)
        : isSet(object.preferred_framework)
        ? inferenceFrameworkFromJSON(object.preferred_framework)
        : undefined,
    };
  },

  toJSON(message: STTConfiguration): unknown {
    const obj: any = {};
    if (message.modelId !== "") {
      obj.modelId = message.modelId;
    }
    if (message.language !== undefined) {
      obj.language = message.language;
    }
    if (message.sampleRate !== 0) {
      obj.sampleRate = Math.round(message.sampleRate);
    }
    if (message.enableVad !== false) {
      obj.enableVad = message.enableVad;
    }
    if (message.audioFormat !== 0) {
      obj.audioFormat = audioFormatToJSON(message.audioFormat);
    }
    if (message.enablePunctuation !== false) {
      obj.enablePunctuation = message.enablePunctuation;
    }
    if (message.enableDiarization !== false) {
      obj.enableDiarization = message.enableDiarization;
    }
    if (message.enableWordTimestamps !== false) {
      obj.enableWordTimestamps = message.enableWordTimestamps;
    }
    if (message.preferredFramework !== undefined) {
      obj.preferredFramework = inferenceFrameworkToJSON(message.preferredFramework);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<STTConfiguration>, I>>(base?: I): STTConfiguration {
    return STTConfiguration.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<STTConfiguration>, I>>(object: I): STTConfiguration {
    const message = createBaseSTTConfiguration();
    message.modelId = object.modelId ?? "";
    message.language = object.language ?? undefined;
    message.sampleRate = object.sampleRate ?? 0;
    message.enableVad = object.enableVad ?? false;
    message.audioFormat = object.audioFormat ?? 0;
    message.enablePunctuation = object.enablePunctuation ?? false;
    message.enableDiarization = object.enableDiarization ?? false;
    message.enableWordTimestamps = object.enableWordTimestamps ?? false;
    message.preferredFramework = object.preferredFramework ?? undefined;
    return message;
  },
};

function createBaseSTTOptions(): STTOptions {
  return {
    language: undefined,
    enablePunctuation: false,
    diarize: false,
    speakersExpected: undefined,
    enableWordTimestamps: false,
    silenceDurationMs: 0,
  };
}

export const STTOptions: MessageFns<STTOptions> = {
  encode(message: STTOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.language !== undefined) {
      writer.uint32(138).string(message.language);
    }
    if (message.enablePunctuation !== false) {
      writer.uint32(16).bool(message.enablePunctuation);
    }
    if (message.diarize !== false) {
      writer.uint32(24).bool(message.diarize);
    }
    if (message.speakersExpected !== undefined) {
      writer.uint32(32).int32(message.speakersExpected);
    }
    if (message.enableWordTimestamps !== false) {
      writer.uint32(48).bool(message.enableWordTimestamps);
    }
    if (message.silenceDurationMs !== 0) {
      writer.uint32(112).int32(message.silenceDurationMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): STTOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSTTOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.language = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.enablePunctuation = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.diarize = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.speakersExpected = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.enableWordTimestamps = reader.bool();
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.silenceDurationMs = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): STTOptions {
    return {
      language: isSet(object.language) ? globalThis.String(object.language) : undefined,
      enablePunctuation: isSet(object.enablePunctuation)
        ? globalThis.Boolean(object.enablePunctuation)
        : isSet(object.enable_punctuation)
        ? globalThis.Boolean(object.enable_punctuation)
        : false,
      diarize: isSet(object.diarize) ? globalThis.Boolean(object.diarize) : false,
      speakersExpected: isSet(object.speakersExpected)
        ? globalThis.Number(object.speakersExpected)
        : isSet(object.speakers_expected)
        ? globalThis.Number(object.speakers_expected)
        : undefined,
      enableWordTimestamps: isSet(object.enableWordTimestamps)
        ? globalThis.Boolean(object.enableWordTimestamps)
        : isSet(object.enable_word_timestamps)
        ? globalThis.Boolean(object.enable_word_timestamps)
        : false,
      silenceDurationMs: isSet(object.silenceDurationMs)
        ? globalThis.Number(object.silenceDurationMs)
        : isSet(object.silence_duration_ms)
        ? globalThis.Number(object.silence_duration_ms)
        : 0,
    };
  },

  toJSON(message: STTOptions): unknown {
    const obj: any = {};
    if (message.language !== undefined) {
      obj.language = message.language;
    }
    if (message.enablePunctuation !== false) {
      obj.enablePunctuation = message.enablePunctuation;
    }
    if (message.diarize !== false) {
      obj.diarize = message.diarize;
    }
    if (message.speakersExpected !== undefined) {
      obj.speakersExpected = Math.round(message.speakersExpected);
    }
    if (message.enableWordTimestamps !== false) {
      obj.enableWordTimestamps = message.enableWordTimestamps;
    }
    if (message.silenceDurationMs !== 0) {
      obj.silenceDurationMs = Math.round(message.silenceDurationMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<STTOptions>, I>>(base?: I): STTOptions {
    return STTOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<STTOptions>, I>>(object: I): STTOptions {
    const message = createBaseSTTOptions();
    message.language = object.language ?? undefined;
    message.enablePunctuation = object.enablePunctuation ?? false;
    message.diarize = object.diarize ?? false;
    message.speakersExpected = object.speakersExpected ?? undefined;
    message.enableWordTimestamps = object.enableWordTimestamps ?? false;
    message.silenceDurationMs = object.silenceDurationMs ?? 0;
    return message;
  },
};

function createBaseSTTAudioSource(): STTAudioSource {
  return {
    audioData: undefined,
    fileUri: undefined,
    encoding: 0,
    audioFormat: 0,
    sampleRate: 0,
    channels: 0,
    durationMs: 0,
  };
}

export const STTAudioSource: MessageFns<STTAudioSource> = {
  encode(message: STTAudioSource, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.audioData !== undefined) {
      writer.uint32(10).bytes(message.audioData);
    }
    if (message.fileUri !== undefined) {
      writer.uint32(18).string(message.fileUri);
    }
    if (message.encoding !== 0) {
      writer.uint32(32).int32(message.encoding);
    }
    if (message.audioFormat !== 0) {
      writer.uint32(40).int32(message.audioFormat);
    }
    if (message.sampleRate !== 0) {
      writer.uint32(48).int32(message.sampleRate);
    }
    if (message.channels !== 0) {
      writer.uint32(56).int32(message.channels);
    }
    if (message.durationMs !== 0) {
      writer.uint32(72).int64(message.durationMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): STTAudioSource {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSTTAudioSource();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.audioData = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fileUri = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.encoding = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.audioFormat = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.sampleRate = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.channels = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.durationMs = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): STTAudioSource {
    return {
      audioData: isSet(object.audioData)
        ? bytesFromBase64(object.audioData)
        : isSet(object.audio_data)
        ? bytesFromBase64(object.audio_data)
        : undefined,
      fileUri: isSet(object.fileUri)
        ? globalThis.String(object.fileUri)
        : isSet(object.file_uri)
        ? globalThis.String(object.file_uri)
        : undefined,
      encoding: isSet(object.encoding) ? audioEncodingFromJSON(object.encoding) : 0,
      audioFormat: isSet(object.audioFormat)
        ? audioFormatFromJSON(object.audioFormat)
        : isSet(object.audio_format)
        ? audioFormatFromJSON(object.audio_format)
        : 0,
      sampleRate: isSet(object.sampleRate)
        ? globalThis.Number(object.sampleRate)
        : isSet(object.sample_rate)
        ? globalThis.Number(object.sample_rate)
        : 0,
      channels: isSet(object.channels) ? globalThis.Number(object.channels) : 0,
      durationMs: isSet(object.durationMs)
        ? globalThis.Number(object.durationMs)
        : isSet(object.duration_ms)
        ? globalThis.Number(object.duration_ms)
        : 0,
    };
  },

  toJSON(message: STTAudioSource): unknown {
    const obj: any = {};
    if (message.audioData !== undefined) {
      obj.audioData = base64FromBytes(message.audioData);
    }
    if (message.fileUri !== undefined) {
      obj.fileUri = message.fileUri;
    }
    if (message.encoding !== 0) {
      obj.encoding = audioEncodingToJSON(message.encoding);
    }
    if (message.audioFormat !== 0) {
      obj.audioFormat = audioFormatToJSON(message.audioFormat);
    }
    if (message.sampleRate !== 0) {
      obj.sampleRate = Math.round(message.sampleRate);
    }
    if (message.channels !== 0) {
      obj.channels = Math.round(message.channels);
    }
    if (message.durationMs !== 0) {
      obj.durationMs = Math.round(message.durationMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<STTAudioSource>, I>>(base?: I): STTAudioSource {
    return STTAudioSource.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<STTAudioSource>, I>>(object: I): STTAudioSource {
    const message = createBaseSTTAudioSource();
    message.audioData = object.audioData ?? undefined;
    message.fileUri = object.fileUri ?? undefined;
    message.encoding = object.encoding ?? 0;
    message.audioFormat = object.audioFormat ?? 0;
    message.sampleRate = object.sampleRate ?? 0;
    message.channels = object.channels ?? 0;
    message.durationMs = object.durationMs ?? 0;
    return message;
  },
};

function createBaseSTTTranscriptionRequest(): STTTranscriptionRequest {
  return { requestId: "", audio: undefined, options: undefined, metadata: {} };
}

export const STTTranscriptionRequest: MessageFns<STTTranscriptionRequest> = {
  encode(message: STTTranscriptionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    if (message.audio !== undefined) {
      STTAudioSource.encode(message.audio, writer.uint32(18).fork()).join();
    }
    if (message.options !== undefined) {
      STTOptions.encode(message.options, writer.uint32(26).fork()).join();
    }
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      STTTranscriptionRequest_MetadataEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): STTTranscriptionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSTTTranscriptionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.audio = STTAudioSource.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.options = STTOptions.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = STTTranscriptionRequest_MetadataEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.metadata[entry4.key] = entry4.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): STTTranscriptionRequest {
    return {
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
      audio: isSet(object.audio) ? STTAudioSource.fromJSON(object.audio) : undefined,
      options: isSet(object.options) ? STTOptions.fromJSON(object.options) : undefined,
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: STTTranscriptionRequest): unknown {
    const obj: any = {};
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.audio !== undefined) {
      obj.audio = STTAudioSource.toJSON(message.audio);
    }
    if (message.options !== undefined) {
      obj.options = STTOptions.toJSON(message.options);
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<STTTranscriptionRequest>, I>>(base?: I): STTTranscriptionRequest {
    return STTTranscriptionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<STTTranscriptionRequest>, I>>(object: I): STTTranscriptionRequest {
    const message = createBaseSTTTranscriptionRequest();
    message.requestId = object.requestId ?? "";
    message.audio = (object.audio !== undefined && object.audio !== null)
      ? STTAudioSource.fromPartial(object.audio)
      : undefined;
    message.options = (object.options !== undefined && object.options !== null)
      ? STTOptions.fromPartial(object.options)
      : undefined;
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseSTTTranscriptionRequest_MetadataEntry(): STTTranscriptionRequest_MetadataEntry {
  return { key: "", value: "" };
}

export const STTTranscriptionRequest_MetadataEntry: MessageFns<STTTranscriptionRequest_MetadataEntry> = {
  encode(message: STTTranscriptionRequest_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): STTTranscriptionRequest_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSTTTranscriptionRequest_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): STTTranscriptionRequest_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: STTTranscriptionRequest_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<STTTranscriptionRequest_MetadataEntry>, I>>(
    base?: I,
  ): STTTranscriptionRequest_MetadataEntry {
    return STTTranscriptionRequest_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<STTTranscriptionRequest_MetadataEntry>, I>>(
    object: I,
  ): STTTranscriptionRequest_MetadataEntry {
    const message = createBaseSTTTranscriptionRequest_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseWordTimestamp(): WordTimestamp {
  return { word: "", startMs: 0, endMs: 0, confidence: 0, speakerId: undefined };
}

export const WordTimestamp: MessageFns<WordTimestamp> = {
  encode(message: WordTimestamp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.word !== "") {
      writer.uint32(10).string(message.word);
    }
    if (message.startMs !== 0) {
      writer.uint32(16).int64(message.startMs);
    }
    if (message.endMs !== 0) {
      writer.uint32(24).int64(message.endMs);
    }
    if (message.confidence !== 0) {
      writer.uint32(37).float(message.confidence);
    }
    if (message.speakerId !== undefined) {
      writer.uint32(42).string(message.speakerId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WordTimestamp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWordTimestamp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.word = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.startMs = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.endMs = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 37) {
            break;
          }

          message.confidence = reader.float();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.speakerId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WordTimestamp {
    return {
      word: isSet(object.word) ? globalThis.String(object.word) : "",
      startMs: isSet(object.startMs)
        ? globalThis.Number(object.startMs)
        : isSet(object.start_ms)
        ? globalThis.Number(object.start_ms)
        : 0,
      endMs: isSet(object.endMs)
        ? globalThis.Number(object.endMs)
        : isSet(object.end_ms)
        ? globalThis.Number(object.end_ms)
        : 0,
      confidence: isSet(object.confidence) ? globalThis.Number(object.confidence) : 0,
      speakerId: isSet(object.speakerId)
        ? globalThis.String(object.speakerId)
        : isSet(object.speaker_id)
        ? globalThis.String(object.speaker_id)
        : undefined,
    };
  },

  toJSON(message: WordTimestamp): unknown {
    const obj: any = {};
    if (message.word !== "") {
      obj.word = message.word;
    }
    if (message.startMs !== 0) {
      obj.startMs = Math.round(message.startMs);
    }
    if (message.endMs !== 0) {
      obj.endMs = Math.round(message.endMs);
    }
    if (message.confidence !== 0) {
      obj.confidence = message.confidence;
    }
    if (message.speakerId !== undefined) {
      obj.speakerId = message.speakerId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WordTimestamp>, I>>(base?: I): WordTimestamp {
    return WordTimestamp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WordTimestamp>, I>>(object: I): WordTimestamp {
    const message = createBaseWordTimestamp();
    message.word = object.word ?? "";
    message.startMs = object.startMs ?? 0;
    message.endMs = object.endMs ?? 0;
    message.confidence = object.confidence ?? 0;
    message.speakerId = object.speakerId ?? undefined;
    return message;
  },
};

function createBaseTranscriptionAlternative(): TranscriptionAlternative {
  return { text: "", confidence: 0, words: [] };
}

export const TranscriptionAlternative: MessageFns<TranscriptionAlternative> = {
  encode(message: TranscriptionAlternative, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.confidence !== 0) {
      writer.uint32(21).float(message.confidence);
    }
    for (const v of message.words) {
      WordTimestamp.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TranscriptionAlternative {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTranscriptionAlternative();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 21) {
            break;
          }

          message.confidence = reader.float();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.words.push(WordTimestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TranscriptionAlternative {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      confidence: isSet(object.confidence) ? globalThis.Number(object.confidence) : 0,
      words: globalThis.Array.isArray(object?.words) ? object.words.map((e: any) => WordTimestamp.fromJSON(e)) : [],
    };
  },

  toJSON(message: TranscriptionAlternative): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.confidence !== 0) {
      obj.confidence = message.confidence;
    }
    if (message.words?.length) {
      obj.words = message.words.map((e) => WordTimestamp.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TranscriptionAlternative>, I>>(base?: I): TranscriptionAlternative {
    return TranscriptionAlternative.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TranscriptionAlternative>, I>>(object: I): TranscriptionAlternative {
    const message = createBaseTranscriptionAlternative();
    message.text = object.text ?? "";
    message.confidence = object.confidence ?? 0;
    message.words = object.words?.map((e) => WordTimestamp.fromPartial(e)) || [];
    return message;
  },
};

function createBaseTranscriptionMetadata(): TranscriptionMetadata {
  return { modelId: "", processingTimeMs: 0 };
}

export const TranscriptionMetadata: MessageFns<TranscriptionMetadata> = {
  encode(message: TranscriptionMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.modelId !== "") {
      writer.uint32(10).string(message.modelId);
    }
    if (message.processingTimeMs !== 0) {
      writer.uint32(16).int64(message.processingTimeMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TranscriptionMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTranscriptionMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.modelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.processingTimeMs = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TranscriptionMetadata {
    return {
      modelId: isSet(object.modelId)
        ? globalThis.String(object.modelId)
        : isSet(object.model_id)
        ? globalThis.String(object.model_id)
        : "",
      processingTimeMs: isSet(object.processingTimeMs)
        ? globalThis.Number(object.processingTimeMs)
        : isSet(object.processing_time_ms)
        ? globalThis.Number(object.processing_time_ms)
        : 0,
    };
  },

  toJSON(message: TranscriptionMetadata): unknown {
    const obj: any = {};
    if (message.modelId !== "") {
      obj.modelId = message.modelId;
    }
    if (message.processingTimeMs !== 0) {
      obj.processingTimeMs = Math.round(message.processingTimeMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TranscriptionMetadata>, I>>(base?: I): TranscriptionMetadata {
    return TranscriptionMetadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TranscriptionMetadata>, I>>(object: I): TranscriptionMetadata {
    const message = createBaseTranscriptionMetadata();
    message.modelId = object.modelId ?? "";
    message.processingTimeMs = object.processingTimeMs ?? 0;
    return message;
  },
};

function createBaseSTTOutput(): STTOutput {
  return {
    text: "",
    confidence: 0,
    language: undefined,
    words: [],
    alternatives: [],
    metadata: undefined,
    timestampMs: 0,
    durationMs: 0,
    segmentIndex: 0,
    error: undefined,
  };
}

export const STTOutput: MessageFns<STTOutput> = {
  encode(message: STTOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.confidence !== 0) {
      writer.uint32(29).float(message.confidence);
    }
    if (message.language !== undefined) {
      writer.uint32(114).string(message.language);
    }
    for (const v of message.words) {
      WordTimestamp.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.alternatives) {
      TranscriptionAlternative.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.metadata !== undefined) {
      TranscriptionMetadata.encode(message.metadata, writer.uint32(50).fork()).join();
    }
    if (message.timestampMs !== 0) {
      writer.uint32(64).int64(message.timestampMs);
    }
    if (message.durationMs !== 0) {
      writer.uint32(72).int64(message.durationMs);
    }
    if (message.segmentIndex !== 0) {
      writer.uint32(104).int32(message.segmentIndex);
    }
    if (message.error !== undefined) {
      SDKError.encode(message.error, writer.uint32(122).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): STTOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSTTOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.confidence = reader.float();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.language = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.words.push(WordTimestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.alternatives.push(TranscriptionAlternative.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.metadata = TranscriptionMetadata.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.timestampMs = longToNumber(reader.int64());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.durationMs = longToNumber(reader.int64());
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.segmentIndex = reader.int32();
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.error = SDKError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): STTOutput {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      confidence: isSet(object.confidence) ? globalThis.Number(object.confidence) : 0,
      language: isSet(object.language) ? globalThis.String(object.language) : undefined,
      words: globalThis.Array.isArray(object?.words) ? object.words.map((e: any) => WordTimestamp.fromJSON(e)) : [],
      alternatives: globalThis.Array.isArray(object?.alternatives)
        ? object.alternatives.map((e: any) => TranscriptionAlternative.fromJSON(e))
        : [],
      metadata: isSet(object.metadata) ? TranscriptionMetadata.fromJSON(object.metadata) : undefined,
      timestampMs: isSet(object.timestampMs)
        ? globalThis.Number(object.timestampMs)
        : isSet(object.timestamp_ms)
        ? globalThis.Number(object.timestamp_ms)
        : 0,
      durationMs: isSet(object.durationMs)
        ? globalThis.Number(object.durationMs)
        : isSet(object.duration_ms)
        ? globalThis.Number(object.duration_ms)
        : 0,
      segmentIndex: isSet(object.segmentIndex)
        ? globalThis.Number(object.segmentIndex)
        : isSet(object.segment_index)
        ? globalThis.Number(object.segment_index)
        : 0,
      error: isSet(object.error) ? SDKError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: STTOutput): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.confidence !== 0) {
      obj.confidence = message.confidence;
    }
    if (message.language !== undefined) {
      obj.language = message.language;
    }
    if (message.words?.length) {
      obj.words = message.words.map((e) => WordTimestamp.toJSON(e));
    }
    if (message.alternatives?.length) {
      obj.alternatives = message.alternatives.map((e) => TranscriptionAlternative.toJSON(e));
    }
    if (message.metadata !== undefined) {
      obj.metadata = TranscriptionMetadata.toJSON(message.metadata);
    }
    if (message.timestampMs !== 0) {
      obj.timestampMs = Math.round(message.timestampMs);
    }
    if (message.durationMs !== 0) {
      obj.durationMs = Math.round(message.durationMs);
    }
    if (message.segmentIndex !== 0) {
      obj.segmentIndex = Math.round(message.segmentIndex);
    }
    if (message.error !== undefined) {
      obj.error = SDKError.toJSON(message.error);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<STTOutput>, I>>(base?: I): STTOutput {
    return STTOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<STTOutput>, I>>(object: I): STTOutput {
    const message = createBaseSTTOutput();
    message.text = object.text ?? "";
    message.confidence = object.confidence ?? 0;
    message.language = object.language ?? undefined;
    message.words = object.words?.map((e) => WordTimestamp.fromPartial(e)) || [];
    message.alternatives = object.alternatives?.map((e) => TranscriptionAlternative.fromPartial(e)) || [];
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? TranscriptionMetadata.fromPartial(object.metadata)
      : undefined;
    message.timestampMs = object.timestampMs ?? 0;
    message.durationMs = object.durationMs ?? 0;
    message.segmentIndex = object.segmentIndex ?? 0;
    message.error = (object.error !== undefined && object.error !== null)
      ? SDKError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

function createBaseSTTPartialResult(): STTPartialResult {
  return { text: "", isFinal: false, language: undefined };
}

export const STTPartialResult: MessageFns<STTPartialResult> = {
  encode(message: STTPartialResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.isFinal !== false) {
      writer.uint32(16).bool(message.isFinal);
    }
    if (message.language !== undefined) {
      writer.uint32(114).string(message.language);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): STTPartialResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSTTPartialResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isFinal = reader.bool();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.language = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): STTPartialResult {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      isFinal: isSet(object.isFinal)
        ? globalThis.Boolean(object.isFinal)
        : isSet(object.is_final)
        ? globalThis.Boolean(object.is_final)
        : false,
      language: isSet(object.language) ? globalThis.String(object.language) : undefined,
    };
  },

  toJSON(message: STTPartialResult): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.isFinal !== false) {
      obj.isFinal = message.isFinal;
    }
    if (message.language !== undefined) {
      obj.language = message.language;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<STTPartialResult>, I>>(base?: I): STTPartialResult {
    return STTPartialResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<STTPartialResult>, I>>(object: I): STTPartialResult {
    const message = createBaseSTTPartialResult();
    message.text = object.text ?? "";
    message.isFinal = object.isFinal ?? false;
    message.language = object.language ?? undefined;
    return message;
  },
};

function createBaseSTTStreamEvent(): STTStreamEvent {
  return {
    seq: 0,
    timestampUs: 0,
    requestId: "",
    kind: 0,
    partial: undefined,
    finalOutput: undefined,
    error: undefined,
  };
}

export const STTStreamEvent: MessageFns<STTStreamEvent> = {
  encode(message: STTStreamEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.seq !== 0) {
      writer.uint32(8).uint64(message.seq);
    }
    if (message.timestampUs !== 0) {
      writer.uint32(16).int64(message.timestampUs);
    }
    if (message.requestId !== "") {
      writer.uint32(26).string(message.requestId);
    }
    if (message.kind !== 0) {
      writer.uint32(32).int32(message.kind);
    }
    if (message.partial !== undefined) {
      STTPartialResult.encode(message.partial, writer.uint32(42).fork()).join();
    }
    if (message.finalOutput !== undefined) {
      STTOutput.encode(message.finalOutput, writer.uint32(50).fork()).join();
    }
    if (message.error !== undefined) {
      SDKError.encode(message.error, writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): STTStreamEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSTTStreamEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.seq = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.timestampUs = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.partial = STTPartialResult.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.finalOutput = STTOutput.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.error = SDKError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): STTStreamEvent {
    return {
      seq: isSet(object.seq) ? globalThis.Number(object.seq) : 0,
      timestampUs: isSet(object.timestampUs)
        ? globalThis.Number(object.timestampUs)
        : isSet(object.timestamp_us)
        ? globalThis.Number(object.timestamp_us)
        : 0,
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
      kind: isSet(object.kind) ? sTTStreamEventKindFromJSON(object.kind) : 0,
      partial: isSet(object.partial) ? STTPartialResult.fromJSON(object.partial) : undefined,
      finalOutput: isSet(object.finalOutput)
        ? STTOutput.fromJSON(object.finalOutput)
        : isSet(object.final_output)
        ? STTOutput.fromJSON(object.final_output)
        : undefined,
      error: isSet(object.error) ? SDKError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: STTStreamEvent): unknown {
    const obj: any = {};
    if (message.seq !== 0) {
      obj.seq = Math.round(message.seq);
    }
    if (message.timestampUs !== 0) {
      obj.timestampUs = Math.round(message.timestampUs);
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    if (message.kind !== 0) {
      obj.kind = sTTStreamEventKindToJSON(message.kind);
    }
    if (message.partial !== undefined) {
      obj.partial = STTPartialResult.toJSON(message.partial);
    }
    if (message.finalOutput !== undefined) {
      obj.finalOutput = STTOutput.toJSON(message.finalOutput);
    }
    if (message.error !== undefined) {
      obj.error = SDKError.toJSON(message.error);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<STTStreamEvent>, I>>(base?: I): STTStreamEvent {
    return STTStreamEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<STTStreamEvent>, I>>(object: I): STTStreamEvent {
    const message = createBaseSTTStreamEvent();
    message.seq = object.seq ?? 0;
    message.timestampUs = object.timestampUs ?? 0;
    message.requestId = object.requestId ?? "";
    message.kind = object.kind ?? 0;
    message.partial = (object.partial !== undefined && object.partial !== null)
      ? STTPartialResult.fromPartial(object.partial)
      : undefined;
    message.finalOutput = (object.finalOutput !== undefined && object.finalOutput !== null)
      ? STTOutput.fromPartial(object.finalOutput)
      : undefined;
    message.error = (object.error !== undefined && object.error !== null)
      ? SDKError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

function createBaseSTTServiceState(): STTServiceState {
  return { isReady: false, currentModel: undefined, supportsStreaming: false, supportedLanguageCodes: [] };
}

export const STTServiceState: MessageFns<STTServiceState> = {
  encode(message: STTServiceState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.isReady !== false) {
      writer.uint32(8).bool(message.isReady);
    }
    if (message.currentModel !== undefined) {
      writer.uint32(18).string(message.currentModel);
    }
    if (message.supportsStreaming !== false) {
      writer.uint32(24).bool(message.supportsStreaming);
    }
    for (const v of message.supportedLanguageCodes) {
      writer.uint32(34).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): STTServiceState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSTTServiceState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.isReady = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.currentModel = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.supportsStreaming = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.supportedLanguageCodes.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): STTServiceState {
    return {
      isReady: isSet(object.isReady)
        ? globalThis.Boolean(object.isReady)
        : isSet(object.is_ready)
        ? globalThis.Boolean(object.is_ready)
        : false,
      currentModel: isSet(object.currentModel)
        ? globalThis.String(object.currentModel)
        : isSet(object.current_model)
        ? globalThis.String(object.current_model)
        : undefined,
      supportsStreaming: isSet(object.supportsStreaming)
        ? globalThis.Boolean(object.supportsStreaming)
        : isSet(object.supports_streaming)
        ? globalThis.Boolean(object.supports_streaming)
        : false,
      supportedLanguageCodes: globalThis.Array.isArray(object?.supportedLanguageCodes)
        ? object.supportedLanguageCodes.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.supported_language_codes)
        ? object.supported_language_codes.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: STTServiceState): unknown {
    const obj: any = {};
    if (message.isReady !== false) {
      obj.isReady = message.isReady;
    }
    if (message.currentModel !== undefined) {
      obj.currentModel = message.currentModel;
    }
    if (message.supportsStreaming !== false) {
      obj.supportsStreaming = message.supportsStreaming;
    }
    if (message.supportedLanguageCodes?.length) {
      obj.supportedLanguageCodes = message.supportedLanguageCodes;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<STTServiceState>, I>>(base?: I): STTServiceState {
    return STTServiceState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<STTServiceState>, I>>(object: I): STTServiceState {
    const message = createBaseSTTServiceState();
    message.isReady = object.isReady ?? false;
    message.currentModel = object.currentModel ?? undefined;
    message.supportsStreaming = object.supportsStreaming ?? false;
    message.supportedLanguageCodes = object.supportedLanguageCodes?.map((e) => e) || [];
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  const bin = globalThis.atob(b64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; ++i) {
    arr[i] = bin.charCodeAt(i);
  }
  return arr;
}

function base64FromBytes(arr: Uint8Array): string {
  const bin: string[] = [];
  arr.forEach((byte) => {
    bin.push(globalThis.String.fromCharCode(byte));
  });
  return globalThis.btoa(bin.join(""));
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
