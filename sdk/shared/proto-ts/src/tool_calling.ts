// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v7.35.1
// source: tool_calling.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { TokenUsage } from "./token_usage";

export const protobufPackage = "runanywhere.v1";

/**
 * ---------------------------------------------------------------------------
 * Tool-call wire formats various LLM families emit. This enum is the single
 * portable format selector across commons and every generated SDK binding.
 * ---------------------------------------------------------------------------
 */
export enum ToolCallFormatName {
  TOOL_CALL_FORMAT_NAME_UNSPECIFIED = 0,
  TOOL_CALL_FORMAT_NAME_JSON = 1,
  TOOL_CALL_FORMAT_NAME_LFM2 = 7,
  UNRECOGNIZED = -1,
}

export function toolCallFormatNameFromJSON(object: any): ToolCallFormatName {
  switch (object) {
    case 0:
    case "TOOL_CALL_FORMAT_NAME_UNSPECIFIED":
      return ToolCallFormatName.TOOL_CALL_FORMAT_NAME_UNSPECIFIED;
    case 1:
    case "TOOL_CALL_FORMAT_NAME_JSON":
      return ToolCallFormatName.TOOL_CALL_FORMAT_NAME_JSON;
    case 7:
    case "TOOL_CALL_FORMAT_NAME_LFM2":
      return ToolCallFormatName.TOOL_CALL_FORMAT_NAME_LFM2;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ToolCallFormatName.UNRECOGNIZED;
  }
}

export function toolCallFormatNameToJSON(object: ToolCallFormatName): string {
  switch (object) {
    case ToolCallFormatName.TOOL_CALL_FORMAT_NAME_UNSPECIFIED:
      return "TOOL_CALL_FORMAT_NAME_UNSPECIFIED";
    case ToolCallFormatName.TOOL_CALL_FORMAT_NAME_JSON:
      return "TOOL_CALL_FORMAT_NAME_JSON";
    case ToolCallFormatName.TOOL_CALL_FORMAT_NAME_LFM2:
      return "TOOL_CALL_FORMAT_NAME_LFM2";
    case ToolCallFormatName.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ToolChoiceMode {
  TOOL_CHOICE_MODE_UNSPECIFIED = 0,
  TOOL_CHOICE_MODE_AUTO = 1,
  TOOL_CHOICE_MODE_NONE = 2,
  TOOL_CHOICE_MODE_REQUIRED = 3,
  TOOL_CHOICE_MODE_SPECIFIC = 4,
  UNRECOGNIZED = -1,
}

export function toolChoiceModeFromJSON(object: any): ToolChoiceMode {
  switch (object) {
    case 0:
    case "TOOL_CHOICE_MODE_UNSPECIFIED":
      return ToolChoiceMode.TOOL_CHOICE_MODE_UNSPECIFIED;
    case 1:
    case "TOOL_CHOICE_MODE_AUTO":
      return ToolChoiceMode.TOOL_CHOICE_MODE_AUTO;
    case 2:
    case "TOOL_CHOICE_MODE_NONE":
      return ToolChoiceMode.TOOL_CHOICE_MODE_NONE;
    case 3:
    case "TOOL_CHOICE_MODE_REQUIRED":
      return ToolChoiceMode.TOOL_CHOICE_MODE_REQUIRED;
    case 4:
    case "TOOL_CHOICE_MODE_SPECIFIC":
      return ToolChoiceMode.TOOL_CHOICE_MODE_SPECIFIC;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ToolChoiceMode.UNRECOGNIZED;
  }
}

export function toolChoiceModeToJSON(object: ToolChoiceMode): string {
  switch (object) {
    case ToolChoiceMode.TOOL_CHOICE_MODE_UNSPECIFIED:
      return "TOOL_CHOICE_MODE_UNSPECIFIED";
    case ToolChoiceMode.TOOL_CHOICE_MODE_AUTO:
      return "TOOL_CHOICE_MODE_AUTO";
    case ToolChoiceMode.TOOL_CHOICE_MODE_NONE:
      return "TOOL_CHOICE_MODE_NONE";
    case ToolChoiceMode.TOOL_CHOICE_MODE_REQUIRED:
      return "TOOL_CHOICE_MODE_REQUIRED";
    case ToolChoiceMode.TOOL_CHOICE_MODE_SPECIFIC:
      return "TOOL_CHOICE_MODE_SPECIFIC";
    case ToolChoiceMode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Conversational role of one prior turn in `history`. */
export enum ToolCallingRole {
  TOOL_CALLING_ROLE_UNSPECIFIED = 0,
  TOOL_CALLING_ROLE_USER = 1,
  TOOL_CALLING_ROLE_ASSISTANT = 2,
  TOOL_CALLING_ROLE_SYSTEM = 3,
  UNRECOGNIZED = -1,
}

export function toolCallingRoleFromJSON(object: any): ToolCallingRole {
  switch (object) {
    case 0:
    case "TOOL_CALLING_ROLE_UNSPECIFIED":
      return ToolCallingRole.TOOL_CALLING_ROLE_UNSPECIFIED;
    case 1:
    case "TOOL_CALLING_ROLE_USER":
      return ToolCallingRole.TOOL_CALLING_ROLE_USER;
    case 2:
    case "TOOL_CALLING_ROLE_ASSISTANT":
      return ToolCallingRole.TOOL_CALLING_ROLE_ASSISTANT;
    case 3:
    case "TOOL_CALLING_ROLE_SYSTEM":
      return ToolCallingRole.TOOL_CALLING_ROLE_SYSTEM;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ToolCallingRole.UNRECOGNIZED;
  }
}

export function toolCallingRoleToJSON(object: ToolCallingRole): string {
  switch (object) {
    case ToolCallingRole.TOOL_CALLING_ROLE_UNSPECIFIED:
      return "TOOL_CALLING_ROLE_UNSPECIFIED";
    case ToolCallingRole.TOOL_CALLING_ROLE_USER:
      return "TOOL_CALLING_ROLE_USER";
    case ToolCallingRole.TOOL_CALLING_ROLE_ASSISTANT:
      return "TOOL_CALLING_ROLE_ASSISTANT";
    case ToolCallingRole.TOOL_CALLING_ROLE_SYSTEM:
      return "TOOL_CALLING_ROLE_SYSTEM";
    case ToolCallingRole.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * ---------------------------------------------------------------------------
 * JSON-typed scalar / composite carrier for tool arguments and results.
 * Mirrors Swift's ToolValue enum, Kotlin's sealed class, and the
 * TypeScript discriminated union. Used as the canonical wire shape when
 * consumers want strongly-typed arguments rather than raw JSON.
 * ---------------------------------------------------------------------------
 */
export interface ToolValue {
  stringValue?: string | undefined;
  numberValue?: number | undefined;
  boolValue?: boolean | undefined;
  arrayValue?: ToolValueArray | undefined;
  objectValue?:
    | ToolValueObject
    | undefined;
  /** true means JSON null */
  nullValue?: boolean | undefined;
}

export interface ToolValueArray {
  values: ToolValue[];
}

export interface ToolValueObject {
  fields: { [key: string]: ToolValue };
}

export interface ToolValueObject_FieldsEntry {
  key: string;
  value?: ToolValue | undefined;
}

/**
 * ---------------------------------------------------------------------------
 * String wrapper used by the rac_tool_value_to_json_proto /
 * rac_tool_value_from_json_proto ABIs. Carries either the JSON text rendered
 * from a ToolValue, or the JSON text that should be parsed back into a
 * ToolValue. Defined here (rather than reusing a stand-alone wrapper) so the
 * tool-calling round-trip stays self-contained in this proto.
 * ---------------------------------------------------------------------------
 */
export interface ToolValueJSON {
  json: string;
}

/**
 * ---------------------------------------------------------------------------
 * Definition of a tool that the LLM can call.
 * ---------------------------------------------------------------------------
 */
export interface ToolDefinition {
  name: string;
  description: string;
  /**
   * OpenAI-compatible parameters schema: ONE JSON Schema object describing
   * this tool's arguments — the same shape solutions.proto's ToolSpec
   * already carries. One schema per tool is what OpenAI (`parameters`),
   * Anthropic (`input_schema`) and MCP (`inputSchema`) each publish.
   * "" or "{}" advertises a zero-argument tool.
   */
  parameters: string;
  /** Optional category for grouping tools in catalogs / UIs. */
  category?: string | undefined;
}

/**
 * ---------------------------------------------------------------------------
 * A tool call requested by the LLM. `arguments_json` is a JSON object
 * matching the parameter shape declared in the corresponding ToolDefinition.
 * ---------------------------------------------------------------------------
 */
export interface ToolCall {
  /**
   * Correlation id, echoed back on ToolResult.tool_call_id. Caller-supplied
   * or generated; carried through parse AND validate unchanged. Never
   * derived from created_at_ms. Empty = unset.
   */
  id: string;
  /** Tool name (matches ToolDefinition.name). */
  name: string;
  /**
   * JSON-encoded arguments. Empty object "{}" if no args.
   *
   * The C++ tokenizer / tool-prompt formatter
   * (sdk/runanywhere-commons/src/features/llm/tool_calling.cpp) reads
   * `arguments_json` directly when building LLM prompts. It is the
   * canonical wire shape for the prompt-formatting path.
   */
  argumentsJson: string;
  /**
   * Wall-clock parse time, ms since epoch (second resolution today).
   * Diagnostic ONLY — never an identity, never used to correlate a call
   * with its result.
   */
  createdAtMs: number;
  /**
   * The exact model text this call was extracted FROM, including the tool
   * envelope. Diagnostic. Not the envelope-stripped text — that is
   * ToolParseResult.remaining_text.
   */
  rawText?: string | undefined;
}

/**
 * ---------------------------------------------------------------------------
 * Result of executing a tool. `result_json` is a JSON-encoded payload;
 * `error` is non-empty when the execution failed.
 * ---------------------------------------------------------------------------
 */
export interface ToolResult {
  toolCallId: string;
  name: string;
  /**
   * JSON-encoded tool execution result.
   *
   * The C++ tool-prompt formatter
   * (`sdk/runanywhere-commons/src/features/llm/tool_calling.cpp:1870-1885`)
   * reads `result_json` directly when building follow-up LLM prompts after
   * tool execution. It is the canonical wire shape.
   */
  resultJson: string;
  error?:
    | string
    | undefined;
  /**
   * True when the tool failed, so commons tells the model the call errored
   * instead of feeding result_json back as data and the model can
   * self-correct. The proto3 zero value (false) is the correct default: a
   * ToolResult nobody touched reads as a good result, not a failed one.
   * Industry: Anthropic `is_error`, MCP `isError`.
   */
  isError: boolean;
  startedAtMs: number;
  completedAtMs: number;
}

/**
 * ---------------------------------------------------------------------------
 * Options for tool-enabled generation.
 * ---------------------------------------------------------------------------
 */
export interface ToolCallingOptions {
  /**
   * Available tools for this generation. If empty, the SDK falls back to
   * its registered tools (per-SDK convention).
   */
  tools: ToolDefinition[];
  /**
   * Unset = true: the SDK runs your registered executor and closes the
   * loop. Explicit false returns the parsed ToolCall without invoking it.
   * Presence-tracked so "unset" is never confused with "explicitly false".
   */
  autoExecute?:
    | boolean
    | undefined;
  /**
   * System prompt for tool-enabled generation. This is the ONLY channel
   * ToolPromptFormatRequest (a standalone verb with no enclosing
   * LLMGenerationOptions) has for a system prompt; when ToolCallingOptions
   * is embedded in LLMGenerationOptions, the child value wins when present
   * and options.system_prompt is the fallback.
   */
  systemPrompt?:
    | string
    | undefined;
  /**
   * If true, replaces the system prompt entirely (no auto-injected
   * tool instructions).
   */
  replaceSystemPrompt: boolean;
  /**
   * If true, keeps tool definitions available across multiple sequential
   * tool calls in one generation.
   */
  keepToolsAvailable: boolean;
  /** Typed tool-call format. Unset lets commons select the model default. */
  format?:
    | ToolCallFormatName
    | undefined;
  /**
   * When true, one model turn may emit multiple tool-call envelopes;
   * commons parses and executes all of them before building a single
   * follow-up prompt. Default false preserves the historical
   * one-call-per-turn behavior.
   *
   * Wire history: field 15 originally carried this same bool flag, was
   * briefly reserved during a cleanup pass, then restored with identical
   * type and meaning. Do not reuse 15 for any other type. Schema-skew
   * fixtures under idl/codegen/tests cover old-writer/new-reader for this
   * field.
   */
  parallelToolCalls: boolean;
  /**
   * Maximum tool calls in one conversation turn. Unset/0 = the annotated
   * default applies.
   */
  maxToolCalls?: number | undefined;
  toolChoice: ToolChoiceMode;
  forcedToolName?: string | undefined;
  requireJsonArguments: boolean;
  /**
   * When true, suppress the model's thinking/reasoning phase during
   * tool-enabled generation (commons prepends the model no-think directive
   * at the prompt level — same contract as
   * LLMGenerationOptions.disable_thinking). Default false.
   */
  disableThinking?:
    | boolean
    | undefined;
  /**
   * Moved here from ToolCallingSessionCreateRequest so one message carries
   * the whole tool-generation policy.
   */
  topP?:
    | number
    | undefined;
  /**
   * Unset = true: unknown tool calls short-circuit before host execution.
   * Callers that delegate validation/authorization to their executor or
   * use dynamic tool registries must explicitly set validate_calls=false.
   */
  validateCalls?: boolean | undefined;
}

/**
 * ---------------------------------------------------------------------------
 * Result of a tool-enabled generation.
 * ---------------------------------------------------------------------------
 */
export interface ToolCallingResult {
  /** Final text response from the assistant. */
  text: string;
  /** Tool calls the LLM made. */
  toolCalls: ToolCall[];
  /** Results of executed tools (only populated when auto_execute was true). */
  toolResults: ToolResult[];
  /** Whether the response is complete or waiting for more tool results. */
  isComplete: boolean;
  /** Number of LLM generation turns used, including the final synthesis turn. */
  iterationsUsed: number;
  errorMessage?: string | undefined;
  errorCode: number;
  /** Optional thinking/reasoning content extracted from the final response. */
  thinkingContent?:
    | string
    | undefined;
  /**
   * Token usage aggregated across every LLM generation turn in the loop
   * (including the final synthesis turn). Lets a plain generate() that routed
   * through the tool loop report the same usage a non-tool generate would.
   */
  usage?: TokenUsage | undefined;
}

export interface ToolParseRequest {
  text: string;
  options?: ToolCallingOptions | undefined;
}

export interface ToolParseResult {
  hasToolCall: boolean;
  toolCalls: ToolCall[];
  remainingText: string;
  errorMessage?: string | undefined;
  errorCode: number;
}

export interface ToolPromptFormatRequest {
  /**
   * User prompt to merge with tool instructions. Empty means return only
   * the tool-instruction block for the selected format.
   */
  userPrompt: string;
  /** Carries available tools plus format/choice/iteration constraints. */
  options?:
    | ToolCallingOptions
    | undefined;
  /**
   * Tool results to include when formatting a follow-up prompt after host
   * execution. Empty means an initial tool-enabled prompt.
   */
  toolResults: ToolResult[];
}

export interface ToolPromptFormatResult {
  formattedPrompt: string;
  format: ToolCallFormatName;
  errorMessage?: string | undefined;
  errorCode: number;
}

export interface ToolCallValidationRequest {
  toolCall?:
    | ToolCall
    | undefined;
  /**
   * Validation uses options.tools as the registry snapshot and honors
   * portable flags such as require_json_arguments and forced_tool_name.
   */
  options?: ToolCallingOptions | undefined;
}

export interface ToolCallValidationResult {
  isValid: boolean;
  validationErrors: string[];
  matchedTool?: ToolDefinition | undefined;
  normalizedArgumentsJson: string;
  errorMessage?: string | undefined;
  errorCode: number;
}

/**
 * One prior conversation turn, with its speaker stated rather than inferred
 * from list position.
 *
 * Declared here rather than importing chat.proto's ChatMessage/MessageRole:
 * chat.proto already imports tool_calling.proto, so that import would cycle.
 * Extracting ChatMessage + MessageRole into a leaf proto was considered and
 * rejected as a larger change than this surface warrants.
 *
 * Mapping from chat.proto: MESSAGE_ROLE_USER -> TOOL_CALLING_ROLE_USER,
 * MESSAGE_ROLE_ASSISTANT -> TOOL_CALLING_ROLE_ASSISTANT,
 * MESSAGE_ROLE_SYSTEM -> TOOL_CALLING_ROLE_SYSTEM. Tool calls and tool
 * results do NOT round-trip through history — only role + content do.
 */
export interface ToolCallingHistoryTurn {
  role: ToolCallingRole;
  content: string;
}

export interface ToolCallingSessionCreateRequest {
  /** The current turn's user prompt. */
  prompt: string;
  /**
   * Prior turns, EXCLUDING the current turn (which is `prompt`). commons
   * threads these into every generate in the loop so multi-turn tool use
   * keeps context.
   */
  history: ToolCallingHistoryTurn[];
  /**
   * THE single home for tool policy + sampling. No re-published copies:
   * every knob that used to be duplicated on this message (tools, format,
   * max_tool_calls, keep_tools_available, validate_calls, tool_choice,
   * forced_tool_name, max_output_tokens, temperature, top_p, system_prompt,
   * disable_thinking, auto_execute, replace_system_prompt,
   * require_json_arguments, parallel_tool_calls) lives on ToolCallingOptions.
   */
  options?: ToolCallingOptions | undefined;
}

export interface ToolCallingSessionEvent {
  /** serialized LLMStreamEvent proto */
  llmStreamEventBytes?: Uint8Array | undefined;
  toolCall?: ToolCall | undefined;
  finalResult?:
    | ToolCallingResult
    | undefined;
  /** serialized SDKError proto */
  errorBytes?: Uint8Array | undefined;
  seq: number;
}

export interface ToolCallingSessionStepWithResultRequest {
  sessionHandle: number;
  toolCallId: string;
  resultJson: string;
  error?: string | undefined;
}

function createBaseToolValue(): ToolValue {
  return {
    stringValue: undefined,
    numberValue: undefined,
    boolValue: undefined,
    arrayValue: undefined,
    objectValue: undefined,
    nullValue: undefined,
  };
}

export const ToolValue: MessageFns<ToolValue> = {
  encode(message: ToolValue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stringValue !== undefined) {
      writer.uint32(10).string(message.stringValue);
    }
    if (message.numberValue !== undefined) {
      writer.uint32(17).double(message.numberValue);
    }
    if (message.boolValue !== undefined) {
      writer.uint32(24).bool(message.boolValue);
    }
    if (message.arrayValue !== undefined) {
      ToolValueArray.encode(message.arrayValue, writer.uint32(34).fork()).join();
    }
    if (message.objectValue !== undefined) {
      ToolValueObject.encode(message.objectValue, writer.uint32(42).fork()).join();
    }
    if (message.nullValue !== undefined) {
      writer.uint32(48).bool(message.nullValue);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolValue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolValue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stringValue = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.numberValue = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.boolValue = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.arrayValue = ToolValueArray.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.objectValue = ToolValueObject.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.nullValue = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolValue {
    return {
      stringValue: isSet(object.stringValue)
        ? globalThis.String(object.stringValue)
        : isSet(object.string_value)
        ? globalThis.String(object.string_value)
        : undefined,
      numberValue: isSet(object.numberValue)
        ? globalThis.Number(object.numberValue)
        : isSet(object.number_value)
        ? globalThis.Number(object.number_value)
        : undefined,
      boolValue: isSet(object.boolValue)
        ? globalThis.Boolean(object.boolValue)
        : isSet(object.bool_value)
        ? globalThis.Boolean(object.bool_value)
        : undefined,
      arrayValue: isSet(object.arrayValue)
        ? ToolValueArray.fromJSON(object.arrayValue)
        : isSet(object.array_value)
        ? ToolValueArray.fromJSON(object.array_value)
        : undefined,
      objectValue: isSet(object.objectValue)
        ? ToolValueObject.fromJSON(object.objectValue)
        : isSet(object.object_value)
        ? ToolValueObject.fromJSON(object.object_value)
        : undefined,
      nullValue: isSet(object.nullValue)
        ? globalThis.Boolean(object.nullValue)
        : isSet(object.null_value)
        ? globalThis.Boolean(object.null_value)
        : undefined,
    };
  },

  toJSON(message: ToolValue): unknown {
    const obj: any = {};
    if (message.stringValue !== undefined) {
      obj.stringValue = message.stringValue;
    }
    if (message.numberValue !== undefined) {
      obj.numberValue = message.numberValue;
    }
    if (message.boolValue !== undefined) {
      obj.boolValue = message.boolValue;
    }
    if (message.arrayValue !== undefined) {
      obj.arrayValue = ToolValueArray.toJSON(message.arrayValue);
    }
    if (message.objectValue !== undefined) {
      obj.objectValue = ToolValueObject.toJSON(message.objectValue);
    }
    if (message.nullValue !== undefined) {
      obj.nullValue = message.nullValue;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolValue>, I>>(base?: I): ToolValue {
    return ToolValue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolValue>, I>>(object: I): ToolValue {
    const message = createBaseToolValue();
    message.stringValue = object.stringValue ?? undefined;
    message.numberValue = object.numberValue ?? undefined;
    message.boolValue = object.boolValue ?? undefined;
    message.arrayValue = (object.arrayValue !== undefined && object.arrayValue !== null)
      ? ToolValueArray.fromPartial(object.arrayValue)
      : undefined;
    message.objectValue = (object.objectValue !== undefined && object.objectValue !== null)
      ? ToolValueObject.fromPartial(object.objectValue)
      : undefined;
    message.nullValue = object.nullValue ?? undefined;
    return message;
  },
};

function createBaseToolValueArray(): ToolValueArray {
  return { values: [] };
}

export const ToolValueArray: MessageFns<ToolValueArray> = {
  encode(message: ToolValueArray, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.values) {
      ToolValue.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolValueArray {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolValueArray();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.values.push(ToolValue.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolValueArray {
    return {
      values: globalThis.Array.isArray(object?.values) ? object.values.map((e: any) => ToolValue.fromJSON(e)) : [],
    };
  },

  toJSON(message: ToolValueArray): unknown {
    const obj: any = {};
    if (message.values?.length) {
      obj.values = message.values.map((e) => ToolValue.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolValueArray>, I>>(base?: I): ToolValueArray {
    return ToolValueArray.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolValueArray>, I>>(object: I): ToolValueArray {
    const message = createBaseToolValueArray();
    message.values = object.values?.map((e) => ToolValue.fromPartial(e)) || [];
    return message;
  },
};

function createBaseToolValueObject(): ToolValueObject {
  return { fields: {} };
}

export const ToolValueObject: MessageFns<ToolValueObject> = {
  encode(message: ToolValueObject, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    globalThis.Object.entries(message.fields).forEach(([key, value]: [string, ToolValue]) => {
      ToolValueObject_FieldsEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolValueObject {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolValueObject();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = ToolValueObject_FieldsEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.fields[entry1.key] = entry1.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolValueObject {
    return {
      fields: isObject(object.fields)
        ? (globalThis.Object.entries(object.fields) as [string, any][]).reduce(
          (acc: { [key: string]: ToolValue }, [key, value]: [string, any]) => {
            acc[key] = ToolValue.fromJSON(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: ToolValueObject): unknown {
    const obj: any = {};
    if (message.fields) {
      const entries = globalThis.Object.entries(message.fields) as [string, ToolValue][];
      if (entries.length > 0) {
        obj.fields = {};
        entries.forEach(([k, v]) => {
          obj.fields[k] = ToolValue.toJSON(v);
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolValueObject>, I>>(base?: I): ToolValueObject {
    return ToolValueObject.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolValueObject>, I>>(object: I): ToolValueObject {
    const message = createBaseToolValueObject();
    message.fields = (globalThis.Object.entries(object.fields ?? {}) as [string, ToolValue][]).reduce(
      (acc: { [key: string]: ToolValue }, [key, value]: [string, ToolValue]) => {
        if (value !== undefined) {
          acc[key] = ToolValue.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseToolValueObject_FieldsEntry(): ToolValueObject_FieldsEntry {
  return { key: "", value: undefined };
}

export const ToolValueObject_FieldsEntry: MessageFns<ToolValueObject_FieldsEntry> = {
  encode(message: ToolValueObject_FieldsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      ToolValue.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolValueObject_FieldsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolValueObject_FieldsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = ToolValue.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolValueObject_FieldsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? ToolValue.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: ToolValueObject_FieldsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = ToolValue.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolValueObject_FieldsEntry>, I>>(base?: I): ToolValueObject_FieldsEntry {
    return ToolValueObject_FieldsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolValueObject_FieldsEntry>, I>>(object: I): ToolValueObject_FieldsEntry {
    const message = createBaseToolValueObject_FieldsEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? ToolValue.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseToolValueJSON(): ToolValueJSON {
  return { json: "" };
}

export const ToolValueJSON: MessageFns<ToolValueJSON> = {
  encode(message: ToolValueJSON, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.json !== "") {
      writer.uint32(10).string(message.json);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolValueJSON {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolValueJSON();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.json = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolValueJSON {
    return { json: isSet(object.json) ? globalThis.String(object.json) : "" };
  },

  toJSON(message: ToolValueJSON): unknown {
    const obj: any = {};
    if (message.json !== "") {
      obj.json = message.json;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolValueJSON>, I>>(base?: I): ToolValueJSON {
    return ToolValueJSON.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolValueJSON>, I>>(object: I): ToolValueJSON {
    const message = createBaseToolValueJSON();
    message.json = object.json ?? "";
    return message;
  },
};

function createBaseToolDefinition(): ToolDefinition {
  return { name: "", description: "", parameters: "", category: undefined };
}

export const ToolDefinition: MessageFns<ToolDefinition> = {
  encode(message: ToolDefinition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.parameters !== "") {
      writer.uint32(26).string(message.parameters);
    }
    if (message.category !== undefined) {
      writer.uint32(34).string(message.category);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolDefinition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolDefinition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.parameters = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.category = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolDefinition {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      parameters: isSet(object.parameters) ? globalThis.String(object.parameters) : "",
      category: isSet(object.category) ? globalThis.String(object.category) : undefined,
    };
  },

  toJSON(message: ToolDefinition): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.parameters !== "") {
      obj.parameters = message.parameters;
    }
    if (message.category !== undefined) {
      obj.category = message.category;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolDefinition>, I>>(base?: I): ToolDefinition {
    return ToolDefinition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolDefinition>, I>>(object: I): ToolDefinition {
    const message = createBaseToolDefinition();
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.parameters = object.parameters ?? "";
    message.category = object.category ?? undefined;
    return message;
  },
};

function createBaseToolCall(): ToolCall {
  return { id: "", name: "", argumentsJson: "", createdAtMs: 0, rawText: undefined };
}

export const ToolCall: MessageFns<ToolCall> = {
  encode(message: ToolCall, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.argumentsJson !== "") {
      writer.uint32(26).string(message.argumentsJson);
    }
    if (message.createdAtMs !== 0) {
      writer.uint32(32).int64(message.createdAtMs);
    }
    if (message.rawText !== undefined) {
      writer.uint32(42).string(message.rawText);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolCall {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolCall();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.argumentsJson = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.createdAtMs = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.rawText = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolCall {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      argumentsJson: isSet(object.argumentsJson)
        ? globalThis.String(object.argumentsJson)
        : isSet(object.arguments_json)
        ? globalThis.String(object.arguments_json)
        : "",
      createdAtMs: isSet(object.createdAtMs)
        ? globalThis.Number(object.createdAtMs)
        : isSet(object.created_at_ms)
        ? globalThis.Number(object.created_at_ms)
        : 0,
      rawText: isSet(object.rawText)
        ? globalThis.String(object.rawText)
        : isSet(object.raw_text)
        ? globalThis.String(object.raw_text)
        : undefined,
    };
  },

  toJSON(message: ToolCall): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.argumentsJson !== "") {
      obj.argumentsJson = message.argumentsJson;
    }
    if (message.createdAtMs !== 0) {
      obj.createdAtMs = Math.round(message.createdAtMs);
    }
    if (message.rawText !== undefined) {
      obj.rawText = message.rawText;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolCall>, I>>(base?: I): ToolCall {
    return ToolCall.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolCall>, I>>(object: I): ToolCall {
    const message = createBaseToolCall();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.argumentsJson = object.argumentsJson ?? "";
    message.createdAtMs = object.createdAtMs ?? 0;
    message.rawText = object.rawText ?? undefined;
    return message;
  },
};

function createBaseToolResult(): ToolResult {
  return {
    toolCallId: "",
    name: "",
    resultJson: "",
    error: undefined,
    isError: false,
    startedAtMs: 0,
    completedAtMs: 0,
  };
}

export const ToolResult: MessageFns<ToolResult> = {
  encode(message: ToolResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.toolCallId !== "") {
      writer.uint32(10).string(message.toolCallId);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.resultJson !== "") {
      writer.uint32(26).string(message.resultJson);
    }
    if (message.error !== undefined) {
      writer.uint32(34).string(message.error);
    }
    if (message.isError !== false) {
      writer.uint32(40).bool(message.isError);
    }
    if (message.startedAtMs !== 0) {
      writer.uint32(48).int64(message.startedAtMs);
    }
    if (message.completedAtMs !== 0) {
      writer.uint32(56).int64(message.completedAtMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.toolCallId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.resultJson = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.error = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.isError = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.startedAtMs = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.completedAtMs = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolResult {
    return {
      toolCallId: isSet(object.toolCallId)
        ? globalThis.String(object.toolCallId)
        : isSet(object.tool_call_id)
        ? globalThis.String(object.tool_call_id)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      resultJson: isSet(object.resultJson)
        ? globalThis.String(object.resultJson)
        : isSet(object.result_json)
        ? globalThis.String(object.result_json)
        : "",
      error: isSet(object.error) ? globalThis.String(object.error) : undefined,
      isError: isSet(object.isError)
        ? globalThis.Boolean(object.isError)
        : isSet(object.is_error)
        ? globalThis.Boolean(object.is_error)
        : false,
      startedAtMs: isSet(object.startedAtMs)
        ? globalThis.Number(object.startedAtMs)
        : isSet(object.started_at_ms)
        ? globalThis.Number(object.started_at_ms)
        : 0,
      completedAtMs: isSet(object.completedAtMs)
        ? globalThis.Number(object.completedAtMs)
        : isSet(object.completed_at_ms)
        ? globalThis.Number(object.completed_at_ms)
        : 0,
    };
  },

  toJSON(message: ToolResult): unknown {
    const obj: any = {};
    if (message.toolCallId !== "") {
      obj.toolCallId = message.toolCallId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.resultJson !== "") {
      obj.resultJson = message.resultJson;
    }
    if (message.error !== undefined) {
      obj.error = message.error;
    }
    if (message.isError !== false) {
      obj.isError = message.isError;
    }
    if (message.startedAtMs !== 0) {
      obj.startedAtMs = Math.round(message.startedAtMs);
    }
    if (message.completedAtMs !== 0) {
      obj.completedAtMs = Math.round(message.completedAtMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolResult>, I>>(base?: I): ToolResult {
    return ToolResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolResult>, I>>(object: I): ToolResult {
    const message = createBaseToolResult();
    message.toolCallId = object.toolCallId ?? "";
    message.name = object.name ?? "";
    message.resultJson = object.resultJson ?? "";
    message.error = object.error ?? undefined;
    message.isError = object.isError ?? false;
    message.startedAtMs = object.startedAtMs ?? 0;
    message.completedAtMs = object.completedAtMs ?? 0;
    return message;
  },
};

function createBaseToolCallingOptions(): ToolCallingOptions {
  return {
    tools: [],
    autoExecute: undefined,
    systemPrompt: undefined,
    replaceSystemPrompt: false,
    keepToolsAvailable: false,
    format: undefined,
    parallelToolCalls: false,
    maxToolCalls: undefined,
    toolChoice: 0,
    forcedToolName: undefined,
    requireJsonArguments: false,
    disableThinking: undefined,
    topP: undefined,
    validateCalls: undefined,
  };
}

export const ToolCallingOptions: MessageFns<ToolCallingOptions> = {
  encode(message: ToolCallingOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.tools) {
      ToolDefinition.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.autoExecute !== undefined) {
      writer.uint32(24).bool(message.autoExecute);
    }
    if (message.systemPrompt !== undefined) {
      writer.uint32(50).string(message.systemPrompt);
    }
    if (message.replaceSystemPrompt !== false) {
      writer.uint32(56).bool(message.replaceSystemPrompt);
    }
    if (message.keepToolsAvailable !== false) {
      writer.uint32(64).bool(message.keepToolsAvailable);
    }
    if (message.format !== undefined) {
      writer.uint32(80).int32(message.format);
    }
    if (message.parallelToolCalls !== false) {
      writer.uint32(120).bool(message.parallelToolCalls);
    }
    if (message.maxToolCalls !== undefined) {
      writer.uint32(96).int32(message.maxToolCalls);
    }
    if (message.toolChoice !== 0) {
      writer.uint32(104).int32(message.toolChoice);
    }
    if (message.forcedToolName !== undefined) {
      writer.uint32(114).string(message.forcedToolName);
    }
    if (message.requireJsonArguments !== false) {
      writer.uint32(128).bool(message.requireJsonArguments);
    }
    if (message.disableThinking !== undefined) {
      writer.uint32(136).bool(message.disableThinking);
    }
    if (message.topP !== undefined) {
      writer.uint32(149).float(message.topP);
    }
    if (message.validateCalls !== undefined) {
      writer.uint32(152).bool(message.validateCalls);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolCallingOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolCallingOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tools.push(ToolDefinition.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.autoExecute = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.systemPrompt = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.replaceSystemPrompt = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.keepToolsAvailable = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.format = reader.int32() as any;
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.parallelToolCalls = reader.bool();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.maxToolCalls = reader.int32();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.toolChoice = reader.int32() as any;
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.forcedToolName = reader.string();
          continue;
        }
        case 16: {
          if (tag !== 128) {
            break;
          }

          message.requireJsonArguments = reader.bool();
          continue;
        }
        case 17: {
          if (tag !== 136) {
            break;
          }

          message.disableThinking = reader.bool();
          continue;
        }
        case 18: {
          if (tag !== 149) {
            break;
          }

          message.topP = reader.float();
          continue;
        }
        case 19: {
          if (tag !== 152) {
            break;
          }

          message.validateCalls = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolCallingOptions {
    return {
      tools: globalThis.Array.isArray(object?.tools) ? object.tools.map((e: any) => ToolDefinition.fromJSON(e)) : [],
      autoExecute: isSet(object.autoExecute)
        ? globalThis.Boolean(object.autoExecute)
        : isSet(object.auto_execute)
        ? globalThis.Boolean(object.auto_execute)
        : undefined,
      systemPrompt: isSet(object.systemPrompt)
        ? globalThis.String(object.systemPrompt)
        : isSet(object.system_prompt)
        ? globalThis.String(object.system_prompt)
        : undefined,
      replaceSystemPrompt: isSet(object.replaceSystemPrompt)
        ? globalThis.Boolean(object.replaceSystemPrompt)
        : isSet(object.replace_system_prompt)
        ? globalThis.Boolean(object.replace_system_prompt)
        : false,
      keepToolsAvailable: isSet(object.keepToolsAvailable)
        ? globalThis.Boolean(object.keepToolsAvailable)
        : isSet(object.keep_tools_available)
        ? globalThis.Boolean(object.keep_tools_available)
        : false,
      format: isSet(object.format) ? toolCallFormatNameFromJSON(object.format) : undefined,
      parallelToolCalls: isSet(object.parallelToolCalls)
        ? globalThis.Boolean(object.parallelToolCalls)
        : isSet(object.parallel_tool_calls)
        ? globalThis.Boolean(object.parallel_tool_calls)
        : false,
      maxToolCalls: isSet(object.maxToolCalls)
        ? globalThis.Number(object.maxToolCalls)
        : isSet(object.max_tool_calls)
        ? globalThis.Number(object.max_tool_calls)
        : undefined,
      toolChoice: isSet(object.toolChoice)
        ? toolChoiceModeFromJSON(object.toolChoice)
        : isSet(object.tool_choice)
        ? toolChoiceModeFromJSON(object.tool_choice)
        : 0,
      forcedToolName: isSet(object.forcedToolName)
        ? globalThis.String(object.forcedToolName)
        : isSet(object.forced_tool_name)
        ? globalThis.String(object.forced_tool_name)
        : undefined,
      requireJsonArguments: isSet(object.requireJsonArguments)
        ? globalThis.Boolean(object.requireJsonArguments)
        : isSet(object.require_json_arguments)
        ? globalThis.Boolean(object.require_json_arguments)
        : false,
      disableThinking: isSet(object.disableThinking)
        ? globalThis.Boolean(object.disableThinking)
        : isSet(object.disable_thinking)
        ? globalThis.Boolean(object.disable_thinking)
        : undefined,
      topP: isSet(object.topP)
        ? globalThis.Number(object.topP)
        : isSet(object.top_p)
        ? globalThis.Number(object.top_p)
        : undefined,
      validateCalls: isSet(object.validateCalls)
        ? globalThis.Boolean(object.validateCalls)
        : isSet(object.validate_calls)
        ? globalThis.Boolean(object.validate_calls)
        : undefined,
    };
  },

  toJSON(message: ToolCallingOptions): unknown {
    const obj: any = {};
    if (message.tools?.length) {
      obj.tools = message.tools.map((e) => ToolDefinition.toJSON(e));
    }
    if (message.autoExecute !== undefined) {
      obj.autoExecute = message.autoExecute;
    }
    if (message.systemPrompt !== undefined) {
      obj.systemPrompt = message.systemPrompt;
    }
    if (message.replaceSystemPrompt !== false) {
      obj.replaceSystemPrompt = message.replaceSystemPrompt;
    }
    if (message.keepToolsAvailable !== false) {
      obj.keepToolsAvailable = message.keepToolsAvailable;
    }
    if (message.format !== undefined) {
      obj.format = toolCallFormatNameToJSON(message.format);
    }
    if (message.parallelToolCalls !== false) {
      obj.parallelToolCalls = message.parallelToolCalls;
    }
    if (message.maxToolCalls !== undefined) {
      obj.maxToolCalls = Math.round(message.maxToolCalls);
    }
    if (message.toolChoice !== 0) {
      obj.toolChoice = toolChoiceModeToJSON(message.toolChoice);
    }
    if (message.forcedToolName !== undefined) {
      obj.forcedToolName = message.forcedToolName;
    }
    if (message.requireJsonArguments !== false) {
      obj.requireJsonArguments = message.requireJsonArguments;
    }
    if (message.disableThinking !== undefined) {
      obj.disableThinking = message.disableThinking;
    }
    if (message.topP !== undefined) {
      obj.topP = message.topP;
    }
    if (message.validateCalls !== undefined) {
      obj.validateCalls = message.validateCalls;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolCallingOptions>, I>>(base?: I): ToolCallingOptions {
    return ToolCallingOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolCallingOptions>, I>>(object: I): ToolCallingOptions {
    const message = createBaseToolCallingOptions();
    message.tools = object.tools?.map((e) => ToolDefinition.fromPartial(e)) || [];
    message.autoExecute = object.autoExecute ?? undefined;
    message.systemPrompt = object.systemPrompt ?? undefined;
    message.replaceSystemPrompt = object.replaceSystemPrompt ?? false;
    message.keepToolsAvailable = object.keepToolsAvailable ?? false;
    message.format = object.format ?? undefined;
    message.parallelToolCalls = object.parallelToolCalls ?? false;
    message.maxToolCalls = object.maxToolCalls ?? undefined;
    message.toolChoice = object.toolChoice ?? 0;
    message.forcedToolName = object.forcedToolName ?? undefined;
    message.requireJsonArguments = object.requireJsonArguments ?? false;
    message.disableThinking = object.disableThinking ?? undefined;
    message.topP = object.topP ?? undefined;
    message.validateCalls = object.validateCalls ?? undefined;
    return message;
  },
};

function createBaseToolCallingResult(): ToolCallingResult {
  return {
    text: "",
    toolCalls: [],
    toolResults: [],
    isComplete: false,
    iterationsUsed: 0,
    errorMessage: undefined,
    errorCode: 0,
    thinkingContent: undefined,
    usage: undefined,
  };
}

export const ToolCallingResult: MessageFns<ToolCallingResult> = {
  encode(message: ToolCallingResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    for (const v of message.toolCalls) {
      ToolCall.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.toolResults) {
      ToolResult.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.isComplete !== false) {
      writer.uint32(32).bool(message.isComplete);
    }
    if (message.iterationsUsed !== 0) {
      writer.uint32(40).int32(message.iterationsUsed);
    }
    if (message.errorMessage !== undefined) {
      writer.uint32(50).string(message.errorMessage);
    }
    if (message.errorCode !== 0) {
      writer.uint32(56).int32(message.errorCode);
    }
    if (message.thinkingContent !== undefined) {
      writer.uint32(66).string(message.thinkingContent);
    }
    if (message.usage !== undefined) {
      TokenUsage.encode(message.usage, writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolCallingResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolCallingResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.toolCalls.push(ToolCall.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.toolResults.push(ToolResult.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.isComplete = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.iterationsUsed = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.errorCode = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.thinkingContent = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.usage = TokenUsage.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolCallingResult {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      toolCalls: globalThis.Array.isArray(object?.toolCalls)
        ? object.toolCalls.map((e: any) => ToolCall.fromJSON(e))
        : globalThis.Array.isArray(object?.tool_calls)
        ? object.tool_calls.map((e: any) => ToolCall.fromJSON(e))
        : [],
      toolResults: globalThis.Array.isArray(object?.toolResults)
        ? object.toolResults.map((e: any) => ToolResult.fromJSON(e))
        : globalThis.Array.isArray(object?.tool_results)
        ? object.tool_results.map((e: any) => ToolResult.fromJSON(e))
        : [],
      isComplete: isSet(object.isComplete)
        ? globalThis.Boolean(object.isComplete)
        : isSet(object.is_complete)
        ? globalThis.Boolean(object.is_complete)
        : false,
      iterationsUsed: isSet(object.iterationsUsed)
        ? globalThis.Number(object.iterationsUsed)
        : isSet(object.iterations_used)
        ? globalThis.Number(object.iterations_used)
        : 0,
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : undefined,
      errorCode: isSet(object.errorCode)
        ? globalThis.Number(object.errorCode)
        : isSet(object.error_code)
        ? globalThis.Number(object.error_code)
        : 0,
      thinkingContent: isSet(object.thinkingContent)
        ? globalThis.String(object.thinkingContent)
        : isSet(object.thinking_content)
        ? globalThis.String(object.thinking_content)
        : undefined,
      usage: isSet(object.usage) ? TokenUsage.fromJSON(object.usage) : undefined,
    };
  },

  toJSON(message: ToolCallingResult): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.toolCalls?.length) {
      obj.toolCalls = message.toolCalls.map((e) => ToolCall.toJSON(e));
    }
    if (message.toolResults?.length) {
      obj.toolResults = message.toolResults.map((e) => ToolResult.toJSON(e));
    }
    if (message.isComplete !== false) {
      obj.isComplete = message.isComplete;
    }
    if (message.iterationsUsed !== 0) {
      obj.iterationsUsed = Math.round(message.iterationsUsed);
    }
    if (message.errorMessage !== undefined) {
      obj.errorMessage = message.errorMessage;
    }
    if (message.errorCode !== 0) {
      obj.errorCode = Math.round(message.errorCode);
    }
    if (message.thinkingContent !== undefined) {
      obj.thinkingContent = message.thinkingContent;
    }
    if (message.usage !== undefined) {
      obj.usage = TokenUsage.toJSON(message.usage);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolCallingResult>, I>>(base?: I): ToolCallingResult {
    return ToolCallingResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolCallingResult>, I>>(object: I): ToolCallingResult {
    const message = createBaseToolCallingResult();
    message.text = object.text ?? "";
    message.toolCalls = object.toolCalls?.map((e) => ToolCall.fromPartial(e)) || [];
    message.toolResults = object.toolResults?.map((e) => ToolResult.fromPartial(e)) || [];
    message.isComplete = object.isComplete ?? false;
    message.iterationsUsed = object.iterationsUsed ?? 0;
    message.errorMessage = object.errorMessage ?? undefined;
    message.errorCode = object.errorCode ?? 0;
    message.thinkingContent = object.thinkingContent ?? undefined;
    message.usage = (object.usage !== undefined && object.usage !== null)
      ? TokenUsage.fromPartial(object.usage)
      : undefined;
    return message;
  },
};

function createBaseToolParseRequest(): ToolParseRequest {
  return { text: "", options: undefined };
}

export const ToolParseRequest: MessageFns<ToolParseRequest> = {
  encode(message: ToolParseRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.options !== undefined) {
      ToolCallingOptions.encode(message.options, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolParseRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolParseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.options = ToolCallingOptions.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolParseRequest {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      options: isSet(object.options) ? ToolCallingOptions.fromJSON(object.options) : undefined,
    };
  },

  toJSON(message: ToolParseRequest): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.options !== undefined) {
      obj.options = ToolCallingOptions.toJSON(message.options);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolParseRequest>, I>>(base?: I): ToolParseRequest {
    return ToolParseRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolParseRequest>, I>>(object: I): ToolParseRequest {
    const message = createBaseToolParseRequest();
    message.text = object.text ?? "";
    message.options = (object.options !== undefined && object.options !== null)
      ? ToolCallingOptions.fromPartial(object.options)
      : undefined;
    return message;
  },
};

function createBaseToolParseResult(): ToolParseResult {
  return { hasToolCall: false, toolCalls: [], remainingText: "", errorMessage: undefined, errorCode: 0 };
}

export const ToolParseResult: MessageFns<ToolParseResult> = {
  encode(message: ToolParseResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hasToolCall !== false) {
      writer.uint32(8).bool(message.hasToolCall);
    }
    for (const v of message.toolCalls) {
      ToolCall.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.remainingText !== "") {
      writer.uint32(26).string(message.remainingText);
    }
    if (message.errorMessage !== undefined) {
      writer.uint32(34).string(message.errorMessage);
    }
    if (message.errorCode !== 0) {
      writer.uint32(40).int32(message.errorCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolParseResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolParseResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.hasToolCall = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.toolCalls.push(ToolCall.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.remainingText = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.errorCode = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolParseResult {
    return {
      hasToolCall: isSet(object.hasToolCall)
        ? globalThis.Boolean(object.hasToolCall)
        : isSet(object.has_tool_call)
        ? globalThis.Boolean(object.has_tool_call)
        : false,
      toolCalls: globalThis.Array.isArray(object?.toolCalls)
        ? object.toolCalls.map((e: any) => ToolCall.fromJSON(e))
        : globalThis.Array.isArray(object?.tool_calls)
        ? object.tool_calls.map((e: any) => ToolCall.fromJSON(e))
        : [],
      remainingText: isSet(object.remainingText)
        ? globalThis.String(object.remainingText)
        : isSet(object.remaining_text)
        ? globalThis.String(object.remaining_text)
        : "",
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : undefined,
      errorCode: isSet(object.errorCode)
        ? globalThis.Number(object.errorCode)
        : isSet(object.error_code)
        ? globalThis.Number(object.error_code)
        : 0,
    };
  },

  toJSON(message: ToolParseResult): unknown {
    const obj: any = {};
    if (message.hasToolCall !== false) {
      obj.hasToolCall = message.hasToolCall;
    }
    if (message.toolCalls?.length) {
      obj.toolCalls = message.toolCalls.map((e) => ToolCall.toJSON(e));
    }
    if (message.remainingText !== "") {
      obj.remainingText = message.remainingText;
    }
    if (message.errorMessage !== undefined) {
      obj.errorMessage = message.errorMessage;
    }
    if (message.errorCode !== 0) {
      obj.errorCode = Math.round(message.errorCode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolParseResult>, I>>(base?: I): ToolParseResult {
    return ToolParseResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolParseResult>, I>>(object: I): ToolParseResult {
    const message = createBaseToolParseResult();
    message.hasToolCall = object.hasToolCall ?? false;
    message.toolCalls = object.toolCalls?.map((e) => ToolCall.fromPartial(e)) || [];
    message.remainingText = object.remainingText ?? "";
    message.errorMessage = object.errorMessage ?? undefined;
    message.errorCode = object.errorCode ?? 0;
    return message;
  },
};

function createBaseToolPromptFormatRequest(): ToolPromptFormatRequest {
  return { userPrompt: "", options: undefined, toolResults: [] };
}

export const ToolPromptFormatRequest: MessageFns<ToolPromptFormatRequest> = {
  encode(message: ToolPromptFormatRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userPrompt !== "") {
      writer.uint32(10).string(message.userPrompt);
    }
    if (message.options !== undefined) {
      ToolCallingOptions.encode(message.options, writer.uint32(18).fork()).join();
    }
    for (const v of message.toolResults) {
      ToolResult.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolPromptFormatRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolPromptFormatRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userPrompt = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.options = ToolCallingOptions.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.toolResults.push(ToolResult.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolPromptFormatRequest {
    return {
      userPrompt: isSet(object.userPrompt)
        ? globalThis.String(object.userPrompt)
        : isSet(object.user_prompt)
        ? globalThis.String(object.user_prompt)
        : "",
      options: isSet(object.options) ? ToolCallingOptions.fromJSON(object.options) : undefined,
      toolResults: globalThis.Array.isArray(object?.toolResults)
        ? object.toolResults.map((e: any) => ToolResult.fromJSON(e))
        : globalThis.Array.isArray(object?.tool_results)
        ? object.tool_results.map((e: any) => ToolResult.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ToolPromptFormatRequest): unknown {
    const obj: any = {};
    if (message.userPrompt !== "") {
      obj.userPrompt = message.userPrompt;
    }
    if (message.options !== undefined) {
      obj.options = ToolCallingOptions.toJSON(message.options);
    }
    if (message.toolResults?.length) {
      obj.toolResults = message.toolResults.map((e) => ToolResult.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolPromptFormatRequest>, I>>(base?: I): ToolPromptFormatRequest {
    return ToolPromptFormatRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolPromptFormatRequest>, I>>(object: I): ToolPromptFormatRequest {
    const message = createBaseToolPromptFormatRequest();
    message.userPrompt = object.userPrompt ?? "";
    message.options = (object.options !== undefined && object.options !== null)
      ? ToolCallingOptions.fromPartial(object.options)
      : undefined;
    message.toolResults = object.toolResults?.map((e) => ToolResult.fromPartial(e)) || [];
    return message;
  },
};

function createBaseToolPromptFormatResult(): ToolPromptFormatResult {
  return { formattedPrompt: "", format: 0, errorMessage: undefined, errorCode: 0 };
}

export const ToolPromptFormatResult: MessageFns<ToolPromptFormatResult> = {
  encode(message: ToolPromptFormatResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.formattedPrompt !== "") {
      writer.uint32(10).string(message.formattedPrompt);
    }
    if (message.format !== 0) {
      writer.uint32(16).int32(message.format);
    }
    if (message.errorMessage !== undefined) {
      writer.uint32(34).string(message.errorMessage);
    }
    if (message.errorCode !== 0) {
      writer.uint32(40).int32(message.errorCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolPromptFormatResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolPromptFormatResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.formattedPrompt = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.format = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.errorCode = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolPromptFormatResult {
    return {
      formattedPrompt: isSet(object.formattedPrompt)
        ? globalThis.String(object.formattedPrompt)
        : isSet(object.formatted_prompt)
        ? globalThis.String(object.formatted_prompt)
        : "",
      format: isSet(object.format) ? toolCallFormatNameFromJSON(object.format) : 0,
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : undefined,
      errorCode: isSet(object.errorCode)
        ? globalThis.Number(object.errorCode)
        : isSet(object.error_code)
        ? globalThis.Number(object.error_code)
        : 0,
    };
  },

  toJSON(message: ToolPromptFormatResult): unknown {
    const obj: any = {};
    if (message.formattedPrompt !== "") {
      obj.formattedPrompt = message.formattedPrompt;
    }
    if (message.format !== 0) {
      obj.format = toolCallFormatNameToJSON(message.format);
    }
    if (message.errorMessage !== undefined) {
      obj.errorMessage = message.errorMessage;
    }
    if (message.errorCode !== 0) {
      obj.errorCode = Math.round(message.errorCode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolPromptFormatResult>, I>>(base?: I): ToolPromptFormatResult {
    return ToolPromptFormatResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolPromptFormatResult>, I>>(object: I): ToolPromptFormatResult {
    const message = createBaseToolPromptFormatResult();
    message.formattedPrompt = object.formattedPrompt ?? "";
    message.format = object.format ?? 0;
    message.errorMessage = object.errorMessage ?? undefined;
    message.errorCode = object.errorCode ?? 0;
    return message;
  },
};

function createBaseToolCallValidationRequest(): ToolCallValidationRequest {
  return { toolCall: undefined, options: undefined };
}

export const ToolCallValidationRequest: MessageFns<ToolCallValidationRequest> = {
  encode(message: ToolCallValidationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.toolCall !== undefined) {
      ToolCall.encode(message.toolCall, writer.uint32(10).fork()).join();
    }
    if (message.options !== undefined) {
      ToolCallingOptions.encode(message.options, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolCallValidationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolCallValidationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.toolCall = ToolCall.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.options = ToolCallingOptions.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolCallValidationRequest {
    return {
      toolCall: isSet(object.toolCall)
        ? ToolCall.fromJSON(object.toolCall)
        : isSet(object.tool_call)
        ? ToolCall.fromJSON(object.tool_call)
        : undefined,
      options: isSet(object.options) ? ToolCallingOptions.fromJSON(object.options) : undefined,
    };
  },

  toJSON(message: ToolCallValidationRequest): unknown {
    const obj: any = {};
    if (message.toolCall !== undefined) {
      obj.toolCall = ToolCall.toJSON(message.toolCall);
    }
    if (message.options !== undefined) {
      obj.options = ToolCallingOptions.toJSON(message.options);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolCallValidationRequest>, I>>(base?: I): ToolCallValidationRequest {
    return ToolCallValidationRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolCallValidationRequest>, I>>(object: I): ToolCallValidationRequest {
    const message = createBaseToolCallValidationRequest();
    message.toolCall = (object.toolCall !== undefined && object.toolCall !== null)
      ? ToolCall.fromPartial(object.toolCall)
      : undefined;
    message.options = (object.options !== undefined && object.options !== null)
      ? ToolCallingOptions.fromPartial(object.options)
      : undefined;
    return message;
  },
};

function createBaseToolCallValidationResult(): ToolCallValidationResult {
  return {
    isValid: false,
    validationErrors: [],
    matchedTool: undefined,
    normalizedArgumentsJson: "",
    errorMessage: undefined,
    errorCode: 0,
  };
}

export const ToolCallValidationResult: MessageFns<ToolCallValidationResult> = {
  encode(message: ToolCallValidationResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.isValid !== false) {
      writer.uint32(8).bool(message.isValid);
    }
    for (const v of message.validationErrors) {
      writer.uint32(18).string(v!);
    }
    if (message.matchedTool !== undefined) {
      ToolDefinition.encode(message.matchedTool, writer.uint32(26).fork()).join();
    }
    if (message.normalizedArgumentsJson !== "") {
      writer.uint32(34).string(message.normalizedArgumentsJson);
    }
    if (message.errorMessage !== undefined) {
      writer.uint32(42).string(message.errorMessage);
    }
    if (message.errorCode !== 0) {
      writer.uint32(48).int32(message.errorCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolCallValidationResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolCallValidationResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.isValid = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.validationErrors.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.matchedTool = ToolDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.normalizedArgumentsJson = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.errorCode = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolCallValidationResult {
    return {
      isValid: isSet(object.isValid)
        ? globalThis.Boolean(object.isValid)
        : isSet(object.is_valid)
        ? globalThis.Boolean(object.is_valid)
        : false,
      validationErrors: globalThis.Array.isArray(object?.validationErrors)
        ? object.validationErrors.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.validation_errors)
        ? object.validation_errors.map((e: any) => globalThis.String(e))
        : [],
      matchedTool: isSet(object.matchedTool)
        ? ToolDefinition.fromJSON(object.matchedTool)
        : isSet(object.matched_tool)
        ? ToolDefinition.fromJSON(object.matched_tool)
        : undefined,
      normalizedArgumentsJson: isSet(object.normalizedArgumentsJson)
        ? globalThis.String(object.normalizedArgumentsJson)
        : isSet(object.normalized_arguments_json)
        ? globalThis.String(object.normalized_arguments_json)
        : "",
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : undefined,
      errorCode: isSet(object.errorCode)
        ? globalThis.Number(object.errorCode)
        : isSet(object.error_code)
        ? globalThis.Number(object.error_code)
        : 0,
    };
  },

  toJSON(message: ToolCallValidationResult): unknown {
    const obj: any = {};
    if (message.isValid !== false) {
      obj.isValid = message.isValid;
    }
    if (message.validationErrors?.length) {
      obj.validationErrors = message.validationErrors;
    }
    if (message.matchedTool !== undefined) {
      obj.matchedTool = ToolDefinition.toJSON(message.matchedTool);
    }
    if (message.normalizedArgumentsJson !== "") {
      obj.normalizedArgumentsJson = message.normalizedArgumentsJson;
    }
    if (message.errorMessage !== undefined) {
      obj.errorMessage = message.errorMessage;
    }
    if (message.errorCode !== 0) {
      obj.errorCode = Math.round(message.errorCode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolCallValidationResult>, I>>(base?: I): ToolCallValidationResult {
    return ToolCallValidationResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolCallValidationResult>, I>>(object: I): ToolCallValidationResult {
    const message = createBaseToolCallValidationResult();
    message.isValid = object.isValid ?? false;
    message.validationErrors = object.validationErrors?.map((e) => e) || [];
    message.matchedTool = (object.matchedTool !== undefined && object.matchedTool !== null)
      ? ToolDefinition.fromPartial(object.matchedTool)
      : undefined;
    message.normalizedArgumentsJson = object.normalizedArgumentsJson ?? "";
    message.errorMessage = object.errorMessage ?? undefined;
    message.errorCode = object.errorCode ?? 0;
    return message;
  },
};

function createBaseToolCallingHistoryTurn(): ToolCallingHistoryTurn {
  return { role: 0, content: "" };
}

export const ToolCallingHistoryTurn: MessageFns<ToolCallingHistoryTurn> = {
  encode(message: ToolCallingHistoryTurn, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.role !== 0) {
      writer.uint32(8).int32(message.role);
    }
    if (message.content !== "") {
      writer.uint32(18).string(message.content);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolCallingHistoryTurn {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolCallingHistoryTurn();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.role = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolCallingHistoryTurn {
    return {
      role: isSet(object.role) ? toolCallingRoleFromJSON(object.role) : 0,
      content: isSet(object.content) ? globalThis.String(object.content) : "",
    };
  },

  toJSON(message: ToolCallingHistoryTurn): unknown {
    const obj: any = {};
    if (message.role !== 0) {
      obj.role = toolCallingRoleToJSON(message.role);
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolCallingHistoryTurn>, I>>(base?: I): ToolCallingHistoryTurn {
    return ToolCallingHistoryTurn.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolCallingHistoryTurn>, I>>(object: I): ToolCallingHistoryTurn {
    const message = createBaseToolCallingHistoryTurn();
    message.role = object.role ?? 0;
    message.content = object.content ?? "";
    return message;
  },
};

function createBaseToolCallingSessionCreateRequest(): ToolCallingSessionCreateRequest {
  return { prompt: "", history: [], options: undefined };
}

export const ToolCallingSessionCreateRequest: MessageFns<ToolCallingSessionCreateRequest> = {
  encode(message: ToolCallingSessionCreateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.prompt !== "") {
      writer.uint32(10).string(message.prompt);
    }
    for (const v of message.history) {
      ToolCallingHistoryTurn.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.options !== undefined) {
      ToolCallingOptions.encode(message.options, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolCallingSessionCreateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolCallingSessionCreateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.prompt = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.history.push(ToolCallingHistoryTurn.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.options = ToolCallingOptions.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolCallingSessionCreateRequest {
    return {
      prompt: isSet(object.prompt) ? globalThis.String(object.prompt) : "",
      history: globalThis.Array.isArray(object?.history)
        ? object.history.map((e: any) => ToolCallingHistoryTurn.fromJSON(e))
        : [],
      options: isSet(object.options) ? ToolCallingOptions.fromJSON(object.options) : undefined,
    };
  },

  toJSON(message: ToolCallingSessionCreateRequest): unknown {
    const obj: any = {};
    if (message.prompt !== "") {
      obj.prompt = message.prompt;
    }
    if (message.history?.length) {
      obj.history = message.history.map((e) => ToolCallingHistoryTurn.toJSON(e));
    }
    if (message.options !== undefined) {
      obj.options = ToolCallingOptions.toJSON(message.options);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolCallingSessionCreateRequest>, I>>(base?: I): ToolCallingSessionCreateRequest {
    return ToolCallingSessionCreateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolCallingSessionCreateRequest>, I>>(
    object: I,
  ): ToolCallingSessionCreateRequest {
    const message = createBaseToolCallingSessionCreateRequest();
    message.prompt = object.prompt ?? "";
    message.history = object.history?.map((e) => ToolCallingHistoryTurn.fromPartial(e)) || [];
    message.options = (object.options !== undefined && object.options !== null)
      ? ToolCallingOptions.fromPartial(object.options)
      : undefined;
    return message;
  },
};

function createBaseToolCallingSessionEvent(): ToolCallingSessionEvent {
  return { llmStreamEventBytes: undefined, toolCall: undefined, finalResult: undefined, errorBytes: undefined, seq: 0 };
}

export const ToolCallingSessionEvent: MessageFns<ToolCallingSessionEvent> = {
  encode(message: ToolCallingSessionEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.llmStreamEventBytes !== undefined) {
      writer.uint32(10).bytes(message.llmStreamEventBytes);
    }
    if (message.toolCall !== undefined) {
      ToolCall.encode(message.toolCall, writer.uint32(18).fork()).join();
    }
    if (message.finalResult !== undefined) {
      ToolCallingResult.encode(message.finalResult, writer.uint32(26).fork()).join();
    }
    if (message.errorBytes !== undefined) {
      writer.uint32(34).bytes(message.errorBytes);
    }
    if (message.seq !== 0) {
      writer.uint32(40).uint64(message.seq);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolCallingSessionEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolCallingSessionEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.llmStreamEventBytes = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.toolCall = ToolCall.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.finalResult = ToolCallingResult.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.errorBytes = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.seq = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolCallingSessionEvent {
    return {
      llmStreamEventBytes: isSet(object.llmStreamEventBytes)
        ? bytesFromBase64(object.llmStreamEventBytes)
        : isSet(object.llm_stream_event_bytes)
        ? bytesFromBase64(object.llm_stream_event_bytes)
        : undefined,
      toolCall: isSet(object.toolCall)
        ? ToolCall.fromJSON(object.toolCall)
        : isSet(object.tool_call)
        ? ToolCall.fromJSON(object.tool_call)
        : undefined,
      finalResult: isSet(object.finalResult)
        ? ToolCallingResult.fromJSON(object.finalResult)
        : isSet(object.final_result)
        ? ToolCallingResult.fromJSON(object.final_result)
        : undefined,
      errorBytes: isSet(object.errorBytes)
        ? bytesFromBase64(object.errorBytes)
        : isSet(object.error_bytes)
        ? bytesFromBase64(object.error_bytes)
        : undefined,
      seq: isSet(object.seq) ? globalThis.Number(object.seq) : 0,
    };
  },

  toJSON(message: ToolCallingSessionEvent): unknown {
    const obj: any = {};
    if (message.llmStreamEventBytes !== undefined) {
      obj.llmStreamEventBytes = base64FromBytes(message.llmStreamEventBytes);
    }
    if (message.toolCall !== undefined) {
      obj.toolCall = ToolCall.toJSON(message.toolCall);
    }
    if (message.finalResult !== undefined) {
      obj.finalResult = ToolCallingResult.toJSON(message.finalResult);
    }
    if (message.errorBytes !== undefined) {
      obj.errorBytes = base64FromBytes(message.errorBytes);
    }
    if (message.seq !== 0) {
      obj.seq = Math.round(message.seq);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolCallingSessionEvent>, I>>(base?: I): ToolCallingSessionEvent {
    return ToolCallingSessionEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolCallingSessionEvent>, I>>(object: I): ToolCallingSessionEvent {
    const message = createBaseToolCallingSessionEvent();
    message.llmStreamEventBytes = object.llmStreamEventBytes ?? undefined;
    message.toolCall = (object.toolCall !== undefined && object.toolCall !== null)
      ? ToolCall.fromPartial(object.toolCall)
      : undefined;
    message.finalResult = (object.finalResult !== undefined && object.finalResult !== null)
      ? ToolCallingResult.fromPartial(object.finalResult)
      : undefined;
    message.errorBytes = object.errorBytes ?? undefined;
    message.seq = object.seq ?? 0;
    return message;
  },
};

function createBaseToolCallingSessionStepWithResultRequest(): ToolCallingSessionStepWithResultRequest {
  return { sessionHandle: 0, toolCallId: "", resultJson: "", error: undefined };
}

export const ToolCallingSessionStepWithResultRequest: MessageFns<ToolCallingSessionStepWithResultRequest> = {
  encode(message: ToolCallingSessionStepWithResultRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionHandle !== 0) {
      writer.uint32(8).uint64(message.sessionHandle);
    }
    if (message.toolCallId !== "") {
      writer.uint32(18).string(message.toolCallId);
    }
    if (message.resultJson !== "") {
      writer.uint32(26).string(message.resultJson);
    }
    if (message.error !== undefined) {
      writer.uint32(34).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolCallingSessionStepWithResultRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolCallingSessionStepWithResultRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.sessionHandle = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.toolCallId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.resultJson = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolCallingSessionStepWithResultRequest {
    return {
      sessionHandle: isSet(object.sessionHandle)
        ? globalThis.Number(object.sessionHandle)
        : isSet(object.session_handle)
        ? globalThis.Number(object.session_handle)
        : 0,
      toolCallId: isSet(object.toolCallId)
        ? globalThis.String(object.toolCallId)
        : isSet(object.tool_call_id)
        ? globalThis.String(object.tool_call_id)
        : "",
      resultJson: isSet(object.resultJson)
        ? globalThis.String(object.resultJson)
        : isSet(object.result_json)
        ? globalThis.String(object.result_json)
        : "",
      error: isSet(object.error) ? globalThis.String(object.error) : undefined,
    };
  },

  toJSON(message: ToolCallingSessionStepWithResultRequest): unknown {
    const obj: any = {};
    if (message.sessionHandle !== 0) {
      obj.sessionHandle = Math.round(message.sessionHandle);
    }
    if (message.toolCallId !== "") {
      obj.toolCallId = message.toolCallId;
    }
    if (message.resultJson !== "") {
      obj.resultJson = message.resultJson;
    }
    if (message.error !== undefined) {
      obj.error = message.error;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolCallingSessionStepWithResultRequest>, I>>(
    base?: I,
  ): ToolCallingSessionStepWithResultRequest {
    return ToolCallingSessionStepWithResultRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolCallingSessionStepWithResultRequest>, I>>(
    object: I,
  ): ToolCallingSessionStepWithResultRequest {
    const message = createBaseToolCallingSessionStepWithResultRequest();
    message.sessionHandle = object.sessionHandle ?? 0;
    message.toolCallId = object.toolCallId ?? "";
    message.resultJson = object.resultJson ?? "";
    message.error = object.error ?? undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  const bin = globalThis.atob(b64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; ++i) {
    arr[i] = bin.charCodeAt(i);
  }
  return arr;
}

function base64FromBytes(arr: Uint8Array): string {
  const bin: string[] = [];
  arr.forEach((byte) => {
    bin.push(globalThis.String.fromCharCode(byte));
  });
  return globalThis.btoa(bin.join(""));
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
