// GENERATED by idl/codegen/generate_ts_convenience.py — DO NOT EDIT.
//
// Source: idl/hybrid_router.proto
//
// Convenience helpers derived from RunAnywhere's `rac_*` proto
// annotations (see idl/rac_options.proto):
//   * `<enumName>WireString`         (rac_wire_string)
//   * `<enumName>FromWireString`     (reverse of rac_wire_string)
//   * `<enumName>DisplayName`        (rac_display_name)
//   * `<enumName>AnalyticsKey`       (rac_analytics_key)
//   * `<msgName>Defaults`            (rac_default)
//   * `validate<MsgName>`            (rac_required / rac_min / rac_max /
//                                     rac_min_float / rac_max_float)

/* eslint-disable */

import { BatteryFilter, ConfidenceCascade, HybridRoutingPolicy } from '../hybrid_router';
import { ValidationError } from './_errors';

export const batteryFilterDefaults = (): BatteryFilter => ({
  minBatteryPercent: 20,
});

export const validateBatteryFilter = (m: BatteryFilter): void => {
  if (m.minBatteryPercent < 0 || m.minBatteryPercent > 100) {
    throw new ValidationError({
      fieldPath: 'BatteryFilter.min_battery_percent',
      message: `min_battery_percent must be in 0...100 (got ${m.minBatteryPercent})`,
    });
  }
};

export const confidenceCascadeDefaults = (): ConfidenceCascade => ({
  threshold: 0.5,
});

export const validateConfidenceCascade = (m: ConfidenceCascade): void => {
  if (!Number.isFinite(m.threshold) || m.threshold < 0.0 || m.threshold > 1.0) {
    throw new ValidationError({
      fieldPath: 'ConfidenceCascade.threshold',
      message: `threshold must be in 0.0...1.0 (got ${m.threshold})`,
    });
  }
};

export const hybridRoutingPolicyDefaults = (): HybridRoutingPolicy => ({
  hardFilters: [],
  mode: 0,
  attemptTimeoutMs: 0,
  models: [],
});

export const validateHybridRoutingPolicy = (m: HybridRoutingPolicy): void => {
  if (m.attemptTimeoutMs < 0) {
    throw new ValidationError({
      fieldPath: 'HybridRoutingPolicy.attempt_timeout_ms',
      message: `attempt_timeout_ms must be >= 0 (got ${m.attemptTimeoutMs})`,
    });
  }
};
