name: runanywhere_qhexrt
description: Private Android-only Qualcomm Hexagon NPU (QHexRT) backend for RunAnywhere. Runs prebuilt QNN context binaries on Snapdragon v75+ NPUs.
version: 0.19.13
homepage: https://runanywhere.ai
repository: https://github.com/RunanywhereAI/runanywhere-sdks
issue_tracker: https://github.com/RunanywhereAI/runanywhere-sdks/issues
topics:
  - ai
  - llm
  - npu
  - qualcomm
  - on-device

environment:
  sdk: '>=3.10.0 <4.0.0'
  flutter: '>=3.24.0'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  runanywhere: ^0.19.13
  # Keep this bounded for pub.dev publish validation, matching the core
  # package's major-version range.
  ffi: '>=2.2.0 <3.0.0'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter:
  uses-material-design: true
  plugin:
    platforms:
      android:
        package: ai.runanywhere.sdk.qhexrt
        pluginClass: QhexrtPlugin
      ios:
        pluginClass: QhexrtPlugin
