/// Event categories for filtering/grouping.
///
/// Mirrors iOS `EventCategory` from RunAnywhere SDK.
enum EventCategory {
  sdk,
  model,
  llm,
  stt,
  tts,
  voice,
  storage,
  device,
  network,
  error,
}
