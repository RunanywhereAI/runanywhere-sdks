// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.1
//   protoc               v7.34.1
// source: vlm_options.proto
/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
export const protobufPackage = "runanywhere.v1";
/**
 * ---------------------------------------------------------------------------
 * VLM image input format — union across all SDKs and the C ABI.
 *
 * SDK ↔ proto enum mapping pre-IDL:
 *   C ABI  / Kotlin / RN / Web all expose three numeric formats (FILE_PATH=0,
 *          RGB_PIXELS=1, BASE64=2). Mapped to FILE_PATH, RAW_RGB, BASE64.
 *   Swift  Format enum adds Apple-only cases uiImage / pixelBuffer that are
 *          flattened to RAW_RGB before crossing the C ABI (see VLMTypes.swift
 *          lines 70-89). RAW_RGBA is reserved for SDKs that pass straight
 *          RGBA pixel buffers without the BGRA→RGB downsample step.
 *   Dart   sealed class with the same three formats (filePath / rgbPixels /
 *          base64); Flutter adapter passes RGB pixels through to the C ABI.
 *
 * JPEG / PNG / WEBP are container hints carried in the encoded `bytes`
 * payload (no current SDK declares these as enum cases — they are
 * reserved here so we can disambiguate decoded vs encoded sources without a
 * schema migration once a backend exposes container detection).
 * ---------------------------------------------------------------------------
 */
export var VLMImageFormat;
(function (VLMImageFormat) {
    VLMImageFormat[VLMImageFormat["VLM_IMAGE_FORMAT_UNSPECIFIED"] = 0] = "VLM_IMAGE_FORMAT_UNSPECIFIED";
    /** VLM_IMAGE_FORMAT_JPEG - reserved — encoded JPEG bytes */
    VLMImageFormat[VLMImageFormat["VLM_IMAGE_FORMAT_JPEG"] = 1] = "VLM_IMAGE_FORMAT_JPEG";
    /** VLM_IMAGE_FORMAT_PNG - reserved — encoded PNG bytes */
    VLMImageFormat[VLMImageFormat["VLM_IMAGE_FORMAT_PNG"] = 2] = "VLM_IMAGE_FORMAT_PNG";
    /** VLM_IMAGE_FORMAT_WEBP - reserved — encoded WebP bytes */
    VLMImageFormat[VLMImageFormat["VLM_IMAGE_FORMAT_WEBP"] = 3] = "VLM_IMAGE_FORMAT_WEBP";
    /** VLM_IMAGE_FORMAT_RAW_RGB - Swift rgbPixels / Kotlin RGB_PIXELS / */
    VLMImageFormat[VLMImageFormat["VLM_IMAGE_FORMAT_RAW_RGB"] = 4] = "VLM_IMAGE_FORMAT_RAW_RGB";
    /**
     * VLM_IMAGE_FORMAT_RAW_RGBA - RN RGBPixels / Web RGBPixels /
     * C ABI RAC_VLM_IMAGE_FORMAT_RGB_PIXELS
     */
    VLMImageFormat[VLMImageFormat["VLM_IMAGE_FORMAT_RAW_RGBA"] = 5] = "VLM_IMAGE_FORMAT_RAW_RGBA";
    /**
     * VLM_IMAGE_FORMAT_BASE64 - (Swift UIImage path produces RGBA
     * before downsample; pre-IDL no SDK
     * exposes RGBA over the C ABI)
     */
    VLMImageFormat[VLMImageFormat["VLM_IMAGE_FORMAT_BASE64"] = 6] = "VLM_IMAGE_FORMAT_BASE64";
    /**
     * VLM_IMAGE_FORMAT_FILE_PATH - Dart base64 / RN Base64 /
     * Web Base64 /
     * C ABI RAC_VLM_IMAGE_FORMAT_BASE64
     */
    VLMImageFormat[VLMImageFormat["VLM_IMAGE_FORMAT_FILE_PATH"] = 7] = "VLM_IMAGE_FORMAT_FILE_PATH";
    VLMImageFormat[VLMImageFormat["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(VLMImageFormat || (VLMImageFormat = {}));
export function vLMImageFormatFromJSON(object) {
    switch (object) {
        case 0:
        case "VLM_IMAGE_FORMAT_UNSPECIFIED":
            return VLMImageFormat.VLM_IMAGE_FORMAT_UNSPECIFIED;
        case 1:
        case "VLM_IMAGE_FORMAT_JPEG":
            return VLMImageFormat.VLM_IMAGE_FORMAT_JPEG;
        case 2:
        case "VLM_IMAGE_FORMAT_PNG":
            return VLMImageFormat.VLM_IMAGE_FORMAT_PNG;
        case 3:
        case "VLM_IMAGE_FORMAT_WEBP":
            return VLMImageFormat.VLM_IMAGE_FORMAT_WEBP;
        case 4:
        case "VLM_IMAGE_FORMAT_RAW_RGB":
            return VLMImageFormat.VLM_IMAGE_FORMAT_RAW_RGB;
        case 5:
        case "VLM_IMAGE_FORMAT_RAW_RGBA":
            return VLMImageFormat.VLM_IMAGE_FORMAT_RAW_RGBA;
        case 6:
        case "VLM_IMAGE_FORMAT_BASE64":
            return VLMImageFormat.VLM_IMAGE_FORMAT_BASE64;
        case 7:
        case "VLM_IMAGE_FORMAT_FILE_PATH":
            return VLMImageFormat.VLM_IMAGE_FORMAT_FILE_PATH;
        case -1:
        case "UNRECOGNIZED":
        default:
            return VLMImageFormat.UNRECOGNIZED;
    }
}
export function vLMImageFormatToJSON(object) {
    switch (object) {
        case VLMImageFormat.VLM_IMAGE_FORMAT_UNSPECIFIED:
            return "VLM_IMAGE_FORMAT_UNSPECIFIED";
        case VLMImageFormat.VLM_IMAGE_FORMAT_JPEG:
            return "VLM_IMAGE_FORMAT_JPEG";
        case VLMImageFormat.VLM_IMAGE_FORMAT_PNG:
            return "VLM_IMAGE_FORMAT_PNG";
        case VLMImageFormat.VLM_IMAGE_FORMAT_WEBP:
            return "VLM_IMAGE_FORMAT_WEBP";
        case VLMImageFormat.VLM_IMAGE_FORMAT_RAW_RGB:
            return "VLM_IMAGE_FORMAT_RAW_RGB";
        case VLMImageFormat.VLM_IMAGE_FORMAT_RAW_RGBA:
            return "VLM_IMAGE_FORMAT_RAW_RGBA";
        case VLMImageFormat.VLM_IMAGE_FORMAT_BASE64:
            return "VLM_IMAGE_FORMAT_BASE64";
        case VLMImageFormat.VLM_IMAGE_FORMAT_FILE_PATH:
            return "VLM_IMAGE_FORMAT_FILE_PATH";
        case VLMImageFormat.UNRECOGNIZED:
        default:
            return "UNRECOGNIZED";
    }
}
/**
 * ---------------------------------------------------------------------------
 * VLM error codes — canonical SDK-facing surface.
 * Sources pre-IDL:
 *   Swift  CppBridge+VLM.swift:184  (notInitialized=1, modelLoadFailed=2,
 *                                    processingFailed=3, invalidImage=4,
 *                                    cancelled=5)
 *   Dart   vlm_types.dart:164       (notInitialized=1, modelLoadFailed=2,
 *                                    processingFailed=3, invalidImage=4,
 *                                    cancelled=5)
 *   RN     VLMTypes.ts:44           (NotInitialized=1, ModelLoadFailed=2,
 *                                    ProcessingFailed=3, InvalidImage=4,
 *                                    Cancelled=5)
 *   Kotlin / Web                    (no enum declared pre-IDL)
 *
 * The canonicalized set below narrows the surface to image-specific failure
 * modes that the C ABI can distinguish at the boundary; transport / lifecycle
 * errors (notInitialized, modelLoadFailed, processingFailed, cancelled) are
 * folded back into the shared rac_result_t error codes in rac_error.h and do
 * not appear here.
 * ---------------------------------------------------------------------------
 */
export var VLMErrorCode;
(function (VLMErrorCode) {
    VLMErrorCode[VLMErrorCode["VLM_ERROR_CODE_UNSPECIFIED"] = 0] = "VLM_ERROR_CODE_UNSPECIFIED";
    /** VLM_ERROR_CODE_INVALID_IMAGE - Swift/Dart/RN invalidImage */
    VLMErrorCode[VLMErrorCode["VLM_ERROR_CODE_INVALID_IMAGE"] = 1] = "VLM_ERROR_CODE_INVALID_IMAGE";
    /** VLM_ERROR_CODE_MODEL_NOT_LOADED - Swift/Dart/RN notInitialized + */
    VLMErrorCode[VLMErrorCode["VLM_ERROR_CODE_MODEL_NOT_LOADED"] = 2] = "VLM_ERROR_CODE_MODEL_NOT_LOADED";
    /** VLM_ERROR_CODE_UNSUPPORTED_FORMAT - modelLoadFailed */
    VLMErrorCode[VLMErrorCode["VLM_ERROR_CODE_UNSUPPORTED_FORMAT"] = 3] = "VLM_ERROR_CODE_UNSUPPORTED_FORMAT";
    /** VLM_ERROR_CODE_IMAGE_TOO_LARGE - backend cannot decode */
    VLMErrorCode[VLMErrorCode["VLM_ERROR_CODE_IMAGE_TOO_LARGE"] = 4] = "VLM_ERROR_CODE_IMAGE_TOO_LARGE";
    VLMErrorCode[VLMErrorCode["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(VLMErrorCode || (VLMErrorCode = {}));
export function vLMErrorCodeFromJSON(object) {
    switch (object) {
        case 0:
        case "VLM_ERROR_CODE_UNSPECIFIED":
            return VLMErrorCode.VLM_ERROR_CODE_UNSPECIFIED;
        case 1:
        case "VLM_ERROR_CODE_INVALID_IMAGE":
            return VLMErrorCode.VLM_ERROR_CODE_INVALID_IMAGE;
        case 2:
        case "VLM_ERROR_CODE_MODEL_NOT_LOADED":
            return VLMErrorCode.VLM_ERROR_CODE_MODEL_NOT_LOADED;
        case 3:
        case "VLM_ERROR_CODE_UNSUPPORTED_FORMAT":
            return VLMErrorCode.VLM_ERROR_CODE_UNSUPPORTED_FORMAT;
        case 4:
        case "VLM_ERROR_CODE_IMAGE_TOO_LARGE":
            return VLMErrorCode.VLM_ERROR_CODE_IMAGE_TOO_LARGE;
        case -1:
        case "UNRECOGNIZED":
        default:
            return VLMErrorCode.UNRECOGNIZED;
    }
}
export function vLMErrorCodeToJSON(object) {
    switch (object) {
        case VLMErrorCode.VLM_ERROR_CODE_UNSPECIFIED:
            return "VLM_ERROR_CODE_UNSPECIFIED";
        case VLMErrorCode.VLM_ERROR_CODE_INVALID_IMAGE:
            return "VLM_ERROR_CODE_INVALID_IMAGE";
        case VLMErrorCode.VLM_ERROR_CODE_MODEL_NOT_LOADED:
            return "VLM_ERROR_CODE_MODEL_NOT_LOADED";
        case VLMErrorCode.VLM_ERROR_CODE_UNSUPPORTED_FORMAT:
            return "VLM_ERROR_CODE_UNSUPPORTED_FORMAT";
        case VLMErrorCode.VLM_ERROR_CODE_IMAGE_TOO_LARGE:
            return "VLM_ERROR_CODE_IMAGE_TOO_LARGE";
        case VLMErrorCode.UNRECOGNIZED:
        default:
            return "UNRECOGNIZED";
    }
}
function createBaseVLMImage() {
    return {
        filePath: undefined,
        encoded: undefined,
        rawRgb: undefined,
        base64: undefined,
        width: 0,
        height: 0,
        format: 0,
    };
}
export const VLMImage = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.filePath !== undefined) {
            writer.uint32(10).string(message.filePath);
        }
        if (message.encoded !== undefined) {
            writer.uint32(18).bytes(message.encoded);
        }
        if (message.rawRgb !== undefined) {
            writer.uint32(26).bytes(message.rawRgb);
        }
        if (message.base64 !== undefined) {
            writer.uint32(34).string(message.base64);
        }
        if (message.width !== 0) {
            writer.uint32(40).int32(message.width);
        }
        if (message.height !== 0) {
            writer.uint32(48).int32(message.height);
        }
        if (message.format !== 0) {
            writer.uint32(56).int32(message.format);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseVLMImage();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.filePath = reader.string();
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.encoded = reader.bytes();
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.rawRgb = reader.bytes();
                    continue;
                case 4:
                    if (tag !== 34) {
                        break;
                    }
                    message.base64 = reader.string();
                    continue;
                case 5:
                    if (tag !== 40) {
                        break;
                    }
                    message.width = reader.int32();
                    continue;
                case 6:
                    if (tag !== 48) {
                        break;
                    }
                    message.height = reader.int32();
                    continue;
                case 7:
                    if (tag !== 56) {
                        break;
                    }
                    message.format = reader.int32();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            filePath: isSet(object.filePath) ? globalThis.String(object.filePath) : undefined,
            encoded: isSet(object.encoded) ? bytesFromBase64(object.encoded) : undefined,
            rawRgb: isSet(object.rawRgb) ? bytesFromBase64(object.rawRgb) : undefined,
            base64: isSet(object.base64) ? globalThis.String(object.base64) : undefined,
            width: isSet(object.width) ? globalThis.Number(object.width) : 0,
            height: isSet(object.height) ? globalThis.Number(object.height) : 0,
            format: isSet(object.format) ? vLMImageFormatFromJSON(object.format) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.filePath !== undefined) {
            obj.filePath = message.filePath;
        }
        if (message.encoded !== undefined) {
            obj.encoded = base64FromBytes(message.encoded);
        }
        if (message.rawRgb !== undefined) {
            obj.rawRgb = base64FromBytes(message.rawRgb);
        }
        if (message.base64 !== undefined) {
            obj.base64 = message.base64;
        }
        if (message.width !== 0) {
            obj.width = Math.round(message.width);
        }
        if (message.height !== 0) {
            obj.height = Math.round(message.height);
        }
        if (message.format !== 0) {
            obj.format = vLMImageFormatToJSON(message.format);
        }
        return obj;
    },
    create(base) {
        return VLMImage.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseVLMImage();
        message.filePath = object.filePath ?? undefined;
        message.encoded = object.encoded ?? undefined;
        message.rawRgb = object.rawRgb ?? undefined;
        message.base64 = object.base64 ?? undefined;
        message.width = object.width ?? 0;
        message.height = object.height ?? 0;
        message.format = object.format ?? 0;
        return message;
    },
};
function createBaseVLMConfiguration() {
    return { modelId: "", maxImageSizePx: 0, maxTokens: 0 };
}
export const VLMConfiguration = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.modelId !== "") {
            writer.uint32(10).string(message.modelId);
        }
        if (message.maxImageSizePx !== 0) {
            writer.uint32(16).int32(message.maxImageSizePx);
        }
        if (message.maxTokens !== 0) {
            writer.uint32(24).int32(message.maxTokens);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseVLMConfiguration();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.modelId = reader.string();
                    continue;
                case 2:
                    if (tag !== 16) {
                        break;
                    }
                    message.maxImageSizePx = reader.int32();
                    continue;
                case 3:
                    if (tag !== 24) {
                        break;
                    }
                    message.maxTokens = reader.int32();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            modelId: isSet(object.modelId) ? globalThis.String(object.modelId) : "",
            maxImageSizePx: isSet(object.maxImageSizePx) ? globalThis.Number(object.maxImageSizePx) : 0,
            maxTokens: isSet(object.maxTokens) ? globalThis.Number(object.maxTokens) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.modelId !== "") {
            obj.modelId = message.modelId;
        }
        if (message.maxImageSizePx !== 0) {
            obj.maxImageSizePx = Math.round(message.maxImageSizePx);
        }
        if (message.maxTokens !== 0) {
            obj.maxTokens = Math.round(message.maxTokens);
        }
        return obj;
    },
    create(base) {
        return VLMConfiguration.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseVLMConfiguration();
        message.modelId = object.modelId ?? "";
        message.maxImageSizePx = object.maxImageSizePx ?? 0;
        message.maxTokens = object.maxTokens ?? 0;
        return message;
    },
};
function createBaseVLMGenerationOptions() {
    return { prompt: "", maxTokens: 0, temperature: 0, topP: 0, topK: 0 };
}
export const VLMGenerationOptions = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.prompt !== "") {
            writer.uint32(10).string(message.prompt);
        }
        if (message.maxTokens !== 0) {
            writer.uint32(16).int32(message.maxTokens);
        }
        if (message.temperature !== 0) {
            writer.uint32(29).float(message.temperature);
        }
        if (message.topP !== 0) {
            writer.uint32(37).float(message.topP);
        }
        if (message.topK !== 0) {
            writer.uint32(40).int32(message.topK);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseVLMGenerationOptions();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.prompt = reader.string();
                    continue;
                case 2:
                    if (tag !== 16) {
                        break;
                    }
                    message.maxTokens = reader.int32();
                    continue;
                case 3:
                    if (tag !== 29) {
                        break;
                    }
                    message.temperature = reader.float();
                    continue;
                case 4:
                    if (tag !== 37) {
                        break;
                    }
                    message.topP = reader.float();
                    continue;
                case 5:
                    if (tag !== 40) {
                        break;
                    }
                    message.topK = reader.int32();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            prompt: isSet(object.prompt) ? globalThis.String(object.prompt) : "",
            maxTokens: isSet(object.maxTokens) ? globalThis.Number(object.maxTokens) : 0,
            temperature: isSet(object.temperature) ? globalThis.Number(object.temperature) : 0,
            topP: isSet(object.topP) ? globalThis.Number(object.topP) : 0,
            topK: isSet(object.topK) ? globalThis.Number(object.topK) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.prompt !== "") {
            obj.prompt = message.prompt;
        }
        if (message.maxTokens !== 0) {
            obj.maxTokens = Math.round(message.maxTokens);
        }
        if (message.temperature !== 0) {
            obj.temperature = message.temperature;
        }
        if (message.topP !== 0) {
            obj.topP = message.topP;
        }
        if (message.topK !== 0) {
            obj.topK = Math.round(message.topK);
        }
        return obj;
    },
    create(base) {
        return VLMGenerationOptions.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseVLMGenerationOptions();
        message.prompt = object.prompt ?? "";
        message.maxTokens = object.maxTokens ?? 0;
        message.temperature = object.temperature ?? 0;
        message.topP = object.topP ?? 0;
        message.topK = object.topK ?? 0;
        return message;
    },
};
function createBaseVLMResult() {
    return { text: "", promptTokens: 0, completionTokens: 0, totalTokens: 0, processingTimeMs: 0, tokensPerSecond: 0 };
}
export const VLMResult = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.text !== "") {
            writer.uint32(10).string(message.text);
        }
        if (message.promptTokens !== 0) {
            writer.uint32(16).int32(message.promptTokens);
        }
        if (message.completionTokens !== 0) {
            writer.uint32(24).int32(message.completionTokens);
        }
        if (message.totalTokens !== 0) {
            writer.uint32(32).int64(message.totalTokens);
        }
        if (message.processingTimeMs !== 0) {
            writer.uint32(40).int64(message.processingTimeMs);
        }
        if (message.tokensPerSecond !== 0) {
            writer.uint32(53).float(message.tokensPerSecond);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseVLMResult();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.text = reader.string();
                    continue;
                case 2:
                    if (tag !== 16) {
                        break;
                    }
                    message.promptTokens = reader.int32();
                    continue;
                case 3:
                    if (tag !== 24) {
                        break;
                    }
                    message.completionTokens = reader.int32();
                    continue;
                case 4:
                    if (tag !== 32) {
                        break;
                    }
                    message.totalTokens = longToNumber(reader.int64());
                    continue;
                case 5:
                    if (tag !== 40) {
                        break;
                    }
                    message.processingTimeMs = longToNumber(reader.int64());
                    continue;
                case 6:
                    if (tag !== 53) {
                        break;
                    }
                    message.tokensPerSecond = reader.float();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            text: isSet(object.text) ? globalThis.String(object.text) : "",
            promptTokens: isSet(object.promptTokens) ? globalThis.Number(object.promptTokens) : 0,
            completionTokens: isSet(object.completionTokens) ? globalThis.Number(object.completionTokens) : 0,
            totalTokens: isSet(object.totalTokens) ? globalThis.Number(object.totalTokens) : 0,
            processingTimeMs: isSet(object.processingTimeMs) ? globalThis.Number(object.processingTimeMs) : 0,
            tokensPerSecond: isSet(object.tokensPerSecond) ? globalThis.Number(object.tokensPerSecond) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.text !== "") {
            obj.text = message.text;
        }
        if (message.promptTokens !== 0) {
            obj.promptTokens = Math.round(message.promptTokens);
        }
        if (message.completionTokens !== 0) {
            obj.completionTokens = Math.round(message.completionTokens);
        }
        if (message.totalTokens !== 0) {
            obj.totalTokens = Math.round(message.totalTokens);
        }
        if (message.processingTimeMs !== 0) {
            obj.processingTimeMs = Math.round(message.processingTimeMs);
        }
        if (message.tokensPerSecond !== 0) {
            obj.tokensPerSecond = message.tokensPerSecond;
        }
        return obj;
    },
    create(base) {
        return VLMResult.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseVLMResult();
        message.text = object.text ?? "";
        message.promptTokens = object.promptTokens ?? 0;
        message.completionTokens = object.completionTokens ?? 0;
        message.totalTokens = object.totalTokens ?? 0;
        message.processingTimeMs = object.processingTimeMs ?? 0;
        message.tokensPerSecond = object.tokensPerSecond ?? 0;
        return message;
    },
};
function bytesFromBase64(b64) {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
        arr[i] = bin.charCodeAt(i);
    }
    return arr;
}
function base64FromBytes(arr) {
    const bin = [];
    arr.forEach((byte) => {
        bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
}
function longToNumber(long) {
    if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
        throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
    }
    if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
        throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
    }
    return long.toNumber();
}
if (_m0.util.Long !== Long) {
    _m0.util.Long = Long;
    _m0.configure();
}
function isSet(value) {
    return value !== null && value !== undefined;
}
//# sourceMappingURL=vlm_options.js.map