// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.1
//   protoc               v7.34.1
// source: hardware_profile.proto
/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
export const protobufPackage = "runanywhere.v1";
export var AcceleratorPreference;
(function (AcceleratorPreference) {
    AcceleratorPreference[AcceleratorPreference["ACCELERATOR_PREFERENCE_AUTO"] = 0] = "ACCELERATOR_PREFERENCE_AUTO";
    AcceleratorPreference[AcceleratorPreference["ACCELERATOR_PREFERENCE_ANE"] = 1] = "ACCELERATOR_PREFERENCE_ANE";
    AcceleratorPreference[AcceleratorPreference["ACCELERATOR_PREFERENCE_GPU"] = 2] = "ACCELERATOR_PREFERENCE_GPU";
    AcceleratorPreference[AcceleratorPreference["ACCELERATOR_PREFERENCE_CPU"] = 3] = "ACCELERATOR_PREFERENCE_CPU";
    AcceleratorPreference[AcceleratorPreference["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(AcceleratorPreference || (AcceleratorPreference = {}));
export function acceleratorPreferenceFromJSON(object) {
    switch (object) {
        case 0:
        case "ACCELERATOR_PREFERENCE_AUTO":
            return AcceleratorPreference.ACCELERATOR_PREFERENCE_AUTO;
        case 1:
        case "ACCELERATOR_PREFERENCE_ANE":
            return AcceleratorPreference.ACCELERATOR_PREFERENCE_ANE;
        case 2:
        case "ACCELERATOR_PREFERENCE_GPU":
            return AcceleratorPreference.ACCELERATOR_PREFERENCE_GPU;
        case 3:
        case "ACCELERATOR_PREFERENCE_CPU":
            return AcceleratorPreference.ACCELERATOR_PREFERENCE_CPU;
        case -1:
        case "UNRECOGNIZED":
        default:
            return AcceleratorPreference.UNRECOGNIZED;
    }
}
export function acceleratorPreferenceToJSON(object) {
    switch (object) {
        case AcceleratorPreference.ACCELERATOR_PREFERENCE_AUTO:
            return "ACCELERATOR_PREFERENCE_AUTO";
        case AcceleratorPreference.ACCELERATOR_PREFERENCE_ANE:
            return "ACCELERATOR_PREFERENCE_ANE";
        case AcceleratorPreference.ACCELERATOR_PREFERENCE_GPU:
            return "ACCELERATOR_PREFERENCE_GPU";
        case AcceleratorPreference.ACCELERATOR_PREFERENCE_CPU:
            return "ACCELERATOR_PREFERENCE_CPU";
        case AcceleratorPreference.UNRECOGNIZED:
        default:
            return "UNRECOGNIZED";
    }
}
function createBaseHardwareProfile() {
    return {
        chip: "",
        hasNeuralEngine: false,
        accelerationMode: "",
        totalMemoryBytes: 0,
        coreCount: 0,
        performanceCores: 0,
        efficiencyCores: 0,
        architecture: "",
        platform: "",
    };
}
export const HardwareProfile = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.chip !== "") {
            writer.uint32(10).string(message.chip);
        }
        if (message.hasNeuralEngine !== false) {
            writer.uint32(16).bool(message.hasNeuralEngine);
        }
        if (message.accelerationMode !== "") {
            writer.uint32(26).string(message.accelerationMode);
        }
        if (message.totalMemoryBytes !== 0) {
            writer.uint32(32).uint64(message.totalMemoryBytes);
        }
        if (message.coreCount !== 0) {
            writer.uint32(40).uint32(message.coreCount);
        }
        if (message.performanceCores !== 0) {
            writer.uint32(48).uint32(message.performanceCores);
        }
        if (message.efficiencyCores !== 0) {
            writer.uint32(56).uint32(message.efficiencyCores);
        }
        if (message.architecture !== "") {
            writer.uint32(66).string(message.architecture);
        }
        if (message.platform !== "") {
            writer.uint32(74).string(message.platform);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseHardwareProfile();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.chip = reader.string();
                    continue;
                case 2:
                    if (tag !== 16) {
                        break;
                    }
                    message.hasNeuralEngine = reader.bool();
                    continue;
                case 3:
                    if (tag !== 26) {
                        break;
                    }
                    message.accelerationMode = reader.string();
                    continue;
                case 4:
                    if (tag !== 32) {
                        break;
                    }
                    message.totalMemoryBytes = longToNumber(reader.uint64());
                    continue;
                case 5:
                    if (tag !== 40) {
                        break;
                    }
                    message.coreCount = reader.uint32();
                    continue;
                case 6:
                    if (tag !== 48) {
                        break;
                    }
                    message.performanceCores = reader.uint32();
                    continue;
                case 7:
                    if (tag !== 56) {
                        break;
                    }
                    message.efficiencyCores = reader.uint32();
                    continue;
                case 8:
                    if (tag !== 66) {
                        break;
                    }
                    message.architecture = reader.string();
                    continue;
                case 9:
                    if (tag !== 74) {
                        break;
                    }
                    message.platform = reader.string();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            chip: isSet(object.chip) ? globalThis.String(object.chip) : "",
            hasNeuralEngine: isSet(object.hasNeuralEngine) ? globalThis.Boolean(object.hasNeuralEngine) : false,
            accelerationMode: isSet(object.accelerationMode) ? globalThis.String(object.accelerationMode) : "",
            totalMemoryBytes: isSet(object.totalMemoryBytes) ? globalThis.Number(object.totalMemoryBytes) : 0,
            coreCount: isSet(object.coreCount) ? globalThis.Number(object.coreCount) : 0,
            performanceCores: isSet(object.performanceCores) ? globalThis.Number(object.performanceCores) : 0,
            efficiencyCores: isSet(object.efficiencyCores) ? globalThis.Number(object.efficiencyCores) : 0,
            architecture: isSet(object.architecture) ? globalThis.String(object.architecture) : "",
            platform: isSet(object.platform) ? globalThis.String(object.platform) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.chip !== "") {
            obj.chip = message.chip;
        }
        if (message.hasNeuralEngine !== false) {
            obj.hasNeuralEngine = message.hasNeuralEngine;
        }
        if (message.accelerationMode !== "") {
            obj.accelerationMode = message.accelerationMode;
        }
        if (message.totalMemoryBytes !== 0) {
            obj.totalMemoryBytes = Math.round(message.totalMemoryBytes);
        }
        if (message.coreCount !== 0) {
            obj.coreCount = Math.round(message.coreCount);
        }
        if (message.performanceCores !== 0) {
            obj.performanceCores = Math.round(message.performanceCores);
        }
        if (message.efficiencyCores !== 0) {
            obj.efficiencyCores = Math.round(message.efficiencyCores);
        }
        if (message.architecture !== "") {
            obj.architecture = message.architecture;
        }
        if (message.platform !== "") {
            obj.platform = message.platform;
        }
        return obj;
    },
    create(base) {
        return HardwareProfile.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseHardwareProfile();
        message.chip = object.chip ?? "";
        message.hasNeuralEngine = object.hasNeuralEngine ?? false;
        message.accelerationMode = object.accelerationMode ?? "";
        message.totalMemoryBytes = object.totalMemoryBytes ?? 0;
        message.coreCount = object.coreCount ?? 0;
        message.performanceCores = object.performanceCores ?? 0;
        message.efficiencyCores = object.efficiencyCores ?? 0;
        message.architecture = object.architecture ?? "";
        message.platform = object.platform ?? "";
        return message;
    },
};
function createBaseAcceleratorInfo() {
    return { name: "", type: 0, available: false };
}
export const AcceleratorInfo = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.name !== "") {
            writer.uint32(10).string(message.name);
        }
        if (message.type !== 0) {
            writer.uint32(16).int32(message.type);
        }
        if (message.available !== false) {
            writer.uint32(24).bool(message.available);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseAcceleratorInfo();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.name = reader.string();
                    continue;
                case 2:
                    if (tag !== 16) {
                        break;
                    }
                    message.type = reader.int32();
                    continue;
                case 3:
                    if (tag !== 24) {
                        break;
                    }
                    message.available = reader.bool();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            name: isSet(object.name) ? globalThis.String(object.name) : "",
            type: isSet(object.type) ? acceleratorPreferenceFromJSON(object.type) : 0,
            available: isSet(object.available) ? globalThis.Boolean(object.available) : false,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.name !== "") {
            obj.name = message.name;
        }
        if (message.type !== 0) {
            obj.type = acceleratorPreferenceToJSON(message.type);
        }
        if (message.available !== false) {
            obj.available = message.available;
        }
        return obj;
    },
    create(base) {
        return AcceleratorInfo.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseAcceleratorInfo();
        message.name = object.name ?? "";
        message.type = object.type ?? 0;
        message.available = object.available ?? false;
        return message;
    },
};
function createBaseHardwareProfileResult() {
    return { profile: undefined, accelerators: [] };
}
export const HardwareProfileResult = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.profile !== undefined) {
            HardwareProfile.encode(message.profile, writer.uint32(10).fork()).ldelim();
        }
        for (const v of message.accelerators) {
            AcceleratorInfo.encode(v, writer.uint32(18).fork()).ldelim();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseHardwareProfileResult();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.profile = HardwareProfile.decode(reader, reader.uint32());
                    continue;
                case 2:
                    if (tag !== 18) {
                        break;
                    }
                    message.accelerators.push(AcceleratorInfo.decode(reader, reader.uint32()));
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            profile: isSet(object.profile) ? HardwareProfile.fromJSON(object.profile) : undefined,
            accelerators: globalThis.Array.isArray(object?.accelerators)
                ? object.accelerators.map((e) => AcceleratorInfo.fromJSON(e))
                : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.profile !== undefined) {
            obj.profile = HardwareProfile.toJSON(message.profile);
        }
        if (message.accelerators?.length) {
            obj.accelerators = message.accelerators.map((e) => AcceleratorInfo.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return HardwareProfileResult.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseHardwareProfileResult();
        message.profile = (object.profile !== undefined && object.profile !== null)
            ? HardwareProfile.fromPartial(object.profile)
            : undefined;
        message.accelerators = object.accelerators?.map((e) => AcceleratorInfo.fromPartial(e)) || [];
        return message;
    },
};
function longToNumber(long) {
    if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
        throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
    }
    if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
        throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
    }
    return long.toNumber();
}
if (_m0.util.Long !== Long) {
    _m0.util.Long = Long;
    _m0.configure();
}
function isSet(value) {
    return value !== null && value !== undefined;
}
//# sourceMappingURL=hardware_profile.js.map