// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.1
//   protoc               v7.34.1
// source: router.proto
/* eslint-disable */
import _m0 from "protobufjs/minimal";
import { inferenceFrameworkFromJSON, inferenceFrameworkToJSON } from "./model_types";
import { sDKComponentFromJSON, sDKComponentToJSON } from "./sdk_events";
export const protobufPackage = "runanywhere.v1";
function createBaseFrameworksForCapabilityRequest() {
    return { component: 0 };
}
export const FrameworksForCapabilityRequest = {
    encode(message, writer = _m0.Writer.create()) {
        if (message.component !== 0) {
            writer.uint32(8).int32(message.component);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseFrameworksForCapabilityRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 8) {
                        break;
                    }
                    message.component = reader.int32();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { component: isSet(object.component) ? sDKComponentFromJSON(object.component) : 0 };
    },
    toJSON(message) {
        const obj = {};
        if (message.component !== 0) {
            obj.component = sDKComponentToJSON(message.component);
        }
        return obj;
    },
    create(base) {
        return FrameworksForCapabilityRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseFrameworksForCapabilityRequest();
        message.component = object.component ?? 0;
        return message;
    },
};
function createBaseFrameworksForCapabilityResponse() {
    return { frameworks: [] };
}
export const FrameworksForCapabilityResponse = {
    encode(message, writer = _m0.Writer.create()) {
        writer.uint32(10).fork();
        for (const v of message.frameworks) {
            writer.int32(v);
        }
        writer.ldelim();
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseFrameworksForCapabilityResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag === 8) {
                        message.frameworks.push(reader.int32());
                        continue;
                    }
                    if (tag === 10) {
                        const end2 = reader.uint32() + reader.pos;
                        while (reader.pos < end2) {
                            message.frameworks.push(reader.int32());
                        }
                        continue;
                    }
                    break;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            frameworks: globalThis.Array.isArray(object?.frameworks)
                ? object.frameworks.map((e) => inferenceFrameworkFromJSON(e))
                : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.frameworks?.length) {
            obj.frameworks = message.frameworks.map((e) => inferenceFrameworkToJSON(e));
        }
        return obj;
    },
    create(base) {
        return FrameworksForCapabilityResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseFrameworksForCapabilityResponse();
        message.frameworks = object.frameworks?.map((e) => e) || [];
        return message;
    },
};
function isSet(value) {
    return value !== null && value !== undefined;
}
//# sourceMappingURL=router.js.map