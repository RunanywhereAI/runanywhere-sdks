# Build + packaging artifacts (regenerated by `npm run build` / `bundle:native`).
dist/
# Backend packages under packages/* (Track B)
packages/*/dist/
packages/*/node_modules/
# The test / script / native-smoke projects compile to their own out-dirs
# (tsconfig.test.json, tsconfig.scripts.json, tsconfig.native.json).
dist-test/
dist-scripts/
dist-native/
node_modules/
prebuilds/
*.tgz
*.log
