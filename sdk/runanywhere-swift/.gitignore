.DS_Store
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/configuration/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc

# Local XCFrameworks (deprecated - now consumed via remote SPM binary targets)
# The SDK now uses remote XCFramework distribution from runanywhere-binaries releases
# Local builds in Binaries/ are no longer needed for normal development
Binaries/*.xcframework/
XCFrameworks/

# SPM build artifacts (recreated during swift build)
Modules/
