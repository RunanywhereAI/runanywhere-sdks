jdk:
  - openjdk17
before_install:
  - sdk install java 17.0.2-open || true
  - sdk use java 17.0.2-open || true
install:
  - cd sdk/runanywhere-kotlin
  - chmod +x gradlew
  - ./gradlew clean build publishToMavenLocal --refresh-dependencies --no-daemon --stacktrace
