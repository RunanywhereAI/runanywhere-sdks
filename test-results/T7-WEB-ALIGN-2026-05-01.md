# T7 Web E2E — Alignment 2026-05-01

**Commit:** 79975ae0

## TypeScript compile
- @runanywhere/core: **PASS** (clean, zero output from `tsc --noEmit`)
- @runanywhere/llamacpp: **PASS**
- @runanywhere/onnx: **PASS**
- examples/web (RunAnywhereAI): **PASS** (`tsc --noEmit` clean)

## WASM symbols
- `_rac_http_transport_register_from_js` exported: **YES** (`strings | grep -c` → 1)
  - Source: `sdk/runanywhere-commons/src/infrastructure/http/rac_http_client_emscripten.cpp:457`
  - Wraps JS-provided function-table indices through `kJsOps` vtable and dispatches via `rac_http_transport_register(&kJsOps, …)` — **no registry bypass**.

## M1.3 FetchHttpTransport trampolines
- `addFunction` calls in dist: **YES**
  - `sdk/runanywhere-web/packages/core/dist/Adapters/FetchHttpTransport.js:116` — `requestStreamPtr = this.m.addFunction(...)`
  - `sdk/runanywhere-web/packages/core/dist/Adapters/FetchHttpTransport.js:122` — `requestResumePtr = this.m.addFunction(...)`
  - `sdk/runanywhere-web/packages/core/dist/Adapters/FetchHttpTransport.js:128` — `registerFn = this.m._rac_http_transport_register_from_js`
- Activation log string present in source: **YES** (line 198: `FetchHttpTransport: JS HTTP transport activated ...`)
- Synchronous XHR for `stream`+`resume`, `emscripten_fetch` for `send` — structure verified in source.
- Src/dist parity check: src has 14 occurrences of `register_from_js`/`addFunction`, dist has 11 (minimization expected — imports/comments stripped). Critical call sites intact.

## M6 SHA256 verifier
- `verifyStoredFileSha256()` present at `ModelDownloader.ts:755`
- Uses `crypto.subtle.digest('SHA-256', bytes)` at line 767
- Called in 2 code paths: single-file (`ModelDownloader.ts:137`) and multi-file (`ModelDownloader.ts:180`)

## M8 proto enum usage
- No hand-rolled `interface DownloadProgress` outside proto-ts: **PASS** (grep empty)
  - `ModelRegistry.ts:21` only aliases the proto type: `export type DownloadProgress = ProtoDownloadProgress;`
- No string literal `DownloadStage` values (`"downloading"`, `"extracting"`, `"completed"`) in `ModelDownloader.ts`: **PASS** (grep empty)
- Extra fields deleted (`currentFile`, `filesCompleted`, `filesTotal`): **PASS** (grep empty across core/src/)
- `types/enums.ts` re-exports proto integer enum at line 213: `export { DownloadStage } from '@runanywhere/proto-ts/download_service';`
- `ModelDownloader.ts` uses integer enum members: `DOWNLOAD_STAGE_DOWNLOADING` (1), `DOWNLOAD_STAGE_VALIDATING` (3), `DOWNLOAD_STAGE_COMPLETED` (4)
- Proto source `sdk/runanywhere-proto-ts/src/download_service.ts:13` defines 5-value integer enum (UNSPECIFIED=0, DOWNLOADING=1, EXTRACTING=2, VALIDATING=3, COMPLETED=4)

## Dev server
- `localhost:5173` served: **YES** (Vite 6.4.1 ready in 166ms, HTML served)
- Zero console errors on load: **YES** (filter types=`error,warn` returned `<no console messages found>`)
- SDK init sequence (47 console messages, all `info`/`log`):
  - `[COI] Already cross-origin isolated` ✓
  - `RunAnywhere Web SDK initialized successfully` ✓
  - OPFS storage initialized ✓
  - LlamaCpp WASM module loaded (webgpu variant) ✓
  - `RAC:RAC] RunAnywhere Commons initialized` ✓
  - llama.cpp backend + VLM backend registered ✓
  - ONNX backend registered ✓
  - TelemetryService initialized (HTTP configured for Supabase dev) ✓
  - `DeviceCapabilities: WebGPU=true, SharedArrayBuffer=true, SIMD=true, OPFS=true` ✓
  - Final: `SDK initialized, version: 0.1.0 | acceleration: webgpu | local storage: OPFS` ✓
- Network: WASM loaded `racommons-llamacpp-webgpu.wasm` → 304 (cached)
- **No `emscripten_fetch status 0` errors observed** (no errors/warnings logged at all).
- Note: No explicit `FetchHttpTransport: JS HTTP transport activated` line was printed, because the app only loads the llamacpp module and reaches SDK-ready state; HTTP transport activation happens on first HTTP-using code path (e.g., model download). Code paths verified statically in both src and dist.

## Overall: **PASS**

All M1.3 (JS trampolines + vtable registry), M6 (SHA256 verifier), and M8 (proto enum migration) checks pass. TypeScript compiles clean across all 4 targets. WASM symbol exported. Dev server boots SDK to `ready` state with WebGPU + OPFS + both backends registered and zero errors/warnings.

### Evidence artifacts
- TS compile: `cd sdk/runanywhere-web/packages/{core,llamacpp,onnx} && npx tsc --noEmit` → all empty output
- WASM symbol: `strings sdk/runanywhere-web/packages/llamacpp/wasm/racommons-llamacpp.wasm | grep -c rac_http_transport_register_from_js` → 1
- Dist trampolines: `sdk/runanywhere-web/packages/core/dist/Adapters/FetchHttpTransport.js:116,122,128`
- Proto enum: `sdk/runanywhere-proto-ts/src/download_service.ts:13-18` (5-value integer)
- Browser logs: 47 messages, 0 errors/warnings, final state `ready`
